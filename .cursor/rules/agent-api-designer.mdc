---
description: "API Designer rules: OpenAPI 3.0, Domain Separation, Struct Alignment, Ogen compatibility. Creates REST specs, enforced strictly."
globs: ["**/proto/openapi/**/*.yaml", "**/openapi*.yaml", "**/api-spec*.yaml"]
priority: 1
tags: ["api", "openapi", "spec", "design"]
version: "1.0"
---

# API Designer Agent Rules

## 1. Роль и Область ответственности

**Role:** Создание OpenAPI 3.0 спецификаций для REST API (ogen-compatible).
**Outputs:** `.yaml` файлы в `proto/openapi/`.
**NOT Responsible:**
- `.proto` файлы (Real-time/Voice/Sync -> Network Engineer).
- Backend implementation.
- Content generation.

## 2. 🏗️ Архитектура: Enterprise Domain Separation

**CRITICAL:** Все спецификации СТРОГО используют наследование (Inheritance) от Common Core. Дублирование полей ЗАПРЕЩЕНО.

### Структура Директорий (SOLID/DRY)

```text
proto/openapi/
├── common/                     # SOLID/DRY Foundation (ОБЯЗАТЕЛЬНО использовать!)
│   ├── schemas/                # Domain Entities (Game, Economy, Social, Infrastructure)
│   │   ├── game-entities.yaml           # CharacterEntity, ItemEntity, WeaponEntity
│   │   ├── economy-entities.yaml        # WalletEntity, TransactionEntity, AuctionEntity
│   │   ├── social-entities.yaml         # UserProfileEntity, GuildEntity, ChatMessageEntity
│   │   ├── infrastructure-entities.yaml # UserAccountEntity, SessionEntity, AuditLogEntity
│   │   ├── seasonal-challenges-entities.yaml # SeasonEntity, ChallengeEntity, PlayerProgressEntity
│   │   ├── health.yaml                  # HealthResponse, DetailedHealthResponse
│   │   └── pagination.yaml              # Pagination schemas
│   ├── responses/              # Standard Responses
│   │   ├── success.yaml        # OK, Created, Updated responses
│   │   └── error.yaml          # BadRequest, NotFound, Conflict, etc.
│   └── security/               # SecuritySchemes
│       └── security.yaml       # BearerAuth, ApiKeyAuth
├── {service-name}-service/     # Service Specifications (<1000 lines)
│   ├── main.yaml               # Service Endpoint Definition (НАСЛЕДУЕТ от common!)
│   └── README.md               # Service documentation
└── example/                    # Enterprise-grade template example
    ├── main.yaml               # Полный пример с domain inheritance
    └── README.md               # Документация по шаблону
```

**КРИТИЧНО:** Все сервисы ДОЛЖНЫ наследовать от `common/schemas/{domain}-entities.yaml`. Дублирование общих полей ЗАПРЕЩЕНО!

### Принципы Наследования (SOLID/DRY Inheritance Pattern)

**КРИТИЧНО:** Все сущности ДОЛЖНЫ наследовать базовые поля из `common/schemas/` через `allOf` и `$ref`. Дублирование общих полей ЗАПРЕЩЕНО!

**Правило:** Используйте `allOf` для наследования, добавляйте только уникальные поля сервиса.

```yaml
# ✅ ПРАВИЛЬНО: Domain Inheritance через allOf
CombatUnit:
  description: Combat unit entity inheriting base structure
  allOf:
    - $ref: '../common/schemas/game-entities.yaml#/components/schemas/CharacterEntity'
    - type: object
      required: [unit_type]
      properties:
        unit_type: {type: string, enum: ['infantry', 'mech']}  # Только уникальные поля
        # Базовые поля (id, timestamps, version) наследуются автоматически!

# ❌ НЕПРАВИЛЬНО: Дублирование базовых полей
CombatUnit:
  type: object
  properties:
    id: {type: string, format: uuid}              # ❌ ДУБЛИРОВАНИЕ!
    created_at: {type: string, format: date-time} # ❌ ДУБЛИРОВАНИЕ!
    version: {type: integer}                      # ❌ ДУБЛИРОВАНИЕ!
    unit_type: {type: string}                    # Только это уникально
```

**Общие компоненты для переиспользования:**

1. **Health endpoints:** `../common/schemas/health.yaml#/components/schemas/HealthResponse`
2. **Responses:** `../common/responses/success.yaml`, `../common/responses/error.yaml`
3. **Security:** `../common/security/security.yaml#/components/securitySchemes/BearerAuth`
4. **Domain entities:** `../common/schemas/{domain}-entities.yaml#/components/schemas/{Entity}Entity`

**Домены и их entities:**

- **Game Domain:** `game-entities.yaml` → `CharacterEntity`, `ItemEntity`, `WeaponEntity`, `CombatSessionEntity`
- **Economy Domain:** `economy-entities.yaml` → `WalletEntity`, `TransactionEntity`, `MarketplaceListingEntity`, `AuctionEntity`
- **Social Domain:** `social-entities.yaml` → `UserProfileEntity`, `FriendshipEntity`, `GuildEntity`, `ChatMessageEntity`
- **Infrastructure Domain:** `infrastructure-entities.yaml` → `UserAccountEntity`, `SessionEntity`, `AuditLogEntity`, `ConfigurationEntity`
- **Seasonal Challenges:** `seasonal-challenges-entities.yaml` → `SeasonEntity`, `ChallengeEntity`, `PlayerProgressEntity`

### Основные Домены
1. **System:** `auth`, `session`, `profile` (Inherits: `infrastructure-entities.yaml`)
2. **Game:** `combat`, `ability`, `movement` (Inherits: `game-entities.yaml`)
3. **Economy:** `trading`, `auction`, `currency` (Inherits: `economy-entities.yaml`)
4. **Social:** `guild`, `friend`, `chat` (Inherits: `social-entities.yaml`)
5. **World:** `location`, `city` (Inherits: `game-entities.yaml`)

## 3. 🔧 Правила Проектирования (Design Rules)

### Strict Typing & Constraints
- **All Fields:** Must have `type`, `example`, `description`.
- **Strings:** Define `minLength`, `maxLength`, `pattern`.
- **Integers:** Define `minimum`, `maximum`, `format` (`int32`/`int64`).
- **Enums:** Use for all fixed sets of values.
- **Arrays:** Use `maxItems` where possible (Fixed-size arrays preferred).

### Performance: Struct Alignment (Backend Optimization)
**GOAL:** Save 30-50% memory in Go structs.
**Rule:** Order fields from Largest to Smallest size.

1. **Large (16-24B):** `string`, `array`, `object` ($ref), `time`
2. **Medium (8B):** `int64`, `float64`, `uint64`
3. **Small (4B):** `int32`, `float32`, `enum` (if int based)
4. **Byte (1B):** `boolean`, `byte`

**Nullable:** AVOID `nullable: true` (creates pointer). Use `default` values instead.

### Concurrency: Optimistic Locking
Для операций обновления (PUT/PATCH) обязательна проверка версии.
- Используйте `common/operations/crud.yaml#/UpdateRequest`.
- Поле `version` (int) инкрементируется при каждом изменении.

## 4. 🔄 GitHub MCP Integration

**ВАЖНО:** Используй комбинированный подход: GH CLI для поиска, MCP GitHub для обновления статусов.

**Детальный workflow:** `@.cursor/AGENT_TASK_SEARCH_WORKFLOW.md`

### Поиск задач API Designer

**ШАГ 1: GH CLI для поиска**
```bash
# Поиск открытых задач
gh issue list --repo gc-lover/necpgame-monorepo --state open --limit 30 --json number,title,state

# Поиск по префиксу
gh issue list --repo gc-lover/necpgame-monorepo --state open | grep "\[API\]"
```

**ШАГ 2: MCP GitHub для деталей и обновления**
См. `@.cursor/AGENT_STATUS_CHANGE_GUIDE.md` для полных примеров.

### Взятие задач

**ТОЛЬКО через MCP GitHub!**
См. `@.cursor/AGENT_STATUS_CHANGE_GUIDE.md` - взятие задачи (In Progress).

### Передача задач

**ТОЛЬКО через MCP GitHub!**
См. `@.cursor/AGENT_STATUS_CHANGE_GUIDE.md` - передача задачи (Todo + следующий агент).

**Field IDs:** `@.cursor/GITHUB_PROJECT_FIELD_IDS.md`

## 5. 🚀 Workflow

### Алгоритм работы
1. **НАЙТИ:** GH CLI для поиска открытых задач (см. `@.cursor/AGENT_TASK_SEARCH_WORKFLOW.md`)
2. **ВЗЯТЬ:** MCP GitHub для изменения статуса на In Progress (см. `@.cursor/AGENT_STATUS_CHANGE_GUIDE.md`)
3. **АНАЛИЗ:**
   - Если Protocol == `OpenAPI 3.0` → Work.
   - Если Protocol == `Protobuf` (Real-time) → Pass to `Network` Agent.
4. **РАБОТАТЬ:**
   - Создать/Обновить YAML в `proto/openapi/{domain}/{service}/`.
   - Применить Domain Separation.
   - Валидировать (Validation).
5. **ПЕРЕДАТЬ:** MCP GitHub для изменения статуса на Todo и назначения следующего агента (см. `@.cursor/AGENT_STATUS_CHANGE_GUIDE.md`)

### Инструменты Валидации (ОБЯЗАТЕЛЬНО)
```bash
# 1. Linting & Validation
redocly lint proto/openapi/{domain}/{service}/main.yaml
python scripts/tools/openapi/validate-domains-openapi.py --domain {domain}

# 2. Optimization (Struct Alignment)
python scripts/batch-optimize-openapi-struct-alignment.py proto/openapi/{domain}/main.yaml
```

## 6. Шаблоны (ОБЯЗАТЕЛЬНО использовать!)

**Все новые сервисы создавайте из шаблона для единообразия:**

- **OpenAPI Service:** `.cursor/templates/openapi-service-template.yaml`
- **Enterprise Template:** `proto/openapi/example/main.yaml` (детальный пример)

**Использование:**
```bash
# Создание нового сервиса
mkdir -p proto/openapi/{service-name}-service
cp .cursor/templates/openapi-service-template.yaml proto/openapi/{service-name}-service/main.yaml

# Заменить placeholders:
# {ServiceName} → ServiceName
# {ServiceDomain} → Domain
# {service-name} → service-name
# {domain} → appropriate-domain (game | economy | social | infrastructure)

# Затем добавить специфичные endpoints и схемы
```

**Документация:**
- `.cursor/templates/README.md` - краткое руководство по шаблонам
- `proto/openapi/example/README.md` - детальное руководство по enterprise-grade шаблону

## 7. File Constraints & Logic
- **Source Limits:** YAML файлы исходников < 1000 строк. Если больше — разбивать на `$ref`.
- **Generated Files:** `openapi-bundled.yaml` и `oas_*_gen.go` МОГУТ быть большими (исключены из лимитов через `.githooks`).
- **Tools:** Используй `scripts/` для массовой генерации/валидации.
