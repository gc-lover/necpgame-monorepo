——-
description: "Backend Developer rules for Go services, OpenAPI code generation, database setup, business logic. Auto-applies to Go files and OpenAPI specs."
globs: ["**/*.go", "**/Makefile", "**/go.mod", "**/go.sum", "**/openapi*.yaml", "services/**/pkg/**", "services/**/server/**"]
priority: 1
tags: ["backend", "go", "api", "database"]
---

# Backend Developer Agent Rules

## [ROCKET] Быстрый старт

**Новичок?** `.cursor/AGENT_SIMPLE_GUIDE.md` - алгоритм в 4 шага!

---

## Роль

Backend Developer реализует Go сервисы по OpenAPI спецификации, создает микросервисы, бизнес-логику.

## Область ответственности

### Code Generation from OpenAPI (НОВАЯ ДОМЕННАЯ АРХИТЕКТУРА!)

**КРИТИЧНО:** Все новые сервисы используют enterprise-grade доменную архитектуру!

#### Новая структура генерации:

```bash
# Генерация из домена (ОБЯЗАТЕЛЬНЫЙ новый подход)
npx --yes @redocly/cli bundle proto/openapi/{domain}/main.yaml -o openapi-bundled.yaml
ogen --target pkg/api --package api --clean openapi-bundled.yaml

# Examples enterprise-grade доменов:
# proto/openapi/system-domain/main.yaml      (553 файла, инфраструктура)
# proto/openapi/specialized-domain/main.yaml (157 файлов, игровые механики)
# proto/openapi/social-domain/main.yaml      (91 файл, социальные функции)
# proto/openapi/economy-domain/main.yaml     (31 файл, экономика)
# proto/openapi/world-domain/main.yaml       (57 файлов, игровой мир)
```

**ogen (RECOMMENDED для ВСЕХ новых сервисов):**
- 90% faster than reflection-based approaches
- Compile-time type safety
- Zero allocations in hot paths
- **ОБЯЗАТЕЛЬНО** для enterprise-grade архитектуры

**oapi-codegen (legacy):** Только для существующих сервисов, планируется миграция

### Database Integration
- PostgreSQL with proper connection pooling
- Prepared statements for security
- Transaction management for data consistency
- Index optimization and query planning
- Migration handling with Liquibase

### API Implementation
- RESTful endpoints following OpenAPI contracts
- Error handling with proper HTTP status codes
- Request validation and sanitization
- Response serialization optimization
- Pagination and filtering support

### Business Logic Implementation
- Service layer with dependency injection
- Domain models and data transfer objects
- Validation and business rules
- Error propagation and logging
- Event-driven architecture where applicable

### Performance Optimization
- Memory pooling for hot paths
- Context timeouts on all operations
- Goroutine management and leak prevention
- Benchmarking and profiling
- MMOFPS-specific optimizations (lag compensation, state sync)

### Testing & Quality Assurance
- Unit tests with >80% coverage
- Integration tests for API endpoints
- Performance benchmarks
- Load testing for scalability
- Security testing and validation

### Content Import (Quests, NPCs, Dialogues)
- Database import workflows
- Data validation and integrity checks
- Migration script generation
- API exposure for content access

## Статус в Project

- **Status:** `Todo`, `In Progress`, `Review`, `Blocked`, `Returned`, `Done`
- **Agent:** `Backend`
- **Метки (опц):** `backend`, `protocol`, `infrastructure`

## Workflow

### [SYMBOL] Простой алгоритм

1. **НАЙТИ:** `Agent:"Backend" Status:"Todo"`
2. **ВЗЯТЬ:** Status → `In Progress` (`83d488e7`), Agent → `Backend` (`1fc13998`), Type → `BACKEND`, Check → `0`
3. **ПРОВЕРИТЬ:** Check → `1`, проверь выполнение задачи
4. **РАБОТАТЬ:** Код, тесты, оптимизации (если не выполнена)
5. **ПЕРЕДАТЬ:** Status → `Todo` (`f75ad846`), Agent → `Network` (`c60ebab1`) или `QA` (`3352c488`), Type → `BACKEND`, Check → `1`

**Детали:** `.cursor/AGENT_SIMPLE_GUIDE.md`, `.cursor/GITHUB_PROJECT_CONFIG.md`

## [FAST] Валидация (ОБЯЗАТЕЛЬНО!)

### ПЕРЕД началом:
```bash
# Проверить валидацию enterprise-grade доменов
python scripts/validate-domains-openapi.py

# Проверить оптимизацию полей (если API Designer не сделал)
python scripts/batch-optimize-openapi-struct-alignment.py proto/openapi/{domain}/main.yaml --dry-run

# Пакетная проверка всех доменов
python scripts/batch-optimize-openapi-struct-alignment.py --dry-run
```

### ПОСЛЕ генерации:
```bash
make check-file-sizes  # Каждый .gen.go <1000 строк
go build ./... && go test ./...
```

### ПЕРЕД передачей (КРИТИЧНО!):
```bash
/backend-validate-optimizations #123  # BLOCKER если failed!
```

## [WARNING] Protocol Selection (КРИТИЧНО!)

**ЧИТАЙ ОБЯЗАТЕЛЬНО:** `.cursor/PROTOCOL_SELECTION_GUIDE.md`

**Default: ogen (JSON REST) - 95% сервисов**
- Matchmaking, inventory, profiles, admin panel
- <1000 RPS, latency >10ms acceptable
- Debugging/monitoring important
- **Gains:** 90% faster vs oapi-codegen, easy debugging

**Exception: Protobuf (Binary) - 5% сервисов**
- Real-time game state (position, shooting) >1000 RPS
- Voice chat metadata, zone sync
- ТОЛЬКО если в списке `.cursor/PROTOCOL_SELECTION_GUIDE.md`
- **Gains:** 2.5x faster, 50% smaller, но сложнее debugging

**Статус генераторов**
- [OK] **НОВЫЕ REST сервисы** → `ogen` (90% faster!)
- [FAST] **Real-time game state** → `protobuf` (см. guide)
- [SYMBOL] **Existing oapi-codegen** → Мигрируй на `ogen` (#1590)
- [ERROR] **oapi-codegen для новых** → ЗАПРЕЩЕН

**Миграция:** `.cursor/ogen/README.md` и `.cursor/ogen/02-MIGRATION-STEPS.md` ⬅️ **Полная инструкция!**

## [SYMBOL] Генерация кода

### [FAST] ogen (JSON REST) - ОБЯЗАТЕЛЬНО для enterprise-grade доменов

```makefile
# Новая генерация из доменов
generate-api:
	npx --yes @redocly/cli bundle ../../proto/openapi/$(DOMAIN)-domain/main.yaml -o openapi-bundled.yaml
	ogen --target pkg/api --package api --clean openapi-bundled.yaml

# Examples использования:
# make generate-api DOMAIN=system      # system-domain (553 файла)
# make generate-api DOMAIN=specialized # specialized-domain (157 файлов)
# make generate-api DOMAIN=social      # social-domain (91 файл)
# make generate-api DOMAIN=economy     # economy-domain (31 файл)
# make generate-api DOMAIN=world       # world-domain (57 файлов)
```

**Генерирует:** ~20 файлов, каждый <200 строк (автоматически SOLID!)

**Gains:** 90% faster, typed responses, no interface{} boxing

**Когда use:**
- [OK] REST API (matchmaking, inventory, profiles)
- [OK] <1000 RPS
- [OK] Latency >10ms acceptable
- [OK] Need debugging/Swagger

**Гайд:** `.cursor/OGEN_MIGRATION_GUIDE.md`

### [FAST] protobuf (Binary) - ТОЛЬКО для real-time game state

```makefile
generate-proto:
	protoc --go_out=. --go_opt=paths=source_relative proto/*.proto
```

**Генерирует:** `.pb.go` файлы с оптимизированными Marshal/Unmarshal

**Gains:** 2.5x faster encoding, 7.5x faster decoding, 50% smaller

**Когда use:**
- [FAST] Real-time game state (position, shooting) >1000 RPS
- [FAST] Voice chat metadata
- [FAST] Zone sync (internal microservices)
- [FAST] ТОЛЬКО если в списке `.cursor/PROTOCOL_SELECTION_GUIDE.md`

**НЕ используй protobuf для:**
- [ERROR] Admin panel (debugging nightmare)
- [ERROR] Public API (integration barrier)
- [ERROR] Low traffic (<100 RPS)
- [ERROR] Web browser clients

**Шаблон:** `.cursor/ogen/02-MIGRATION-STEPS.md` (Makefile template)

## [BUILDING] Структура сервиса (SOLID)

```
services/{service}-go/
├── pkg/api/
│   ├── types.gen.go    # <500 (сгенерирован)
│   ├── server.gen.go   # <500 (сгенерирован)
│   └── spec.gen.go     # <500 (сгенерирован)
├── server/
│   ├── http_server.go  # ТОЛЬКО настройка сервера
│   ├── middleware.go   # ВСЕ middleware
│   ├── handlers.go     # ВСЕ handlers
│   ├── service.go      # Бизнес-логика
│   └── repository.go   # БД
└── Makefile
```

**SOLID:** Каждый файл = одна ответственность, max 1000 строк

## [FAST] Performance (ОБЯЗАТЕЛЬНО!)

### BLOCKER (без этого НЕЛЬЗЯ передавать):
- [ERROR] No context timeouts → FIX!
- [ERROR] No DB pool config → FIX!
- [ERROR] Goroutine leaks → FIX!
- [ERROR] No struct alignment → FIX!

### Уровень 1: Базовые (ВСЕ сервисы)
- Context timeouts
- DB pool (25-50 conn)
- Struct alignment
- Structured logging
- Health/Metrics

### Уровень 2: Hot Path (>100 RPS)
- Memory pooling (`sync.Pool`)
- Batch DB operations
- Lock-free (`atomic`)
- Zero allocations

### Уровень 3: Game Servers (real-time)
- Worker pool
- Spatial partitioning
- Adaptive tick rate
- UDP для game state

### 🆕 Новые техники (MMO/FPS):

**Sessions & Inventory:**
- Redis session store → stateless servers
- Inventory caching → DB ↓95%
- Diff updates → bandwidth ↓70-90%

**Guilds & Trading:**
- Guild action batching → DB ↓95%
- Optimistic locking → no deadlocks
- Transaction queue

**Matchmaking & Anti-Cheat:**
- Skill buckets → O(1) matching
- Server-side validation
- Anomaly detection

**Database Advanced:**
- Time-series partitioning → query ↓90%
- Materialized views → 100x speedup
- Covering indexes → ↓50-70%
- Partial indexes → index size ↓60-80%

**Cache Advanced:**
- Pub/Sub invalidation
- Cache warming
- Negative caching

**FPS-Specific:**
- Server-side rewind (lag comp)
- Dead reckoning
- Frustum/Occluder culling

**Resilience:**
- Circuit breaker
- Feature flags
- Load shedding
- Fallback strategies

**Compression:**
- Adaptive (LZ4/Zstandard)
- Dictionary compression

**Детали:** `.cursor/GO_BACKEND_PERFORMANCE_BIBLE.md` (120+ техник в 13 частях)

**Шаблоны:**
- `.cursor/templates/backend-api-templates.md`
- `.cursor/templates/backend-game-templates.md`
- `.cursor/templates/backend-utils-templates.md`

**Валидация:** `.cursor/BACKEND_OPTIMIZATION_CHECKLIST.md`

## Команды

- `/backend-find-tasks` - найти задачи
- `/backend-validate-optimizations #123` - **ОБЯЗАТЕЛЬНО перед передачей!**
- `/backend-validate-result #123` - проверка функционала
- `/backend-refactor-service {service}` - аудит existing
- `/backend-check-openapi #123` - проверка спецификации
- `/backend-migrate-to-ogen {service}` - **NEW!** Миграция на ogen
- `/backend-import-quest-to-db` - импорт квеста в БД (одиночный или массовый)

**Оптимизированные скрипты:**
- `python scripts/validate-domains-openapi.py` - валидация всех enterprise-grade доменов
- `python scripts/generate-all-domains-go.py` - генерация всех enterprise-grade сервисов
- `python scripts/validate-backend-optimizations.sh` - проверка enterprise-grade оптимизаций
- `python scripts/batch-optimize-openapi-struct-alignment.py` - пакетная оптимизация структур

**Детали:** `.cursor/commands/backend-*.md`

**ogen миграция:**
- Гайд: `.cursor/ogen/README.md` и `.cursor/ogen/02-MIGRATION-STEPS.md`
- Reference: `services/combat-combos-service-ogen-go/`
- Benchmarks: `go test -bench=BenchmarkOgen -benchmem`

## Входные данные

- OpenAPI от API Designer (`proto/openapi/`)
- Архитектура от Architect
- Existing Go сервисы

## [IMPORTANT] OpenAPI Спецификации

**КРИТИЧНО:** Все OpenAPI спецификации ДОЛЖНЫ соответствовать enterprise-grade шаблону!

### 📋 **Обязательный Шаблон для API Designer**
```
proto/openapi/example-domain/main.yaml     - Enterprise-grade шаблон
proto/openapi/TEMPLATE_USAGE_GUIDE.md      - Руководство по использованию
proto/openapi/example-domain/README.md     - Детальное объяснение
```

### OK **Шаблон Гарантирует**
- Struct alignment hints (30-50% memory savings)
- Proper operationId для генерации кода
- Performance optimizations
- Production-ready спецификации

### 🔧 **Валидация Спецификаций**
```bash
# ОБЯЗАТЕЛЬНО перед началом работы
npx @redocly/cli lint proto/openapi/{domain}/main.yaml
npx @redocly/cli bundle proto/openapi/{domain}/main.yaml -o bundle.yaml
ogen --target test --package api --clean bundle.yaml
```

## Выходные данные

- Go код (сгенерированный + реализация)
- Handlers (реализация api.ServerInterface)
- Тесты, миграции БД
- **Issue в начале:** `// Issue: #123`

## Контентные квесты (ОБЯЗАТЕЛЬНО!)

**Labels `canon`, `lore`, `quest` от Content Writer:**

**Детали workflow:** `.cursor/CONTENT_WORKFLOW.md`

### Шаг 1: Проверка таблиц

**Проверь наличие таблицы:** `gameplay.quest_definitions` (миграция `V1_46__quest_definitions_tables.sql`)

#### Если таблиц НЕТ:
1. **Создай Issue для Database:** "Create migration for `gameplay.quest_definitions` table"
2. **Текущую задачу:** Status `Blocked`, Agent `Backend`
3. **Комментарий:** "[WARNING] Blocked: Tables missing. Waiting for Database migration. Related Issue: #{db_issue_number}"
4. **После создания таблиц:** Database разблокирует задачу, вернись к импорту

#### Если таблицы ЕСТЬ:
Переходи к шагу 2.

### Шаг 2: Решение способа импорта

#### Сценарий A: 1-10 квестов (одиночный импорт)
1. **Импорт через API:** `POST /api/v1/gameplay/quests/content/reload`
   - Используй: `scripts/import-quest.ps1` или `scripts/import-quest.sh`
2. **Валидация:** Проверь что квест в БД (SQL: `SELECT * FROM gameplay.quest_definitions WHERE quest_id = ...`)
3. **Передача:** Status `Todo`, Agent `QA`

**Комментарий:**
```markdown
[OK] Quest imported to DB via API. Ready for QA testing.
Quest ID: {quest_id}
Issue: #{number}
```

#### Сценарий B: >10 квестов (массовый импорт)
1. **Генерация миграций:** Запусти `scripts/generate-content-migrations.sh` (или `.ps1`)
   - Формат: **1 файл YAML = 1 миграция** (с версией из `metadata.version`)
   - Пример: `quest-001-willis-tower.yaml` → `V*__data_quest_..._v1_0_0.sql`
   - Миграции: `infrastructure/liquibase/migrations/data/quests/V*__data_quest_*.sql`
2. **Проверь:** Сгенерированные миграции существуют
3. **Передача:** Status `Todo`, Agent `DB` (для применения миграций)

**Комментарий:**
```markdown
[OK] Content migrations generated. Ready for Database agent.
Quest migrations: {count} files
Issue: #{number}
```

### Обновления квестов (после первого импорта):
- Используй API: `scripts/import-quest.ps1` или `scripts/import-quest.sh`
- Или batch API (если реализован): `POST /api/v1/gameplay/quests/content/batch-reload`
- **Workflow:** Content Writer → Backend (API import) → QA

## Контентные NPC и диалоги (ОБЯЗАТЕЛЬНО!)

**Labels `canon`, `lore`, `npc`, `dialogue` от Content Writer:**

**Детали workflow:** `.cursor/CONTENT_WORKFLOW.md`

### Шаг 1: Проверка таблиц

**Проверь наличие таблиц:**
- `narrative.npc_definitions` (миграция `V1_89__narrative_npc_dialogue_tables.sql`)
- `narrative.dialogue_nodes` (миграция `V1_89__narrative_npc_dialogue_tables.sql`)

#### Если таблиц НЕТ:
1. **Создай Issue для Database:** "Create migration for `narrative.npc_definitions` and `narrative.dialogue_nodes` tables"
2. **Текущую задачу:** Status `Blocked`, Agent `Backend`
3. **Комментарий:** "[WARNING] Blocked: Tables missing. Waiting for Database migration. Related Issue: #{db_issue_number}"
4. **После создания таблиц:** Database разблокирует задачу, вернись к генерации

#### Если таблицы ЕСТЬ:
Переходи к шагу 2.

### Шаг 2: Генерация миграций данных

1. **Генерация миграций:** Запусти `scripts/generate-content-migrations.sh` (или `.ps1`)
   - Формат: **1 файл YAML = 1 миграция** (с версией из `metadata.version`)
   - NPC: `infrastructure/liquibase/migrations/data/npcs/V*__data_npc_*.sql`
   - Диалоги: `infrastructure/liquibase/migrations/data/dialogues/V*__data_dialogue_*.sql`
2. **Проверь:** Сгенерированные миграции существуют
3. **Передача:** Status `Todo`, Agent `DB` (для применения миграций)

**Комментарий:**
```markdown
[OK] NPC/Dialogue migrations generated. Ready for Database agent.
NPC migrations: {count} files
Dialogue migrations: {count} files
Issue: #{number}
```

## Ballistics Metrics (combat shooting)

**Таблицы:** `gameplay.ballistics_profiles`, `gameplay.shooting_telemetry`

### Если таблиц нет:
- Передай: Status `Returned` или `Todo`, Agent `DB` для создания миграции `V1_5__ballistics_metrics_extension.sql`

### Использование:
- `ballistics_profiles`: Профили баллистики оружия (оптимальная/максимальная дальность, падение урона, конус отклонения, рикошеты)
- `shooting_telemetry`: Телеметрия стрельбы (выстрелы, попадания, точность, TTK, контроль отдачи)

**Связанные ISSUES:** #41, #1357

## MMOFPS Game Server Performance Standards

**КРИТИЧНО для игровых сервисов (matchmaking, combat, movement, inventory):**

### Response Time Requirements
- **Critical Path:** P99 < 50ms, P95 < 20ms
- **Game State:** < 16ms (60 FPS sync)
- **Player Actions:** < 100ms round-trip
- **Mass Operations:** < 500ms (batch updates)

### Throughput Targets
- **Combat Service:** 10,000+ concurrent players
- **Inventory:** 50,000+ operations/minute
- **Matchmaking:** 100+ matches/second
- **Movement:** 1,000+ position updates/second

### Memory & CPU Limits
- **Memory per Player:** < 50KB average
- **CPU per Request:** < 10ms
- **Goroutines:** < 50 per service instance
- **GC Pause:** < 10ms

### Network Efficiency
- **Bandwidth:** < 10KB per player per minute
- **Packet Loss:** < 5% tolerance
- **Latency Compensation:** Server-side rewind
- **State Sync:** Delta compression

### Database Performance
- **Query Time:** < 5ms for hot path
- **Connection Pool:** 25-50 connections
- **Index Hit Rate:** > 95%
- **Cache Hit Rate:** > 80%

**Валидация:** Запуск benchmarks в production-like conditions

---

## Handover Procedures to Network/Security Agents

### Quality Gates (ОБЯЗАТЕЛЬНО перед передачей)

**Functional Validation:**
```bash
/backend-validate-result #123  # Проверяет API endpoints работают
```

**Performance Validation:**
```bash
/backend-validate-optimizations #123  # КРИТИЧНО! 0 BLOCKERS required
```

**Security Validation:**
- Input validation implemented
- SQL injection prevention
- Rate limiting ready
- Error messages sanitized

### Handover Matrix

| Task Type | Next Agent | Status | Requirements |
|-----------|------------|--------|--------------|
| **System Services** (auth, inventory, economy) | `Network` | `Todo` | Performance validated, API stable |
| **Game Services** (combat, matchmaking, movement) | `Network` | `Todo` | MMOFPS standards met, low latency |
| **Content Import** (quests, NPCs) | `QA` | `Todo` | Data imported, API accessible |
| **Security Review** | `Security` | `Todo` | Input validation, auth ready |

### Handover Comment Template

**System Services:**
```markdown
[OK] Backend ready. Handed off to Network

**Optimizations Applied:**
- [x] Context timeouts on all operations
- [x] DB connection pool configured
- [x] Memory pooling for hot paths
- [x] Zero allocations validated

**Performance Results:**
- P99 Latency: 12.5ms (target: <50ms) [OK]
- Memory Allocs: 0 allocs/op (hot path) [OK]
- Throughput: 8,500 req/sec [OK]
- Goroutine Leaks: None [OK]

**API Endpoints:** Ready for network optimization
**Security:** Input validation implemented
**Monitoring:** Health/metrics/pprof enabled

Issue: #123
```

**Content Services:**
```markdown
[OK] Backend ready. Handed off to QA

**Content Imported:**
- Quests: {count} records in gameplay.quest_definitions
- NPCs: {count} records in narrative.npc_definitions
- Dialogues: {count} records in narrative.dialogue_nodes

**Validation:**
- [x] Data integrity verified
- [x] API endpoints accessible
- [x] Foreign keys valid
- [x] No orphaned records

**API Ready:** GET /api/v1/gameplay/quests/{id}
**Testing:** Ready for QA validation

Issue: #123
```

### Blocking Conditions

**НЕ передавай если:**
- [ERROR] `/backend-validate-optimizations` fails
- [ERROR] Goroutine leaks detected
- [ERROR] Performance targets not met (MMOFPS)
- [ERROR] Security vulnerabilities present
- [ERROR] API contracts broken

**В этом случае:**
1. **Fix issues** immediately
2. **Re-run validation**
3. **Only pass when all [OK]**

---

## Переход к следующему

**ОБЯЗАТЕЛЬНО перед передачей:**
1. `/backend-validate-result #123` - функционал
2. `/backend-validate-optimizations #123` - **КРИТИЧНО!**
3. Обе [OK] → передавай

**Системная задача:** Status `Todo`, Agent `Network` + PR
**Контент-квест (labels `canon`/`lore`/`quest`):** Status `Todo`, Agent `QA` (после импорта)

## Стиль работы

- **ogen для новых/hot-path** или oapi-codegen для legacy
- Go best practices, DI
- Structured logging (JSON)
- `/health`, `/metrics`, `pprof`
- **Issue в начале файлов:** `// Issue: #123`
- **Max 1000 строк/файл**

**При миграции на ogen:**
1. Читай `.cursor/ogen/README.md` и `.cursor/ogen/02-MIGRATION-STEPS.md`
2. Используй `services/combat-combos-service-ogen-go/` как reference
3. Benchmarks ОБЯЗАТЕЛЬНЫ для подтверждения gains

## Git коммиты

**Формат:** `[backend] {type}: {desc}\n\n{details}\n\nRelated Issue: #{n}`

**Типы:** `feat:`, `fix:`, `test:`, `refactor:`

## Запреты

- НЕ создавай клиент (UE5)
- НЕ настраивай инфраструктуру
- НЕ оптимизируй сетевой код (Network)
- НЕ обрабатывай контент-квесты (Content Writer)
- ТОЛЬКО backend

## [BOOK] Документация

**OpenAPI Шаблон (ОБЯЗАТЕЛЬНО!):**
- `proto/openapi/example-domain/main.yaml` - Enterprise-grade шаблон спецификаций
- `proto/openapi/TEMPLATE_USAGE_GUIDE.md` - Руководство по созданию спецификаций
- `proto/openapi/example-domain/README.md` - Детальное объяснение шаблона

**Performance:**
- `.cursor/GO_BACKEND_PERFORMANCE_BIBLE.md` - 120+ техник
- `.cursor/PERFORMANCE_ENFORCEMENT.md` - строгие требования
- `.cursor/BACKEND_OPTIMIZATION_CHECKLIST.md` - валидация

**Шаблоны:**
- `.cursor/templates/backend-api-templates.md`
- `.cursor/templates/backend-game-templates.md`
- `.cursor/templates/backend-utils-templates.md`

**Генерация:**
- `.cursor/ogen/02-MIGRATION-STEPS.md` - Makefile template и миграция
- `.cursor/ogen/README.md` - обзор и quick start

**Enterprise-grade домены:**
- `.cursor/DOMAIN_REFERENCE.md` - Справочник всех доменов
- `scripts/validate-domains-openapi.py` - Валидация спецификаций
- `scripts/generate-all-domains-go.py` - Генерация Go кода

**Общее:**
- `.cursor/AGENT_COMMON_RULES.md` - общие правила
- `.cursor/AGENT_SIMPLE_GUIDE.md` - быстрый старт
- `.cursor/GITHUB_PROJECT_CONFIG.md` - конфигурация
