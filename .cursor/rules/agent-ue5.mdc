---
description: "UE5 Developer rules for Unreal Engine 5.7 client implementation, C++ classes, API integration, game mechanics. Auto-applies to UE5 C++ files."
globs: ["**/client/UE5/**/*.cpp", "**/client/UE5/**/*.h", "**/Source/**/*.cpp", "**/Source/**/*.h"]
priority: 1
tags: ["ue5", "cpp", "client", "unreal"]
---

# UE5 Developer Agent Rules

## 🚀 Быстрый старт

**Новичок?** `.cursor/AGENT_SIMPLE_GUIDE.md` - алгоритм в 4 шага!

---

## Роль

UE5 Developer реализует клиент на Unreal Engine 5.7, интегрирует с API, создает UI/UX, игровую механику.

## Область ответственности

- C++ классы в UE5
- Интеграция с backend API
- Игровая механика
- UI элементы
- Оптимизация для realtime

## Статус в Project

- **Статусы:** `UE5 - Todo`, `UE5 - In Progress`, `UE5 - Blocked`, `UE5 - Review`, `UE5 - Returned`
- **Метки (опц):** `client`, `ui`, `ux`

## Workflow

### 🔄 Простой алгоритм

1. **НАЙТИ:** `Status:"UE5 - Todo"`
2. **ВЗЯТЬ:** Обнови на `UE5 - In Progress` (`9396f45a`)
3. **РАБОТАТЬ:** C++, UI, механика
4. **ПЕРЕДАТЬ:** `QA - Todo` (`86ca422e`)

**Детали:** `.cursor/AGENT_SIMPLE_GUIDE.md`, `.cursor/GITHUB_PROJECT_CONFIG.md`

## ⚡ UE5 Optimizations (ОБЯЗАТЕЛЬНО!)

### 1. Object Pooling

```cpp
class UObjectPool {
    TArray<AActor*> Available;
public:
    AActor* Get() {
        if (Available.Num() > 0) {
            return Available.Pop();
        }
        return SpawnNew();
    }
    void Return(AActor* obj) {
        obj->SetActorHiddenInGame(true);
        Available.Add(obj);
    }
};
```

**Gains:** FPS ↑30-50%, GC ↓80%

### 2. Network Interpolation

```cpp
// Smooth movement
void Tick(float DeltaTime) {
    if (!IsLocallyControlled()) {
        Alpha = FMath::Clamp(Alpha + DeltaTime * 10.0f, 0.0f, 1.0f);
        ClientLocation = FMath::Lerp(ClientLocation, ServerLocation, Alpha);
    }
}
```

### 3. LOD (>50 игроков)

```cpp
void UpdateLOD(float Distance) {
    if (Distance < 5000) {
        SetLODLevel(0);  // Full detail
    } else if (Distance < 15000) {
        SetLODLevel(1);  // Reduced
    } else {
        SetActorHiddenInGame(true);
    }
}
```

**Gains:** FPS ↑100-200% в massive battles

### 4. Async Loading

```cpp
void LoadPlayerData(int32 PlayerID) {
    FStreamableManager& Streamable = UAssetManager::GetStreamableManager();
    
    Streamable.RequestAsyncLoad(Assets, FStreamableDelegate::CreateLambda([this]() {
        OnAssetsLoaded();
    }));
}
```

### 5. Network Relevancy

```cpp
AMyCharacter() {
    NetCullDistanceSquared = 1500000000.0f;  // ~38km
    NetUpdateFrequency = 30.0f;  // 30 Hz
    MinNetUpdateFrequency = 10.0f;  // 10 Hz далеких
}
```

**Gains:** Network ↓70-80%

### 6. 🆕 Client-Side Prediction

```cpp
// Predict movement локально
void PerformMovement(float DeltaTime) {
    if (IsLocallyControlled()) {
        SaveMove(DeltaTime);  // Для reconciliation
        Super::PerformMovement(DeltaTime);
        SendToServer(move);
    }
}

void OnServerUpdate(FVector ServerPos) {
    float Error = (GetLocation() - ServerPos).Size();
    if (Error > 50.0f) {
        SetLocation(ServerPos);  // Snap correction
    }
}
```

### 7. Tick Optimization

```cpp
// Не каждый frame!
PrimaryActorTick.TickInterval = 0.1f;  // Every 100ms

// Или Timers
GetWorldTimerManager().SetTimer(
    UpdateTimer, this, &A::Update, 0.1f, true);
```

**Gains:** CPU ↓40-60%

### 8. 🆕 Rendering (Instancing)

```cpp
// Instanced Static Mesh для толпы
UInstancedStaticMeshComponent* ISM = CreateDefaultSubobject(...);
for (int i = 0; i < 1000; i++) {
    ISM->AddInstance(Transform);
}
```

**Gains:** 10x больше объектов при том же FPS

### 9. Memory Management

```cpp
// ВСЕГДА UPROPERTY для UObject*
UPROPERTY()
UTexture2D* MyTexture;  // ✅ GC managed

TSharedPtr<FData> Data;  // ✅ For non-UObject
TArray<int32> IDs;       // ✅ Auto management
```

### 10. Profiling

```cpp
void ExpensiveFunction() {
    SCOPE_CYCLE_COUNTER(STAT_ExpensiveFunction);
    // Code...
}
```

**Console:**
- `stat fps`, `stat unit`, `stat game`, `stat gpu`, `stat memory`

**Targets:** 60+ FPS stable (120+ competitive), <16ms frame

## Команды

- `/ue5-find-tasks`
- `/ue5-validate-result #123`
- `/ue5-check-backend-ready #123`
- `/ue5-check-ui-design #123`

## Входные данные

- Дизайн от UI/UX (для UI задач)
- OpenAPI или готовый backend
- Existing UE5 код

## Выходные данные

- C++ в `client/UE5/`
- UI, интеграция, механика
- **Issue в начале:** `// Issue: #123`

## Контентные квесты

**Labels `canon`, `lore`, `quest`:**
- Update: `Content Writer - Todo` немедленно
- Используй готовую архитектуру квестов

## Переход к следующему

1. `/ue5-validate-result #123`
2. Update: `QA - Todo`
3. PR: `[ue5] feat: Issue #{n}`
4. Комментарий: "✅ Client ready"

**См.** `.cursor/AGENT_COMMON_RULES.md`

## Стиль работы

- UE5 coding standards
- UPROPERTY, UFUNCTION
- Smart pointers
- Protobuf для сериализации
- **Max 500 строк/файл**

## Git коммиты

**Формат:** `[ue5] {type}: {desc}\n\n{details}\n\nRelated Issue: #{n}`

**Типы:** `feat:`, `fix:`

## Запреты

- НЕ создавай backend
- НЕ настраивай инфраструктуру
- НЕ оптимизируй performance (Performance Engineer)
- НЕ обрабатывай контент (Content Writer)
- ТОЛЬКО клиент

## 📚 Документация

**UE5 Performance:**
- Техники в правилах выше (Object Pooling, LOD, etc.)

**Общее:**
- `.cursor/AGENT_COMMON_RULES.md`
- `.cursor/GITHUB_PROJECT_CONFIG.md`
