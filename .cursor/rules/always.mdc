---
description: "Critical global rules: no reports/summaries, code-focused communication, backend performance requirements. Always applies to all files."
alwaysApply: true
priority: 0
tags: ["global", "communication", "performance"]
---

## WARNING КРИТИЧЕСКИ ВАЖНО: Коммуникация и отчеты

**НИКОГДА НЕ ДЕЛАЙ:**
- ❌ НЕ создавай отчеты, summary, анализы, обзоры
- ❌ НЕ пиши длинные объяснения - только краткие факты
- ❌ НЕ создавай markdown файлы с отчетами/анализом
- ❌ НЕ делай "summary of changes" - просто делай изменения
- ❌ НЕ пиши "Overview", "Analysis", "Report" документы
- ❌ НЕ создавай таблицы статистики без запроса
- ❌ НЕ делай verbose комментарии - только необходимое

**ВСЕГДА ДЕЛАЙ:**
- OK Работай с кодом напрямую
- OK Краткие ответы (1-2 предложения максимум)
- OK Показывай только код и изменения
- OK Минимум текста, максимум действий
- OK Фокус на реализации, не на описании

**Формат комментариев при передаче задачи:**
```markdown
OK Ready. Handed off to {NextAgent}
Issue: #{number}
```

**НЕ пиши:**
- "Summary of work completed..."
- "Analysis shows..."
- "Overview of changes..."
- "Report on progress..."
- Длинные списки что сделано

**Пиши:**
- "OK Done. Issue: #123"
- "OK Ready. Issue: #123"

НИКОГДА НЕ ДЕЛАЙ ЭТО МУСОРНЫЕ РАЗНЫЕ MD ЕСЛИ ТЕБЯ НЕ ПРОСЯТ! ОБЩАЙСЯ ПРОСТО В ЧАТЕ!

## 📂 КРИТИЧЕСКИ ВАЖНО: Размещение файлов

**СТРОГО ЗАПРЕЩЕНО в корне проекта:**
- ❌ Промежуточные/тестовые файлы (`.py`, `.sh`, `.tmp`)
- ❌ Одноразовые скрипты для отладки
- ❌ Временные конфигурации
- ❌ Личные заметки/черновики
- ❌ Дубликаты файлов из других папок

**ОБЯЗАТЕЛЬНОЕ размещение файлов:**

### Backend код
- `services/{service-name}-go/` - Go микросервисы
- `proto/openapi/` - API спецификации

### Контент
- `knowledge/canon/` - YAML файлы квестов, NPC, лора

### Скрипты и автоматизация
- `scripts/` - валидация, импорт, генерация, развертывание

### Database
- `infrastructure/liquibase/migrations/` - SQL миграции

### Клиент
- `client/UE5/` - Unreal Engine код

### Документация
- `.cursor/` - правила агентов, гайды
- Корень: только `README.md`, `CHANGELOG*.md`, основные конфиги

**ПРАВИЛО:** Каждый файл должен быть в правильной папке по типу. Корень - только для высокоуровневой документации и основных конфигов.

## 🚨 КРИТИЧЕСКИ ВАЖНО: ЗАПРЕТ НА ОПАСНЫЕ GIT КОМАНДЫ

**АГЕНТАМ СТРОГО ЗАПРЕЩЕНО использовать следующие команды:**

### ❌ КАТЕГОРИЧЕСКИ ЗАПРЕЩЕННЫЕ КОМАНДЫ:
- `git reset --hard` - откатывает все изменения
- `git reset --soft` - откатывает коммиты
- `git reset --mixed` - откатывает индекс
- `git clean -fd` - удаляет неотслеживаемые файлы
- `git clean -fdx` - удаляет все неотслеживаемые файлы включая игнорируемые
- `git checkout --force` - принудительный чекаут
- `git branch -D` - принудительное удаление ветки
- `git push --force` - принудительный пуш
- `git rebase --abort` - прерывание ребейза (может потерять работу)
- `git stash drop` - удаление стэша без восстановления

### OK РАЗРЕШЕННЫЕ БЕЗОПАСНЫЕ КОМАНДЫ:
- `git add .` - добавление файлов в индекс
- `git commit -m "message"` - коммит изменений
- `git push` - обычный пуш
- `git pull` - обычный пул
- `git checkout branch` - переключение ветки (без --force)
- `git merge branch` - мерж ветки
- `git stash` - сохранение изменений
- `git stash pop` - восстановление стэша

### 🔍 ПРАВИЛА ИСПОЛЬЗОВАНИЯ GIT:

1. **НИКОГДА** не используй `--force`, `--hard`, `-D` флаги
2. **НИКОГДА** не удаляй коммиты, стэши или ветки без явного запроса
3. **ТОЛЬКО** безопасные операции: add, commit, push, pull, checkout
4. **Если сомневаешься** - НЕ используй команду, спроси в чате

### 🚨 НАРУШЕНИЕ = АВТОМАТИЧЕСКИЙ ВОЗВРАТ ЗАДАЧИ

**Использование запрещенной команды → задача возвращается с комментарием о нарушении безопасности!**

## Task Analysis

Always analyze task CONTENT (body, description, requirements), not just title or labels. Title and labels may be outdated or inaccurate from previous agents. If task content matches your responsibilities, work on it regardless of title/labels. Example: Backend agent should implement quest DB import even if title says "QUEST" - check if task requires database work.

## Issue Numbers in Files

**КРИТИЧНО:** Если в файле уже есть номер Issue, НЕ заменяй его - дописывай через запятую: `// Issue: #123, #234, #345` (в одну строку). Формат зависит от типа файла: `// Issue: #123` (Go), `# Issue: #123` (YAML/Python), `-- Issue: #123` (SQL), `<!-- Issue: #123 -->` (HTML/Markdown).

## Backend Performance Requirements

**Backend Agent ОБЯЗАН применять оптимизации для MMOFPS RPG:**

### BLOCKER (без этого задачу НЕЛЬЗЯ передавать):
- ❌ НЕТ context timeouts → FIX before handoff
- ❌ НЕТ DB pool config → FIX before handoff
- ❌ Goroutine leaks → FIX before handoff

### Validation ОБЯЗАТЕЛЬНА:
- Запускай `/backend-validate-optimizations #123` перед передачей
- Если BLOCKER → исправь и повтори
- Если OK → передавай кратко

**Детали:** `.cursor/BACKEND_OPTIMIZATION_CHECKLIST.md`