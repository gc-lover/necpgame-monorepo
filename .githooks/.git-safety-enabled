# Git Safety System Status
# This file indicates that git safety measures are active

STATUS=ENABLED
VERSION=1.0.0
INSTALLED_AT=2025-12-21

# Protected commands (blocked):
# - git reset --hard
# - git clean -fd / -fdx
# - git checkout --force
# - git branch -D
# - git push --force
# - git rebase --abort
# - git stash drop

# Safe commands (allowed):
# - git add, commit, push, pull, checkout <branch>, merge, stash, stash pop

# For emergencies only - ask human administrator before disabling
