# 🛡️ Git Safety System

## Описание

Система защиты Git от опасных команд, которые могут привести к потере прогресса проекта. Предотвращает использование деструктивных команд агентами.

## 🚨 Запрещенные команды

Эти команды **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНЫ** для всех агентов:

- `git reset` - ЛЮБАЯ команда reset (даже без --hard может потерять работу)
- `git clean` - ЛЮБАЯ команда clean (даже с -n может удалить файлы)
- `git checkout --force` - принудительный чекаут с перезаписью локальных изменений
- `git branch -D` - принудительное удаление ветки
- `git push --force` - принудительный пуш с перезаписью удаленной ветки
- `git rebase --abort` - прерывание ребейза (может потерять работу)
- `git stash drop` - удаление стэша без восстановления

## OK Разрешенные команды

Эти команды **БЕЗОПАСНЫ** и разрешены:

- `git add <files>` - добавление файлов в индекс
- `git commit -m "message"` - коммит изменений
- `git push` - отправка коммитов на сервер
- `git pull` - получение изменений с сервера
- `git checkout <branch>` - переключение на ветку
- `git merge <branch>` - слияние ветки
- `git stash` - сохранение изменений
- `git stash pop` - восстановление сохраненных изменений

## 🛠️ Установка

### Автоматическая настройка (Windows)

```bash
# Запустить setup скрипт
.githooks\setup-git-safety.bat
```

### Ручная настройка

1. **Настроить Git hooks:**
```bash
git config core.hooksPath .githooks
```

2. **Добавить wrapper в PATH (опционально):**
```bash
# Для Windows - добавить в PATH папку .githooks\wrappers
# Для Linux/Mac - добавить в PATH папку .githooks/wrappers
```

## 🔧 Компоненты защиты

### 1. Pre-commit Hook (`.githooks/pre-commit`)
- Проверяет staged файлы на наличие следов опасных команд
- Блокирует коммит если найдены запрещенные паттерны

### 2. Pre-push Hook (`.githooks/pre-push`)
- Дополнительная проверка перед отправкой на сервер
- Проверяет сообщения коммитов в диапазоне пуша

### 3. Commit-msg Hook (`.githooks/commit-msg`)
- Проверяет сообщение коммита на упоминания опасных команд
- Предотвращает коммиты с запрещенными ссылками

### 4. Git Wrapper (`.githooks/wrappers/git-safe-wrapper.*`)
- Перехватывает вызовы git с опасными командами
- Показывает подробное объяснение почему команда заблокирована

## 🚨 Что происходит при нарушении

Если агент попытается использовать запрещенную команду:

1. **Команда блокируется** с подробным объяснением
2. **Показывается список** безопасных альтернатив
3. **Выводится предупреждение** о возможной потере данных
4. **Git операция прерывается** с кодом ошибки 1

## 📋 Примеры блокировки

```bash
$ git reset HEAD~1
🚨 DANGER: Git command BLOCKED!

Blocked command: git reset HEAD~1

This command can cause IRREVERSIBLE DATA LOSS!
It is FORBIDDEN for agents to prevent project damage.

OK SAFE git commands you can use:
  git add <files>           Stage files for commit
  git commit -m "message"   Commit staged changes
  git push                  Push to remote repository
  ...

$ git clean -n
🚨 DANGER: Git command BLOCKED!

Blocked command: git clean -n

This command can cause IRREVERSIBLE DATA LOSS!
It is FORBIDDEN for agents to prevent project damage.
```

## 🔧 Техническое обслуживание

### Проверка статуса защиты

```bash
# Проверить настройку hooks
git config core.hooksPath

# Проверить что hooks исполняемые
ls -la .githooks/
```

### Обновление правил

При добавлении новых запрещенных команд:
1. Обновить массивы в hooks и wrappers
2. Перезапустить setup скрипт
3. Проверить работу на тестовых командах

## 🚨 Экстренные ситуации

Если нужно выполнить запрещенную команду (ТОЛЬКО для администраторов):

1. **Отключить защиту временно:**
```bash
git config --unset core.hooksPath
```

2. **Выполнить необходимую операцию**

3. **Восстановить защиту:**
```bash
git config core.hooksPath .githooks
```

**WARNING ВНИМАНИЕ:** Отключение защиты должно быть исключением, а не нормой!

## 📞 Поддержка

При проблемах с системой защиты:
1. Проверить что hooks установлены правильно
2. Проверить права на исполнение файлов
3. Проверить логи на наличие ошибок

## 🎯 Цели системы

- **Предотвратить** случайную потерю кода
- **Защитить** прогресс проекта
- **Обучить** агентов безопасным практикам
- **Обеспечить** стабильность разработки
