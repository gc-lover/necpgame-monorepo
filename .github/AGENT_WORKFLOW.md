# Система агентов для разработки NECPGAME

## Концепция

Система разделяет разработку на этапы, где каждый этап обрабатывается специализированным AI агентом в Cursor. Задачи автоматически передаются от одного агента к другому по мере готовности.

## Статусы разработки (Development Stages)

### 1. 🎨 Idea Writer (Писатель идей)
**Ответственность:**
- Придумывание идей для игры
- Разработка лора (lore)
- Создание квестов
- Текстовое описание механик
- Минимальная проработка концепций

**Статус в Project:** `idea-writer`
**Метки:** `agent:idea-writer`, `stage:idea`

**Что делает агент:**
- Создает детальные описания идей
- Разрабатывает сюжетные линии
- Пишет диалоги и тексты
- Создает концепты квестов
- Документирует игровую механику текстом

**Когда переходит дальше:**
- **Для системных задач:** → Architect (архитектурная проработка)
- **Для контентных квестов:** → Content Writer (создание YAML файла квеста)

---

### 2. ✍️ Content Writer (Писатель контента)
**Ответственность:**
- Создание контентных квестов (Canon/Lore)
- Разработка детального лора для квестов
- Создание диалогов и ветвлений
- Реализация NPC взаимодействий
- Создание YAML файлов для квестов

**Статус в Project:** `content-writer`
**Метки:** `agent:content-writer`, `stage:content`, `content`, `canon`, `lore`, `quest`

**Что делает агент:**
- Создает YAML файлы квестов в `knowledge/canon/lore/timeline-author/quests/`
- Прорабатывает детальный лор для квестов
- Создает диалоги и ветвления
- Интегрирует с NPC и локациями
- Определяет награды и проверки навыков

**Когда переходит дальше:**
- YAML файл квеста создан
- Лор проработан
- Диалоги созданы
- **YAML файл валидирован** (синтаксис, структура, данные)
- **ВСЕГДА передает Backend Developer для импорта в БД**

**Важно:** 
- Content Writer работает с готовой архитектурой системы квестов (Issue #103). Не создает архитектуру.
- Content Writer сам валидирует созданные YAML файлы перед завершением работы.
- **ВСЕГДА передает Backend Developer для импорта в БД** - без импорта контент не попадет в игру.
- Backend Developer импортирует контент в БД через API endpoint `POST /api/v1/gameplay/quests/content/reload`.
- QA тестирует функционал после импорта в БД.

---

### 3. 📐 Architect (Архитектор)
**Ответственность:**
- Структурирование информации от Idea Writer
- Проектирование архитектуры решения
- Определение компонентов системы
- Создание технической структуры
- Разбиение на подзадачи

**Статус в Project:** `architect`
**Метки:** `agent:architect`, `stage:design`

**Что делает агент:**
- Анализирует идею от Idea Writer
- Создает архитектурную схему
- Определяет необходимые API endpoints
- Разбивает задачу на микросервисы
- Создает техническое задание

**Когда переходит дальше:**
- Архитектура спроектирована
- Компоненты определены
- API endpoints описаны
- Готово к созданию OpenAPI спецификации

---

### 4. 📋 API Designer (Дизайнер API)
**Ответственность:**
- Создание OpenAPI спецификации
- Описание всех endpoints
- Определение схем данных
- Документирование API
- Валидация архитектуры через API

**Статус в Project:** `api-designer`
**Метки:** `agent:api-designer`, `stage:api-design`

**Что делает агент:**
- Создает OpenAPI 3.0 спецификацию
- Описывает request/response схемы
- Определяет типы данных
- Добавляет примеры запросов
- Валидирует спецификацию

**Когда переходит дальше:**
- OpenAPI спецификация готова
- Все endpoints описаны
- Схемы данных определены
- Готово к реализации бекенда

---

### 5. 🗄️ Database Engineer (Инженер БД)
**Ответственность:**
- Проектирование схем БД
- Создание Liquibase миграций
- Оптимизация запросов и индексов
- Работа с производительностью БД

**Статус в Project:** `database-dev`
**Метки:** `agent:database`, `stage:database`, `database`

**Что делает агент:**
- Проектирует детальные схемы БД (таблицы, связи, индексы)
- Создает Liquibase миграции в `infrastructure/liquibase/`
- Оптимизирует запросы и индексы
- Определяет стратегию миграций

**Когда переходит дальше:**
- Схема БД спроектирована
- Миграции созданы
- Готово к работе API Designer и Backend

**Важно:** Database Engineer работает параллельно с Architect. API Designer описывает API схемы, а Database Engineer проектирует БД схемы.

---

### 6. ⚙️ Backend Developer (Бекенд разработчик)
**Ответственность:**
- Реализация бекенда на Go
- Создание микросервисов
- Реализация API endpoints
- Работа с базой данных
- Интеграция сервисов

**Статус в Project:** `backend-dev`
**Метки:** `agent:backend`, `stage:backend-dev`

**Что делает агент:**
- Реализует Go сервисы по OpenAPI
- Создает handlers для endpoints
- Настраивает базу данных
- Реализует бизнес-логику
- Пишет тесты

**Когда переходит дальше:**
- Бекенд реализован
- API работает согласно спецификации
- Тесты пройдены
- Готово к интеграции с клиентом

---

### 7. 🔒 Security Agent (Агент безопасности)
**Ответственность:**
- Аудит безопасности всех API endpoints
- Валидация входных данных
- Защита от читов и эксплойтов
- Интеграция с anti-cheat-service
- Проверка уязвимостей

**Статус в Project:** `security`
**Метки:** `agent:security`, `stage:security`, `security`

**Что делает агент:**
- Проводит аудит безопасности
- Проверяет валидацию входных данных
- Интегрирует с anti-cheat-service
- Находит и исправляет уязвимости
- Настраивает rate limiting

**Когда переходит дальше:**
- Аудит безопасности проведен
- Уязвимости исправлены
- Готово к тестированию

---

### 8. 🎮 UE5 Developer (Unreal Engine разработчик)
**Ответственность:**
- Реализация клиента на Unreal Engine 5.7
- Интеграция с бекенд API
- Создание UI/UX
- Реализация игровой механики
- Оптимизация производительности

**Статус в Project:** `ue5-dev`
**Метки:** `agent:ue5`, `stage:client-dev`

**Что делает агент:**
- Создает C++ классы в UE5
- Интегрирует с бекенд API
- Реализует игровую механику
- Создает UI элементы
- Оптимизирует сетевой код

**Когда переходит дальше:**
- Клиент реализован
- Интеграция с бекендом работает
- UI готов
- Готово к тестированию

---

### 9. ⚖️ Game Balance Agent (Агент балансировки)
**Ответственность:**
- Балансировка оружия, брони, навыков
- Настройка экономики
- Балансировка сложности квестов
- Настройка игровых формул

**Статус в Project:** `game-balance`
**Метки:** `agent:game-balance`, `stage:balance`, `game-balance`

**Что делает агент:**
- Балансирует игровые механики
- Настраивает экономику (цены, награды)
- Создает конфигурационные файлы баланса
- Настраивает формулы урона/лечения/опыта

**Когда переходит дальше:**
- Балансировка проведена
- Конфигурации обновлены
- Готово к релизу

---

### 10. 🧪 QA/Testing (Тестировщик)
**Ответственность:**
- Тестирование функциональности
- Проверка интеграции
- Нагрузочное тестирование
- Поиск багов
- Валидация требований

**Статус в Project:** `testing`
**Метки:** `agent:qa`, `stage:testing`

**Что делает агент:**
- Создает тест-кейсы
- Запускает автоматические тесты
- Проверяет интеграцию
- Ищет баги и edge cases
- Валидирует соответствие требованиям

**Когда переходит дальше:**
- Все тесты пройдены
- Баги исправлены
- Интеграция работает
- Готово к релизу

---

### 11. 🚀 Release (Релиз)
**Ответственность:**
- Подготовка к релизу
- Создание релизных заметок
- Деплой в продакшен
- Мониторинг после релиза

**Статус в Project:** `release`
**Метки:** `agent:release`, `stage:release`

**Что делает агент:**
- Создает release notes
- Готовит деплой
- Настраивает мониторинг
- Проверяет готовность к релизу

**Когда завершается:**
- Релиз выполнен
- Система работает в продакшене
- Мониторинг настроен

---

## Workflow переходов

### Системные задачи (требуют архитектуры):
```
[Idea Writer] → [Architect] 
                    ↓
              [Database Engineer] (проектирует БД)
                    ↓
              [API Designer] (описывает API)
                    ↓
              [Backend Dev] (реализует, используя готовые миграции)
                    ↓
              [Network] (настраивает коммуникацию)
                    ↓
              [Security] (аудит безопасности)
                    ↓
              [DevOps] (инфраструктура + observability)
                    ↓
              [UE5 Dev] → [QA] → [Game Balance] → [Release]
     ↓              ↓              ↓                ↓              ↓          ↓         ↓         ↓
   idea-writer    architect    database-dev    api-designer    backend-dev  network-dev  security  devops
                    ↓                                                                        ↓
              [Performance] (может работать параллельно на любом этапе)                  [Performance]
```

### Контентные задачи (НЕ требуют архитектуры):

**Контентные квесты ОБЯЗАТЕЛЬНО проходят через импорт в БД:**

```
[Idea Writer] → [Content Writer (создает + валидирует YAML)] → [Backend (импорт в БД)] → [QA (тестирование)] → [Release]
     ↓                ↓                                           ↓                        ↓                  ↓
   idea-writer   content-writer                              backend-dev              testing            release
```

**Что делает каждый этап:**

1. **Content Writer:**
   - Создает и валидирует YAML файл квеста
   - ВСЕГДА передает Backend Developer для импорта в БД

2. **Backend Developer:**
   - Импортирует YAML в таблицу `quest_definitions` через API endpoint `POST /api/v1/gameplay/quests/content/reload`
   - Проверяет, что квест загружен в БД корректно
   - Передает QA для тестирования

3. **QA:**
   - Тестирует функционал квеста в игре после импорта в БД
   - Проверяет, что квест работает корректно
   - Передает Release

4. **Release:**
   - Контент импортирован в БД и протестирован
   - Готов к релизу

**Важно:** 
- Content Writer сам валидирует созданные YAML файлы (синтаксис, структура, данные)
- **ВСЕГДА передает Backend Developer для импорта в БД** - без импорта контент не попадет в игру
- Backend Developer отвечает за импорт контента в БД
- QA тестирует функционал после импорта в БД

### Дополнительные агенты

**Content Writer** - работает после Idea Writer для контентных квестов (Canon/Lore), создает YAML файлы квестов
**Database Engineer** - работает параллельно с Architect, проектирует схемы БД и создает миграции
**Network Engineer** - работает между Backend и UE5, настраивает сетевую часть и Protocol Buffers
**Security Agent** - работает после Backend/Network, проводит аудит безопасности
**DevOps** - настраивает инфраструктуру и observability (Grafana, Prometheus, Loki, Tempo)
**Performance Engineer** - может работать на любом этапе для оптимизации
**Game Balance Agent** - работает после реализации игровых механик, балансирует игру

## Использование в Cursor AI

### Команды для агентов

**Idea Writer:**
```
"Создай идею для нового квеста в стиле Cyberpunk"
"Разработай лор для нового персонажа"
"Придумай механику лутинга для шутера"
```

**Architect:**
```
"Структурируй идею из Issue #X"
"Спроектируй архитектуру для системы инвентаря"
"Разбей задачу на микросервисы"
```

**API Designer:**
```
"Создай OpenAPI спецификацию для системы инвентаря"
"Опиши endpoints для character-service"
```

**Backend Developer:**
```
"Реализуй бекенд по OpenAPI из Issue #X"
"Создай Go сервис для inventory-service"
```

**UE5 Developer:**
```
"Реализуй клиент для API из Issue #X"
"Создай UI для системы инвентаря в UE5"
```

### Автоматическая маршрутизация

Система автоматически маршрутизирует задачи на основе:
- Меток (labels)
- Содержимого Issue
- Изменений в коде
- Статуса в Project

## Настройка GitHub Project

### Custom Field: "Development Stage"

Тип: **Single select**

Значения:
1. `idea-writer` - Idea Writer
2. `content-writer` - Content Writer
3. `architect` - Architect
4. `database-dev` - Database Engineer
5. `api-designer` - API Designer
6. `backend-dev` - Backend Developer
7. `network-dev` - Network Engineer
8. `security` - Security Agent
9. `devops` - DevOps/Infrastructure
10. `performance` - Performance Engineer
11. `ue5-dev` - UE5 Developer
12. `testing` - QA/Testing
13. `game-balance` - Game Balance Agent
14. `release` - Release

### Custom Field: "Branch" (рекомендуется)

Тип: **Text**

Автоматически заполняется при создании ветки для задачи.
Формат: `feature/issue-{number}-{description}`

### Views для каждого агента

Создайте отдельные views, фильтруя по:
- `Development Stage` = соответствующий статус
- Метки `agent:*` для дополнительной фильтрации

## Автоматизация через GitHub Actions

Workflow автоматически:
- Добавляет метки при создании Issue
- Обновляет статус в Project при изменении
- Маршрутизирует задачи между агентами
- Уведомляет о готовности к следующему этапу

