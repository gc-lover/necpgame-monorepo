# Настройка GitHub Project #1 для системы агентов

## OK Что уже сделано автоматически

1. **Workflows обновлены:**
   - `project-status-automation.yml` - автоматически добавляет Issues в проект и обновляет статусы
   - `agent-workflow.yml` - маршрутизирует задачи к агентам
   - `label-sync.yml` - синхронизирует метки из `.github/labels.json`

2. **Тестовая задача создана:**
   - Issue #8: "[TEST] Тестовая задача для системы агентов"

## 📋 Что нужно сделать вручную в GitHub UI

### Шаг 1: Создать Custom Field "Development Stage"

1. Откройте проект: https://github.com/users/gc-lover/projects/1
2. Нажмите ⚙️ **Configure** (шестеренка в правом верхнем углу)
3. В левом меню выберите **Custom fields**
4. Нажмите **+ Add field** → **Single select**
5. Название поля: `Development Stage`
6. Добавьте следующие опции (по одной):
   - `idea-writer`
   - `content-writer`
   - `architect`
   - `database-dev`
   - `api-designer`
   - `backend-dev`
   - `network-dev`
   - `security`
   - `devops`
   - `performance`
   - `ue5-dev`
   - `testing`
   - `game-balance`
   - `release`

### Шаг 2: Создать Views для каждого агента (опционально)

Для удобства можно создать отдельные views:

1. В Project нажмите **+ Add view**
2. Название: **"Idea Writer Queue"**
3. Фильтр: `Development Stage` = `idea-writer`
4. Повторите для каждого агента:
   - Content Writer Queue → `content-writer`
   - Architect Queue → `architect`
   - Database Dev Queue → `database-dev`
   - API Designer Queue → `api-designer`
   - Backend Dev Queue → `backend-dev`
   - Network Dev Queue → `network-dev`
   - Security Queue → `security`
   - DevOps Queue → `devops`
   - Performance Queue → `performance`
   - UE5 Dev Queue → `ue5-dev`
   - Testing Queue → `testing`
   - Game Balance Queue → `game-balance`
   - Release Queue → `release`

### Шаг 3: Запустить синхронизацию labels

1. Перейдите в **Actions** → **Sync Labels**
2. Нажмите **Run workflow** → **Run workflow**
3. Дождитесь завершения (обычно 10-30 секунд)

Или через GitHub CLI:
```bash
gh workflow run label-sync.yml
```

### Шаг 4: Добавить существующие Issues в проект

После создания поля "Development Stage", workflow автоматически добавит новые Issues в проект.

Для существующих Issues (если они еще не в проекте):

1. Откройте каждую Issue
2. В правой панели найдите **Projects**
3. Выберите проект **NECPGAME Development**
4. Workflow автоматически обновит статус на основе меток

## 🧪 Тестирование системы

### Тест 1: Создание новой задачи

1. Создайте новую Issue через шаблон `agent_task.yml`
2. Выберите начальный этап: `idea-writer`
3. Workflow автоматически:
   - Добавит Issue в проект
   - Установит метки `agent:idea-writer` и `stage:idea`
   - Обновит поле "Development Stage" = `idea-writer`

### Тест 2: Переход между этапами

1. Откройте Issue #8 (тестовая задача)
2. Добавьте метку `agent:architect`
3. Workflow автоматически:
   - Обновит метки на `agent:architect` и `stage:design`
   - Обновит поле "Development Stage" = `architect`

### Тест 3: Автоматический переход при мерже PR

1. Создайте PR с меткой `backend`
2. В описании PR укажите: `Closes #8`
3. После мержа PR workflow автоматически:
   - Обновит Issue #8 на этап `network-dev`

## 🔍 Проверка работы

### Проверить, что Issue в проекте:

1. Откройте проект: https://github.com/users/gc-lover/projects/1
2. Найдите Issue в списке
3. Проверьте, что поле "Development Stage" заполнено

### Проверить логи workflow:

1. Перейдите в **Actions**
2. Откройте последний запуск `Project Status Automation`
3. Проверьте логи на ошибки

## WARNING Troubleshooting

### Проблема: Issue не добавляется в проект

**Решение:**
1. Проверьте, что поле "Development Stage" создано
2. Проверьте права workflow (нужен `projects: write`)
3. Проверьте логи в Actions

### Проблема: Статус не обновляется

**Решение:**
1. Убедитесь, что названия опций в поле точно совпадают с ожидаемыми (см. список выше)
2. Проверьте, что метки правильно названы
3. Проверьте логи workflow

### Проблема: Workflow не запускается

**Решение:**
1. Проверьте, что workflow файлы в ветке `main`
2. Проверьте права репозитория (Settings → Actions → General)
3. Убедитесь, что `PROJECT_NUMBER: 1` в workflow

## 📝 Следующие шаги

После настройки:

1. OK Создайте Custom Field "Development Stage"
2. OK Запустите синхронизацию labels
3. OK Протестируйте на Issue #8
4. OK Создайте Views для удобства (опционально)

Система готова к работе! 🚀


