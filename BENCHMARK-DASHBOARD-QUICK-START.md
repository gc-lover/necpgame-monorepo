# 🚀 Benchmark Dashboard - Quick Start

**Веб-дашборд для визуализации результатов бенчмарков**

---

## Быстрый запуск

### 1. Локально

```bash
cd infrastructure/benchmark-dashboard
make dev
```

Откройте `http://localhost:8080`

### 2. Через Docker

```bash
cd infrastructure/benchmark-dashboard
docker build -t benchmark-dashboard .
docker run -p 8080:8080 -v $(pwd)/../../.benchmarks:/app/.benchmarks benchmark-dashboard
```

### 3. В Kubernetes

```bash
kubectl apply -f k8s/benchmark-dashboard-deployment.yaml
kubectl port-forward -n monitoring svc/benchmark-dashboard 8080:80
```

---

## Функции дашборда

- 📊 **Графики производительности** - тренды ns/op по времени
- 🔍 **Фильтрация** - по сервисам и бенчмаркам
- 📈 **Сравнение** - между разными запусками
- 📋 **Таблица результатов** - последние значения
- 🔄 **Автообновление** - каждую минуту

---

## API Endpoints

- `GET /` - Главная страница
- `GET /api/data` - Все данные (JSON)
- `GET /api/runs` - Список запусков
- `GET /api/run/{filename}` - Конкретный запуск

---

## Интеграция

Дашборд автоматически читает результаты из `.benchmarks/results/`, которые создаются:

1. **Локально:** `./scripts/run-all-benchmarks.sh`
2. **CI/CD:** GitHub Actions workflow (`.github/workflows/benchmarks.yml`)

Результаты коммитятся в репозиторий и доступны дашборду.

---

## Troubleshooting

**Нет данных:**
- Проверьте наличие файлов в `.benchmarks/results/`
- Запустите бенчмарки: `./scripts/run-all-benchmarks.sh`

**Дашборд не запускается:**
- Проверьте порт 8080 (может быть занят)
- Измените порт в `main.go`: `const port = "8081"`

**Графики не отображаются:**
- Проверьте консоль браузера (F12)
- Убедитесь что Chart.js загружается (CDN)

---

**См. также:**
- `infrastructure/benchmark-dashboard/README.md` - детали
- `.benchmarks/README.md` - формат данных
