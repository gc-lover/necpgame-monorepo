# 🔒 Git Hooks - Автоматическая проверка кода

## Быстрый старт

Перед началом работы установите Git hooks:

```bash
# Linux/Mac
./scripts/install-git-hooks.sh

# Windows
scripts\install-git-hooks.bat
```

## Что это дает?

✅ **Проверка размера файлов ДО коммита**
- Не даст создать коммит с файлами > 600 строк
- Мгновенная обратная связь (не нужно ждать GitHub Actions)
- Экономит время команды

✅ **Единая конфигурация**
- Настройки в одном месте: `.github/file-size-config.sh`
- Та же логика в Git hooks и GitHub Actions

✅ **Автоматические исключения**
- Сгенерированные файлы (`*.gen.go`, `*.pb.go`)
- Vendor/node_modules
- Bundled/merged файлы

## Пример работы

```bash
git add large-file.go
git commit -m "Add feature"

🔍 Checking file sizes before commit...
Max lines per file: 600

❌ src/large-file.go has 1000 lines (max: 600)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ COMMIT REJECTED: Files exceeding 600 lines:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  • src/large-file.go: 1000 lines (exceeds by 400 lines)

💡 Please split these files into smaller files (max 600 lines).
   Recommended: ~300-400 lines per file for better maintainability.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Настройка

Изменить лимит строк в `.github/file-size-config.sh`:

```bash
export MAX_LINES=600  # Изменить на нужное значение
```

После изменения конфигурации она применится:
- В локальных Git hooks
- В GitHub Actions автоматически

## Обход проверки

**Не рекомендуется**, но возможно:

```bash
git commit --no-verify
```

⚠️ Файл все равно будет проверен в GitHub Actions!

## Удаление hooks

```bash
git config --unset core.hooksPath
```

## Подробная документация

См. `.github/GIT_HOOKS_SETUP.md`

