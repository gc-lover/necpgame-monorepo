# NECPGAME Validation Tools

Этот документ описывает инструменты валидации качества кода и архитектуры для проекта NECPGAME.

## 🏗️ Архитектурная Валидация

### validate-architecture.sh
Проверяет соответствие кода архитектурным принципам NECPGAME.

```bash
# Запуск полной валидации
./infrastructure/scripts/validate-architecture.sh

# Проверка только конкретных аспектов
./infrastructure/scripts/validate-architecture.sh --check security
./infrastructure/scripts/validate-architecture.sh --check performance
```

**Проверяет:**
- OK SOLID принципы (dependency injection, single responsibility)
- OK Performance паттерны (memory pooling, context timeouts)
- OK Security паттерны (input validation, SQL injection prevention)
- OK Code quality (logging, error handling)

## 📄 Валидация Структуры Файлов

### validate-file-structure.sh
Проверяет размер файлов, структуру и соглашения об именовании.

```bash
# Полная валидация структуры
./infrastructure/scripts/validate-file-structure.sh
```

**Проверяет:**
- 📏 Размер файлов (Go: ≤500 строк, YAML: ≤500 строк)
- 🏷️ Соглашения об именовании (snake_case для Go файлов)
- 📂 Структуру директорий
- 📝 Синтаксис YAML файлов

## 📋 OpenAPI Валидация

### validate-openapi.sh
Валидирует OpenAPI спецификации с помощью redocly и кастомных правил.

```bash
# Валидация всех OpenAPI файлов
./infrastructure/scripts/validate-openapi.sh
```

**Проверяет:**
- 🔍 Redocly linting (если установлен)
- 🏗️ Структуру OpenAPI (openapi, info, paths)
- 📐 Схемы для оптимизации памяти (struct alignment)
- 🔒 Безопасность (rate limiting, error responses)

## 🪝 Git Hooks

### Установка
```bash
# Установка hooks в локальный репозиторий
./infrastructure/scripts/install-git-hooks.sh
```

### Pre-commit Hook
Запускается перед каждым коммитом:
- OK Валидация staged файлов
- 📏 Проверка размера файлов (<10MB)
- 🔒 Поиск чувствительных данных
- 💬 Проверка формата commit message

### Pre-push Hook
Запускается перед отправкой в remote:
- 📊 Валидация пушимых коммитов
- 🌿 Проверка именования веток
- 🔒 Security проверки
- 🔄 Проверка готовности к CI

### Конфигурация
Создайте файл `.necp-game-hooks` в корне проекта:

```bash
# Включить полную валидацию на pre-commit
RUN_FULL_VALIDATION=true

# Отключить security проверки
ENABLE_SECURITY_CHECKS=false

# Паттерны файлов для пропуска
SKIP_HOOKS_PATTERNS="*.log *.tmp"
```

## 🔄 CI/CD Quality Gates

### Автоматическая Валидация
Quality gates запускаются автоматически в GitHub Actions:

- **На Push/PR**: Полная валидация всех инструментов
- **Security Scan**: Trivy vulnerability scanning
- **Performance Check**: Проверка performance паттернов
- **Dependency Check**: Анализ зависимостей Go

### Workflow Структура
```
Quality Gates → Backend CI → Deployment
```

## 📊 Отчеты

Все инструменты валидации генерируют отчеты в `infrastructure/reports/`:

- `architecture-validation-YYYYMMDD-HHMMSS.txt`
- `file-structure-validation-YYYYMMDD-HHMMSS.txt`
- `openapi-validation-YYYYMMDD-HHMMSS.txt`

## 🚨 Обработка Ошибок

### Exit Codes
- `0`: Успешная валидация
- `1`: Найдены ошибки (blocking)
- `2`: Найдены предупреждения (non-blocking)

### Логи
Все инструменты используют цветной вывод:
- 🔴 Красный: Ошибки (требуют исправления)
- 🟡 Желтый: Предупреждения (рекомендуется исправить)
- 🟢 Зеленый: Успешные проверки

## 🛠️ Разработка

### Добавление Новых Правил
1. Добавьте проверки в соответствующий скрипт
2. Обновите документацию
3. Протестируйте на существующем коде

### Кастомные Конфигурации
Создайте `.validation-config.json` для кастомных правил:

```json
{
  "max_go_lines": 350,
  "max_yaml_lines": 400,
  "required_patterns": ["# Issue:"],
  "forbidden_patterns": ["fmt\\.Print", "log\\.Fatal"]
}
```

## 📞 Поддержка

При проблемах с валидацией:
1. Проверьте логи выполнения
2. Посмотрите отчеты в `infrastructure/reports/`
3. Убедитесь, что все зависимости установлены
4. Создайте Issue с описанием проблемы