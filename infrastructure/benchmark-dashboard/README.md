# Benchmark Dashboard

Веб-дашборд для визуализации результатов бенчмарков всех микросервисов.

## Запуск

```bash
# Локально
make dev

# Или с билдом
make build
./benchmark-dashboard
```

Дашборд будет доступен на `http://localhost:8080`

## Функции

- 📊 Графики производительности по времени
- 🔍 Фильтрация по сервисам и бенчмаркам
- 📈 Сравнение результатов между запусками
- 📋 Таблица последних результатов
- 🔄 Автообновление каждую минуту

## API Endpoints

- `GET /` - Главная страница дашборда
- `GET /api/data` - Все данные для дашборда
- `GET /api/runs` - Список всех запусков
- `GET /api/run/{filename}` - Данные конкретного запуска

## Интеграция с CI/CD

Дашборд автоматически читает результаты из `.benchmarks/results/`, которые создаются GitHub Actions workflow.

