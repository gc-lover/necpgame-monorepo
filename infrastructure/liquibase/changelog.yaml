databaseChangeLog:
  # Schema migrations (DDL) - run first
  - includeAll:
      path: schema/
      relativeToChangelogFile: true

  # Data migrations (DML) - run after schema
  - includeAll:
      path: data/
      relativeToChangelogFile: true
