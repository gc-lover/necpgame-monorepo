apiVersion: apps/v1
kind: Deployment
metadata:
  name: housing-service-go
  namespace: necpgame
  labels:
    app: housing-service-go
    version: v1
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: housing-service-go
  template:
    metadata:
      labels:
        app: housing-service-go
        version: v1
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9093"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: housing-service-go
          image: necpgame-housing-service-go:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: 8093
              protocol: TCP
            - name: metrics
              containerPort: 9093
              protocol: TCP
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: database-secrets
                  key: url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: redis-secrets
                  key: url
            - name: KEYCLOAK_ISSUER
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: keycloak-issuer
            - name: KEYCLOAK_JWKS_URL
              value: "http://keycloak:8080/realms/necpgame/protocol/openid-connect/certs"
            - name: AUTH_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: auth-enabled
            - name: HTTP_ADDR
              value: ":8093"
            - name: METRICS_ADDR
              value: ":9093"
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: log-level
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          startupProbe:
            httpGet:
              path: /metrics
              port: 9093
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /metrics
              port: 9093
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /metrics
              port: 9093
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: { }
---
apiVersion: v1
kind: Service
metadata:
  name: housing-service-go
  namespace: necpgame
  labels:
    app: housing-service-go
    prometheus: scrape
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8093
      targetPort: 8093
      protocol: TCP
    - name: metrics
      port: 9093
      targetPort: 9093
      protocol: TCP
  selector:
    app: housing-service-go
