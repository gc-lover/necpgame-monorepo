apiVersion: apps/v1
kind: Deployment
metadata:
  name: reset-service-go
  namespace: necpgame
  labels:
    app: reset-service-go
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reset-service-go
  template:
    metadata:
      labels:
        app: reset-service-go
        version: v1
    spec:
      containers:
      - name: reset-service-go
        image: necpgame-reset-service-go:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8088
          protocol: TCP
        - name: metrics
          containerPort: 9098
          protocol: TCP
        env:
        - name: ADDR
          value: "0.0.0.0:8088"
        - name: METRICS_ADDR
          value: ":9098"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: redis-secrets
              key: url
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /metrics
            port: 9098
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /metrics
            port: 9098
          initialDelaySeconds: 5
          periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: reset-service-go
  namespace: necpgame
  labels:
    app: reset-service-go
    prometheus: scrape
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8088
    targetPort: 8088
    protocol: TCP
  - name: metrics
    port: 9098
    targetPort: 9098
    protocol: TCP
  selector:
    app: reset-service-go

