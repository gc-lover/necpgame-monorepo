extends:
  - recommended

rules:
  no-unresolved-refs: off  # Allow external $ref links to common schemas
  no-server-example.com: off  # Allow localhost servers for development

theme:
  openapi:
    disableTryIt: false
    hideDownloadButton: false
    hideInfoSection: false