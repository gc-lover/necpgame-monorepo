# Affiliate Service - OpenAPI Specification

## Назначение

**Affiliate Service** предоставляет enterprise-grade API для партнерской программы NECPGAME. Сервис управляет реферальными программами, комиссионными выплатами, партнерскими кампаниями и аналитикой производительности в экосистеме Night City.

## Ключевые Функциональности

### 🔗 Реферальная Система
- **Генерация реферальных ссылок** с уникальными кодами
- **Отслеживание конверсий** в реальном времени
- **Валидация рефералов** и предотвращение мошенничества

### 💰 Комиссионные Выплаты
- **Динамический расчет комиссий** на основе различных показателей
- **Многоуровневые ставки** комиссий по категориям
- **Автоматические выплаты** и история транзакций

### 📊 Кампании и Партнерства
- **Управление кампаниями** с различными условиями участия
- **Аналитика производительности** партнеров
- **Сегментация партнеров** по уровням и категориям

### 🛡️ Безопасность и Соответствие
- **Предотвращение мошенничества** с рефералами
- **Аудит всех транзакций** и выплат
- **GDPR соответствие** для данных партнеров

## Структура API

```
affiliate-service/
├── main.yaml              # Основная спецификация OpenAPI
├── README.md             # Эта документация
├── docs/                 # Сгенерированная документация (опционально)
└── tests/               # Тесты API (опционально)
```

## Основные Эндпоинты

### Профили Партнеров
- `GET /affiliates/{affiliateId}/profile` - Получение профиля партнера
- `GET /affiliates/{affiliateId}/referrals` - Список рефералов партнера
- `GET /affiliates/{affiliateId}/commissions` - История комиссий

### Реферальная Система
- `POST /affiliates/{affiliateId}/referrals` - Создание реферальной ссылки
- `POST /referrals/{referralCode}/track` - Отслеживание конверсии

### Кампании
- `GET /campaigns` - Список активных кампаний
- `POST /campaigns/{campaignId}/join` - Присоединение к кампании

### Мониторинг Здоровья
- `GET /health` - Проверка здоровья сервиса
- `POST /health/batch` - Массовый health check
- `GET /health/ws` - WebSocket мониторинг

## Зависимости от Других Сервисов

### Требуемые Сервисы
- **user-profile-service** - Профили пользователей для партнеров
- **transaction-service** - Обработка комиссионных выплат
- **player-analytics-service** - Аналитика производительности партнеров

### Общие Компоненты
- `../common/schemas/health.yaml` - Схемы здоровья сервиса
- `../common/schemas/error.yaml` - Стандартные ошибки
- `../common/responses/success.yaml` - Успешные ответы

## Производительность

### Целевые Показатели
- **P99 Latency**: <50ms для операций отслеживания рефералов
- **Память**: <25KB на активную партнерскую сессию
- **Одновременные пользователи**: 100,000+ активных партнеров
- **Расчет комиссий**: <10ms на транзакцию

### Оптимизации
- **Struct alignment hints** для экономии памяти (30-50%)
- **Кеширование Redis** для профилей партнеров
- **Database indexing** для быстрого поиска рефералов
- **Event-driven архитектура** для обработки конверсий

## Использование

### Валидация Спецификации
```bash
# Линтинг с Redocly
npx @redocly/cli lint main.yaml

# Бандлинг для проверки $ref
npx @redocly/cli bundle main.yaml -o bundled.yaml
```

### Генерация Go Кода
```bash
# Генерация с ogen
ogen --target ../../services/affiliate-service-go/pkg/api \
     --package api --clean main.yaml
```

### Документация
```bash
# Генерация HTML документации
npx @redocly/cli build-docs main.yaml -o docs/index.html
```

## Примеры Использования

### Создание Реферальной Ссылки
```bash
curl -X POST \
     -H "Authorization: Bearer {token}" \
     -H "Content-Type: application/json" \
     -d '{
       "custom_code": "NIGHT2025",
       "expires_in_days": 60,
       "max_uses": 100
     }' \
     https://api.necpgame.com/v1/affiliate/affiliates/123e4567-e89b-12d3-a456-426614174000/referrals
```

### Отслеживание Конверсии
```bash
curl -X POST \
     -H "Content-Type: application/json" \
     -d '{
       "user_id": "456e7890-e89b-12d3-a456-426614174001",
       "conversion_type": "registration"
     }' \
     https://api.necpgame.com/v1/affiliate/referrals/NIGHT2025/track
```

### Получение Профиля Партнера
```bash
curl -H "Authorization: Bearer {token}" \
     https://api.necpgame.com/v1/affiliate/affiliates/123e4567-e89b-12d3-a456-426614174000/profile
```

### WebSocket Мониторинг
```javascript
const ws = new WebSocket('ws://localhost:8080/api/v1/affiliate/health/ws');
ws.onmessage = (event) => {
    const health = JSON.parse(event.data);
    console.log('Affiliate service health:', health);
};
```

## Архитектурные Решения

### Domain-Driven Design
- **Ограниченный контекст**: Партнерские программы и рефералы
- **Агрегаты**: AffiliateProfile, Referral, Commission, Campaign
- **Value Objects**: ReferralCode, CommissionRate, ConversionType

### Комиссионные Стратегии
- **Многоуровневые ставки** по категориям продуктов
- **Бонусные программы** за достижение целей
- **Сезонные кампании** с повышенными ставками

### Масштабируемость
- **Горизонтальное масштабирование** для обработки конверсий
- **Event sourcing** для аудита всех транзакций
- **CQRS паттерн** для аналитики и отчетов

## Безопасность

### Аутентификация
- **JWT Bearer tokens** для доступа к партнерскому API
- **API Key authentication** для внешних интеграций
- **Service-to-service** аутентификация

### Авторизация
- **Role-based access** для различных уровней партнеров
- **Resource ownership** - партнеры видят только свои данные
- **Audit trails** для всех комиссионных операций

### Предотвращение Мошенничества
- **IP tracking** и геолокационный анализ
- **Device fingerprinting** для обнаружения ботов
- **Conversion validation** с бизнес-правилами

## Мониторинг и Observability

### Метрики
- **Конверсионные события** - частота и источники
- **Комиссионные выплаты** - объемы и частота
- **Производительность партнеров** - ROI и retention

### Логирование
```json
{
  "timestamp": "2025-12-21T10:00:00Z",
  "level": "INFO",
  "service": "affiliate-service",
  "operation": "referral_conversion",
  "affiliate_id": "123e4567-e89b-12d3-a456-426614174000",
  "referral_code": "NIGHT2025",
  "conversion": {
    "user_id": "456e7890-e89b-12d3-a456-426614174001",
    "type": "registration",
    "commission": 25.00
  }
}
```

### Распределенная Трассировка
- **OpenTelemetry** для end-to-end tracing конверсий
- **Jaeger** для визуализации реферальных потоков
- **Service mesh** интеграция

## Тестирование

### Unit Tests
```bash
go test ./pkg/affiliate/
go test ./pkg/referral/
go test ./pkg/commission/
```

### Integration Tests
```bash
# Тестирование с другими сервисами
./scripts/run-integration-tests.sh affiliate-service
```

### Performance Tests
```bash
# Нагрузочное тестирование
./scripts/run-performance-tests.sh --service affiliate-service --load 100000
```

### Fraud Detection Tests
```bash
# Тестирование систем предотвращения мошенничества
./scripts/test-fraud-detection.sh
```

## Развертывание

### Kubernetes
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: affiliate-service
spec:
  replicas: 5
  template:
    spec:
      containers:
      - name: affiliate
        image: necpgame/affiliate-service:v1.0.0
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
        env:
        - name: FRAUD_DETECTION_ENABLED
          value: "true"
        - name: COMMISSION_CALCULATION_MODE
          value: "real-time"
```

### Helm Chart
```bash
helm install affiliate-service ./charts/affiliate-service \
  --set fraudDetection.enabled=true \
  --set commissionCalculation.mode=real-time
```

## Бизнес-Правила

### Комиссионные Ставки
- **Базовая ставка**: 5% от покупок рефералов
- **Повышенные ставки**: 8-10% для премиум-партнеров
- **Бонусные ставки**: до 15% для специальных кампаний

### Условия Участия
- **Минимальный уровень**: 10 успешных рефералов
- **Конверсионный rate**: минимум 5%
- **Активность**: ежемесячное подтверждение

### Выплаты
- **Периодичность**: еженедельно для активных партнеров
- **Минимум для выплаты**: 50 единиц валюты
- **Методы**: игровая валюта, реальные деньги (где применимо)

## Troubleshooting

### Распространенные Проблемы

#### Высокая латентность отслеживания
**Симптомы**: >50ms P99 latency на tracking endpoints
**Решение**:
- Проверить индексы базы данных
- Оптимизировать кеширование Redis
- Масштабировать сервис

#### Ложные конверсии
**Симптомы**: Необоснованные комиссионные выплаты
**Решение**:
- Усилить fraud detection правила
- Добавить дополнительные проверки
- Внедрить manual review для подозрительных случаев

#### Проблемы с реферальными кодами
**Симптомы**: 404 ошибки на валидные коды
**Решение**:
- Проверить expiration логику
- Валидировать uniqueness constraints
- Добавить monitoring для expired codes

## Контакты

- **Владелец сервиса**: @affiliate-team
- **DevOps**: @platform-team
- **Security**: @security-team
- **Business**: @partnerships-team
- **Документация**: docs@necpgame.com

## Следующие Шаги

1. **Интеграционное тестирование** с transaction-service
2. **A/B тестирование** комиссионных стратегий
3. **Мониторинг конверсий** и оптимизация fraud detection
4. **Расширение партнерских программ** с внешними интеграциями

---

*Affiliate Service является ключевым компонентом монетизации и роста пользовательской базы NECPGAME.*