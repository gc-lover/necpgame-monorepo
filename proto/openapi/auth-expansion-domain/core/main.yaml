openapi: 3.0.3
info:
  title: Auth Expansion Core API Module
  description: Extended authentication and authorization systems
  version: 1.0.0
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

paths:
  # Core auth services
  $ref: './auth-service.yaml#/paths'
  $ref: './auth-service-ext.yaml#/paths'

  # Auth features
  $ref: './auth-login-service.yaml#/paths'
  $ref: './auth-oauth-service.yaml#/paths'
  $ref: './auth-password-service.yaml#/paths'

  # Structured auth service
  $ref: './auth-service/main.yaml#/paths'

components:
  schemas:
    # Include structured auth schemas
    $ref: './auth-service/main.yaml#/components/schemas'
