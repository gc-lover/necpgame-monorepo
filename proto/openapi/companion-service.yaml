# Issue: SOLID refactoring - main for companion-service (management domain)
openapi: 3.0.3
info:
  title: Companion Service API (Management Domain)
  version: 1.0.0
  description: Companion system - using management as primary module
servers:
  - url: http://localhost:8116
tags:
  - name: companions
paths:
  /social/companions:
    $ref: "companion-management-service.yaml#/paths/~1social~1companions"
components:
  schemas:
    Companion:
      type: object
  securitySchemes:
    $ref: "common.yaml#/components/securitySchemes"
  responses:
    $ref: "common.yaml#/components/responses"
security:
  - BearerAuth: []

