# Issue: SOLID refactoring - main for cosmetic-service (catalog domain)
openapi: 3.0.3
info:
  title: Cosmetic Service API (Catalog Domain)
  version: 1.0.0
  description: Cosmetic system - using catalog as primary module
servers:
  - url: http://localhost:8117
tags:
  - name: cosmetics
paths:
  /economy/cosmetics/catalog:
    $ref: "cosmetic-catalog-service.yaml#/paths/~1economy~1cosmetics~1catalog"
components:
  schemas:
    CosmeticItem:
      type: object
  securitySchemes:
    $ref: "common.yaml#/components/securitySchemes"
  responses:
    $ref: "common.yaml#/components/responses"
security:
  - BearerAuth: []

