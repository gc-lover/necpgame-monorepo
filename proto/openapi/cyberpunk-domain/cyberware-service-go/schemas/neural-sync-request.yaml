type: object
required:
  - playerId
properties:
  playerId:
    type: string
    format: uuid
  syncType:
    type: string
    enum: [FULL, PARTIAL, EMERGENCY]
    default: FULL
  forceSync:
    type: boolean
    default: false
    description: Force sync even if risks detected
