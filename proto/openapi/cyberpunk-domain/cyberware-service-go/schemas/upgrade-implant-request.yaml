type: object
required:
  - targetTier
properties:
  targetTier:
    type: integer
    minimum: 1
    maximum: 5
    description: Target upgrade tier
  priority:
    type: string
    enum: [STANDARD, EXPRESS, EMERGENCY]
    default: STANDARD
    description: Upgrade priority
