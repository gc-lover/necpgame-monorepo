type: object
required:
- trades
- total_count
- pagination
properties:
  trades:
    type: array
    items:
      $ref: '#/components/schemas/TradeListing'
    description: 'BACKEND NOTE: Array of trade listings, ordered for optimal struct layout'
  total_count:
    type: integer
    format: int32
    description: Total number of active trades matching filters
  pagination:
    type: object
    properties:
      offset:
        type: integer
        format: int32
      limit:
        type: integer
        format: int32
      has_more:
        type: boolean
        description: 'BACKEND NOTE: bool - 1 byte, ordered last for struct alignment'
    description: Pagination information
description: 'BACKEND NOTE: Response optimized for struct alignment with large arrays first.'
