type: object
required:
  - id
  - seller
  - item
  - price
  - created_at
properties:
  id:
    type: string
    format: uuid
    description: 'BACKEND NOTE: UUID - 16 bytes, ordered first for struct alignment'
  seller:

  item:

  price:
    type: integer
    format: int64
    description: Price in game currency
  currency_type:
    type: string
    enum:
      - credits
      - premium_currency
    default: credits
  created_at:
    type: string
    format: date-time
    description: When the trade was created
  expires_at:
    type: string
    format: date-time
    description: When the trade expires
  view_count:
    type: integer
    format: int32
    description: Number of times this trade was viewed
  is_featured:
    type: boolean
    description: 'BACKEND NOTE: bool - 1 byte, ordered last for struct alignment'
description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small types).'





