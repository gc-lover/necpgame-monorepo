openapi: 3.0.3
info:
  title: Gameplay Progression API Module
  description: Character progression, attributes, and difficulty modes
  version: 1.0.0

paths:
  # Core gameplay
  $ref: './gameplay-service.yaml#/paths'

  # Character attributes and progression
  $ref: './gameplay-progression-attributes-service.yaml#/paths'

  # Difficulty and game modes
  $ref: './gameplay-difficulty-modes-service.yaml#/paths'

components:
  schemas:
    $ref: './gameplay-service.yaml#/components/schemas'
    $ref: './gameplay-progression-attributes-service.yaml#/components/schemas'
    $ref: './gameplay-difficulty-modes-service.yaml#/components/schemas'
