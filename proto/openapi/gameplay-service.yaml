# Issue: SOLID refactoring - main file for gameplay-service (combat domain)
openapi: 3.0.3

info:
  title: Gameplay Service API (Combat Domain)
  version: 1.0.0
  description: |
    Gameplay combat mechanics.
    Note: Gameplay has 44 modules across 15 domains.
    This spec uses combat-core as primary module.

servers:
  - url: http://localhost:8120
    description: Local development

tags:
  - name: combat

paths:
  /gameplay/combat/core:
    $ref: "gameplay-combat-core-service.yaml#/paths/~1gameplay~1combat~1core"

components:
  schemas:
    CombatSession:
      type: object
  
  securitySchemes:
    $ref: "common.yaml#/components/securitySchemes"
  
  responses:
    $ref: "common.yaml#/components/responses"

security:
  - BearerAuth: []

