openapi: 3.0.3
info:
  title: Communication Session API Module
  description: Session management and user state tracking
  version: 1.0.0

paths:
  $ref: './session-management-service.yaml#/paths'

components:
  schemas:
    $ref: './session-management-schemas.yaml#/components/schemas'
