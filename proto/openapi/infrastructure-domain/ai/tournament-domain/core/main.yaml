openapi: 3.0.3
info:
  title: Tournament Core API Module
  description: Core tournament and competition systems
  version: 1.0.0

paths:
  # Main tournament services
  $ref: './tournament-service.yaml#/paths'

  # Structured tournament service
  $ref: './tournament-service/main.yaml#/paths'

components:
  schemas:
    # Tournament schemas
    $ref: './tournament-service-core-schemas.yaml#/components/schemas'
    $ref: './tournament-service-match-schemas.yaml#/components/schemas'
    $ref: './tournament-service-reward-schemas.yaml#/components/schemas'
    $ref: './tournament-service-schemas.yaml#/components/schemas'

    # Structured schemas
    $ref: './tournament-service/main.yaml#/components/schemas'
