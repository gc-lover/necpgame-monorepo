# Issue: SOLID refactoring - simplified league-service (health only)
openapi: 3.0.3

info:
  title: League Service API
  version: 1.0.0
  description: |
    League and ranking system - simplified spec for chi migration.
    Note: League has 9 modules. Full spec will be created later.

servers:
  - url: http://localhost:8157

tags:
  - name: health

paths:
  /health:
    get:
      tags: [health]
      operationId: healthCheck
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string

components:
  securitySchemes:
    $ref: "common.yaml#/components/securitySchemes"
  
  responses:
    $ref: "common.yaml#/components/responses"

