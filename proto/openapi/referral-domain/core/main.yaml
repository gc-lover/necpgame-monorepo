openapi: 3.0.3
info:
  title: Referral Core API Module
  description: Core referral and invite systems
  version: 1.0.0
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

paths:
  # Core referral services

  # Referral tracking
  $ref: './referral-events-service.yaml#/paths'
  $ref: './referral-statistics-service.yaml#/paths'

components:
  schemas:
    $ref: './referral-schemas.yaml#/components/schemas'
