- task_description
properties:
  task_description:
    type: string
  task_type:
    type: string
    enum:
      - trade
      - quest
      - fix
      - protect
      - combat
      - negotiate
      - repair
      - hack
  priority:
    type: string
    enum:
      - low
      - medium
      - high
      - urgent
    default: medium
  deadline:
    type: string
    format: date-time
    nullable: true
  resources:
    type: object
    description: Ресурсы для задачи (JSON)
  parameters:
    type: object
    description: Параметры задачи (JSON)
description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'
UpdateTaskRequest:
  type: object
  properties:
    priority:
      type: string
      enum:
        - low
        - medium
        - high
        - urgent
    status:
      type: string
      enum:
        - pending
        - in_progress
        - completed
        - failed
        - cancelled
    deadline:
      type: string
      format: date-time
    resources:
      type: object
    parameters:
      type: object
  description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'
NPCTask:
  type: object
  required:
    - id
    - npc_id
    - contract_id
    - task_type
    - status
  properties:
    id:
      type: string
      format: uuid
    npc_id:
      type: string
      format: uuid
    contract_id:
      type: string
      format: uuid
    task_description:
      type: string
    task_type:
      type: string
      enum:
        - trade
        - quest
        - fix
        - protect
        - combat
        - negotiate
        - repair
        - hack
    priority:
      type: string
      enum:
        - low
        - medium
        - high
        - urgent
    status:
      type: string
      enum:
        - pending
        - in_progress
        - completed
        - failed
        - cancelled
    deadline:
      type: string
      format: date-time
      nullable: true
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time
    completed_at:
      type: string
      format: date-time
      nullable: true
    resources:
      type: object
    parameters:
      type: object
    result:
      type: object
      nullable: true
    progress:
      type: number
      format: float
      minimum: 0
      maximum: 100
  description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'
NPCPerformance:
  type: object
  required:
    - npc_id
    - contract_id
  properties:
    npc_id:
      type: string
      format: uuid
    contract_id:
      type: string
      format: uuid
    last_updated:
      type: string
      format: date-time
    skills_improved:
      type: array
      items:
        type: string
    tasks_completed:
      type: integer
    tasks_failed:
      type: integer
    efficiency:
      type: number
      format: float
      minimum: 0
      maximum: 1
    average_task_time:
      type: number
      format: float
    revenue_generated:
      type: number
      format: float
    loyalty:
      type: number
      format: float
      minimum: 0
      maximum: 1
    performance_rating:
      type: number
      format: float
      minimum: 0
      maximum: 5
  description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'
NPCStatus:
  type: object
  required:
    - npc_id
    - status
  properties:
    npc_id:
      type: string
      format: uuid
    contract_id:
      type: string
      format: uuid
    current_task:
      type: string
      format: uuid
      nullable: true
    location:
      type: string
    status:
      type: string
      enum:
        - active
        - busy
        - idle
        - terminated
    autonomy_level:
      type: string
      enum:
        - direct
        - autonomous
        - hybrid
    last_activity:
      type: string
      format: date-time
    next_available:
      type: string
      format: date-time
      nullable: true
  description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'
InteractWithNPCRequest:
  type: object
  required:
    - interaction_type
  properties:
    message:
      type: string
    interaction_type:
      type: string
      enum:
        - communicate
        - train
        - reward
        - criticize
        - dismiss
    resources:
      type: object
      description: Ресурсы для взаимодействия (JSON)
  description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'
InteractionResponse:
  type: object
  required:
    - interaction_id
    - npc_id
    - interaction_type
  properties:
    interaction_id:
      type: string
      format: uuid
    npc_id:
      type: string
      format: uuid
    npc_response:
      type: string
    interaction_type:
      type: string
      enum:
        - communicate
        - train
        - reward
        - criticize
        - dismiss
    created_at:
      type: string
      format: date-time
    loyalty_change:
      type: number
      format: float
    performance_change:
      type: number
      format: float
  description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'
security:
  - BearerAuth: [ ]
