# Role Service - OpenAPI Specification

## 📋 **Назначение**

Role Service предоставляет enterprise-grade управление ролями и разрешениями для NECPGAME платформы. Реализует
Role-Based Access Control (RBAC) для обеспечения безопасности и контроля доступа ко всем системам.

## 🎯 **Функциональность**

- **Управление ролями**: создание, обновление, удаление ролей
- **Назначение разрешений**: привязка разрешений к ролям
- **Проверка доступа**: валидация прав пользователей
- **Иерархия ролей**: поддержка наследования и приоритетов
- **Аудит доступа**: логирование всех операций с ролями

## 📁 **Структура**

```
role-service/
├── main.yaml           # Основная спецификация
├── README.md          # Эта документация
├── roles.yaml         # CRUD операции с ролями
├── permissions.yaml   # Управление разрешениями
├── assignments.yaml   # Назначение ролей пользователям
├── policies.yaml      # Политики безопасности
└── audit.yaml         # Аудит и логирование
```

## 🔗 **Зависимости**

- **common**: Общие схемы и ответы для ошибок и пагинации
- **auth-service**: Использует UserId для назначения ролей
- **session-service**: Проверяет сессии при валидации прав

## 📊 **Performance**

- **P99 Latency**: <10ms для проверки разрешений
- **Memory per Instance**: <100KB (кеш ролей)
- **Concurrent Users**: 10,000+ одновременных проверок
- **Cache Hit Rate**: >95% для часто используемых ролей

## 🚀 **Использование**

### Валидация

```bash
npx @redocly/cli lint main.yaml
```

### Генерация Go кода

```bash
ogen --target ../../services/role-service-go/pkg/api \
     --package api --clean main.yaml
```

### Документация

```bash
npx @redocly/cli build-docs main.yaml -o docs/index.html
```

## 🔐 **Безопасность**

- **Zero-trust модель**: каждая операция проверяется
- **Audit logging**: все изменения ролей логируются
- **Permission inheritance**: роли наследуют разрешения
- **Session validation**: интеграция с session-service

## 📈 **Метрики**

- Количество активных ролей
- Частота проверок разрешений
- Время выполнения операций
- Уровень отказов в доступе

## 🧪 **Тестирование**

- **Unit tests**: валидация логики ролей
- **Integration tests**: проверка с auth-service
- **Performance tests**: нагрузочное тестирование проверок
- **Security tests**: попытки обхода разрешений

---

**Role Service является критическим компонентом безопасности NECPGAME платформы.**
