description: Single variable response
content:
  application/json:
    schema:
      type: object
      properties:
        variable:
          $ref: './variable-rule.yaml'
      description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'
