description: Single variant response
content:
  application/json:
    schema:
      type: object
      properties:
        variant:
          $ref: './template-variant.yaml'
      description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'
