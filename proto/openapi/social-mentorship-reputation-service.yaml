openapi: 3.0.3
info:
  title: Social Mentorship Reputation Service API
  description: |
    API для управления репутацией и отзывами наставников.
    
    **Функциональность:**
    - Получение репутации наставников
    - Создание отзывов о наставниках
  version: 2.0.0
  contact:
    name: NECPGAME API Support
    email: api@necp.game

servers:
  - url: http://localhost:8084/api/v1
    description: Локальный сервер разработки
  - url: https://api.necp.game/api/v1
    description: Продакшен сервер

tags:
  - name: Mentorship Reputation
    description: Репутация и отзывы наставников

paths:
  /social/mentorship/reputation/{mentorId}:
    get:
      tags:
        - Mentorship Reputation
      summary: Получить репутацию наставника
      operationId: getMentorReputation
      security:
        - BearerAuth: []
      parameters:
        - name: mentorId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Репутация наставника
          content:
            application/json:
              schema:
                $ref: 'social-mentorship-schemas.yaml#/components/schemas/MentorReputation'
        '404':
          $ref: 'common.yaml#/components/responses/NotFound'
        '401':
          $ref: 'common.yaml#/components/responses/Unauthorized'
        '500':
          $ref: 'common.yaml#/components/responses/InternalServerError'

  /social/mentorship/reputation/{mentorId}/review:
    post:
      tags:
        - Mentorship Reputation
      summary: Оставить отзыв о наставнике
      operationId: reviewMentor
      security:
        - BearerAuth: []
      parameters:
        - name: mentorId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: 'social-mentorship-schemas.yaml#/components/schemas/CreateMentorReviewRequest'
      responses:
        '200':
          description: Отзыв создан
          content:
            application/json:
              schema:
                $ref: 'social-mentorship-schemas.yaml#/components/schemas/MentorReview'
        '400':
          $ref: 'common.yaml#/components/responses/BadRequest'
        '401':
          $ref: 'common.yaml#/components/responses/Unauthorized'
        '500':
          $ref: 'common.yaml#/components/responses/InternalServerError'

components:
  securitySchemes:
    BearerAuth:
      $ref: 'common.yaml#/components/securitySchemes/BearerAuth'

  schemas:
    MentorReputation:
      $ref: 'social-mentorship-schemas.yaml#/components/schemas/MentorReputation'
    CreateMentorReviewRequest:
      $ref: 'social-mentorship-schemas.yaml#/components/schemas/CreateMentorReviewRequest'
    MentorReview:
      $ref: 'social-mentorship-schemas.yaml#/components/schemas/MentorReview'

security:
  - BearerAuth: []
