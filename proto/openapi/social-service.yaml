# Issue: SOLID refactoring - main for social-service (friends domain)
openapi: 3.0.3
info:
  title: Social Service API (Friends Domain)
  version: 1.0.0
  description: Social system - using friends as primary module
servers:
  - url: http://localhost:8143
tags:
  - name: social
paths:
  /social/friends:
    $ref: "social-friends-service.yaml#/paths/~1social~1friends"
components:
  schemas:
    Friend:
      type: object
  securitySchemes:
    $ref: "common.yaml#/components/securitySchemes"
  responses:
    $ref: "common.yaml#/components/responses"
security:
  - BearerAuth: []

