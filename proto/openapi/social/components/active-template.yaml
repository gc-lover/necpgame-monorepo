type: object
required: [ id, type, category, base_template, variables ]
properties:
  id:
    type: string
    format: uuid
  type:
    type: string
    enum: [ combat, social, economic, exploration ]
  category:
    type: string
  base_template:
    type: string
  variables:
    type: array
    items:
      type: string
  variants:
    type: array
    items:
      type: string
description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'
