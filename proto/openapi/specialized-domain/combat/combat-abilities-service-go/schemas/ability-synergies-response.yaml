type: object
required:
  - ability_id
  - synergies
properties:
  ability_id:
    type: string
    format: uuid
    description: 'BACKEND NOTE: UUID - 16 bytes, ordered first for struct alignment'
  synergies:
    type: array
    items:
      $ref: '#/components/schemas/SynergyInfo'
    description: List of possible synergies
description: 'BACKEND NOTE: Optimized for struct alignment with large arrays first.'






