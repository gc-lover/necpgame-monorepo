type: object
required:
  - abilities
  - total_count
properties:
  abilities:
    type: array
    items:
      $ref: '#/components/schemas/AbilityInfo'
    description: 'BACKEND NOTE: Array of ability objects, ordered for optimal struct layout'
  total_count:
    type: integer
    format: int32
    description: Total number of abilities available
  pagination:
    type: object
    properties:
      offset:
        type: integer
        format: int32
      limit:
        type: integer
        format: int32
      has_more:
        type: boolean
    description: Pagination information
description: 'BACKEND NOTE: Response optimized for struct alignment with large arrays first.'

