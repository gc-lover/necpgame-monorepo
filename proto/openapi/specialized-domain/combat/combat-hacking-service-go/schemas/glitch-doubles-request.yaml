type: object
required:
  - player_id
  - skill_level
properties:
  player_id:
    type: string
    format: uuid
    description: Player unique identifier
  skill_level:
    type: integer
    minimum: 1
    maximum: 3
    description: Skill upgrade level (1-3)
  implant_id:
    type: string
    description: Cyberware implant ID used for the skill (optional, auto-detected if not provided)
  enemy_count:
    type: integer
    minimum: 3
    description: Number of enemies in range (required for activation)
description: 'Request to activate glitch doubles skill.

**BACKEND NOTE:** Minimal struct for fast processing.
Fields ordered for optimal CPU cache usage.

**Issue:** #143875814'
