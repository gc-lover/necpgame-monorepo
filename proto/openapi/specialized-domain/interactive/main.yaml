paths: {}

components:
  schemas:
    InteractiveObject:
      type: object
      properties:
        object_id:
          type: string
          format: uuid
        object_type:
          type: string
          enum: [chest, door, lever, terminal, container]
        position:
          $ref: '#/components/schemas/WorldPosition'
        state:
          type: string
          enum: [locked, unlocked, active, inactive]
        interactions:
          type: array
          items:
            type: string

    WorldPosition:
      type: object
      properties:
        x:
          type: number
        y:
          type: number
        z:
          type: number
        zone_id:
          type: string
          format: uuid

components:
  schemas:
    InteractiveObject:
      type: object
      properties:
        object_id:
          type: string
          format: uuid
        object_type:
          type: string
          enum: [chest, door, lever, terminal, container]
        position:
          $ref: '#/components/schemas/WorldPosition'
        state:
          type: string
          enum: [locked, unlocked, active, inactive]
        interactions:
          type: array
          items:
            type: string

    WorldPosition:
      type: object
      properties:
        x:
          type: number
        y:
          type: number
        z:
          type: number
        zone_id:
          type: string
          format: uuid

