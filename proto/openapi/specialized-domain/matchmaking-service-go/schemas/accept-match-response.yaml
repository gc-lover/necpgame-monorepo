type: object
required:
  - accepted
properties:
  accepted:
    type: boolean
  acceptedAt:
    type: string
    format: date-time
  allAccepted:
    type: boolean
    description: Whether all players have accepted
  matchStartingIn:
    type: integer
    description: Seconds until match starts (if all accepted)
