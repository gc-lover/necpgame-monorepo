type: object
required:
  - valid
properties:
  valid:
    type: boolean
  violations:
    type: array
    items:
      type: object
      required:
        - type
        - severity
      properties:
        type:
          type: string
          enum: [QUEUE_MANIPULATION, STATS_FALSIFICATION, TIMING_EXPLOIT]
        severity:
          type: string
          enum: [LOW, MEDIUM, HIGH, CRITICAL]
        description:
          type: string
  riskScore:
    type: number
    format: float
    minimum: 0
    maximum: 1
    description: Overall risk score
