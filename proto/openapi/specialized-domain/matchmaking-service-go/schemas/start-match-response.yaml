type: object
required:
  - started
properties:
  started:
    type: boolean
  startedAt:
    type: string
    format: date-time
  serverAddress:
    type: string
    description: Game server address
  serverPort:
    type: integer
  sessionToken:
    type: string
    description: Session token for server authentication
