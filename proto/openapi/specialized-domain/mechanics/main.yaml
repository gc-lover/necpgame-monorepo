paths: {}

components:
  schemas:
    WeaponMechanic:
      type: object
      properties:
        mechanic_id:
          type: string
          format: uuid
        weapon_type:
          type: string
        special_ability:
          type: string
        damage_modifier:
          type: number
        cooldown_seconds:
          type: integer

components:
  schemas:
    WeaponMechanic:
      type: object
      properties:
        mechanic_id:
          type: string
          format: uuid
        weapon_type:
          type: string
        special_ability:
          type: string
        damage_modifier:
          type: number
        cooldown_seconds:
          type: integer

