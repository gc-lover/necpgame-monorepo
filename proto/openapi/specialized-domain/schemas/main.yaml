paths: {}

components:
  schemas:
    CommonEntity:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    SpecializedConfig:
      type: object
      properties:
        config_id:
          type: string
          format: uuid
        config_type:
          type: string
        parameters:
          type: object
          additionalProperties: true

components:
  schemas:
    CommonEntity:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    SpecializedConfig:
      type: object
      properties:
        config_id:
          type: string
          format: uuid
        config_type:
          type: string
        parameters:
          type: object
          additionalProperties: true

