paths: {}

components:
  schemas:
    SpecializedService:
      type: object
      properties:
        service_id:
          type: string
          format: uuid
        service_type:
          type: string
        integration_points:
          type: array
          items:
            type: string
        status:
          type: string
          enum: [active, inactive, maintenance]

components:
  schemas:
    SpecializedService:
      type: object
      properties:
        service_id:
          type: string
          format: uuid
        service_type:
          type: string
        integration_points:
          type: array
          items:
            type: string
        status:
          type: string
          enum: [active, inactive, maintenance]

