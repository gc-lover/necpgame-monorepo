# Session management paths
# Issue: #2197

# NOTE: Session management endpoints are defined in main.yaml
# This file serves as documentation reference for session-related operations

# Core Session Operations:
# - POST /sessions - Create new combat session
# - GET /sessions/{session_id} - Get session details
# - PUT /sessions/{session_id} - Update session parameters
# - DELETE /sessions/{session_id} - End session
#
# Player Participation:
# - POST /sessions/{session_id}/join - Join as active player
# - POST /sessions/{session_id}/leave - Leave session
# - POST /sessions/{session_id}/spectate - Join as spectator
#
# Real-time Connections:
# - GET /ws/sessions/{session_id} - Player WebSocket
# - GET /ws/sessions/{session_id}/spectate - Spectator WebSocket

paths: { }
