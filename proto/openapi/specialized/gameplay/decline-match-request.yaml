type: object
required:
  - playerId
properties:
  playerId:
    type: string
    format: uuid
  reason:
    type: string
    enum: [ NOT_INTERESTED, BAD_MATCHUP, TECHNICAL_ISSUES ]
    default: NOT_INTERESTED
