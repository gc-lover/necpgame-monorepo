type: object
required:
  - playerId
properties:
  playerId:
    type: string
    format: uuid
  reason:
    type: string
    enum: [ USER_CANCELLED, TIMEOUT, MATCH_FOUND ]
    default: USER_CANCELLED
