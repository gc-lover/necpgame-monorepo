type: object
required:
  - player_id
  - screen_position
  - skill_level
properties:
  player_id:
    type: string
    format: uuid
    description: Player unique identifier
  screen_position:
    $ref: '#/components/schemas/Vector3'
    description: Position of the target screen in world coordinates
  skill_level:
    type: integer
    minimum: 1
    maximum: 3
    description: Skill upgrade level (1-3)
  implant_id:
    type: string
    description: Cyberware implant ID used for the skill (optional, auto-detected if not provided)
description: 'Request to activate screen hack blind skill.

**BACKEND NOTE:** Struct aligned for optimal memory layout.
Position fields ordered for cache efficiency.
Expected memory savings: 30-50%.

**Issue:** #143875347'
