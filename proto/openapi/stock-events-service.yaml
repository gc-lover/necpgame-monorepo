# Issue: SOLID refactoring - create OpenAPI spec for stock-events-service
openapi: 3.0.3
info:
  title: Stock Events Service API
  version: 1.0.0
  description: Stock market events API
servers:
  - url: http://localhost:8147
tags:
  - name: health
paths:
  /health:
    get:
      tags: [health]
      operationId: healthCheck
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
components:
  securitySchemes:
    $ref: "common.yaml#/components/securitySchemes"
  responses:
    $ref: "common.yaml#/components/responses"

