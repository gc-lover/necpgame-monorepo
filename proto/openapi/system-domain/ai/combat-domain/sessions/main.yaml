openapi: 3.0.3
info:
  title: Combat Sessions API Module
  description: Combat session management and state handling
  version: 1.0.0
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

paths:
  # Main session services

  # Gameplay integration
  $ref: './gameplay-combat-sessions-core-service.yaml#/paths'
  $ref: './gameplay-combat-sessions-actions-service.yaml#/paths'

  # Structured sessions service
  $ref: './combat-sessions-service/main.yaml#/paths'

components:
  schemas:
    $ref: './combat-session-schemas.yaml#/components/schemas'
    $ref: './combat-sessions-schemas.yaml#/components/schemas'
    $ref: './combat-sessions.yaml#/components/schemas'

    # Include structured schemas
    $ref: './combat-sessions-service/main.yaml#/components/schemas'
