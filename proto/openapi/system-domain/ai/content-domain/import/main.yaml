# Issue: #1547 - Content Import API for Backend
openapi: 3.0.3
info:
  title: "System AI Content Import API"
  description: "API for importing various content types (quests, NPCs, dialogues, lore) into the game system."
  version: 1.0.0
servers:
  - url: http://localhost:8080/api/v1/system-domain/ai/content-domain/import
    description: Local Development Server
paths:
  /quests:
    $ref: './paths/quest-import.yaml'
  /npcs:
    $ref: './paths/npc-import.yaml'
  /dialogues:
    $ref: './paths/dialogue-import.yaml'
  /lore:
    $ref: './paths/lore-import.yaml'
  /validate-export:
    $ref: './paths/validation-export.yaml'
components:
  schemas:
    $ref: './schemas/import-schemas.yaml'
  securitySchemes:
    $ref: '../../../../../common.yaml#/components/securitySchemes'
  responses:
    $ref: '../../../../../common.yaml#/components/responses'