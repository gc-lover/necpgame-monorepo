openapi: 3.0.3
info:
  title: Inventory Companion API Module
  description: Companion management and pet systems
  version: 1.0.0
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

paths:
  # Companion core services

  # Companion features
  $ref: './companion-combat-service.yaml#/paths'
  $ref: './companion-customization-service.yaml#/paths'
  $ref: './companion-events-service.yaml#/paths'
  $ref: './companion-progression-service.yaml#/paths'

  # Companion utilities
  $ref: './companion-catalog-service.yaml#/paths'
  $ref: './companion-summon-service.yaml#/paths'

components:
  schemas:
    $ref: './companion-schemas.yaml#/components/schemas'
