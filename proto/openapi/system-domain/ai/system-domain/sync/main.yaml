openapi: 3.0.3
info:
  title: System Sync API Module
  description: Data synchronization and conflict resolution
  version: 1.0.0
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

paths:
# Sync conflicts

# Sync state


components:
  schemas:
    $ref: './sync-conflicts-service.yaml#/components/schemas'
    $ref: './sync-state-service.yaml#/components/schemas'
