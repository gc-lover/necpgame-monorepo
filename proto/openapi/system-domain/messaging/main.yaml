openapi: 3.0.3
info:
  title: System Messaging API Module
  description: Message queue and messaging infrastructure
  version: 1.0.0

paths:
  $ref: './message-queue-service.yaml#/paths'

components:
  schemas:
    $ref: './message-queue-service.yaml#/components/schemas'
