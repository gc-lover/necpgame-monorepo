openapi: 3.0.3
info:
  title: Infrastructure Network API Module
  description: Network infrastructure and real-time communication
  version: 1.0.0

paths:
  # Network service
  $ref: './network-service/main.yaml#/paths'

components:
  schemas:
    # Network schemas
    $ref: './network-service/main.yaml#/components/schemas'
