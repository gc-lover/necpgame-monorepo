// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "abuse/"

				if l := len("abuse/"); len(elem) >= l && elem[0:l] == "abuse/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'b': // Prefix: "blacklist"

					if l := len("blacklist"); len(elem) >= l && elem[0:l] == "blacklist" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						switch r.Method {
						case "GET":
							s.handleGetBlacklistRequest([0]string{}, elemIsEscaped, w, r)
						case "POST":
							s.handleBlacklistEntityRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET,POST")
						}

						return
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "entry_id"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "DELETE":
								s.handleRemoveFromBlacklistRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "DELETE")
							}

							return
						}

					}

				case 'r': // Prefix: "report"

					if l := len("report"); len(elem) >= l && elem[0:l] == "report" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleReportAbuseRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleRateLimitingHealthRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			case 'm': // Prefix: "monitoring/rate-limits"

				if l := len("monitoring/rate-limits"); len(elem) >= l && elem[0:l] == "monitoring/rate-limits" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleGetRateLimitStatsRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			case 'q': // Prefix: "quotas/"

				if l := len("quotas/"); len(elem) >= l && elem[0:l] == "quotas/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "user_id"
				// Leaf parameter, slashes are prohibited
				idx := strings.IndexByte(elem, '/')
				if idx >= 0 {
					break
				}
				args[0] = elem
				elem = ""

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleGetUserQuotaRequest([1]string{
							args[0],
						}, elemIsEscaped, w, r)
					case "PUT":
						s.handleUpdateUserQuotaRequest([1]string{
							args[0],
						}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET,PUT")
					}

					return
				}

			case 'r': // Prefix: "rate-limits"

				if l := len("rate-limits"); len(elem) >= l && elem[0:l] == "rate-limits" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleListRateLimitsRequest([0]string{}, elemIsEscaped, w, r)
					case "POST":
						s.handleCreateRateLimitRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET,POST")
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "rule_id"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "DELETE":
							s.handleDeleteRateLimitRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "GET":
							s.handleGetRateLimitRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "PUT":
							s.handleUpdateRateLimitRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "DELETE,GET,PUT")
						}

						return
					}

				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "abuse/"

				if l := len("abuse/"); len(elem) >= l && elem[0:l] == "abuse/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'b': // Prefix: "blacklist"

					if l := len("blacklist"); len(elem) >= l && elem[0:l] == "blacklist" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						switch method {
						case "GET":
							r.name = GetBlacklistOperation
							r.summary = "Get blacklist entries"
							r.operationID = "getBlacklist"
							r.operationGroup = ""
							r.pathPattern = "/abuse/blacklist"
							r.args = args
							r.count = 0
							return r, true
						case "POST":
							r.name = BlacklistEntityOperation
							r.summary = "Add IP/service to blacklist"
							r.operationID = "blacklistEntity"
							r.operationGroup = ""
							r.pathPattern = "/abuse/blacklist"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "entry_id"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "DELETE":
								r.name = RemoveFromBlacklistOperation
								r.summary = "Remove from blacklist"
								r.operationID = "removeFromBlacklist"
								r.operationGroup = ""
								r.pathPattern = "/abuse/blacklist/{entry_id}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				case 'r': // Prefix: "report"

					if l := len("report"); len(elem) >= l && elem[0:l] == "report" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = ReportAbuseOperation
							r.summary = "Report potential abuse"
							r.operationID = "reportAbuse"
							r.operationGroup = ""
							r.pathPattern = "/abuse/report"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = RateLimitingHealthOperation
						r.summary = "Rate limiting service health check"
						r.operationID = "rateLimitingHealth"
						r.operationGroup = ""
						r.pathPattern = "/health"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 'm': // Prefix: "monitoring/rate-limits"

				if l := len("monitoring/rate-limits"); len(elem) >= l && elem[0:l] == "monitoring/rate-limits" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = GetRateLimitStatsOperation
						r.summary = "Get real-time rate limiting statistics"
						r.operationID = "getRateLimitStats"
						r.operationGroup = ""
						r.pathPattern = "/monitoring/rate-limits"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 'q': // Prefix: "quotas/"

				if l := len("quotas/"); len(elem) >= l && elem[0:l] == "quotas/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "user_id"
				// Leaf parameter, slashes are prohibited
				idx := strings.IndexByte(elem, '/')
				if idx >= 0 {
					break
				}
				args[0] = elem
				elem = ""

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = GetUserQuotaOperation
						r.summary = "Get user quota information"
						r.operationID = "getUserQuota"
						r.operationGroup = ""
						r.pathPattern = "/quotas/{user_id}"
						r.args = args
						r.count = 1
						return r, true
					case "PUT":
						r.name = UpdateUserQuotaOperation
						r.summary = "Update user quota limits"
						r.operationID = "updateUserQuota"
						r.operationGroup = ""
						r.pathPattern = "/quotas/{user_id}"
						r.args = args
						r.count = 1
						return r, true
					default:
						return
					}
				}

			case 'r': // Prefix: "rate-limits"

				if l := len("rate-limits"); len(elem) >= l && elem[0:l] == "rate-limits" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = ListRateLimitsOperation
						r.summary = "List rate limit rules"
						r.operationID = "listRateLimits"
						r.operationGroup = ""
						r.pathPattern = "/rate-limits"
						r.args = args
						r.count = 0
						return r, true
					case "POST":
						r.name = CreateRateLimitOperation
						r.summary = "Create a new rate limit rule"
						r.operationID = "createRateLimit"
						r.operationGroup = ""
						r.pathPattern = "/rate-limits"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "rule_id"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "DELETE":
							r.name = DeleteRateLimitOperation
							r.summary = "Delete rate limit rule"
							r.operationID = "deleteRateLimit"
							r.operationGroup = ""
							r.pathPattern = "/rate-limits/{rule_id}"
							r.args = args
							r.count = 1
							return r, true
						case "GET":
							r.name = GetRateLimitOperation
							r.summary = "Get rate limit rule details"
							r.operationID = "getRateLimit"
							r.operationGroup = ""
							r.pathPattern = "/rate-limits/{rule_id}"
							r.args = args
							r.count = 1
							return r, true
						case "PUT":
							r.name = UpdateRateLimitOperation
							r.summary = "Update rate limit rule configuration"
							r.operationID = "updateRateLimit"
							r.operationGroup = ""
							r.pathPattern = "/rate-limits/{rule_id}"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			}

		}
	}
	return r, false
}
