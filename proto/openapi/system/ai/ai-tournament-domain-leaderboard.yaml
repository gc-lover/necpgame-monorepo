openapi: 3.0.3
info:
  title: Tournament Leaderboard API Module
  description: Leaderboards and ranking systems
  version: 1.0.0
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

paths:
  # Core leaderboard services

  # Leaderboard features
  $ref: './leaderboard-ranks-service.yaml#/paths'
  $ref: './leaderboard-seasons-service.yaml#/paths'

  # Achievement leaderboards
  $ref: './achievement-leaderboard-service.yaml#/paths'
  $ref: './achievement-leaderboard-service.merged.yaml#/paths'

  # Referral leaderboards
  $ref: './referral-leaderboard-service.yaml#/paths'

components:
  schemas:
    # Include leaderboard schemas from core service
    $ref: './leaderboard-core-service.yaml#/components/schemas'
