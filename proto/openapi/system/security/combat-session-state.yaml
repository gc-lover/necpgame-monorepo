# Issue: #141665188
type: object
description: Combat session state schema
properties:
  session_id:
    type: string
    format: uuid
    description: Session identifier
  current_turn:
    type: integer
    description: Current turn number
  active_participants:
    type: array
    items:
      type: string
      format: uuid
    description: IDs of active participants
  environment_effects:
    type: array
    items:
      $ref: "./combat-effect.yaml"
    description: Current environment effects
