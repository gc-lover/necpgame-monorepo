openapi: 3.0.3
paths:
  /health/ws:
    get:
      summary: WebSocket
      operationId: healthWebSocketCheck
      tags:
      - Health
      responses:
        '101':
          description: WebSocket
        '503':
          description: WebSocket
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: null
