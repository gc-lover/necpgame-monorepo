openapi: 3.0.3
info:
  title: World Core API Module
  description: Core world state and city management systems
  version: 1.0.0

paths:
  # World state management
  $ref: './world-state-core-service.yaml#/paths'
  $ref: './world-state-sync-service.yaml#/paths'

  # World cities
  $ref: './world-cities-service-ext.yaml#/paths'

components:
  schemas:
    $ref: './world-state-core-service.yaml#/components/schemas'
