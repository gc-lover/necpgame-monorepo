type: object
required:
  - events
properties:
  events:
    type: array
    items:
      $ref: './world-event.yaml'
  totalEvents:
    type: integer
  region:
    type: string
  lastUpdated:
    type: string
    format: date-time
