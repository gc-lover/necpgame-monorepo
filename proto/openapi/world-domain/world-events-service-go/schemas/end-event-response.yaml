type: object
properties:
  eventId:
    type: string
    format: uuid
  status:
    type: string
    enum: [ended, cancelled]
    example: ended
  endedAt:
    type: string
    format: date-time
  totalParticipants:
    type: integer
    minimum: 0
  rewardsDistributed:
    type: boolean
    default: false
description: 'BACKEND NOTE: Fields ordered for struct alignment (large → small). Expected memory savings: 30-50%.'