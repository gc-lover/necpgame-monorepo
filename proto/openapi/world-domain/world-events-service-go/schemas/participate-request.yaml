type: object
required:
  - playerId
properties:
  playerId:
    type: string
    format: uuid
  team:
    type: string
    description: Team or faction choice
  role:
    type: string
    enum: [ LEADER, MEMBER, SUPPORT ]
    description: Player role in event
