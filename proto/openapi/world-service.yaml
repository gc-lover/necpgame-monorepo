# Issue: SOLID refactoring - main for world-service (continent-core domain)
openapi: 3.0.3
info:
  title: World Service API (Continents Domain)
  version: 1.0.0
  description: World system - using continent-core as primary module
servers:
  - url: http://localhost:8155
tags:
  - name: world
paths:
  /world/continents:
    $ref: "world-continent-core-service.yaml#/paths/~1world~1continents"
components:
  schemas:
    Continent:
      type: object
  securitySchemes:
    $ref: "common.yaml#/components/securitySchemes"
  responses:
    $ref: "common.yaml#/components/responses"
security:
  - BearerAuth: []

