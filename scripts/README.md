# NECP Game Scripts & Automation

## Обзор

Полная коллекция скриптов для разработки, тестирования, развертывания и обслуживания NECP Game микросервисов.

## 🚀 Быстрый Старт

```bash
# Полная проверка системы
./scripts/full-system-check.sh

# Создание нового сервиса
./scripts/create-service.sh my-service "My Service Description" 8123

# Генерация JWT токена для тестирования
python3 scripts/generate-jwt-token.py

# Резервное копирование
./scripts/backup-databases.sh
```

## 📋 Каталог Скриптов

### 🔍 Диагностика и Мониторинг

#### `system-check.sh/ps1`
**Назначение:** Проверка здоровья всех сервисов
```bash
./scripts/system-check.sh          # Linux/Mac
./scripts/system-check.ps1         # Windows
```

#### `full-system-check.sh`
**Назначение:** Полный набор проверок системы
```bash
./scripts/full-system-check.sh
# Запускает: health checks, API tests, load tests, security audit, performance analysis
```

#### `performance-analysis.sh`
**Назначение:** Анализ производительности системы
```bash
./scripts/performance-analysis.sh
# Генерирует отчет о CPU, памяти, API response times
```

#### `security-audit.sh`
**Назначение:** Проверка безопасности системы
```bash
./scripts/security-audit.sh
# Проверяет: file permissions, secrets, network security, API auth
```

### 🧪 Тестирование

#### `api-test.sh/ps1`
**Назначение:** Тестирование API endpoints
```bash
./scripts/api-test.sh              # Linux/Mac
./scripts/api-test.ps1             # Windows
```

#### `load-test.sh`
**Назначение:** Нагрузочное тестирование
```bash
./scripts/load-test.sh
# Настраивается через переменные окружения:
CONCURRENT_REQUESTS=10 TOTAL_REQUESTS=100 DURATION=30
```

#### `generate-jwt-token.py`
**Назначение:** Генерация JWT токенов для тестирования
```bash
python3 scripts/generate-jwt-token.py --user-id "test-user" --roles "player,premium"
# Вывод: токен, curl команда, полный JSON
```

### 🚀 Развертывание и Обслуживание

#### `create-service.sh`
**Назначение:** Создание нового микросервиса
```bash
./scripts/create-service.sh inventory-service "Inventory Management" 8131
# Создает: структуру папок, Dockerfile, main.go, handlers, service, repository, Makefile
```

#### `deploy-update.sh`
**Назначение:** Безопасное обновление сервисов
```bash
./scripts/deploy-update.sh achievement-service
./scripts/deploy-update.sh                      # Обновить все
```

#### `backup-databases.sh`
**Назначение:** Резервное копирование PostgreSQL и Redis
```bash
./scripts/backup-databases.sh
# Создает: SQL dump, RDB backup, manifest
```

#### `generate-release-notes.sh`
**Назначение:** Генерация release notes и changelog
```bash
./scripts/generate-release-notes.sh
# Создает: RELEASE_NOTES_v1.0.0.md, обновляет CHANGELOG.md, создает git tag
```

## 🔧 Использование в CI/CD

### GitHub Actions Пример

```yaml
- name: System Health Check
  run: ./scripts/system-check.sh

- name: API Testing
  run: ./scripts/api-test.sh

- name: Security Audit
  run: ./scripts/security-audit.sh

- name: Performance Analysis
  run: ./scripts/performance-analysis.sh

- name: Database Backup
  run: ./scripts/backup-databases.sh
```

### Docker Compose с Скриптами

```yaml
services:
  health-check:
    image: alpine
    command: sh -c "./scripts/system-check.sh && ./scripts/api-test.sh"
    volumes:
      - .:/app
    depends_on:
      - all-services
```

## 📊 Выходные Данные

### Отчеты и Логи

Все скрипты создают структурированные отчеты:

```
system_reports/
├── system_check_summary_20231220_143000.md    # Сводный отчет
├── system_health_20231220_143000.log          # Детали health checks
├── api_tests_20231220_143000.log              # API тесты
├── performance_20231220_143000.log            # Производительность
└── security_audit_20231220_143000.log         # Безопасность
```

### Форматы Выхода

- **Текстовые отчеты:** Человеко-читаемые с цветовой кодировкой
- **JSON данные:** Для интеграции с другими инструментами
- **Markdown документация:** Автоматически генерируемые отчеты
- **Логи:** Структурированные для анализа

## ⚙️ Конфигурация

### Переменные Окружения

```bash
# Общие настройки
BASE_URL=http://localhost
REPORT_DIR=./reports

# API тестирование
JWT_SECRET=your-secret-here

# Нагрузочное тестирование
CONCURRENT_REQUESTS=10
TOTAL_REQUESTS=1000
DURATION=60

# Резервное копирование
BACKUP_DIR=/mnt/backups
KEEP_BACKUPS=7

# Развертывание
ROLLBACK_ON_FAILURE=true
HEALTH_CHECK_TIMEOUT=60
```

### Кастомизация

Скрипты можно модифицировать:

1. **Пороги и лимиты** в переменных
2. **Список сервисов** для проверки
3. **Формат отчетов** (JSON, XML, Markdown)
4. **Интеграции** с внешними системами

## 🐛 Troubleshooting

### Распространенные Проблемы

#### Скрипт не запускается
```bash
chmod +x scripts/*.sh
# Или в PowerShell:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

#### Docker не доступен
```bash
# Проверить статус Docker
docker ps

# Запустить сервисы
docker-compose up -d
```

#### Сервисы не отвечают
```bash
# Проверить логи
docker logs necpgame-service-name-1

# Проверить health
curl http://localhost:PORT/health
```

#### Отчеты не создаются
```bash
# Проверить права на запись
ls -la reports/

# Создать директорию
mkdir -p reports
```

## 🔐 Безопасность

### Лучшие Практики

- **Не коммитить секреты** в скрипты
- **Использовать переменные окружения** для конфиденциальных данных
- **Валидировать входные данные** в скриптах
- **Логировать действия** для аудита

### Секретные Данные

```bash
# Использовать .env файлы
echo "JWT_SECRET=your-secret" > .env

# Или переменные CI/CD
JWT_SECRET: \${{ secrets.JWT_SECRET }}
```

## 📈 Производительность

### Оптимизации

- **Параллельное выполнение** проверок
- **Кеширование результатов** для повторных запусков
- **Инкрементальные проверки** только измененных сервисов
- **Распределенное выполнение** для больших кластеров

### Масштабирование

```bash
# Параллельный запуск
./scripts/system-check.sh &
./scripts/api-test.sh &
./scripts/security-audit.sh &
wait
```

## 🤝 Contributing

### Добавление Новых Скриптов

1. **Создать скрипт** в папке `scripts/`
2. **Добавить документацию** в этот README
3. **Сделать исполняемым**: `chmod +x scripts/new-script.sh`
4. **Добавить в CI/CD** если применимо

### Соглашения по Кодингу

- **Bash**: Google Shell Style Guide
- **PowerShell**: PowerShell Best Practices
- **Python**: PEP 8
- **Документация**: Всегда добавлять docstrings и комментарии

## 📞 Поддержка

- **Документация**: Этот README
- **Примеры**: Смотреть существующие скрипты
- **Issues**: Создавать в GitHub для багов и фич
- **Wiki**: Расширенная документация

---

**🎯 NECP Game Scripts - Полная автоматизация DevOps для микросервисов!**
