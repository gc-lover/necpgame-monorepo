{
  "name": "necpgame-monorepo",
  "version": "1.0.0",
  "description": "NECPGAME Enterprise-Grade Gaming Platform",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js,.ts,.jsx,.tsx",
    "lint:fix": "eslint . --ext .js,.ts,.jsx,.tsx --fix",
    "lint:check": "eslint . --ext .js,.ts,.jsx,.tsx --max-warnings 0",
    "lint:emoji": "node -e \"const fs = require('fs'); const files = require('glob').sync('**/*.{js,ts,jsx,tsx}', {ignore: ['node_modules/**', '.git/**']}); files.forEach(f => { const content = fs.readFileSync(f, 'utf8'); if (/[^\x00-\x7F]/.test(content)) { console.log('Found Unicode in:', f); process.exit(1); } }); console.log('No Unicode characters found');\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-plugin-unicode-comments": "^2.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
