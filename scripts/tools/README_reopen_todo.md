# Скрипт переоткрытия задач Todo

Python скрипт для автоматического переоткрытия GitHub Issues, которые имеют статус "Todo" в GitHub Projects.

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
cd scripts/tools
pip install -r requirements.txt
```

### 2. Настройка токена GitHub
```bash
# Создайте .env файл на основе примера
cp env-example.txt .env

# Отредактируйте .env файл и добавьте ваш GitHub токен
# GITHUB_TOKEN=ghp_your_personal_access_token
```

### 3. Получение GitHub токена
1. Перейдите в [GitHub Settings → Developer settings → Personal access tokens](https://github.com/settings/tokens)
2. Создайте новый токен с правами:
   - `repo` (для доступа к репозиториям)
   - `project` (для работы с Projects)
3. Скопируйте токен и вставьте в `.env` файл

### 4. Запуск скрипта
```bash
# Интерактивный режим (запросит подтверждение)
python reopen_todo_issues.py

# Автоматический режим (без подтверждения)
python reopen_todo_issues.py --yes
```

## ⚙️ Конфигурация

Скрипт использует следующие переменные окружения:

| Переменная | Значение по умолчанию | Описание |
|------------|------------------------|----------|
| `GITHUB_TOKEN` | - | **Обязательно**. GitHub Personal Access Token |
| `GITHUB_OWNER` | `gc-lover` | Владелец репозитория |
| `GITHUB_REPO` | `necpgame-monorepo` | Название репозитория |
| `GITHUB_PROJECT_NUMBER` | `1` | Номер проекта в GitHub Projects |

## 🔍 Как работает скрипт

1. **Получает ID проекта** по номеру через GitHub API
2. **Загружает все items** из GitHub Projects (до 1000 items)
3. **Фильтрует задачи** со статусом "Todo"
4. **Переоткрывает issues** через GitHub Issues API

## 📋 Что делает скрипт

- ✅ Переоткрывает только задачи со статусом "Todo"
- ✅ Показывает список задач перед переоткрытием
- ✅ Запрашивает подтверждение (если не указан `--yes`)
- ✅ Показывает прогресс выполнения
- ✅ Игнорирует задачи со статусом "Done"

## 🛠️ Устранение проблем

### Ошибка аутентификации
```
❌ Ошибка: Не найден GITHUB_TOKEN в переменных окружения
```
**Решение:** Проверьте, что `.env` файл создан и содержит правильный токен.

### Проект не найден
```
❌ Проект с номером X не найден
```
**Решение:** Проверьте `GITHUB_PROJECT_NUMBER` в `.env` файле.

### Недостаточно прав токена
```
❌ Ошибка получения проектов: 403
```
**Решение:** Убедитесь, что токен имеет права `repo` и `project`.

## 🔧 Расширенное использование

### Изменение репозитория
```bash
# Для другого репозитория
export GITHUB_OWNER=your-org
export GITHUB_REPO=your-repo
export GITHUB_PROJECT_NUMBER=2
python reopen_todo_issues.py
```

### Использование в CI/CD
```yaml
- name: Reopen Todo Issues
  run: |
    cd scripts/tools
    python reopen_todo_issues.py --yes
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

## 📊 Логика работы

Скрипт ищет поле `Status` в GitHub Projects и переоткрывает соответствующие issues:

```python
# Пример структуры данных
{
  "status": "Todo",  # <- Это ищет скрипт
  "content": {
    "type": "Issue",
    "number": 123,
    "title": "Some task"
  }
}
```

## 🤝 Альтернативы

Если скрипт не подходит, можно использовать:
- **GH CLI**: `gh issue reopen <number>`
- **MCP серверы** для интеграции с IDE
- **GitHub Actions** для автоматизации

## 📝 Примечания

- Скрипт работает только с GitHub Projects V2 API
- Максимум 1000 items на проект (ограничение GitHub API)
- Требуется Python 3.6+