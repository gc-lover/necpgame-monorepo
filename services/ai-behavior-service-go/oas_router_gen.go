// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "a"

				if l := len("a"); len(elem) >= l && elem[0:l] == "a" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'd': // Prefix: "daptive-difficulty/player/"

					if l := len("daptive-difficulty/player/"); len(elem) >= l && elem[0:l] == "daptive-difficulty/player/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "account_id"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case '/': // Prefix: "/assessment"

						if l := len("/assessment"); len(elem) >= l && elem[0:l] == "/assessment" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetPlayerSkillAssessmentRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							case "POST":
								s.handleUpdatePlayerSkillAssessmentRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET,POST")
							}

							return
						}

					}

				case 'n': // Prefix: "n"

					if l := len("n"); len(elem) >= l && elem[0:l] == "n" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'a': // Prefix: "alytics/player/"

						if l := len("alytics/player/"); len(elem) >= l && elem[0:l] == "alytics/player/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "account_id"
						// Match until "/"
						idx := strings.IndexByte(elem, '/')
						if idx < 0 {
							idx = len(elem)
						}
						args[0] = elem[:idx]
						elem = elem[idx:]

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case '/': // Prefix: "/behavior"

							if l := len("/behavior"); len(elem) >= l && elem[0:l] == "/behavior" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleGetPlayerBehaviorAnalyticsRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

						}

					case 't': // Prefix: "ti-cheat/analyze/"

						if l := len("ti-cheat/analyze/"); len(elem) >= l && elem[0:l] == "ti-cheat/analyze/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "account_id"
						// Match until "/"
						idx := strings.IndexByte(elem, '/')
						if idx < 0 {
							idx = len(elem)
						}
						args[0] = elem[:idx]
						elem = elem[idx:]

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case '/': // Prefix: "/suspicious"

							if l := len("/suspicious"); len(elem) >= l && elem[0:l] == "/suspicious" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleAnalyzeSuspiciousBehaviorRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "POST")
								}

								return
							}

						}

					}

				}

			case 'c': // Prefix: "combat-ai/predict/"

				if l := len("combat-ai/predict/"); len(elem) >= l && elem[0:l] == "combat-ai/predict/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "account_id"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/strategy"

					if l := len("/strategy"); len(elem) >= l && elem[0:l] == "/strategy" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetCombatStrategyPredictionRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleAiBehaviorServiceHealthCheckRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'b': // Prefix: "batch"

						if l := len("batch"); len(elem) >= l && elem[0:l] == "batch" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleExampleDomainBatchHealthCheckRequest([0]string{}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					case 'w': // Prefix: "ws"

						if l := len("ws"); len(elem) >= l && elem[0:l] == "ws" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleExampleDomainHealthWebSocketRequest([0]string{}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					}

				}

			case 'm': // Prefix: "models/train"

				if l := len("models/train"); len(elem) >= l && elem[0:l] == "models/train" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "POST":
						s.handleTriggerModelTrainingRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "POST")
					}

					return
				}

			case 'n': // Prefix: "npc-behavior/"

				if l := len("npc-behavior/"); len(elem) >= l && elem[0:l] == "npc-behavior/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'g': // Prefix: "generate"

					if l := len("generate"); len(elem) >= l && elem[0:l] == "generate" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleGenerateProceduralNpcRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				case 'n': // Prefix: "npcs/"

					if l := len("npcs/"); len(elem) >= l && elem[0:l] == "npcs/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "npc_id"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case '/': // Prefix: "/behavior"

						if l := len("/behavior"); len(elem) >= l && elem[0:l] == "/behavior" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetNpcBehaviorRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							case "PUT":
								s.handleUpdateNpcBehaviorRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET,PUT")
							}

							return
						}

					}

				}

			case 'p': // Prefix: "procedural/quest/generate"

				if l := len("procedural/quest/generate"); len(elem) >= l && elem[0:l] == "procedural/quest/generate" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "POST":
						s.handleGenerateProceduralQuestRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "POST")
					}

					return
				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "a"

				if l := len("a"); len(elem) >= l && elem[0:l] == "a" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'd': // Prefix: "daptive-difficulty/player/"

					if l := len("daptive-difficulty/player/"); len(elem) >= l && elem[0:l] == "daptive-difficulty/player/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "account_id"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case '/': // Prefix: "/assessment"

						if l := len("/assessment"); len(elem) >= l && elem[0:l] == "/assessment" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetPlayerSkillAssessmentOperation
								r.summary = "Get player skill assessment for adaptive difficulty"
								r.operationID = "getPlayerSkillAssessment"
								r.operationGroup = ""
								r.pathPattern = "/adaptive-difficulty/player/{account_id}/assessment"
								r.args = args
								r.count = 1
								return r, true
							case "POST":
								r.name = UpdatePlayerSkillAssessmentOperation
								r.summary = "Update player skill assessment"
								r.operationID = "updatePlayerSkillAssessment"
								r.operationGroup = ""
								r.pathPattern = "/adaptive-difficulty/player/{account_id}/assessment"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				case 'n': // Prefix: "n"

					if l := len("n"); len(elem) >= l && elem[0:l] == "n" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'a': // Prefix: "alytics/player/"

						if l := len("alytics/player/"); len(elem) >= l && elem[0:l] == "alytics/player/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "account_id"
						// Match until "/"
						idx := strings.IndexByte(elem, '/')
						if idx < 0 {
							idx = len(elem)
						}
						args[0] = elem[:idx]
						elem = elem[idx:]

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case '/': // Prefix: "/behavior"

							if l := len("/behavior"); len(elem) >= l && elem[0:l] == "/behavior" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = GetPlayerBehaviorAnalyticsOperation
									r.summary = "Get player behavior analytics for AI training"
									r.operationID = "getPlayerBehaviorAnalytics"
									r.operationGroup = ""
									r.pathPattern = "/analytics/player/{account_id}/behavior"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						}

					case 't': // Prefix: "ti-cheat/analyze/"

						if l := len("ti-cheat/analyze/"); len(elem) >= l && elem[0:l] == "ti-cheat/analyze/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "account_id"
						// Match until "/"
						idx := strings.IndexByte(elem, '/')
						if idx < 0 {
							idx = len(elem)
						}
						args[0] = elem[:idx]
						elem = elem[idx:]

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case '/': // Prefix: "/suspicious"

							if l := len("/suspicious"); len(elem) >= l && elem[0:l] == "/suspicious" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = AnalyzeSuspiciousBehaviorOperation
									r.summary = "Analyze player for suspicious behavior using AI"
									r.operationID = "analyzeSuspiciousBehavior"
									r.operationGroup = ""
									r.pathPattern = "/anti-cheat/analyze/{account_id}/suspicious"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						}

					}

				}

			case 'c': // Prefix: "combat-ai/predict/"

				if l := len("combat-ai/predict/"); len(elem) >= l && elem[0:l] == "combat-ai/predict/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "account_id"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/strategy"

					if l := len("/strategy"); len(elem) >= l && elem[0:l] == "/strategy" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetCombatStrategyPredictionOperation
							r.summary = "Get combat strategy prediction for player"
							r.operationID = "getCombatStrategyPrediction"
							r.operationGroup = ""
							r.pathPattern = "/combat-ai/predict/{account_id}/strategy"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = AiBehaviorServiceHealthCheckOperation
						r.summary = "AI behavior service health check"
						r.operationID = "aiBehaviorServiceHealthCheck"
						r.operationGroup = ""
						r.pathPattern = "/health"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'b': // Prefix: "batch"

						if l := len("batch"); len(elem) >= l && elem[0:l] == "batch" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = ExampleDomainBatchHealthCheckOperation
								r.summary = "Batch health check for multiple domain services"
								r.operationID = "exampleDomainBatchHealthCheck"
								r.operationGroup = ""
								r.pathPattern = "/health/batch"
								r.args = args
								r.count = 0
								return r, true
							default:
								return
							}
						}

					case 'w': // Prefix: "ws"

						if l := len("ws"); len(elem) >= l && elem[0:l] == "ws" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = ExampleDomainHealthWebSocketOperation
								r.summary = "Real-time health monitoring WebSocket"
								r.operationID = "exampleDomainHealthWebSocket"
								r.operationGroup = ""
								r.pathPattern = "/health/ws"
								r.args = args
								r.count = 0
								return r, true
							default:
								return
							}
						}

					}

				}

			case 'm': // Prefix: "models/train"

				if l := len("models/train"); len(elem) >= l && elem[0:l] == "models/train" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "POST":
						r.name = TriggerModelTrainingOperation
						r.summary = "Trigger AI model training for behavior prediction"
						r.operationID = "triggerModelTraining"
						r.operationGroup = ""
						r.pathPattern = "/models/train"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 'n': // Prefix: "npc-behavior/"

				if l := len("npc-behavior/"); len(elem) >= l && elem[0:l] == "npc-behavior/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'g': // Prefix: "generate"

					if l := len("generate"); len(elem) >= l && elem[0:l] == "generate" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = GenerateProceduralNpcOperation
							r.summary = "Generate procedural NPC with AI behavior"
							r.operationID = "generateProceduralNpc"
							r.operationGroup = ""
							r.pathPattern = "/npc-behavior/generate"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				case 'n': // Prefix: "npcs/"

					if l := len("npcs/"); len(elem) >= l && elem[0:l] == "npcs/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "npc_id"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case '/': // Prefix: "/behavior"

						if l := len("/behavior"); len(elem) >= l && elem[0:l] == "/behavior" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetNpcBehaviorOperation
								r.summary = "Get NPC behavior configuration"
								r.operationID = "getNpcBehavior"
								r.operationGroup = ""
								r.pathPattern = "/npc-behavior/npcs/{npc_id}/behavior"
								r.args = args
								r.count = 1
								return r, true
							case "PUT":
								r.name = UpdateNpcBehaviorOperation
								r.summary = "Update NPC behavior configuration"
								r.operationID = "updateNpcBehavior"
								r.operationGroup = ""
								r.pathPattern = "/npc-behavior/npcs/{npc_id}/behavior"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				}

			case 'p': // Prefix: "procedural/quest/generate"

				if l := len("procedural/quest/generate"); len(elem) >= l && elem[0:l] == "procedural/quest/generate" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "POST":
						r.name = GenerateProceduralQuestOperation
						r.summary = "Generate procedural quest with AI algorithms"
						r.operationID = "generateProceduralQuest"
						r.operationGroup = ""
						r.pathPattern = "/procedural/quest/generate"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			}

		}
	}
	return r, false
}
