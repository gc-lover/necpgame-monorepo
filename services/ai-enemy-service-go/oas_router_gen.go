// Code generated by ogen, DO NOT EDIT.

package main

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'e': // Prefix: "enemies"

				if l := len("enemies"); len(elem) >= l && elem[0:l] == "enemies" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleGetActiveEnemiesRequest([0]string{}, elemIsEscaped, w, r)
					case "POST":
						s.handleSpawnEnemyRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET,POST")
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "enemy_id"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						switch r.Method {
						case "DELETE":
							s.handleRemoveEnemyRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "GET":
							s.handleGetEnemyRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "PUT":
							s.handleUpdateEnemyRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "DELETE,GET,PUT")
						}

						return
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'a': // Prefix: "abilities"

							if l := len("abilities"); len(elem) >= l && elem[0:l] == "abilities" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleUseEnemyAbilityRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "POST")
								}

								return
							}

						case 'b': // Prefix: "behavior"

							if l := len("behavior"); len(elem) >= l && elem[0:l] == "behavior" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleGetEnemyBehaviorRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								case "POST":
									s.handleTriggerEnemyBehaviorRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET,POST")
								}

								return
							}

						case 'd': // Prefix: "damage"

							if l := len("damage"); len(elem) >= l && elem[0:l] == "damage" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleApplyDamageRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "POST")
								}

								return
							}

						}

					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleGetHealthRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			case 's': // Prefix: "squads"

				if l := len("squads"); len(elem) >= l && elem[0:l] == "squads" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleGetActiveSquadsRequest([0]string{}, elemIsEscaped, w, r)
					case "POST":
						s.handleCreateSquadRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET,POST")
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "squad_id"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetSquadRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "PUT":
							s.handleUpdateSquadRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET,PUT")
						}

						return
					}

				}

			case 't': // Prefix: "telemetry"

				if l := len("telemetry"); len(elem) >= l && elem[0:l] == "telemetry" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleGetTelemetryRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			case 'z': // Prefix: "zones/"

				if l := len("zones/"); len(elem) >= l && elem[0:l] == "zones/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "zone_id"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/sync"

					if l := len("/sync"); len(elem) >= l && elem[0:l] == "/sync" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetZoneSyncRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'e': // Prefix: "enemies"

				if l := len("enemies"); len(elem) >= l && elem[0:l] == "enemies" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = GetActiveEnemiesOperation
						r.summary = "Get all active AI enemies in zone"
						r.operationID = "getActiveEnemies"
						r.operationGroup = ""
						r.pathPattern = "/enemies"
						r.args = args
						r.count = 0
						return r, true
					case "POST":
						r.name = SpawnEnemyOperation
						r.summary = "Spawn new AI enemy"
						r.operationID = "spawnEnemy"
						r.operationGroup = ""
						r.pathPattern = "/enemies"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "enemy_id"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						switch method {
						case "DELETE":
							r.name = RemoveEnemyOperation
							r.summary = "Remove enemy"
							r.operationID = "removeEnemy"
							r.operationGroup = ""
							r.pathPattern = "/enemies/{enemy_id}"
							r.args = args
							r.count = 1
							return r, true
						case "GET":
							r.name = GetEnemyOperation
							r.summary = "Get enemy details"
							r.operationID = "getEnemy"
							r.operationGroup = ""
							r.pathPattern = "/enemies/{enemy_id}"
							r.args = args
							r.count = 1
							return r, true
						case "PUT":
							r.name = UpdateEnemyOperation
							r.summary = "Update enemy state"
							r.operationID = "updateEnemy"
							r.operationGroup = ""
							r.pathPattern = "/enemies/{enemy_id}"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'a': // Prefix: "abilities"

							if l := len("abilities"); len(elem) >= l && elem[0:l] == "abilities" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = UseEnemyAbilityOperation
									r.summary = "Use enemy ability"
									r.operationID = "useEnemyAbility"
									r.operationGroup = ""
									r.pathPattern = "/enemies/{enemy_id}/abilities"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						case 'b': // Prefix: "behavior"

							if l := len("behavior"); len(elem) >= l && elem[0:l] == "behavior" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = GetEnemyBehaviorOperation
									r.summary = "Get enemy behavior state"
									r.operationID = "getEnemyBehavior"
									r.operationGroup = ""
									r.pathPattern = "/enemies/{enemy_id}/behavior"
									r.args = args
									r.count = 1
									return r, true
								case "POST":
									r.name = TriggerEnemyBehaviorOperation
									r.summary = "Trigger enemy behavior"
									r.operationID = "triggerEnemyBehavior"
									r.operationGroup = ""
									r.pathPattern = "/enemies/{enemy_id}/behavior"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						case 'd': // Prefix: "damage"

							if l := len("damage"); len(elem) >= l && elem[0:l] == "damage" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = ApplyDamageOperation
									r.summary = "Apply damage to enemy"
									r.operationID = "applyDamage"
									r.operationGroup = ""
									r.pathPattern = "/enemies/{enemy_id}/damage"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						}

					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = GetHealthOperation
						r.summary = "Health check endpoint"
						r.operationID = "getHealth"
						r.operationGroup = ""
						r.pathPattern = "/health"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 's': // Prefix: "squads"

				if l := len("squads"); len(elem) >= l && elem[0:l] == "squads" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = GetActiveSquadsOperation
						r.summary = "Get all active enemy squads"
						r.operationID = "getActiveSquads"
						r.operationGroup = ""
						r.pathPattern = "/squads"
						r.args = args
						r.count = 0
						return r, true
					case "POST":
						r.name = CreateSquadOperation
						r.summary = "Create new enemy squad"
						r.operationID = "createSquad"
						r.operationGroup = ""
						r.pathPattern = "/squads"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "squad_id"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetSquadOperation
							r.summary = "Get squad details"
							r.operationID = "getSquad"
							r.operationGroup = ""
							r.pathPattern = "/squads/{squad_id}"
							r.args = args
							r.count = 1
							return r, true
						case "PUT":
							r.name = UpdateSquadOperation
							r.summary = "Update squad coordination"
							r.operationID = "updateSquad"
							r.operationGroup = ""
							r.pathPattern = "/squads/{squad_id}"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			case 't': // Prefix: "telemetry"

				if l := len("telemetry"); len(elem) >= l && elem[0:l] == "telemetry" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = GetTelemetryOperation
						r.summary = "Get AI system telemetry"
						r.operationID = "getTelemetry"
						r.operationGroup = ""
						r.pathPattern = "/telemetry"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 'z': // Prefix: "zones/"

				if l := len("zones/"); len(elem) >= l && elem[0:l] == "zones/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "zone_id"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/sync"

					if l := len("/sync"); len(elem) >= l && elem[0:l] == "/sync" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetZoneSyncOperation
							r.summary = "Get zone synchronization state"
							r.operationID = "getZoneSync"
							r.operationGroup = ""
							r.pathPattern = "/zones/{zone_id}/sync"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			}

		}
	}
	return r, false
}
