// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "api/v1/analysis/"

				if l := len("api/v1/analysis/"); len(elem) >= l && elem[0:l] == "api/v1/analysis/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'h': // Prefix: "health"

					if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleExampleDomainHealthCheckRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				case 'n': // Prefix: "network/"

					if l := len("network/"); len(elem) >= l && elem[0:l] == "network/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'a': // Prefix: "architecture/"

						if l := len("architecture/"); len(elem) >= l && elem[0:l] == "architecture/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'b': // Prefix: "bottlenecks"

							if l := len("bottlenecks"); len(elem) >= l && elem[0:l] == "bottlenecks" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleAnalyzeBottlenecksRequest([0]string{}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

						case 's': // Prefix: "scalability"

							if l := len("scalability"); len(elem) >= l && elem[0:l] == "scalability" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleAnalyzeScalabilityRequest([0]string{}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "POST")
								}

								return
							}

						}

					case 'g': // Prefix: "geographic/distribution"

						if l := len("geographic/distribution"); len(elem) >= l && elem[0:l] == "geographic/distribution" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetGeographicDistributionRequest([0]string{}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					case 'h': // Prefix: "health"

						if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleNetworkAnalysisHealthCheckRequest([0]string{}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					case 'm': // Prefix: "monitoring/"

						if l := len("monitoring/"); len(elem) >= l && elem[0:l] == "monitoring/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'l': // Prefix: "latency"

							if l := len("latency"); len(elem) >= l && elem[0:l] == "latency" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleGetLatencyStatsRequest([0]string{}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

						case 's': // Prefix: "sessions"

							if l := len("sessions"); len(elem) >= l && elem[0:l] == "sessions" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleGetActiveSessionsRequest([0]string{}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

						}

					case 's': // Prefix: "security/threats"

						if l := len("security/threats"); len(elem) >= l && elem[0:l] == "security/threats" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetSecurityThreatsRequest([0]string{}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					}

				}

			case 'e': // Prefix: "examples"

				if l := len("examples"); len(elem) >= l && elem[0:l] == "examples" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleListExamplesRequest([0]string{}, elemIsEscaped, w, r)
					case "POST":
						s.handleCreateExampleRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET,POST")
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "example_id"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "DELETE":
							s.handleDeleteExampleRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "GET":
							s.handleGetExampleRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "PUT":
							s.handleUpdateExampleRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "DELETE,GET,PUT")
						}

						return
					}

				}

			case 'h': // Prefix: "health/"

				if l := len("health/"); len(elem) >= l && elem[0:l] == "health/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'b': // Prefix: "batch"

					if l := len("batch"); len(elem) >= l && elem[0:l] == "batch" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleExampleDomainBatchHealthCheckRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				case 'w': // Prefix: "ws"

					if l := len("ws"); len(elem) >= l && elem[0:l] == "ws" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleExampleDomainHealthWebSocketRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "api/v1/analysis/"

				if l := len("api/v1/analysis/"); len(elem) >= l && elem[0:l] == "api/v1/analysis/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'h': // Prefix: "health"

					if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = ExampleDomainHealthCheckOperation
							r.summary = "Analysis domain health check"
							r.operationID = "exampleDomainHealthCheck"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/analysis/health"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				case 'n': // Prefix: "network/"

					if l := len("network/"); len(elem) >= l && elem[0:l] == "network/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'a': // Prefix: "architecture/"

						if l := len("architecture/"); len(elem) >= l && elem[0:l] == "architecture/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'b': // Prefix: "bottlenecks"

							if l := len("bottlenecks"); len(elem) >= l && elem[0:l] == "bottlenecks" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = AnalyzeBottlenecksOperation
									r.summary = "Analyze network bottlenecks"
									r.operationID = "analyzeBottlenecks"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/analysis/network/architecture/bottlenecks"
									r.args = args
									r.count = 0
									return r, true
								default:
									return
								}
							}

						case 's': // Prefix: "scalability"

							if l := len("scalability"); len(elem) >= l && elem[0:l] == "scalability" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = AnalyzeScalabilityOperation
									r.summary = "Analyze network scalability"
									r.operationID = "analyzeScalability"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/analysis/network/architecture/scalability"
									r.args = args
									r.count = 0
									return r, true
								default:
									return
								}
							}

						}

					case 'g': // Prefix: "geographic/distribution"

						if l := len("geographic/distribution"); len(elem) >= l && elem[0:l] == "geographic/distribution" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetGeographicDistributionOperation
								r.summary = "Get player geographic distribution"
								r.operationID = "getGeographicDistribution"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/analysis/network/geographic/distribution"
								r.args = args
								r.count = 0
								return r, true
							default:
								return
							}
						}

					case 'h': // Prefix: "health"

						if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = NetworkAnalysisHealthCheckOperation
								r.summary = "Network analysis service health check"
								r.operationID = "networkAnalysisHealthCheck"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/analysis/network/health"
								r.args = args
								r.count = 0
								return r, true
							default:
								return
							}
						}

					case 'm': // Prefix: "monitoring/"

						if l := len("monitoring/"); len(elem) >= l && elem[0:l] == "monitoring/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'l': // Prefix: "latency"

							if l := len("latency"); len(elem) >= l && elem[0:l] == "latency" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = GetLatencyStatsOperation
									r.summary = "Get latency statistics"
									r.operationID = "getLatencyStats"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/analysis/network/monitoring/latency"
									r.args = args
									r.count = 0
									return r, true
								default:
									return
								}
							}

						case 's': // Prefix: "sessions"

							if l := len("sessions"); len(elem) >= l && elem[0:l] == "sessions" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = GetActiveSessionsOperation
									r.summary = "Get active network sessions"
									r.operationID = "getActiveSessions"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/analysis/network/monitoring/sessions"
									r.args = args
									r.count = 0
									return r, true
								default:
									return
								}
							}

						}

					case 's': // Prefix: "security/threats"

						if l := len("security/threats"); len(elem) >= l && elem[0:l] == "security/threats" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetSecurityThreatsOperation
								r.summary = "Get network security threats"
								r.operationID = "getSecurityThreats"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/analysis/network/security/threats"
								r.args = args
								r.count = 0
								return r, true
							default:
								return
							}
						}

					}

				}

			case 'e': // Prefix: "examples"

				if l := len("examples"); len(elem) >= l && elem[0:l] == "examples" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = ListExamplesOperation
						r.summary = "List examples with filtering and pagination"
						r.operationID = "listExamples"
						r.operationGroup = ""
						r.pathPattern = "/examples"
						r.args = args
						r.count = 0
						return r, true
					case "POST":
						r.name = CreateExampleOperation
						r.summary = "Create new example"
						r.operationID = "createExample"
						r.operationGroup = ""
						r.pathPattern = "/examples"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "example_id"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "DELETE":
							r.name = DeleteExampleOperation
							r.summary = "Delete example"
							r.operationID = "deleteExample"
							r.operationGroup = ""
							r.pathPattern = "/examples/{example_id}"
							r.args = args
							r.count = 1
							return r, true
						case "GET":
							r.name = GetExampleOperation
							r.summary = "Get specific example by ID"
							r.operationID = "getExample"
							r.operationGroup = ""
							r.pathPattern = "/examples/{example_id}"
							r.args = args
							r.count = 1
							return r, true
						case "PUT":
							r.name = UpdateExampleOperation
							r.summary = "Update existing example"
							r.operationID = "updateExample"
							r.operationGroup = ""
							r.pathPattern = "/examples/{example_id}"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			case 'h': // Prefix: "health/"

				if l := len("health/"); len(elem) >= l && elem[0:l] == "health/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'b': // Prefix: "batch"

					if l := len("batch"); len(elem) >= l && elem[0:l] == "batch" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = ExampleDomainBatchHealthCheckOperation
							r.summary = "Batch health check for multiple domain services"
							r.operationID = "exampleDomainBatchHealthCheck"
							r.operationGroup = ""
							r.pathPattern = "/health/batch"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				case 'w': // Prefix: "ws"

					if l := len("ws"); len(elem) >= l && elem[0:l] == "ws" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = ExampleDomainHealthWebSocketOperation
							r.summary = "Real-time health monitoring WebSocket"
							r.operationID = "exampleDomainHealthWebSocket"
							r.operationGroup = ""
							r.pathPattern = "/health/ws"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				}

			}

		}
	}
	return r, false
}
