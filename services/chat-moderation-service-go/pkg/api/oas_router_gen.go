// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/api/v1/moderation/"

			if l := len("/api/v1/moderation/"); len(elem) >= l && elem[0:l] == "/api/v1/moderation/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "actions/"

				if l := len("actions/"); len(elem) >= l && elem[0:l] == "actions/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "violation_id"
				// Leaf parameter, slashes are prohibited
				idx := strings.IndexByte(elem, '/')
				if idx >= 0 {
					break
				}
				args[0] = elem
				elem = ""

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "POST":
						s.handleApplyModerationActionRequest([1]string{
							args[0],
						}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "POST")
					}

					return
				}

			case 'c': // Prefix: "check-message"

				if l := len("check-message"); len(elem) >= l && elem[0:l] == "check-message" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "POST":
						s.handleCheckMessageRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "POST")
					}

					return
				}

			case 'l': // Prefix: "logs"

				if l := len("logs"); len(elem) >= l && elem[0:l] == "logs" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleGetModerationLogsRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			case 'r': // Prefix: "rules"

				if l := len("rules"); len(elem) >= l && elem[0:l] == "rules" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleGetModerationRulesRequest([0]string{}, elemIsEscaped, w, r)
					case "POST":
						s.handleCreateModerationRuleRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET,POST")
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "rule_id"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "DELETE":
							s.handleDeleteModerationRuleRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "GET":
							s.handleGetModerationRuleRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "PUT":
							s.handleUpdateModerationRuleRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "DELETE,GET,PUT")
						}

						return
					}

				}

			case 'v': // Prefix: "violations"

				if l := len("violations"); len(elem) >= l && elem[0:l] == "violations" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleGetModerationViolationsRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "violation_id"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetModerationViolationRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "PATCH":
							s.handleUpdateViolationStatusRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET,PATCH")
						}

						return
					}

				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/api/v1/moderation/"

			if l := len("/api/v1/moderation/"); len(elem) >= l && elem[0:l] == "/api/v1/moderation/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "actions/"

				if l := len("actions/"); len(elem) >= l && elem[0:l] == "actions/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "violation_id"
				// Leaf parameter, slashes are prohibited
				idx := strings.IndexByte(elem, '/')
				if idx >= 0 {
					break
				}
				args[0] = elem
				elem = ""

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "POST":
						r.name = ApplyModerationActionOperation
						r.summary = "Apply moderation action"
						r.operationID = "applyModerationAction"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/moderation/actions/{violation_id}"
						r.args = args
						r.count = 1
						return r, true
					default:
						return
					}
				}

			case 'c': // Prefix: "check-message"

				if l := len("check-message"); len(elem) >= l && elem[0:l] == "check-message" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "POST":
						r.name = CheckMessageOperation
						r.summary = "Check message for violations"
						r.operationID = "checkMessage"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/moderation/check-message"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 'l': // Prefix: "logs"

				if l := len("logs"); len(elem) >= l && elem[0:l] == "logs" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = GetModerationLogsOperation
						r.summary = "Get moderation logs"
						r.operationID = "getModerationLogs"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/moderation/logs"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 'r': // Prefix: "rules"

				if l := len("rules"); len(elem) >= l && elem[0:l] == "rules" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = GetModerationRulesOperation
						r.summary = "Get moderation rules"
						r.operationID = "getModerationRules"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/moderation/rules"
						r.args = args
						r.count = 0
						return r, true
					case "POST":
						r.name = CreateModerationRuleOperation
						r.summary = "Create moderation rule"
						r.operationID = "createModerationRule"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/moderation/rules"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "rule_id"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "DELETE":
							r.name = DeleteModerationRuleOperation
							r.summary = "Delete moderation rule"
							r.operationID = "deleteModerationRule"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/moderation/rules/{rule_id}"
							r.args = args
							r.count = 1
							return r, true
						case "GET":
							r.name = GetModerationRuleOperation
							r.summary = "Get moderation rule by ID"
							r.operationID = "getModerationRule"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/moderation/rules/{rule_id}"
							r.args = args
							r.count = 1
							return r, true
						case "PUT":
							r.name = UpdateModerationRuleOperation
							r.summary = "Update moderation rule"
							r.operationID = "updateModerationRule"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/moderation/rules/{rule_id}"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			case 'v': // Prefix: "violations"

				if l := len("violations"); len(elem) >= l && elem[0:l] == "violations" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = GetModerationViolationsOperation
						r.summary = "Get moderation violations"
						r.operationID = "getModerationViolations"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/moderation/violations"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "violation_id"
					// Leaf parameter, slashes are prohibited
					idx := strings.IndexByte(elem, '/')
					if idx >= 0 {
						break
					}
					args[0] = elem
					elem = ""

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetModerationViolationOperation
							r.summary = "Get violation details"
							r.operationID = "getModerationViolation"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/moderation/violations/{violation_id}"
							r.args = args
							r.count = 1
							return r, true
						case "PATCH":
							r.name = UpdateViolationStatusOperation
							r.summary = "Update violation status"
							r.operationID = "updateViolationStatus"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/moderation/violations/{violation_id}"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			}

		}
	}
	return r, false
}
