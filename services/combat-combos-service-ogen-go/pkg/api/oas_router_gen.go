// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/gameplay/combat/combos/"

			if l := len("/gameplay/combat/combos/"); len(elem) >= l && elem[0:l] == "/gameplay/combat/combos/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "a"
				origElem := elem
				if l := len("a"); len(elem) >= l && elem[0:l] == "a" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'c': // Prefix: "ctivate"

					if l := len("ctivate"); len(elem) >= l && elem[0:l] == "ctivate" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleActivateComboRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				case 'n': // Prefix: "nalytics"

					if l := len("nalytics"); len(elem) >= l && elem[0:l] == "nalytics" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetComboAnalyticsRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				}

				elem = origElem
			case 'c': // Prefix: "catalog"
				origElem := elem
				if l := len("catalog"); len(elem) >= l && elem[0:l] == "catalog" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleGetComboCatalogRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

				elem = origElem
			case 'l': // Prefix: "loadout"
				origElem := elem
				if l := len("loadout"); len(elem) >= l && elem[0:l] == "loadout" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleGetComboLoadoutRequest([0]string{}, elemIsEscaped, w, r)
					case "POST":
						s.handleUpdateComboLoadoutRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET,POST")
					}

					return
				}

				elem = origElem
			case 's': // Prefix: "s"
				origElem := elem
				if l := len("s"); len(elem) >= l && elem[0:l] == "s" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'c': // Prefix: "core"

					if l := len("core"); len(elem) >= l && elem[0:l] == "core" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleSubmitComboScoreRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				case 'y': // Prefix: "ynergy"

					if l := len("ynergy"); len(elem) >= l && elem[0:l] == "ynergy" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleApplySynergyRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				}

				elem = origElem
			}
			// Param: "comboId"
			// Leaf parameter, slashes are prohibited
			idx := strings.IndexByte(elem, '/')
			if idx >= 0 {
				break
			}
			args[0] = elem
			elem = ""

			if len(elem) == 0 {
				// Leaf node.
				switch r.Method {
				case "GET":
					s.handleGetComboDetailsRequest([1]string{
						args[0],
					}, elemIsEscaped, w, r)
				default:
					s.notAllowed(w, r, "GET")
				}

				return
			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/gameplay/combat/combos/"

			if l := len("/gameplay/combat/combos/"); len(elem) >= l && elem[0:l] == "/gameplay/combat/combos/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "a"
				origElem := elem
				if l := len("a"); len(elem) >= l && elem[0:l] == "a" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'c': // Prefix: "ctivate"

					if l := len("ctivate"); len(elem) >= l && elem[0:l] == "ctivate" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = ActivateComboOperation
							r.summary = "Активировать комбо"
							r.operationID = "activateCombo"
							r.operationGroup = ""
							r.pathPattern = "/gameplay/combat/combos/activate"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				case 'n': // Prefix: "nalytics"

					if l := len("nalytics"); len(elem) >= l && elem[0:l] == "nalytics" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetComboAnalyticsOperation
							r.summary = "Получить аналитику комбо"
							r.operationID = "getComboAnalytics"
							r.operationGroup = ""
							r.pathPattern = "/gameplay/combat/combos/analytics"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				}

				elem = origElem
			case 'c': // Prefix: "catalog"
				origElem := elem
				if l := len("catalog"); len(elem) >= l && elem[0:l] == "catalog" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = GetComboCatalogOperation
						r.summary = "Получить каталог комбо"
						r.operationID = "getComboCatalog"
						r.operationGroup = ""
						r.pathPattern = "/gameplay/combat/combos/catalog"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

				elem = origElem
			case 'l': // Prefix: "loadout"
				origElem := elem
				if l := len("loadout"); len(elem) >= l && elem[0:l] == "loadout" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = GetComboLoadoutOperation
						r.summary = "Получить лоадаут комбо"
						r.operationID = "getComboLoadout"
						r.operationGroup = ""
						r.pathPattern = "/gameplay/combat/combos/loadout"
						r.args = args
						r.count = 0
						return r, true
					case "POST":
						r.name = UpdateComboLoadoutOperation
						r.summary = "Обновить лоадаут комбо"
						r.operationID = "updateComboLoadout"
						r.operationGroup = ""
						r.pathPattern = "/gameplay/combat/combos/loadout"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

				elem = origElem
			case 's': // Prefix: "s"
				origElem := elem
				if l := len("s"); len(elem) >= l && elem[0:l] == "s" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'c': // Prefix: "core"

					if l := len("core"); len(elem) >= l && elem[0:l] == "core" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = SubmitComboScoreOperation
							r.summary = "Отправить результаты scoring"
							r.operationID = "submitComboScore"
							r.operationGroup = ""
							r.pathPattern = "/gameplay/combat/combos/score"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				case 'y': // Prefix: "ynergy"

					if l := len("ynergy"); len(elem) >= l && elem[0:l] == "ynergy" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = ApplySynergyOperation
							r.summary = "Применить синергию"
							r.operationID = "applySynergy"
							r.operationGroup = ""
							r.pathPattern = "/gameplay/combat/combos/synergy"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				}

				elem = origElem
			}
			// Param: "comboId"
			// Leaf parameter, slashes are prohibited
			idx := strings.IndexByte(elem, '/')
			if idx >= 0 {
				break
			}
			args[0] = elem
			elem = ""

			if len(elem) == 0 {
				// Leaf node.
				switch method {
				case "GET":
					r.name = GetComboDetailsOperation
					r.summary = "Получить детали комбо"
					r.operationID = "getComboDetails"
					r.operationGroup = ""
					r.pathPattern = "/gameplay/combat/combos/{comboId}"
					r.args = args
					r.count = 1
					return r, true
				default:
					return
				}
			}

		}
	}
	return r, false
}
