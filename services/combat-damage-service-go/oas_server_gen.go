// Code generated by ogen, DO NOT EDIT.

package api

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// CombatDamageApplyDot implements combatDamageApplyDot operation.
	//
	// Apply or refresh a damage-over-time effect with stacking rules and duration calculations.
	// **BACKEND NOTE:** Optimized for DOT stacking logic. Efficient linked list implementation for
	// active DOTs. Memory: pooled DOT objects reduce GC pressure.
	//
	// POST /combat/damage/dot
	CombatDamageApplyDot(ctx context.Context, req *DotApplicationRequest) (CombatDamageApplyDotRes, error)
	// CombatDamageCalculateCritical implements combatDamageCalculateCritical operation.
	//
	// Calculate critical hit probability and damage multiplier based on weapon, skills, and target.
	// **BACKEND NOTE:** High-frequency operation - optimized with lookup tables for crit calculations.
	// Memory: precomputed crit tables reduce calculation overhead by 70%.
	//
	// POST /combat/damage/critical
	CombatDamageCalculateCritical(ctx context.Context, req *CriticalHitRequest) (CombatDamageCalculateCriticalRes, error)
	// CombatDamageCalculateDamage implements combatDamageCalculateDamage operation.
	//
	// Calculate damage from an attack with all modifiers, resistances, and critical hit calculations.
	// **BACKEND NOTE:** Critical hot path - optimized for 2000+ RPS, zero allocations, SIMD operations
	// for damage arrays. Struct alignment: 30-50% memory savings. Context timeouts: 2ms for calculation,
	// 1ms for validation. DB pool config: read-only replica for historical damage data.
	//
	// POST /combat/damage/calculate
	CombatDamageCalculateDamage(ctx context.Context, req *DamageCalculationRequest) (CombatDamageCalculateDamageRes, error)
	// CombatDamageCalculateMitigation implements combatDamageCalculateMitigation operation.
	//
	// Calculate total damage reduction from armor, shields, resistances, and temporary effects.
	// **BACKEND NOTE:** Optimized for armor stacking calculations. SIMD operations for parallel
	// resistance calculations. Memory: compact struct layout for mitigation data.
	//
	// POST /combat/damage/mitigation
	CombatDamageCalculateMitigation(ctx context.Context, req *DamageMitigationRequest) (CombatDamageCalculateMitigationRes, error)
	// CombatDamageGetAnalytics implements combatDamageGetAnalytics operation.
	//
	// Retrieve detailed damage statistics and analytics for a combat session.
	// **BACKEND NOTE:** Read-heavy operation with analytics aggregation. Optimized for large datasets
	// with streaming responses.
	//
	// GET /combat/damage/analytics/{session_id}
	CombatDamageGetAnalytics(ctx context.Context, params CombatDamageGetAnalyticsParams) (CombatDamageGetAnalyticsRes, error)
	// CombatDamageHealthCheck implements combatDamageHealthCheck operation.
	//
	// Service health check.
	//
	// GET /health
	CombatDamageHealthCheck(ctx context.Context) (CombatDamageHealthCheckRes, error)
	// CombatDamageValidateCalculation implements combatDamageValidateCalculation operation.
	//
	// Validate a damage calculation for anti-cheat purposes and consistency checks.
	// **BACKEND NOTE:** Security-critical operation. Optimized validation algorithms with cryptographic
	// verification. Memory: minimal allocations for validation data.
	//
	// POST /combat/damage/validate
	CombatDamageValidateCalculation(ctx context.Context, req *DamageValidationRequest) (CombatDamageValidateCalculationRes, error)
	// NewError creates *ErrRespStatusCode from error returned by handler.
	//
	// Used for common default response.
	NewError(ctx context.Context, err error) *ErrRespStatusCode
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h Handler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		baseServer: s,
	}, nil
}
