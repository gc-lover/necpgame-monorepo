# Issue: NOT USED - generation is configured via Makefile
# This file is kept for reference only
# Parameters are now passed via Makefile flags for split generation

package: api
output-options:
  skip-prune: true



