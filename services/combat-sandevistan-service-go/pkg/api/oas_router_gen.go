// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/combat/players/"

			if l := len("/combat/players/"); len(elem) >= l && elem[0:l] == "/combat/players/" {
				elem = elem[l:]
			} else {
				break
			}

			// Param: "player_id"
			// Match until "/"
			idx := strings.IndexByte(elem, '/')
			if idx < 0 {
				idx = len(elem)
			}
			args[0] = elem[:idx]
			elem = elem[idx:]

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case '/': // Prefix: "/sandevistan/"

				if l := len("/sandevistan/"); len(elem) >= l && elem[0:l] == "/sandevistan/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "acti"

					if l := len("acti"); len(elem) >= l && elem[0:l] == "acti" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'o': // Prefix: "on-budget"

						if l := len("on-budget"); len(elem) >= l && elem[0:l] == "on-budget" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleUseActionBudgetRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					case 'v': // Prefix: "vate"

						if l := len("vate"); len(elem) >= l && elem[0:l] == "vate" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleActivateSandevistanRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					}

				case 'c': // Prefix: "co"

					if l := len("co"); len(elem) >= l && elem[0:l] == "co" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'o': // Prefix: "oling"

						if l := len("oling"); len(elem) >= l && elem[0:l] == "oling" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleApplyCoolingCartridgeRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					case 'u': // Prefix: "unterplay"

						if l := len("unterplay"); len(elem) >= l && elem[0:l] == "unterplay" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleApplyCounterplayRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					}

				case 'd': // Prefix: "deactivate"

					if l := len("deactivate"); len(elem) >= l && elem[0:l] == "deactivate" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleDeactivateSandevistanRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				case 'h': // Prefix: "heat"

					if l := len("heat"); len(elem) >= l && elem[0:l] == "heat" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetHeatStatusRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				case 's': // Prefix: "status"

					if l := len("status"); len(elem) >= l && elem[0:l] == "status" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetSandevistanStatusRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				case 't': // Prefix: "temporal-marks"

					if l := len("temporal-marks"); len(elem) >= l && elem[0:l] == "temporal-marks" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetTemporalMarksRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						case "POST":
							s.handleSetTemporalMarksRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET,POST")
						}

						return
					}

				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/combat/players/"

			if l := len("/combat/players/"); len(elem) >= l && elem[0:l] == "/combat/players/" {
				elem = elem[l:]
			} else {
				break
			}

			// Param: "player_id"
			// Match until "/"
			idx := strings.IndexByte(elem, '/')
			if idx < 0 {
				idx = len(elem)
			}
			args[0] = elem[:idx]
			elem = elem[idx:]

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case '/': // Prefix: "/sandevistan/"

				if l := len("/sandevistan/"); len(elem) >= l && elem[0:l] == "/sandevistan/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "acti"

					if l := len("acti"); len(elem) >= l && elem[0:l] == "acti" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'o': // Prefix: "on-budget"

						if l := len("on-budget"); len(elem) >= l && elem[0:l] == "on-budget" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = UseActionBudgetOperation
								r.summary = "Использовать Action Budget"
								r.operationID = "useActionBudget"
								r.operationGroup = ""
								r.pathPattern = "/combat/players/{player_id}/sandevistan/action-budget"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 'v': // Prefix: "vate"

						if l := len("vate"); len(elem) >= l && elem[0:l] == "vate" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = ActivateSandevistanOperation
								r.summary = "Активировать Sandevistan"
								r.operationID = "activateSandevistan"
								r.operationGroup = ""
								r.pathPattern = "/combat/players/{player_id}/sandevistan/activate"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				case 'c': // Prefix: "co"

					if l := len("co"); len(elem) >= l && elem[0:l] == "co" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'o': // Prefix: "oling"

						if l := len("oling"); len(elem) >= l && elem[0:l] == "oling" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = ApplyCoolingCartridgeOperation
								r.summary = "Использовать охлаждающий картридж"
								r.operationID = "applyCoolingCartridge"
								r.operationGroup = ""
								r.pathPattern = "/combat/players/{player_id}/sandevistan/cooling"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 'u': // Prefix: "unterplay"

						if l := len("unterplay"); len(elem) >= l && elem[0:l] == "unterplay" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = ApplyCounterplayOperation
								r.summary = "Применить контрплей эффект"
								r.operationID = "applyCounterplay"
								r.operationGroup = ""
								r.pathPattern = "/combat/players/{player_id}/sandevistan/counterplay"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				case 'd': // Prefix: "deactivate"

					if l := len("deactivate"); len(elem) >= l && elem[0:l] == "deactivate" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = DeactivateSandevistanOperation
							r.summary = "Досрочно деактивировать Sandevistan"
							r.operationID = "deactivateSandevistan"
							r.operationGroup = ""
							r.pathPattern = "/combat/players/{player_id}/sandevistan/deactivate"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				case 'h': // Prefix: "heat"

					if l := len("heat"); len(elem) >= l && elem[0:l] == "heat" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetHeatStatusOperation
							r.summary = "Получить статус перегрева"
							r.operationID = "getHeatStatus"
							r.operationGroup = ""
							r.pathPattern = "/combat/players/{player_id}/sandevistan/heat"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				case 's': // Prefix: "status"

					if l := len("status"); len(elem) >= l && elem[0:l] == "status" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetSandevistanStatusOperation
							r.summary = "Получить статус Sandevistan"
							r.operationID = "getSandevistanStatus"
							r.operationGroup = ""
							r.pathPattern = "/combat/players/{player_id}/sandevistan/status"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				case 't': // Prefix: "temporal-marks"

					if l := len("temporal-marks"); len(elem) >= l && elem[0:l] == "temporal-marks" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetTemporalMarksOperation
							r.summary = "Получить активные Temporal Marks"
							r.operationID = "getTemporalMarks"
							r.operationGroup = ""
							r.pathPattern = "/combat/players/{player_id}/sandevistan/temporal-marks"
							r.args = args
							r.count = 1
							return r, true
						case "POST":
							r.name = SetTemporalMarksOperation
							r.summary = "Установить Temporal Marks на цели"
							r.operationID = "setTemporalMarks"
							r.operationGroup = ""
							r.pathPattern = "/combat/players/{player_id}/sandevistan/temporal-marks"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			}

		}
	}
	return r, false
}
