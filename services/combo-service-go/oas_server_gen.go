// Code generated by ogen, DO NOT EDIT.

package api

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// ActivateCombo implements activateCombo operation.
	//
	// Activate a combo for a character.
	//
	// POST /combos/activate
	ActivateCombo(ctx context.Context, req *ActivateComboRequest) (ActivateComboRes, error)
	// ComboServiceHealthCheck implements comboServiceHealthCheck operation.
	//
	// **Enterprise-grade health check endpoint**
	// Provides real-time health status of the combo service.
	// Critical for service discovery, load balancing, and monitoring.
	// **Performance:** <1ms response time, cached for 30 seconds.
	//
	// GET /health
	ComboServiceHealthCheck(ctx context.Context, params ComboServiceHealthCheckParams) (ComboServiceHealthCheckRes, error)
	// GetComboAnalytics implements getComboAnalytics operation.
	//
	// Retrieve analytics data for combo usage.
	//
	// GET /combos/analytics
	GetComboAnalytics(ctx context.Context, params GetComboAnalyticsParams) (GetComboAnalyticsRes, error)
	// GetComboCatalog implements getComboCatalog operation.
	//
	// Retrieve a paginated list of available combos with optional filtering.
	//
	// GET /combos/catalog
	GetComboCatalog(ctx context.Context, params GetComboCatalogParams) (GetComboCatalogRes, error)
	// GetComboDetails implements getComboDetails operation.
	//
	// Retrieve detailed information about a specific combo.
	//
	// GET /combos/{comboId}
	GetComboDetails(ctx context.Context, params GetComboDetailsParams) (GetComboDetailsRes, error)
	// GetComboLoadout implements getComboLoadout operation.
	//
	// Retrieve the current combo loadout for the authenticated character.
	//
	// GET /combos/loadout
	GetComboLoadout(ctx context.Context) (GetComboLoadoutRes, error)
	// UpdateComboLoadout implements updateComboLoadout operation.
	//
	// Update the combo loadout for the authenticated character.
	//
	// POST /combos/loadout
	UpdateComboLoadout(ctx context.Context, req *ComboLoadoutRequest) (UpdateComboLoadoutRes, error)
	// NewError creates *ErrRespStatusCode from error returned by handler.
	//
	// Used for common default response.
	NewError(ctx context.Context, err error) *ErrRespStatusCode
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h   Handler
	sec SecurityHandler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, sec SecurityHandler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		sec:        sec,
		baseServer: s,
	}, nil
}
