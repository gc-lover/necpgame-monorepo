// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [2]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "api/v1/"

				if l := len("api/v1/"); len(elem) >= l && elem[0:l] == "api/v1/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'c': // Prefix: "challenges/active"

					if l := len("challenges/active"); len(elem) >= l && elem[0:l] == "challenges/active" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetActiveChallengesRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				case 'e': // Prefix: "easter-eggs"

					if l := len("easter-eggs"); len(elem) >= l && elem[0:l] == "easter-eggs" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						switch r.Method {
						case "GET":
							s.handleGetEasterEggsRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'c': // Prefix: "category/"
							origElem := elem
							if l := len("category/"); len(elem) >= l && elem[0:l] == "category/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "category"
							// Leaf parameter, slashes are prohibited
							idx := strings.IndexByte(elem, '/')
							if idx >= 0 {
								break
							}
							args[0] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleGetEasterEggsByCategoryRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

							elem = origElem
						case 'd': // Prefix: "difficulty/"
							origElem := elem
							if l := len("difficulty/"); len(elem) >= l && elem[0:l] == "difficulty/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "difficulty"
							// Leaf parameter, slashes are prohibited
							idx := strings.IndexByte(elem, '/')
							if idx >= 0 {
								break
							}
							args[0] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleGetEasterEggsByDifficultyRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

							elem = origElem
						}
						// Param: "id"
						// Match until "/"
						idx := strings.IndexByte(elem, '/')
						if idx < 0 {
							idx = len(elem)
						}
						args[0] = elem[:idx]
						elem = elem[idx:]

						if len(elem) == 0 {
							switch r.Method {
							case "GET":
								s.handleGetEasterEggRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}
						switch elem[0] {
						case '/': // Prefix: "/"

							if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'a': // Prefix: "attempt"

								if l := len("attempt"); len(elem) >= l && elem[0:l] == "attempt" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "POST":
										s.handleRecordDiscoveryAttemptRequest([1]string{
											args[0],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, "POST")
									}

									return
								}

							case 'h': // Prefix: "hints"

								if l := len("hints"); len(elem) >= l && elem[0:l] == "hints" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "GET":
										s.handleGetHintsForEasterEggRequest([1]string{
											args[0],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, "GET")
									}

									return
								}

							case 's': // Prefix: "stats"

								if l := len("stats"); len(elem) >= l && elem[0:l] == "stats" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "GET":
										s.handleGetEasterEggStatisticsRequest([1]string{
											args[0],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, "GET")
									}

									return
								}

							}

						}

					}

				case 'p': // Prefix: "players/"

					if l := len("players/"); len(elem) >= l && elem[0:l] == "players/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "playerId"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'c': // Prefix: "challenges/"

							if l := len("challenges/"); len(elem) >= l && elem[0:l] == "challenges/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "challengeId"
							// Match until "/"
							idx := strings.IndexByte(elem, '/')
							if idx < 0 {
								idx = len(elem)
							}
							args[1] = elem[:idx]
							elem = elem[idx:]

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case '/': // Prefix: "/progress"

								if l := len("/progress"); len(elem) >= l && elem[0:l] == "/progress" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "GET":
										s.handleGetPlayerChallengeProgressRequest([2]string{
											args[0],
											args[1],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, "GET")
									}

									return
								}

							}

						case 'e': // Prefix: "easter-eggs/"

							if l := len("easter-eggs/"); len(elem) >= l && elem[0:l] == "easter-eggs/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "eggId"
							// Match until "/"
							idx := strings.IndexByte(elem, '/')
							if idx < 0 {
								idx = len(elem)
							}
							args[1] = elem[:idx]
							elem = elem[idx:]

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case '/': // Prefix: "/discover"

								if l := len("/discover"); len(elem) >= l && elem[0:l] == "/discover" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "POST":
										s.handleDiscoverEasterEggRequest([2]string{
											args[0],
											args[1],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, "POST")
									}

									return
								}

							}

						case 'p': // Prefix: "pro"

							if l := len("pro"); len(elem) >= l && elem[0:l] == "pro" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'f': // Prefix: "file"

								if l := len("file"); len(elem) >= l && elem[0:l] == "file" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "GET":
										s.handleGetPlayerProfileRequest([1]string{
											args[0],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, "GET")
									}

									return
								}

							case 'g': // Prefix: "gress"

								if l := len("gress"); len(elem) >= l && elem[0:l] == "gress" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "GET":
										s.handleGetPlayerProgressRequest([1]string{
											args[0],
										}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, "GET")
									}

									return
								}

							}

						}

					}

				case 's': // Prefix: "stats/categories"

					if l := len("stats/categories"); len(elem) >= l && elem[0:l] == "stats/categories" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetCategoryStatisticsRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleHealthCheckRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [2]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "api/v1/"

				if l := len("api/v1/"); len(elem) >= l && elem[0:l] == "api/v1/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'c': // Prefix: "challenges/active"

					if l := len("challenges/active"); len(elem) >= l && elem[0:l] == "challenges/active" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetActiveChallengesOperation
							r.summary = "Get active challenges"
							r.operationID = "getActiveChallenges"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/challenges/active"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				case 'e': // Prefix: "easter-eggs"

					if l := len("easter-eggs"); len(elem) >= l && elem[0:l] == "easter-eggs" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						switch method {
						case "GET":
							r.name = GetEasterEggsOperation
							r.summary = "Get all easter eggs"
							r.operationID = "getEasterEggs"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/easter-eggs"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'c': // Prefix: "category/"
							origElem := elem
							if l := len("category/"); len(elem) >= l && elem[0:l] == "category/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "category"
							// Leaf parameter, slashes are prohibited
							idx := strings.IndexByte(elem, '/')
							if idx >= 0 {
								break
							}
							args[0] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = GetEasterEggsByCategoryOperation
									r.summary = "Get easter eggs by category"
									r.operationID = "getEasterEggsByCategory"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/easter-eggs/category/{category}"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

							elem = origElem
						case 'd': // Prefix: "difficulty/"
							origElem := elem
							if l := len("difficulty/"); len(elem) >= l && elem[0:l] == "difficulty/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "difficulty"
							// Leaf parameter, slashes are prohibited
							idx := strings.IndexByte(elem, '/')
							if idx >= 0 {
								break
							}
							args[0] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = GetEasterEggsByDifficultyOperation
									r.summary = "Get easter eggs by difficulty"
									r.operationID = "getEasterEggsByDifficulty"
									r.operationGroup = ""
									r.pathPattern = "/api/v1/easter-eggs/difficulty/{difficulty}"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

							elem = origElem
						}
						// Param: "id"
						// Match until "/"
						idx := strings.IndexByte(elem, '/')
						if idx < 0 {
							idx = len(elem)
						}
						args[0] = elem[:idx]
						elem = elem[idx:]

						if len(elem) == 0 {
							switch method {
							case "GET":
								r.name = GetEasterEggOperation
								r.summary = "Get easter egg by ID"
								r.operationID = "getEasterEgg"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/easter-eggs/{id}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}
						switch elem[0] {
						case '/': // Prefix: "/"

							if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'a': // Prefix: "attempt"

								if l := len("attempt"); len(elem) >= l && elem[0:l] == "attempt" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "POST":
										r.name = RecordDiscoveryAttemptOperation
										r.summary = "Record discovery attempt"
										r.operationID = "recordDiscoveryAttempt"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/easter-eggs/{id}/attempt"
										r.args = args
										r.count = 1
										return r, true
									default:
										return
									}
								}

							case 'h': // Prefix: "hints"

								if l := len("hints"); len(elem) >= l && elem[0:l] == "hints" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "GET":
										r.name = GetHintsForEasterEggOperation
										r.summary = "Get hints for easter egg"
										r.operationID = "getHintsForEasterEgg"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/easter-eggs/{id}/hints"
										r.args = args
										r.count = 1
										return r, true
									default:
										return
									}
								}

							case 's': // Prefix: "stats"

								if l := len("stats"); len(elem) >= l && elem[0:l] == "stats" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "GET":
										r.name = GetEasterEggStatisticsOperation
										r.summary = "Get easter egg statistics"
										r.operationID = "getEasterEggStatistics"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/easter-eggs/{id}/stats"
										r.args = args
										r.count = 1
										return r, true
									default:
										return
									}
								}

							}

						}

					}

				case 'p': // Prefix: "players/"

					if l := len("players/"); len(elem) >= l && elem[0:l] == "players/" {
						elem = elem[l:]
					} else {
						break
					}

					// Param: "playerId"
					// Match until "/"
					idx := strings.IndexByte(elem, '/')
					if idx < 0 {
						idx = len(elem)
					}
					args[0] = elem[:idx]
					elem = elem[idx:]

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case '/': // Prefix: "/"

						if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'c': // Prefix: "challenges/"

							if l := len("challenges/"); len(elem) >= l && elem[0:l] == "challenges/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "challengeId"
							// Match until "/"
							idx := strings.IndexByte(elem, '/')
							if idx < 0 {
								idx = len(elem)
							}
							args[1] = elem[:idx]
							elem = elem[idx:]

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case '/': // Prefix: "/progress"

								if l := len("/progress"); len(elem) >= l && elem[0:l] == "/progress" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "GET":
										r.name = GetPlayerChallengeProgressOperation
										r.summary = "Get player challenge progress"
										r.operationID = "getPlayerChallengeProgress"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/players/{playerId}/challenges/{challengeId}/progress"
										r.args = args
										r.count = 2
										return r, true
									default:
										return
									}
								}

							}

						case 'e': // Prefix: "easter-eggs/"

							if l := len("easter-eggs/"); len(elem) >= l && elem[0:l] == "easter-eggs/" {
								elem = elem[l:]
							} else {
								break
							}

							// Param: "eggId"
							// Match until "/"
							idx := strings.IndexByte(elem, '/')
							if idx < 0 {
								idx = len(elem)
							}
							args[1] = elem[:idx]
							elem = elem[idx:]

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case '/': // Prefix: "/discover"

								if l := len("/discover"); len(elem) >= l && elem[0:l] == "/discover" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "POST":
										r.name = DiscoverEasterEggOperation
										r.summary = "Discover easter egg"
										r.operationID = "discoverEasterEgg"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/players/{playerId}/easter-eggs/{eggId}/discover"
										r.args = args
										r.count = 2
										return r, true
									default:
										return
									}
								}

							}

						case 'p': // Prefix: "pro"

							if l := len("pro"); len(elem) >= l && elem[0:l] == "pro" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'f': // Prefix: "file"

								if l := len("file"); len(elem) >= l && elem[0:l] == "file" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "GET":
										r.name = GetPlayerProfileOperation
										r.summary = "Get player easter egg profile"
										r.operationID = "getPlayerProfile"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/players/{playerId}/profile"
										r.args = args
										r.count = 1
										return r, true
									default:
										return
									}
								}

							case 'g': // Prefix: "gress"

								if l := len("gress"); len(elem) >= l && elem[0:l] == "gress" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "GET":
										r.name = GetPlayerProgressOperation
										r.summary = "Get player easter egg progress"
										r.operationID = "getPlayerProgress"
										r.operationGroup = ""
										r.pathPattern = "/api/v1/players/{playerId}/progress"
										r.args = args
										r.count = 1
										return r, true
									default:
										return
									}
								}

							}

						}

					}

				case 's': // Prefix: "stats/categories"

					if l := len("stats/categories"); len(elem) >= l && elem[0:l] == "stats/categories" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetCategoryStatisticsOperation
							r.summary = "Get category statistics"
							r.operationID = "getCategoryStatistics"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/stats/categories"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = HealthCheckOperation
						r.summary = "Health check endpoint"
						r.operationID = "healthCheck"
						r.operationGroup = ""
						r.pathPattern = "/health"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			}

		}
	}
	return r, false
}
