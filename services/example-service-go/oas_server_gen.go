// Code generated by ogen, DO NOT EDIT.

package api

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// CreateExample implements createExample operation.
	//
	// **Create a new example entity**
	// Creates a new example with validation and business rule enforcement.
	// **Performance:** <10ms creation time.
	//
	// POST /examples
	CreateExample(ctx context.Context, req *CreateExampleRequest) (CreateExampleRes, error)
	// DeleteExample implements deleteExample operation.
	//
	// **Delete example entity**
	// Soft deletes example with audit trail.
	// **Performance:** <5ms deletion time.
	//
	// DELETE /examples/{example_id}
	DeleteExample(ctx context.Context, params DeleteExampleParams) (DeleteExampleRes, error)
	// GetExample implements getExample operation.
	//
	// **Get detailed example information**
	// Returns comprehensive example information.
	// **Performance:** Single example lookup with <1ms response time.
	//
	// GET /examples/{example_id}
	GetExample(ctx context.Context, params GetExampleParams) (GetExampleRes, error)
	// HealthCheck implements healthCheck operation.
	//
	// **Enterprise-grade health check endpoint**
	// Returns system health status and performance metrics.
	// **Performance:** <1ms response time.
	//
	// GET /health
	HealthCheck(ctx context.Context) (HealthCheckRes, error)
	// ListExamples implements listExamples operation.
	//
	// **Get paginated example list**
	// Returns examples with filtering and pagination support.
	// **Performance:** Indexed queries with efficient pagination.
	//
	// GET /examples
	ListExamples(ctx context.Context, params ListExamplesParams) (ListExamplesRes, error)
	// UpdateExample implements updateExample operation.
	//
	// **Update existing example**
	// Updates example with optimistic locking and validation.
	// **Atomicity:** Optimistic locking for concurrent updates.
	//
	// PUT /examples/{example_id}
	UpdateExample(ctx context.Context, req *UpdateExampleRequest, params UpdateExampleParams) (UpdateExampleRes, error)
	// NewError creates *ErrRespStatusCode from error returned by handler.
	//
	// Used for common default response.
	NewError(ctx context.Context, err error) *ErrRespStatusCode
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h   Handler
	sec SecurityHandler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, sec SecurityHandler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		sec:        sec,
		baseServer: s,
	}, nil
}
