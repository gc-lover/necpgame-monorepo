# Тесты для компонентов создания персонажа

## 📋 Покрытие тестами

### Протестированные компоненты:

1. **CharacterList.test.tsx** (CharacterList)
   - OK Отображение загрузки
   - OK Отображение ошибки
   - OK Отображение пустого списка
   - OK Отображение списка персонажей
   - OK Счетчик персонажей
   - OK Предупреждение при достижении лимита

2. **ClassSelector.test.tsx** (ClassSelector)
   - OK Отображение загрузки
   - OK Отображение ошибки
   - OK Отображение списка классов
   - OK Выбор класса
   - OK Отображение подклассов
   - OK Выбор подкласса
   - OK Визуальное состояние выбора

3. **AppearanceForm.test.tsx** (AppearanceForm)
   - OK Отображение формы
   - OK Изменение роста
   - OK Изменение телосложения
   - OK Счетчик символов
   - OK Ограничение длины текста
   - OK Визуальное состояние выбора

### Покрытие: ~50%

Основные компоненты протестированы. Дополнительные тесты можно добавить для:
- `OriginSelector`
- `FactionSelector`
- `CitySelector`
- `CharacterCreationForm` (интеграционные тесты)

## 🚀 Запуск тестов

```bash
# Запуск всех тестов
npm run test

# Запуск тестов с UI
npm run test:ui

# Запуск с покрытием
npm run test:coverage
```

## 📚 Структура тестов

Каждый тест следует AAA паттерну:
1. **Arrange** - подготовка данных и моков
2. **Act** - выполнение действия
3. **Assert** - проверка результата

### Пример:

```typescript
it('должен отображать список персонажей', () => {
  // Arrange - мокируем API
  vi.spyOn(charactersApi, 'useListCharacters').mockReturnValue({
    data: mockData,
    isLoading: false,
    error: null,
  })
  
  // Act - рендерим компонент
  renderWithProvider(<CharacterList />)
  
  // Assert - проверяем результат
  expect(screen.getByText('John Doe')).toBeInTheDocument()
})
```

## 🔧 Используемые инструменты

- **Vitest** - тест фреймворк
- **React Testing Library** - тестирование React компонентов
- **@testing-library/user-event** - симуляция пользовательских действий
- **vi** - мокирование (из Vitest)

## 📝 Примечания

- Все тесты используют моки для API вызовов
- QueryClient создается для каждого теста отдельно
- Используется retry: false для быстрого выполнения тестов
- Тесты не требуют запущенного бекенда

