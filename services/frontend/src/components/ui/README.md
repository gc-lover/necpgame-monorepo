# 🎨 UI Kit Documentation - NECPGAME Cyberpunk UI

Библиотека переиспользуемых UI компонентов в стиле Cyberpunk 2077 для NECPGAME.

## 📦 Установка

```tsx
import { Button, Card, Input, ... } from './components/ui'
```

---

## 🔘 Button

Кнопка с различными вариантами стилей и состояниями.

### Пропсы

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `variant` | `'primary' \| 'secondary' \| 'success' \| 'danger' \| 'ghost' \| 'link'` | `'primary'` | Вариант стиля |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Размер кнопки |
| `fullWidth` | `boolean` | `false` | Кнопка на всю ширину |
| `loading` | `boolean` | `false` | Состояние загрузки |
| `leftIcon` | `ReactNode` | - | Иконка слева |
| `rightIcon` | `ReactNode` | - | Иконка справа |

### Примеры

```tsx
// Простая кнопка
<Button>Нажми меня</Button>

// Кнопка с вариантом и размером
<Button variant="danger" size="lg">
  Удалить
</Button>

// Кнопка с иконками
<Button 
  variant="success" 
  leftIcon={<span>▸</span>}
>
  Играть
</Button>

// Состояние загрузки
<Button loading>
  Загрузка...
</Button>
```

---

## 📄 Card

Карточка контента с поддержкой header, body, footer.

### Компоненты

- `Card` - основной контейнер
- `CardHeader` - шапка карточки
- `CardTitle` - заголовок
- `CardBody` - тело карточки
- `CardFooter` - подвал карточки

### Пропсы Card

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `hoverable` | `boolean` | `false` | Эффект при наведении |
| `noPadding` | `boolean` | `false` | Убрать внутренние отступы |

### Примеры

```tsx
// Простая карточка
<Card>
  <CardBody>
    <p>Контент карточки</p>
  </CardBody>
</Card>

// Полная карточка
<Card hoverable>
  <CardHeader>
    <CardTitle>Заголовок</CardTitle>
  </CardHeader>
  <CardBody>
    <p>Основной контент</p>
  </CardBody>
  <CardFooter>
    <Button>Действие</Button>
  </CardFooter>
</Card>
```

---

## ✏️ Input

Текстовое поле ввода с поддержкой лейблов, ошибок и иконок.

### Пропсы

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `label` | `string` | - | Лейбл поля |
| `error` | `string` | - | Текст ошибки |
| `hint` | `string` | - | Текст подсказки |
| `leftIcon` | `ReactNode` | - | Иконка слева |
| `rightIcon` | `ReactNode` | - | Иконка справа |

### Примеры

```tsx
// Простое поле
<Input 
  label="Имя персонажа"
  placeholder="Введите имя"
  value={value}
  onChange={(e) => setValue(e.target.value)}
/>

// С ошибкой
<Input 
  label="Email"
  type="email"
  error="Неверный формат email"
/>

// С иконками
<Input 
  placeholder="Поиск..."
  leftIcon={<span>🔍</span>}
/>
```

---

## 📋 Select

Выпадающий список.

### Пропсы

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `label` | `string` | - | Лейбл поля |
| `options` | `SelectOption[]` | **обязательный** | Список опций |
| `error` | `string` | - | Текст ошибки |
| `placeholder` | `string` | - | Placeholder опция |

### SelectOption

```tsx
interface SelectOption {
  value: string
  label: string
  disabled?: boolean
}
```

### Примеры

```tsx
<Select
  label="Выберите класс"
  placeholder="Выберите класс персонажа"
  options={[
    { value: 'netrunner', label: 'Netrunner' },
    { value: 'solo', label: 'Solo' },
    { value: 'techie', label: 'Techie' },
  ]}
  value={value}
  onChange={(e) => setValue(e.target.value)}
/>
```

---

## 📝 Textarea

Многострочное текстовое поле.

### Пропсы

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `label` | `string` | - | Лейбл поля |
| `error` | `string` | - | Текст ошибки |
| `showCount` | `boolean` | `false` | Показывать счетчик символов |

### Примеры

```tsx
<Textarea
  label="Описание персонажа"
  placeholder="Расскажите о вашем персонаже..."
  value={value}
  onChange={(e) => setValue(e.target.value)}
  maxLength={500}
  showCount
/>
```

---

## 🪟 Modal

Модальное окно.

### Пропсы

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `isOpen` | `boolean` | **обязательный** | Открыто ли окно |
| `onClose` | `() => void` | **обязательный** | Callback при закрытии |
| `title` | `string` | - | Заголовок |
| `size` | `'sm' \| 'md' \| 'lg' \| 'xl' \| 'full'` | `'md'` | Размер окна |
| `showCloseButton` | `boolean` | `true` | Показывать кнопку закрытия |
| `closeOnBackdropClick` | `boolean` | `true` | Закрывать при клике вне окна |

### Примеры

```tsx
const [isOpen, setIsOpen] = useState(false)

<Modal
  isOpen={isOpen}
  onClose={() => setIsOpen(false)}
  title="Заголовок модалки"
  size="lg"
>
  <p>Контент модального окна</p>
  
  <ModalFooter>
    <Button variant="ghost" onClick={() => setIsOpen(false)}>
      Отмена
    </Button>
    <Button onClick={handleSubmit}>
      Подтвердить
    </Button>
  </ModalFooter>
</Modal>
```

---

## 📑 Tabs

Табы для переключения между разделами.

### Пропсы

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `tabs` | `Tab[]` | **обязательный** | Список табов |
| `defaultTab` | `string` | - | ID активного таба по умолчанию |
| `onChange` | `(tabId: string) => void` | - | Callback при смене таба |

### Tab Interface

```tsx
interface Tab {
  id: string
  label: string
  content: ReactNode
  disabled?: boolean
  icon?: ReactNode
}
```

### Примеры

```tsx
<Tabs
  tabs={[
    {
      id: 'profile',
      label: 'Профиль',
      icon: <span>👤</span>,
      content: <ProfileContent />,
    },
    {
      id: 'settings',
      label: 'Настройки',
      content: <SettingsContent />,
    },
  ]}
  defaultTab="profile"
  onChange={(tabId) => console.log('Tab changed:', tabId)}
/>
```

---

## 🏷️ Badge

Небольшой цветной лейбл.

### Пропсы

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `variant` | `'primary' \| 'secondary' \| 'success' \| 'danger' \| 'warning' \| 'info'` | `'primary'` | Вариант стиля |

### Примеры

```tsx
<Badge variant="success">Онлайн</Badge>
<Badge variant="danger">Оффлайн</Badge>
<Badge variant="warning">Внимание</Badge>
```

---

## WARNING Alert

Информационное сообщение.

### Пропсы

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `type` | `'info' \| 'success' \| 'warning' \| 'error'` | `'info'` | Тип алерта |
| `title` | `string` | - | Заголовок |
| `onClose` | `() => void` | - | Callback при закрытии |

### Примеры

```tsx
<Alert type="success" title="Успех">
  Операция выполнена успешно
</Alert>

<Alert type="error" onClose={() => console.log('Closed')}>
  Произошла ошибка
</Alert>
```

---

## 📊 Progress

Прогресс-бар.

### Пропсы

| Проп | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `value` | `number` | **обязательный** | Текущее значение (0-100) |
| `max` | `number` | `100` | Максимальное значение |
| `showPercent` | `boolean` | `false` | Показывать процент |
| `color` | `'cyan' \| 'pink' \| 'green' \| 'purple' \| 'yellow'` | `'cyan'` | Цвет |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Размер |

### Примеры

```tsx
<Progress value={65} showPercent color="cyan" />
<Progress value={80} color="green" size="lg" />
```

---

## 🎨 Стилизация

Все компоненты используют Tailwind CSS классы из файла `cyberpunk-ui.css`. 

### Основные цвета

- **Neon Cyan**: `#00f7ff` - основной акцент
- **Neon Pink**: `#ff2a6d` - опасные действия
- **Neon Purple**: `#d817ff` - дополнительный акцент
- **Neon Green**: `#05ffa1` - успешные действия
- **Dark**: `#0a0e27` - фон поверхностей
- **Darker**: `#050812` - основной фон

### Шрифты

- **Orbitron** - заголовки и UI элементы
- **Rajdhani** - основной текст
- **Share Tech Mono** - монопространственный текст

---

## 🚀 Использование

Импортируйте нужные компоненты:

```tsx
import { Button, Card, Input, Modal } from '@/components/ui'
```

Или импортируйте конкретный компонент:

```tsx
import { Button } from '@/components/ui/Button'
```

---

## 📖 Demo

Откройте страницу UI Demo для просмотра всех компонентов в действии:

```tsx
import { UIDemo } from './pages/UIDemo'
```

Или перейдите на `/ui-demo` в приложении.

---

## 🎯 Best Practices

1. **Используйте семантические варианты** - `primary` для основных действий, `danger` для опасных
2. **Всегда добавляйте labels** к форм-элементам для доступности
3. **Используйте loading состояние** при асинхронных операциях
4. **Добавляйте error сообщения** для валидации форм
5. **Используйте Modal** для критических действий требующих подтверждения

---

Made with 💙 for NECPGAME

