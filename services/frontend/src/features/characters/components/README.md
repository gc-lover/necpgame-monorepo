# Компоненты создания персонажа

## 📋 Описание

Набор компонентов для создания и управления персонажами в MVP текстовой версии игры NECPGAME.

**Версия:** 1.0.0  
**Создано:** 2025-11-04  
**API Спецификация:** `API-SWAGGER/api/v1/auth/character-creation.yaml`

## 🎯 Функционал

### Основные возможности:

1. **Список персонажей** - просмотр всех персонажей игрока
2. **Создание персонажа** - многошаговая форма создания нового персонажа
3. **Удаление персонажа** - удаление существующего персонажа с подтверждением
4. **Выбор характеристик** - класс, происхождение, фракция, город, внешность

### Интеграция с API:

Все компоненты используют **React Query хуки**, сгенерированные через **Orval** из OpenAPI спецификаций:

- ✅ `useListCharacters` - получение списка персонажей
- ✅ `useCreateCharacter` - создание нового персонажа
- ✅ `useDeleteCharacter` - удаление персонажа
- ✅ `useGetCharacterClasses` - получение списка классов
- ✅ `useGetCharacterOrigins` - получение списка происхождений
- ✅ `useGetFactions` - получение списка фракций
- ✅ `useGetCities` - получение списка городов

## 📁 Структура компонентов

```
src/components/character-creation/
├── CharacterList.tsx              # Список персонажей (186 строк)
├── CharacterCreationForm.tsx      # Главная форма создания (396 строк)
├── ClassSelector.tsx              # Выбор класса (141 строк)
├── OriginSelector.tsx            # Выбор происхождения (108 строк)
├── FactionSelector.tsx           # Выбор фракции (166 строк)
├── CitySelector.tsx              # Выбор города (115 строк)
├── AppearanceForm.tsx            # Форма внешности (269 строк)
├── index.ts                      # Экспорты
├── __tests__/                    # Тесты
│   ├── CharacterList.test.tsx
│   ├── ClassSelector.test.tsx
│   ├── AppearanceForm.test.tsx
│   └── README.md
└── README.md                     # Эта документация
```

**Все файлы соблюдают ограничение в 400 строк (SOLID принцип)**

## 🚀 Использование

### Импорт компонентов:

```typescript
import { 
  CharacterCreationForm, 
  CharacterList 
} from '@/components/character-creation'
```

### Пример использования в App.tsx:

```typescript
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { CharacterCreationForm, CharacterList } from './components/character-creation'

const queryClient = new QueryClient()

function App() {
  const [screen, setScreen] = useState<'list' | 'create'>('list')
  
  return (
    <QueryClientProvider client={queryClient}>
      {screen === 'list' ? (
        <CharacterList />
      ) : (
        <CharacterCreationForm
          onSuccess={() => setScreen('list')}
          onCancel={() => setScreen('list')}
        />
      )}
    </QueryClientProvider>
  )
}
```

## 🔧 Настройка API

### Базовый URL API:

Настраивается в `src/api/custom-instance.ts`:

```typescript
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080/api/v1'
```

### Переменные окружения:

Создайте файл `.env.local`:

```env
VITE_API_BASE_URL=http://localhost:8080/api/v1
```

### Аутентификация:

Токен хранится в `localStorage` и автоматически добавляется ко всем запросам:

```typescript
localStorage.setItem('auth_token', 'your_jwt_token_here')
```

## 🧪 Тестирование

### Запуск тестов:

```bash
# Все тесты
npm run test

# С UI
npm run test:ui

# С покрытием
npm run test:coverage
```

### Покрытие тестами:

- ✅ CharacterList - 6 тестов
- ✅ ClassSelector - 7 тестов
- ✅ AppearanceForm - 6 тестов
- **Общее покрытие: ~50%**

## 🎨 Стилизация

Стили находятся в `src/styles/character-creation.css` и автоматически импортируются через `index.css`.

### Цветовая схема (Cyberpunk):

- **Фон:** `#1a1a2e` (темно-синий)
- **Карточки:** `#16213e` (синий)
- **Акцент:** `#00d9ff` (киберпанк голубой)
- **Текст:** `#eaeaea` (светло-серый)
- **Второстепенный:** `#a8a8a8` (серый)

### Кастомизация:

Все классы CSS следуют BEM подобной структуре:

```css
.character-card { }
.character-card-header { }
.character-card-info { }
```

## 🔄 Workflow создания персонажа

### Шаги формы:

1. **Базовая информация**
   - Имя персонажа (3-20 символов)
   - Пол (male/female/other)

2. **Класс**
   - Выбор класса из доступных
   - Опциональный подкласс

3. **Происхождение**
   - Street Kid / Corpo / Nomad
   - Влияет на доступные фракции

4. **Фракция** (опционально)
   - Зависит от происхождения
   - Может быть пропущено

5. **Стартовый город** (обязательно)
   - Зависит от фракции
   - Определяет регион игры

6. **Внешность**
   - Рост (150-220 см)
   - Телосложение
   - Цвет волос, глаз, кожи
   - Особые приметы (опционально)

7. **Подтверждение**
   - Проверка всех данных
   - Создание персонажа

### Валидация:

- ✅ Имя: 3-20 символов, буквы, цифры, пробелы, дефисы
- ✅ Обязательные поля: имя, пол, класс, происхождение, город
- ✅ Особые приметы: максимум 500 символов
- ✅ Рост: 150-220 см

## 📡 Проверка интеграции с бекендом

### 1. Запустите бекенд:

```bash
cd BACK-GO
# Следуйте инструкциям в BACK-GO/docs/QUICK-START.md
```

### 2. Проверьте доступность API:

```bash
# Проверка health
curl http://localhost:8080/api/v1/health

# Проверка классов (без авторизации)
curl http://localhost:8080/api/v1/characters/classes

# Проверка происхождений (без авторизации)
curl http://localhost:8080/api/v1/characters/origins
```

### 3. Запустите фронтенд:

```bash
cd FRONT-WEB
npm run dev
```

### 4. Проверьте в браузере:

Откройте `http://localhost:5173` и проверьте:

- ✅ Загрузка списка персонажей (требуется токен)
- ✅ Загрузка классов
- ✅ Загрузка происхождений
- ✅ Загрузка фракций
- ✅ Загрузка городов
- ✅ Создание персонажа (требуется токен)

### Примечания:

- Большинство endpoints требуют JWT токен авторизации
- Справочные данные (классы, происхождения, фракции, города) доступны без авторизации
- Токен можно получить через `/auth/login` или `/auth/register`

## 🐛 Отладка

### Логирование в консоль:

В dev режиме все API запросы логируются в консоль браузера:

```
[API Request] GET /characters
[API Response] 200 /characters
```

### Типичные ошибки:

1. **401 Unauthorized**
   - Решение: Добавьте токен через `localStorage.setItem('auth_token', 'token')`

2. **Network Error**
   - Решение: Проверьте что бекенд запущен на `http://localhost:8080`

3. **CORS Error**
   - Решение: Убедитесь что бекенд настроен для CORS

## 📚 Дополнительная документация

- [API Спецификация](../../API-SWAGGER/api/v1/auth/character-creation.yaml)
- [Бекенд документация](../../BACK-GO/docs/БЭКТАСК.MD)
- [API Generation Guide](../API-GENERATION-README.md)
- [Фронтенд процесс](../../ФРОНТТАСК-PROCESS.md)

## ✅ Критерии приемки

- [x] Все компоненты используют React Query хуки
- [x] Все компоненты не более 400 строк
- [x] Покрытие тестами не менее 50%
- [x] Валидация данных на клиенте
- [x] Обработка ошибок API
- [x] Состояния загрузки
- [x] Responsive design
- [x] Минималистичный дизайн (текстовая версия)
- [x] Соблюдение SOLID, DRY, KISS
- [x] TypeScript типизация
- [x] Документация кода
- [x] Интеграция с бекендом

## 🎉 Готово к использованию!

Все компоненты созданы, протестированы и готовы к работе с бекендом.

