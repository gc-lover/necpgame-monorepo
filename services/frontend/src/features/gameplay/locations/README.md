# Locations Feature - Система локаций и перемещения

Feature модуль для локаций и перемещения в NECPGAME.

## 📋 Описание

Управление локациями и перемещением:
- Просмотр доступных локаций
- Фильтрация по региону и уровню опасности
- Детальная информация о локации
- Перемещение между локациями
- Соседние локации для быстрого перемещения
- Отображение текущей локации

## 🗂️ Структура

```
features/gameplay/locations/
├── components/
│   ├── LocationCard.tsx              # Карточка локации
│   ├── LocationDetailsPanel.tsx      # Детали локации
│   ├── ConnectedLocationsList.tsx    # Соседние локации
│   ├── index.ts
│   └── __tests__/
├── pages/
│   ├── LocationsPage.tsx            # Страница локаций
│   └── index.ts
└── README.md
```

## 🎨 Компоненты (Material UI)

### LocationCard
Компактная карточка локации с индикацией доступности

**OpenAPI тип:** `GameLocation`

**Функции:**
- Отображение названия, региона, города
- Индикация уровня опасности цветом
- Отображение требований
- Индикация текущей локации
- Блокировка недоступных локаций

### LocationDetailsPanel
Панель с подробной информацией о локации

**OpenAPI тип:** `LocationDetails`

**Функции:**
- Полная информация о локации
- Точки интереса
- Активные события

### ConnectedLocationsList
Список соседних локаций для быстрого перемещения

**OpenAPI тип:** `ConnectedLocation[]`

## 📄 Страница

### LocationsPage
Полноценная карта локаций с перемещением

**Роут:** `/game/locations`

**Структура (3 колонки):**
- Левая: Фильтры (регион, уровень опасности)
- Центр: Список локаций (сетка карточек) или детали выбранной локации
- Правая: Соседние локации для быстрого перемещения

**Режимы:**
- Список локаций (по умолчанию)
- Детали локации (при клике на карточку)

## 🔌 API (Orval Generated)

**Queries:**
- `GET /locations?characterId&region&dangerLevel` → `useGetLocations`
- `GET /locations/{locationId}?characterId` → `useGetLocationDetails`
- `GET /locations/current?characterId` → `useGetCurrentLocation`
- `GET /locations/connected?characterId` → `useGetConnectedLocations`
- `GET /locations/{locationId}/actions?characterId` → `useGetLocationActions`

**Mutations:**
- `POST /locations/travel` → `useTravelToLocation`

## ✅ OpenAPI Compliance

**Все данные из OpenAPI!**

| Компонент | Тип | Hardcoded? |
|-----------|-----|------------|
| LocationCard | GameLocation | ❌ НЕТ |
| LocationDetailsPanel | LocationDetails | ❌ НЕТ |
| ConnectedLocationsList | ConnectedLocation[] | ❌ НЕТ |

## 🚀 Использование

Навигация:
```typescript
navigate('/game/locations')
```

Загрузка:
```typescript
const { data } = useGetLocations({ characterId, region, dangerLevel })
const { data: current } = useGetCurrentLocation({ characterId })
```

Действия:
```typescript
const { mutate: travel } = useTravelToLocation()
travel({ 
  data: { 
    characterId, 
    targetLocationId, 
    travelMethod: 'walk' 
  } 
})
```

## 🎯 Интеграция

**Доступ:**
- GameplayPage → Меню → "Локации"
- Прямой переход: `/game/locations`

**Связи:**
- NPCs (находятся в локациях)
- Квесты (требуют определенных локаций)
- События (происходят в локациях)

## 📝 Источники

- **OpenAPI:** `API-SWAGGER/api/v1/locations/locations.yaml`
- **Task:** API-TASK-028

