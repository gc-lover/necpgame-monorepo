// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "api/v1/"

				if l := len("api/v1/"); len(elem) >= l && elem[0:l] == "api/v1/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "a"

					if l := len("a"); len(elem) >= l && elem[0:l] == "a" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'd': // Prefix: "dmin/gameplay/affixes/"

						if l := len("dmin/gameplay/affixes/"); len(elem) >= l && elem[0:l] == "dmin/gameplay/affixes/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "id"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "DELETE":
								s.handleAPIV1AdminGameplayAffixesIDDeleteRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							case "PUT":
								s.handleAPIV1AdminGameplayAffixesIDPutRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "DELETE,PUT")
							}

							return
						}

					case 'n': // Prefix: "nalytics/affixes/"

						if l := len("nalytics/affixes/"); len(elem) >= l && elem[0:l] == "nalytics/affixes/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'c': // Prefix: "combinations"

							if l := len("combinations"); len(elem) >= l && elem[0:l] == "combinations" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleAPIV1AnalyticsAffixesCombinationsGetRequest([0]string{}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

						case 'd': // Prefix: "difficulty"

							if l := len("difficulty"); len(elem) >= l && elem[0:l] == "difficulty" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleAPIV1AnalyticsAffixesDifficultyGetRequest([0]string{}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

						case 'p': // Prefix: "popularity"

							if l := len("popularity"); len(elem) >= l && elem[0:l] == "popularity" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleAPIV1AnalyticsAffixesPopularityGetRequest([0]string{}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

						}

					}

				case 'g': // Prefix: "gameplay/"

					if l := len("gameplay/"); len(elem) >= l && elem[0:l] == "gameplay/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'a': // Prefix: "affixes"

						if l := len("affixes"); len(elem) >= l && elem[0:l] == "affixes" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							switch r.Method {
							case "GET":
								s.handleAPIV1GameplayAffixesGetRequest([0]string{}, elemIsEscaped, w, r)
							case "POST":
								s.handleAPIV1GameplayAffixesPostRequest([0]string{}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET,POST")
							}

							return
						}
						switch elem[0] {
						case '/': // Prefix: "/"

							if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'a': // Prefix: "active"
								origElem := elem
								if l := len("active"); len(elem) >= l && elem[0:l] == "active" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch r.Method {
									case "GET":
										s.handleAPIV1GameplayAffixesActiveGetRequest([0]string{}, elemIsEscaped, w, r)
									default:
										s.notAllowed(w, r, "GET")
									}

									return
								}

								elem = origElem
							case 'r': // Prefix: "rotation/"
								origElem := elem
								if l := len("rotation/"); len(elem) >= l && elem[0:l] == "rotation/" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									break
								}
								switch elem[0] {
								case 'c': // Prefix: "current"

									if l := len("current"); len(elem) >= l && elem[0:l] == "current" {
										elem = elem[l:]
									} else {
										break
									}

									if len(elem) == 0 {
										// Leaf node.
										switch r.Method {
										case "GET":
											s.handleAPIV1GameplayAffixesRotationCurrentGetRequest([0]string{}, elemIsEscaped, w, r)
										default:
											s.notAllowed(w, r, "GET")
										}

										return
									}

								case 'h': // Prefix: "history"

									if l := len("history"); len(elem) >= l && elem[0:l] == "history" {
										elem = elem[l:]
									} else {
										break
									}

									if len(elem) == 0 {
										// Leaf node.
										switch r.Method {
										case "GET":
											s.handleAPIV1GameplayAffixesRotationHistoryGetRequest([0]string{}, elemIsEscaped, w, r)
										default:
											s.notAllowed(w, r, "GET")
										}

										return
									}

								case 't': // Prefix: "trigger"

									if l := len("trigger"); len(elem) >= l && elem[0:l] == "trigger" {
										elem = elem[l:]
									} else {
										break
									}

									if len(elem) == 0 {
										// Leaf node.
										switch r.Method {
										case "POST":
											s.handleAPIV1GameplayAffixesRotationTriggerPostRequest([0]string{}, elemIsEscaped, w, r)
										default:
											s.notAllowed(w, r, "POST")
										}

										return
									}

								}

								elem = origElem
							}
							// Param: "id"
							// Leaf parameter, slashes are prohibited
							idx := strings.IndexByte(elem, '/')
							if idx >= 0 {
								break
							}
							args[0] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleAPIV1GameplayAffixesIDGetRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

						}

					case 'i': // Prefix: "instances/"

						if l := len("instances/"); len(elem) >= l && elem[0:l] == "instances/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "instanceId"
						// Match until "/"
						idx := strings.IndexByte(elem, '/')
						if idx < 0 {
							idx = len(elem)
						}
						args[0] = elem[:idx]
						elem = elem[idx:]

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case '/': // Prefix: "/affixes"

							if l := len("/affixes"); len(elem) >= l && elem[0:l] == "/affixes" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "GET":
									s.handleAPIV1GameplayInstancesInstanceIdAffixesGetRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "GET")
								}

								return
							}

						}

					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleHealthGetRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "api/v1/"

				if l := len("api/v1/"); len(elem) >= l && elem[0:l] == "api/v1/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "a"

					if l := len("a"); len(elem) >= l && elem[0:l] == "a" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'd': // Prefix: "dmin/gameplay/affixes/"

						if l := len("dmin/gameplay/affixes/"); len(elem) >= l && elem[0:l] == "dmin/gameplay/affixes/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "id"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "DELETE":
								r.name = APIV1AdminGameplayAffixesIDDeleteOperation
								r.summary = "Delete affix"
								r.operationID = ""
								r.operationGroup = ""
								r.pathPattern = "/api/v1/admin/gameplay/affixes/{id}"
								r.args = args
								r.count = 1
								return r, true
							case "PUT":
								r.name = APIV1AdminGameplayAffixesIDPutOperation
								r.summary = "Update affix"
								r.operationID = ""
								r.operationGroup = ""
								r.pathPattern = "/api/v1/admin/gameplay/affixes/{id}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 'n': // Prefix: "nalytics/affixes/"

						if l := len("nalytics/affixes/"); len(elem) >= l && elem[0:l] == "nalytics/affixes/" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'c': // Prefix: "combinations"

							if l := len("combinations"); len(elem) >= l && elem[0:l] == "combinations" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = APIV1AnalyticsAffixesCombinationsGetOperation
									r.summary = "Get combination analysis"
									r.operationID = ""
									r.operationGroup = ""
									r.pathPattern = "/api/v1/analytics/affixes/combinations"
									r.args = args
									r.count = 0
									return r, true
								default:
									return
								}
							}

						case 'd': // Prefix: "difficulty"

							if l := len("difficulty"); len(elem) >= l && elem[0:l] == "difficulty" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = APIV1AnalyticsAffixesDifficultyGetOperation
									r.summary = "Get difficulty metrics"
									r.operationID = ""
									r.operationGroup = ""
									r.pathPattern = "/api/v1/analytics/affixes/difficulty"
									r.args = args
									r.count = 0
									return r, true
								default:
									return
								}
							}

						case 'p': // Prefix: "popularity"

							if l := len("popularity"); len(elem) >= l && elem[0:l] == "popularity" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = APIV1AnalyticsAffixesPopularityGetOperation
									r.summary = "Get affix popularity metrics"
									r.operationID = ""
									r.operationGroup = ""
									r.pathPattern = "/api/v1/analytics/affixes/popularity"
									r.args = args
									r.count = 0
									return r, true
								default:
									return
								}
							}

						}

					}

				case 'g': // Prefix: "gameplay/"

					if l := len("gameplay/"); len(elem) >= l && elem[0:l] == "gameplay/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'a': // Prefix: "affixes"

						if l := len("affixes"); len(elem) >= l && elem[0:l] == "affixes" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							switch method {
							case "GET":
								r.name = APIV1GameplayAffixesGetOperation
								r.summary = "List affixes"
								r.operationID = ""
								r.operationGroup = ""
								r.pathPattern = "/api/v1/gameplay/affixes"
								r.args = args
								r.count = 0
								return r, true
							case "POST":
								r.name = APIV1GameplayAffixesPostOperation
								r.summary = "Create new affix"
								r.operationID = ""
								r.operationGroup = ""
								r.pathPattern = "/api/v1/gameplay/affixes"
								r.args = args
								r.count = 0
								return r, true
							default:
								return
							}
						}
						switch elem[0] {
						case '/': // Prefix: "/"

							if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								break
							}
							switch elem[0] {
							case 'a': // Prefix: "active"
								origElem := elem
								if l := len("active"); len(elem) >= l && elem[0:l] == "active" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									// Leaf node.
									switch method {
									case "GET":
										r.name = APIV1GameplayAffixesActiveGetOperation
										r.summary = "Get active affixes for current week"
										r.operationID = ""
										r.operationGroup = ""
										r.pathPattern = "/api/v1/gameplay/affixes/active"
										r.args = args
										r.count = 0
										return r, true
									default:
										return
									}
								}

								elem = origElem
							case 'r': // Prefix: "rotation/"
								origElem := elem
								if l := len("rotation/"); len(elem) >= l && elem[0:l] == "rotation/" {
									elem = elem[l:]
								} else {
									break
								}

								if len(elem) == 0 {
									break
								}
								switch elem[0] {
								case 'c': // Prefix: "current"

									if l := len("current"); len(elem) >= l && elem[0:l] == "current" {
										elem = elem[l:]
									} else {
										break
									}

									if len(elem) == 0 {
										// Leaf node.
										switch method {
										case "GET":
											r.name = APIV1GameplayAffixesRotationCurrentGetOperation
											r.summary = "Get current rotation"
											r.operationID = ""
											r.operationGroup = ""
											r.pathPattern = "/api/v1/gameplay/affixes/rotation/current"
											r.args = args
											r.count = 0
											return r, true
										default:
											return
										}
									}

								case 'h': // Prefix: "history"

									if l := len("history"); len(elem) >= l && elem[0:l] == "history" {
										elem = elem[l:]
									} else {
										break
									}

									if len(elem) == 0 {
										// Leaf node.
										switch method {
										case "GET":
											r.name = APIV1GameplayAffixesRotationHistoryGetOperation
											r.summary = "Get rotation history"
											r.operationID = ""
											r.operationGroup = ""
											r.pathPattern = "/api/v1/gameplay/affixes/rotation/history"
											r.args = args
											r.count = 0
											return r, true
										default:
											return
										}
									}

								case 't': // Prefix: "trigger"

									if l := len("trigger"); len(elem) >= l && elem[0:l] == "trigger" {
										elem = elem[l:]
									} else {
										break
									}

									if len(elem) == 0 {
										// Leaf node.
										switch method {
										case "POST":
											r.name = APIV1GameplayAffixesRotationTriggerPostOperation
											r.summary = "Trigger manual rotation"
											r.operationID = ""
											r.operationGroup = ""
											r.pathPattern = "/api/v1/gameplay/affixes/rotation/trigger"
											r.args = args
											r.count = 0
											return r, true
										default:
											return
										}
									}

								}

								elem = origElem
							}
							// Param: "id"
							// Leaf parameter, slashes are prohibited
							idx := strings.IndexByte(elem, '/')
							if idx >= 0 {
								break
							}
							args[0] = elem
							elem = ""

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = APIV1GameplayAffixesIDGetOperation
									r.summary = "Get affix details"
									r.operationID = ""
									r.operationGroup = ""
									r.pathPattern = "/api/v1/gameplay/affixes/{id}"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						}

					case 'i': // Prefix: "instances/"

						if l := len("instances/"); len(elem) >= l && elem[0:l] == "instances/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "instanceId"
						// Match until "/"
						idx := strings.IndexByte(elem, '/')
						if idx < 0 {
							idx = len(elem)
						}
						args[0] = elem[:idx]
						elem = elem[idx:]

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case '/': // Prefix: "/affixes"

							if l := len("/affixes"); len(elem) >= l && elem[0:l] == "/affixes" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "GET":
									r.name = APIV1GameplayInstancesInstanceIdAffixesGetOperation
									r.summary = "Get affixes for instance"
									r.operationID = ""
									r.operationGroup = ""
									r.pathPattern = "/api/v1/gameplay/instances/{instanceId}/affixes"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						}

					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = HealthGetOperation
						r.summary = "Health check endpoint"
						r.operationID = ""
						r.operationGroup = ""
						r.pathPattern = "/health"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			}

		}
	}
	return r, false
}
