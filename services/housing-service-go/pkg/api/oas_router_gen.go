// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "apartments/"

				if l := len("apartments/"); len(elem) >= l && elem[0:l] == "apartments/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "available"
					origElem := elem
					if l := len("available"); len(elem) >= l && elem[0:l] == "available" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetAvailableApartmentsRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

					elem = origElem
				case 'p': // Prefix: "purchase"
					origElem := elem
					if l := len("purchase"); len(elem) >= l && elem[0:l] == "purchase" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handlePurchaseApartmentRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

					elem = origElem
				}
				// Param: "apartmentId"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					switch r.Method {
					case "GET":
						s.handleGetApartmentRequest([1]string{
							args[0],
						}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'f': // Prefix: "furniture"

						if l := len("furniture"); len(elem) >= l && elem[0:l] == "furniture" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "DELETE":
								s.handleRemoveFurnitureRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							case "GET":
								s.handleGetApartmentFurnitureRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							case "POST":
								s.handlePlaceFurnitureRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "DELETE,GET,POST")
							}

							return
						}

					case 's': // Prefix: "settings"

						if l := len("settings"); len(elem) >= l && elem[0:l] == "settings" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleUpdateApartmentSettingsRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					case 'v': // Prefix: "visit"

						if l := len("visit"); len(elem) >= l && elem[0:l] == "visit" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleVisitApartmentRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					}

				}

			case 'f': // Prefix: "furniture/"

				if l := len("furniture/"); len(elem) >= l && elem[0:l] == "furniture/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'c': // Prefix: "catalog"

					if l := len("catalog"); len(elem) >= l && elem[0:l] == "catalog" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetFurnitureCatalogRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				case 'p': // Prefix: "purchase"

					if l := len("purchase"); len(elem) >= l && elem[0:l] == "purchase" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handlePurchaseFurnitureRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				}

			case 'h': // Prefix: "h"

				if l := len("h"); len(elem) >= l && elem[0:l] == "h" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'e': // Prefix: "ealth"

					if l := len("ealth"); len(elem) >= l && elem[0:l] == "ealth" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleHousingServiceHealthCheckRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				case 'o': // Prefix: "ousing/prestige/leaderboard"

					if l := len("ousing/prestige/leaderboard"); len(elem) >= l && elem[0:l] == "ousing/prestige/leaderboard" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetPrestigeLeaderboardRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "apartments/"

				if l := len("apartments/"); len(elem) >= l && elem[0:l] == "apartments/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "available"
					origElem := elem
					if l := len("available"); len(elem) >= l && elem[0:l] == "available" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetAvailableApartmentsOperation
							r.summary = "Get available apartment types"
							r.operationID = "getAvailableApartments"
							r.operationGroup = ""
							r.pathPattern = "/apartments/available"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

					elem = origElem
				case 'p': // Prefix: "purchase"
					origElem := elem
					if l := len("purchase"); len(elem) >= l && elem[0:l] == "purchase" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = PurchaseApartmentOperation
							r.summary = "Purchase an apartment"
							r.operationID = "purchaseApartment"
							r.operationGroup = ""
							r.pathPattern = "/apartments/purchase"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

					elem = origElem
				}
				// Param: "apartmentId"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					switch method {
					case "GET":
						r.name = GetApartmentOperation
						r.summary = "Get apartment details"
						r.operationID = "getApartment"
						r.operationGroup = ""
						r.pathPattern = "/apartments/{apartmentId}"
						r.args = args
						r.count = 1
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'f': // Prefix: "furniture"

						if l := len("furniture"); len(elem) >= l && elem[0:l] == "furniture" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "DELETE":
								r.name = RemoveFurnitureOperation
								r.summary = "Remove furniture from apartment"
								r.operationID = "removeFurniture"
								r.operationGroup = ""
								r.pathPattern = "/apartments/{apartmentId}/furniture"
								r.args = args
								r.count = 1
								return r, true
							case "GET":
								r.name = GetApartmentFurnitureOperation
								r.summary = "Get placed furniture in apartment"
								r.operationID = "getApartmentFurniture"
								r.operationGroup = ""
								r.pathPattern = "/apartments/{apartmentId}/furniture"
								r.args = args
								r.count = 1
								return r, true
							case "POST":
								r.name = PlaceFurnitureOperation
								r.summary = "Place furniture in apartment"
								r.operationID = "placeFurniture"
								r.operationGroup = ""
								r.pathPattern = "/apartments/{apartmentId}/furniture"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 's': // Prefix: "settings"

						if l := len("settings"); len(elem) >= l && elem[0:l] == "settings" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = UpdateApartmentSettingsOperation
								r.summary = "Update apartment access settings"
								r.operationID = "updateApartmentSettings"
								r.operationGroup = ""
								r.pathPattern = "/apartments/{apartmentId}/settings"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 'v': // Prefix: "visit"

						if l := len("visit"); len(elem) >= l && elem[0:l] == "visit" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = VisitApartmentOperation
								r.summary = "Visit an apartment"
								r.operationID = "visitApartment"
								r.operationGroup = ""
								r.pathPattern = "/apartments/{apartmentId}/visit"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				}

			case 'f': // Prefix: "furniture/"

				if l := len("furniture/"); len(elem) >= l && elem[0:l] == "furniture/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'c': // Prefix: "catalog"

					if l := len("catalog"); len(elem) >= l && elem[0:l] == "catalog" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetFurnitureCatalogOperation
							r.summary = "Get furniture catalog"
							r.operationID = "getFurnitureCatalog"
							r.operationGroup = ""
							r.pathPattern = "/furniture/catalog"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				case 'p': // Prefix: "purchase"

					if l := len("purchase"); len(elem) >= l && elem[0:l] == "purchase" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = PurchaseFurnitureOperation
							r.summary = "Purchase furniture"
							r.operationID = "purchaseFurniture"
							r.operationGroup = ""
							r.pathPattern = "/furniture/purchase"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				}

			case 'h': // Prefix: "h"

				if l := len("h"); len(elem) >= l && elem[0:l] == "h" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'e': // Prefix: "ealth"

					if l := len("ealth"); len(elem) >= l && elem[0:l] == "ealth" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = HousingServiceHealthCheckOperation
							r.summary = "Housing service health check"
							r.operationID = "housingServiceHealthCheck"
							r.operationGroup = ""
							r.pathPattern = "/health"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				case 'o': // Prefix: "ousing/prestige/leaderboard"

					if l := len("ousing/prestige/leaderboard"); len(elem) >= l && elem[0:l] == "ousing/prestige/leaderboard" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetPrestigeLeaderboardOperation
							r.summary = "Get prestige leaderboard"
							r.operationID = "getPrestigeLeaderboard"
							r.operationGroup = ""
							r.pathPattern = "/housing/prestige/leaderboard"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				}

			}

		}
	}
	return r, false
}
