// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/api/v1/npc/jackie-welles/"

			if l := len("/api/v1/npc/jackie-welles/"); len(elem) >= l && elem[0:l] == "/api/v1/npc/jackie-welles/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'd': // Prefix: "dialogue/"

				if l := len("dialogue/"); len(elem) >= l && elem[0:l] == "dialogue/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 's': // Prefix: "start"
					origElem := elem
					if l := len("start"); len(elem) >= l && elem[0:l] == "start" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleStartJackieDialogueRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

					elem = origElem
				}
				// Param: "dialogue_id"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/respond"

					if l := len("/respond"); len(elem) >= l && elem[0:l] == "/respond" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleRespondToJackieDialogueRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				}

			case 'i': // Prefix: "in"

				if l := len("in"); len(elem) >= l && elem[0:l] == "in" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 't': // Prefix: "teract"

					if l := len("teract"); len(elem) >= l && elem[0:l] == "teract" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleInteractWithJackieRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				case 'v': // Prefix: "ventory"

					if l := len("ventory"); len(elem) >= l && elem[0:l] == "ventory" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetJackieInventoryRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

				}

			case 'p': // Prefix: "profile"

				if l := len("profile"); len(elem) >= l && elem[0:l] == "profile" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleGetJackieProfileRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			case 'q': // Prefix: "quests/"

				if l := len("quests/"); len(elem) >= l && elem[0:l] == "quests/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "available"
					origElem := elem
					if l := len("available"); len(elem) >= l && elem[0:l] == "available" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetJackieAvailableQuestsRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

					elem = origElem
				}
				// Param: "quest_id"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/accept"

					if l := len("/accept"); len(elem) >= l && elem[0:l] == "/accept" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleAcceptJackieQuestRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				}

			case 'r': // Prefix: "relationship"

				if l := len("relationship"); len(elem) >= l && elem[0:l] == "relationship" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleGetJackieRelationshipRequest([0]string{}, elemIsEscaped, w, r)
					case "POST":
						s.handleUpdateJackieRelationshipRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET,POST")
					}

					return
				}

			case 's': // Prefix: "status"

				if l := len("status"); len(elem) >= l && elem[0:l] == "status" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleGetJackieStatusRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			case 't': // Prefix: "trade"

				if l := len("trade"); len(elem) >= l && elem[0:l] == "trade" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "POST":
						s.handleTradeWithJackieRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "POST")
					}

					return
				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/api/v1/npc/jackie-welles/"

			if l := len("/api/v1/npc/jackie-welles/"); len(elem) >= l && elem[0:l] == "/api/v1/npc/jackie-welles/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'd': // Prefix: "dialogue/"

				if l := len("dialogue/"); len(elem) >= l && elem[0:l] == "dialogue/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 's': // Prefix: "start"
					origElem := elem
					if l := len("start"); len(elem) >= l && elem[0:l] == "start" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = StartJackieDialogueOperation
							r.summary = "Начать диалог с Jackie"
							r.operationID = "startJackieDialogue"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/npc/jackie-welles/dialogue/start"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

					elem = origElem
				}
				// Param: "dialogue_id"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/respond"

					if l := len("/respond"); len(elem) >= l && elem[0:l] == "/respond" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = RespondToJackieDialogueOperation
							r.summary = "Ответить в диалоге с Jackie"
							r.operationID = "respondToJackieDialogue"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/npc/jackie-welles/dialogue/{dialogue_id}/respond"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			case 'i': // Prefix: "in"

				if l := len("in"); len(elem) >= l && elem[0:l] == "in" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 't': // Prefix: "teract"

					if l := len("teract"); len(elem) >= l && elem[0:l] == "teract" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = InteractWithJackieOperation
							r.summary = "Взаимодействовать с Jackie"
							r.operationID = "interactWithJackie"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/npc/jackie-welles/interact"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				case 'v': // Prefix: "ventory"

					if l := len("ventory"); len(elem) >= l && elem[0:l] == "ventory" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetJackieInventoryOperation
							r.summary = "Получить инвентарь Jackie"
							r.operationID = "getJackieInventory"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/npc/jackie-welles/inventory"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				}

			case 'p': // Prefix: "profile"

				if l := len("profile"); len(elem) >= l && elem[0:l] == "profile" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = GetJackieProfileOperation
						r.summary = "Получить профиль Jackie Welles"
						r.operationID = "getJackieProfile"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/npc/jackie-welles/profile"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 'q': // Prefix: "quests/"

				if l := len("quests/"); len(elem) >= l && elem[0:l] == "quests/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "available"
					origElem := elem
					if l := len("available"); len(elem) >= l && elem[0:l] == "available" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetJackieAvailableQuestsOperation
							r.summary = "Получить доступные квесты от Jackie"
							r.operationID = "getJackieAvailableQuests"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/npc/jackie-welles/quests/available"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

					elem = origElem
				}
				// Param: "quest_id"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/accept"

					if l := len("/accept"); len(elem) >= l && elem[0:l] == "/accept" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = AcceptJackieQuestOperation
							r.summary = "Принять квест от Jackie"
							r.operationID = "acceptJackieQuest"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/npc/jackie-welles/quests/{quest_id}/accept"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			case 'r': // Prefix: "relationship"

				if l := len("relationship"); len(elem) >= l && elem[0:l] == "relationship" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = GetJackieRelationshipOperation
						r.summary = "Получить уровень отношений с Jackie"
						r.operationID = "getJackieRelationship"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/npc/jackie-welles/relationship"
						r.args = args
						r.count = 0
						return r, true
					case "POST":
						r.name = UpdateJackieRelationshipOperation
						r.summary = "Изменить уровень отношений с Jackie"
						r.operationID = "updateJackieRelationship"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/npc/jackie-welles/relationship"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 's': // Prefix: "status"

				if l := len("status"); len(elem) >= l && elem[0:l] == "status" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = GetJackieStatusOperation
						r.summary = "Получить текущий статус Jackie"
						r.operationID = "getJackieStatus"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/npc/jackie-welles/status"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 't': // Prefix: "trade"

				if l := len("trade"); len(elem) >= l && elem[0:l] == "trade" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "POST":
						r.name = TradeWithJackieOperation
						r.summary = "Торговать с Jackie"
						r.operationID = "tradeWithJackie"
						r.operationGroup = ""
						r.pathPattern = "/api/v1/npc/jackie-welles/trade"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			}

		}
	}
	return r, false
}
