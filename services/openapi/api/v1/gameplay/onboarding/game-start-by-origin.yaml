openapi: 3.0.3
info:
  title: Game Start by Origin API
  version: 1.0.0
  description: API для старта игры по Life Path (Corpo/Street Kid/Nomad)
  x-microservice:
    name: gameplay-service
    port: 8083
    domain: gameplay
    base-path: /api/v1/gameplay/onboarding
    package: com.necpgame.gameplayservice
servers:
  - url: https://api.necp.game/v1
    description: Production API Gateway
  - url: http://localhost:8080/api/v1
    description: Local API Gateway
tags:
  - name: Game Start
paths:
  /gameplay/onboarding/game-start/by-origin:
    post:
      summary: Начать игру с выбранным Life Path
      operationId: startGameByOrigin
      tags: [Game Start]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [character_id, origin_id]
              properties:
                character_id: {type: string}
                origin_id: {type: string, enum: [corpo, street_kid, nomad]}
      responses:
        '200':
          description: Игра начата
          content:
            application/json:
              schema:
                type: object
components:
  schemas: {}


