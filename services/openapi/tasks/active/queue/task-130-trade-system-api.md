# Task ID: API-TASK-130
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** queued  
**Создано:** 2025-11-07 10:20  
**Создатель:** AI Agent  
**Зависимости:** API-TASK-128

---

## 📋 Краткое описание
Создать спецификацию P2P торговли между игроками для экономики.

**Что нужно сделать:** сформировать OpenAPI для trade-сессий, офферов и истории обменов по документу `.BRAIN/05-technical/backend/trade-system.md`.

---

## 🎯 Цель задания
Обеспечить контракт экономического микросервиса для прямой торговли двух игроков с защитой от мошенничества и учётом расстояния.

**Зачем это нужно:**
- Реализовать основу P2P обмена предметами и валютой внутри города.
- Гарантировать безопасность сделки: двойное подтверждение, полный обзор предложений, ограничения по дистанции.
- Подготовить основу для фронтенд-модуля торговли и интеграции с инвентарём, логами и аналитикой.

---

## 📚 Источники информации

### Основной источник концепции
**Репозиторий:** `.BRAIN`  
**Путь к документу:** `.BRAIN/05-technical/backend/trade-system.md`  
**Версия документа:** v1.0.0  
**Дата последнего обновления:** 2025-11-06  
**Статус документа:** ready  

**Что важно из документа:**
- Шаги торговли: приглашение, окно обмена, предложение предметов/эвро, двойное подтверждение.
- Ограничения: дистанция не более 10 метров, запрет на bound-предметы, проверка свободных слотов.
- Журналирование операций и хранение истории по игрокам.

### Дополнительные источники
- `.BRAIN/05-technical/backend/inventory-system.md` — правила владения предметами, проверки слотов и bound-статусов.
- `API-SWAGGER/api/v1/economy/inventory/inventory.yaml` — примеры схем предметов и ответов экономики.

### Связанные документы
- `.BRAIN/05-technical/backend/security-anti-scam.md` — требования к проверке мошенничества.
- `.BRAIN/02-gameplay/social/social-hubs.md` — локации, где разрешены сделки (для контекста расстояния).

---

## 📁 Целевая структура API
### Репозиторий: `API-SWAGGER`
**Целевой файл:** `api/v1/economy/trade/trade-system.yaml`  
> ⚠️ Обязательно указать корректный микросервис и gateway URL'ы `https://api.necp.game/v1/economy` и `http://localhost:8080/api/v1/economy`.

**API версия:** v1  
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── economy/
            └── trade/
                └── trade-system.yaml
```

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервис)
- **Микросервис:** economy-service  
- **Порт:** 8085  
- **API пути:** `/api/v1/economy/trade/*`  
- **Определение:** операции экономики, связанные с торговлей и валютой. Интеграции: inventory-service (через economy), analytics-service (ивенты сделок), security-service (анти-фрод).  
- **Комментарии для спецификации:** добавить заголовок
  ```yaml
  # Target Architecture:
  # - Microservice: economy-service (port 8085)
  # - API Base: /api/v1/economy/trade
  # - Dependencies: inventory-service, analytics-service, security-service
  # - Frontend Module: modules/economy/trade
  # - UI: TradeWindow, ItemCard, PriceDisplay, ConfirmButton
  # - Forms: TradeForm
  # - Hooks: useEconomyStore, useRealtime
  ```

### OpenAPI требования
- `info.x-microservice`:
  ```yaml
  x-microservice:
    name: economy-service
    port: 8085
    domain: economy
    base-path: /api/v1/economy/trade
    directory: api/v1/economy/trade
    package: com.necpgame.economyservice
  ```
- `servers`:
  ```yaml
  servers:
    - url: https://api.necp.game/v1/economy
      description: Production API Gateway
    - url: http://localhost:8080/api/v1/economy
      description: Local API Gateway
  ```
- WebSocket (если понадобится live-обновление): `wss://api.necp.game/v1/economy/trade/sessions/{sessionId}/stream`.

### Frontend (модульная архитектура)
- **Модуль:** economy  
- **Путь:** `modules/economy/trade`  
- **State Store:** `useEconomyStore` (`tradeSession`, `offers`, `history`)  
- **UI компоненты (@shared/ui):** TradeWindow, ItemCard, CurrencyTag, DualConfirmBadge, PriceDisplay  
- **Формы (@shared/forms):** TradeForm, ConfirmExchangeForm  
- **Layouts (@shared/layouts):** GameLayout  
- **Хуки (@shared/hooks):** useRealtime, useDebounce, useCharacter, useInventoryPreview  

---

## ✅ Что нужно сделать (детальный план)

### Шаг 1. Проанализировать документацию
- Изучить этапы сделки, обязательные проверки (дистанция, bound-предметы, лимиты валюты).
- Зафиксировать бизнес-правила для валидации запросов и ответов.

### Шаг 2. Спроектировать endpoints
1. **POST `/api/v1/economy/trade/sessions`** — инициировать торговую сессию между двумя игроками.  
   - Request: инициатор, приглашённый, локация.  
   - Response: идентификатор сессии, тайм-аут, состояние.
2. **POST `/api/v1/economy/trade/sessions/{sessionId}/offer`** — обновить предложение (предметы, валюта).  
   - Валидация: bound-предметы запрещены, проверка наличия предметов и валюты.  
3. **POST `/api/v1/economy/trade/sessions/{sessionId}/accept`** — подтвердить сделку со стороны игрока.  
   - Требуется двойное подтверждение; если оба подтвердили — выполнить обмен.  
4. **POST `/api/v1/economy/trade/sessions/{sessionId}/cancel`** — отменить сделку любым участником.  
5. **GET `/api/v1/economy/trade/sessions/{sessionId}`** — получить текущее состояние окна, предложения, статусы подтверждения.  
6. **GET `/api/v1/economy/trade/history`** с фильтрами по игроку, периоду, типу валюты.  
7. **POST `/api/v1/economy/trade/sessions/{sessionId}/lock`** — финальная проверка и блокировка содержимого перед подтверждением (против «подмены в последний момент»).  
8. **POST `/api/v1/economy/trade/sessions/{sessionId}/distance-check`** — ручной апдейт расстояния (используется клиентом при движении).  

### Шаг 3. Определить модели данных
- `TradeSession` — участники, состояние, тайминги, ограничения.  
- `TradeOffer` — список предметов, валюты, статус готовности.  
- `TradeItem` — ссылка на инвентарь, количество, флаги bound.  
- `TradeHistoryEntry` — результат сделки, участники, время, переданные ресурсы.  
- `TradeValidationError` — код ошибки (VAL_BOUND_ITEM, VAL_DISTANCE, VAL_CAPACITY).  

### Шаг 4. Описать спецификацию
- Добавить все endpoints в `paths` с подробными запросами/ответами и примерами.  
- В `components` разместить схемы данных, параметры (`sessionId`), ответы ошибок через `$ref` на `shared/common/responses.yaml`.  
- Учесть события WebSocket (опционально) в блоке `x-websocket` для realtime-обновлений.  
- Указать `security` с `BearerAuth` и, при необходимости, `ServiceToken` для серверных проверок.  

### Шаг 5. Проверить и согласовать
- Прогнать `scripts/validate-swagger.ps1 -ApiDirectory API-SWAGGER/api/v1/economy/trade/`.  
- Убедиться, что все бизнес-правила покрыты.  
- Обновить `tasks/config/brain-mapping.yaml` и документ `.BRAIN/05-technical/backend/trade-system.md` (секция API Tasks Status).  

---

## 🔍 Критерии приемки
1. Заполнен блок `info.x-microservice` с допустимыми значениями (`economy-service`, порт `8085`, домен `economy`).  
2. Все пути находятся под `/api/v1/economy/trade`.  
3. Реализованы точки `/sessions`, `/offer`, `/accept`, `/cancel`, `/history`, `/lock`, `/distance-check`.  
4. Валидация bound-предметов и запрет на превышение дистанции отражены в ответах `400/409`.  
5. Двойное подтверждение и блокировка после `lock` описаны в логике.  
6. История торгов поддерживает фильтры (query параметры `playerId`, `from`, `to`, `result`).  
7. Все ошибки используют общую модель `Error` через `$ref` на `shared/common/responses.yaml`.  
8. Добавлены примеры запросов/ответов и минимальный WebSocket сценарий (при необходимости).  
9. Проверка `swagger-cli` и `scripts/validate-swagger.ps1` проходит без ошибок.  
10. Обновлён `brain-mapping.yaml` (status: queued → queued/updated) и документ `.BRAIN` содержит ссылку на задачу.  
11. README в `api/v1/economy/trade/` отражает назначение API и ключевые endpoints.  

---

## FAQ
- **Нужно ли поддерживать barter без валюты?** Да, предложение может состоять только из предметов — предусмотреть пустой массив валюты.  
- **Что если игрок вышел из игры?** Сессия переводится в `cancelled`, фронт получает событие через WebSocket.  
- **Можно ли торговать более чем двум игрокам?** Нет, текущая версия строго для пары; расширение потребует новой задачи.  
- **Где хранятся логи?** В экономическом сервисе (таблица `economy_trade_history`), отдельный endpoint лишь читает агрегированные данные.  

---

**Источник:** `.BRAIN/05-technical/backend/trade-system.md` (v1.0.0, ready)

