# Task ID: API-TASK-131
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** queued  
**Создано:** 2025-11-07 10:22  
**Создатель:** AI Agent  
**Зависимости:** API-TASK-128

---

## 📋 Краткое описание
Спроектировать API почтовой системы: обмен писем, вложений и COD-поставок между игроками и системой.

**Что нужно сделать:** подготовить OpenAPI-спецификацию для игрового почтового сервиса на основе `.BRAIN/05-technical/backend/mail-system.md` с учётом микросервисной архитектуры.

---

## 🎯 Цель задания
Формализовать контракты социального микросервиса для отправки и получения писем, работы с вложениями (до 10 предметов + еврокредиты), COD-сделками и системными уведомлениями.

**Зачем это нужно:**
- Обеспечить доставку наград, внутриигровых сообщений и транзакций между игроками.
- Синхронизировать фронтенд-модуль почты с backend API и защитой от мошенничества.
- Подготовить данные для аналитики (история почты, обработка невостребованных вложений).

---

## 📚 Источники информации

### Основной источник концепции
**Репозиторий:** `.BRAIN`  
**Путь к документу:** `.BRAIN/05-technical/backend/mail-system.md`  
**Версия документа:** v1.0.0  
**Дата последнего обновления:** 2025-11-07  
**Статус документа:** ready  

**Что важно из документа:**
- Потоки отправки/получения (player → player, system → player).  
- Ограничения: 10 вложений, 30 дней хранения, auto-return, COD.  
- Требования к журналу событий и интеграции с инвентарём/экономикой.

### Дополнительные источники
- `.BRAIN/05-technical/backend/inventory-system.md` — правила проверки предметов и bound-статусов.  
- `.BRAIN/05-technical/backend/security-anti-scam.md` — политика предотвращения мошенничества (подмена вложений).  
- `API-SWAGGER/api/v1/social/notifications/notifications.yaml` — пример стиля для social-service.

### Связанные документы
- `.BRAIN/02-gameplay/social/social-hubs.md` — контекст социальных зон (где доступна почта).  
- `.BRAIN/05-technical/backend/notification-system.md` — уведомления о новых письмах.  
- `.BRAIN/02-gameplay/economy/economy-events.md` — использование COD и reward почты.

---

## 📁 Целевая структура API
### Репозиторий: `API-SWAGGER`
**Целевой файл:** `api/v1/social/mail/mail-system.yaml`  
> ⚠️ Укажи `servers` с `https://api.necp.game/v1/social` и `http://localhost:8080/api/v1/social`, соблюдай структуру каталогов микросервисов.

**API версия:** v1  
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Каталоги:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── social/
            └── mail/
                └── mail-system.yaml
```

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервис)
- **Микросервис:** social-service  
- **Порт:** 8084  
- **API пути:** `/api/v1/social/mail/*`  
- **Интеграции:** character-service (валидация получателя), economy-service (списание/возврат золота, предметов), notification-service (уведомления).  
- **Комментарий к спецификации:**
  ```yaml
  # Target Architecture:
  # - Microservice: social-service (port 8084)
  # - API Base: /api/v1/social/mail
  # - Dependencies: character-service, economy-service, notification-service
  # - Frontend Module: modules/social/mail
  # - UI: MailList, MailViewer, AttachmentGrid, CODBadge
  # - Forms: MailComposeForm, CODConfirmForm
  # - Hooks: useSocialStore, useRealtime, useDebounce
  ```

### OpenAPI требования
- `info.x-microservice`:
  ```yaml
  x-microservice:
    name: social-service
    port: 8084
    domain: social
    base-path: /api/v1/social/mail
    directory: api/v1/social/mail
    package: com.necpgame.socialservice
  ```
- `servers`:
  ```yaml
  servers:
    - url: https://api.necp.game/v1/social
      description: Production API Gateway
    - url: http://localhost:8080/api/v1/social
      description: Local API Gateway
  ```
- WebSocket (при необходимости): `wss://api.necp.game/v1/social/mail/inbox/{characterId}/stream`.

### Frontend (модуль)
- **Модуль:** social  
- **Путь:** `modules/social/mail`  
- **State Store:** `useSocialStore` (`mailInbox`, `mailUnread`, `selectedMail`, `codPending`)  
- **UI компоненты:** MailList, MailViewer, AttachmentGrid, CODBadge, ExpirationTimer.  
- **Формы:** MailComposeForm, AttachmentPickerForm, CODConfirmForm.  
- **Layouts:** GameLayout.  
- **Хуки:** useRealtime, useDebounce, useCharacter, useInventoryPreview.

---

## ✅ Что нужно сделать (детальный план)

### Шаг 1. Анализ документа
- Зафиксировать сценарии: player mail, system mail, COD, возврат вложений, таймаут хранения.  
- Составить перечень валидаций (лимиты вложений, bound предметы, наличие золота, блокировка вложений после отправки).

### Шаг 2. Спроектировать endpoints
1. **GET `/api/v1/social/mail/inbox`** — пагинация входящих писем (фильтры по статусу, типу).  
2. **GET `/api/v1/social/mail/{mailId}`** — детали письма, включая вложения и COD.  
3. **POST `/api/v1/social/mail/send`** — отправка письма игроком; проверка вложений и баланса.  
4. **POST `/api/v1/social/mail/send/system`** — системные письма (только admin/service token).  
5. **POST `/api/v1/social/mail/{mailId}/claim`** — получение вложений и оплата COD.  
6. **POST `/api/v1/social/mail/{mailId}/return`** — возврат отправителю (auto или manual).  
7. **DELETE `/api/v1/social/mail/{mailId}`** — удаление письма (после получения вложений).  
8. **GET `/api/v1/social/mail/history`** — журнал операций (админ/служебный).  
9. **POST `/api/v1/social/mail/{mailId}/mark-read`** — отметка о прочтении (обновляет счетчик).  

### Шаг 3. Определить модели
- `MailMessage` (метаданные, статус, флаги COD).  
- `MailAttachmentItem`, `MailAttachmentGold`.  
- `MailInboxPage` (pagination info, unreadCount).  
- `MailSendRequest`, `SystemMailRequest`, `MailClaimResponse`.  
- Ошибки: `MailError` с кодами `VAL_ATTACHMENT_LIMIT`, `VAL_BOUND_ITEM`, `VAL_BALANCE`, `VAL_EXPIRED`, `BIZ_MAIL_NOT_FOUND`.

### Шаг 4. Оформить спецификацию
- Добавить все endpoints в `paths` с request/response и примерами (`application/json`).  
- В `components` разместить схемы, параметры (`mailId`), стандартные ответы через `$ref` на `shared/common/responses.yaml`.  
- Определить `security` (`BearerAuth`, `ServiceToken` для системной почты).  
- Опционально описать `x-websocket` для realtime-уведомлений о новых письмах/изменениях.

### Шаг 5. Валидация и синхронизация
- Запустить `scripts/validate-swagger.ps1 -ApiDirectory API-SWAGGER/api/v1/social/mail/`.  
- Сверить критерии докумета: лимиты вложений, COD, возвраты, автоудаление.  
- Обновить `brain-mapping.yaml` и раздел API Tasks в `.BRAIN/05-technical/backend/mail-system.md`.  
- Проверить README (`api/v1/social/mail/README.md`) и при необходимости дополнить.

---

## 🔍 Критерии приемки
1. `info.x-microservice` использует `social-service`, порт `8084`, домен `social`, `base-path` `/api/v1/social/mail`.  
2. Все endpoints расположены под `/api/v1/social/mail/*`.  
3. Поддерживаются сценарии player mail, system mail, COD, возврат, удаление, история.  
4. Валидации отражены: лимит вложений, запрет bound/quest items, баланс COD, expiring писем.  
5. Ответы ошибок используют `shared/common/responses.yaml`.  
6. Примеры запросов/ответов для основных сценариев (отправка, получение, COD).  
7. Указаны параметры фильтрации/пагинации inbox/history.  
8. `swagger-cli validate` и проектный скрипт проходят без ошибок.  
9. Обновлены `brain-mapping.yaml` и `.BRAIN` документ с актуальной ссылкой на задачу (зафиксирован путь `api/v1/social/mail/mail-system.yaml`).  
10. README раздела `social/mail` отражает назначение API и ключевые endpoints.  
11. Отдельный endpoint для служебной почты защищён `ServiceToken`.

---

## FAQ
- **Можно ли отправлять больше 10 предметов?** Нет, спецификация должна возвращать ошибку `VAL_ATTACHMENT_LIMIT`.  
- **Как работать с COD?** Получатель оплачивает через `claim`; при отказе письмо возвращается отправителю.  
- **Что с истёкшими письмами?** Cron/Job переводит их в `expired` и инициирует авто-возврат.  
- **Можно ли переслать письмо?** Не в текущей версии — требуется новое письмо.  
- **Нужно ли push-уведомление?** Да, через WebSocket/notification trigger (описать в `x-websocket` или разделах интеграций).

---

**Источник:** `.BRAIN/05-technical/backend/mail-system.md` (v1.0.0, ready)

