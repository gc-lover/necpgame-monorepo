# Task ID: API-TASK-177
**Тип:** API Generation | **Приоритет:** высокий | **Статус:** queued
**Создано:** 2025-11-07 17:30 | **Создатель:** AI Agent ДУАПИТАСК | **Зависимости:** none

---

## 📋 Описание

Создать API для полного аудита backend систем (2 микрофичи). Анализ всех backend компонентов, архитектурные рекомендации, технический долг.

---

## 📚 Источники (2 документа)

**Backend Audit Parts:**
- `06-tasks/active/CURRENT-WORK/active/backend-audit-part1.md` - Audit part 1 (~259 строк)
- `06-tasks/active/CURRENT-WORK/active/backend-audit-part2.md` - Audit part 2 (~258 строк)

**Оригинал:**
- `06-tasks/active/CURRENT-WORK/active/2025-11-07-BACKEND-COMPLETE-AUDIT.md` - Полный audit

---

## 🎯 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/technical/backend-audit.yaml`
**API версия:** v1
**Тип файла:** OpenAPI 3.0 Specification (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── technical/
            ├── backend-audit.yaml  ← Создать этот файл
            └── global-state.yaml
```

---

## ✅ Что нужно сделать

### Шаг 1: Создание базовой структуры файла

**Действия:**
1. Создать файл `api/v1/technical/backend-audit.yaml`.
2. Добавить базовую информацию OpenAPI (openapi, info, servers, tags).
3. Определить теги: `Backend Audit`, `Technical Analysis`, `Architecture`.

**Ожидаемый результат:**
- Файл `backend-audit.yaml` с корректной базовой структурой OpenAPI.

### Шаг 2: Реализация Endpoints

**Действия:**
1. Добавить endpoint `GET /technical/backend/audit` для получения полного аудита.
2. Добавить endpoint `GET /technical/backend/audit/{component}` для аудита конкретного компонента.
3. Добавить endpoint `POST /technical/backend/audit/analyze` для запуска нового анализа.
4. Добавить endpoint `GET /technical/backend/recommendations` для получения рекомендаций.

**Ожидаемый результат:**
- Endpoints для получения и создания аудитов backend систем.

### Шаг 3: Определение моделей данных

**Действия:**
1. Создать схемы для моделей:
   - `BackendAudit` (audit_id, timestamp, components, recommendations, technical_debt)
   - `ComponentAudit` (component_id, name, status, issues, metrics)
   - `Recommendation` (id, priority, description, estimated_effort)
   - `TechnicalDebt` (category, severity, affected_components)
2. Использовать `PascalCase` для имен моделей.
3. Добавить примеры для каждой модели.

**Ожидаемый результат:**
- Все модели данных определены в секции `components/schemas`.

### Шаг 4: Определение схем безопасности

**Действия:**
1. Использовать `BearerAuth` из `shared/security/security.yaml` для административных эндпоинтов.
2. Определить `security` для каждого защищенного эндпоинта.

**Ожидаемый результат:**
- Корректное применение схем безопасности.

### Шаг 5: Валидация и правила

**Действия:**
1. Добавить валидацию для полей.
2. Указать ограничения и бизнес-правила.
3. Определить коды ошибок.

**Ожидаемый результ:**
- Валидация и бизнес-правила отражены в схемах.

---

## 📚 Дополнительная информация

См. дополнительный файл: **[api-generation-task-template-details.md](../../templates/api-generation-task-template-details.md)**

---

**ВНИМАНИЕ:** Это задание для АПИТАСК агента. Выполняй пошагово.


### OpenAPI (обязательно)

- Заполни `info.x-microservice` (name, port, domain, base-path, package) по данным целевого микросервиса.
- В секции `servers` оставь Production gateway `https://api.necp.game/v1` и пример локальной разработки `http://localhost:8080/api/v1`.
- WebSocket маршруты публикуй только через `wss://api.necp.game/v1/...`.

