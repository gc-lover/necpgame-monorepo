# Task ID: API-TASK-299
**Тип:** API Generation
**Приоритет:** высокий
**Статус:** queued
**Создано:** 2025-11-07 23:05
**Создатель:** AI Agent (GPT-5 Codex)
**Зависимости:** [API-TASK-038], [API-TASK-043], [API-TASK-244], [API-TASK-247]

---

## 📋 Краткое описание

Спроектировать OpenAPI/AsyncAPI спецификацию `gameplay-service` для управления боевыми лодаутами: создание, хранение, активация, пресеты, комплекты и синхронизация с аренами, рейдами и экстракт-системой.

**Что нужно сделать:** На основе `.BRAIN/02-gameplay/combat/combat-loadouts-system.md` описать полный REST API и события для работы с лодаутами, комплектами (kits), пресетами и валидацией ролей, учитывая ограничения имплантов, способностей и экономики.

---

## 🎯 Цель задания

Дать игрокам и системам возможность быстро переключаться между готовыми конфигурациями (loadouts), обеспечив строгие проверки совместимости и тесную интеграцию с боевыми сервисами.

**Зачем это нужно:**
- Ускорить подготовку к аренам, рейдам, экстракциям и PvP сессиям.
- Обеспечить консистентность билдов с учётом имплантов, способностей, инвентаря и ролей.
- Синхронизировать внутриигровые и гильдейские пресеты, обеспечить аудит и аналитический контроль.

---

## 📚 Источники информации

### Основной источник

**Репозиторий:** `.BRAIN`  
**Документ:** `.BRAIN/02-gameplay/combat/combat-loadouts-system.md`  
**Версия:** 0.3.0  
**Дата последнего обновления:** 2025-11-08 00:14  
**Статус документа:** review (готов к API), `api-readiness: ready`

**Что важно:**
- Определения Loadout/Kit, категории (PvE рейды, PvP арена, Экстракция, Стелс, Кибервойна).
- Ограничения по слотам, энергетике, человечности, совместимости с ролями и имплантами.
- Поддержка глобальных пресетов (фракционные, корпоративные) и командных рекомендаций.
- Процессы валидации, аудита изменений, журналирования и синхронизации с матчмейкингом.

### Дополнительные источники

- `.BRAIN/02-gameplay/combat/combat-roles-detailed.md` — роли, перки и требования для loadout.
- `.BRAIN/02-gameplay/combat/combat-abilities.md` — способности и слоты.
- `.BRAIN/02-gameplay/combat/combat-implants-limits.md`, `.BRAIN/02-gameplay/combat/combat-implants-types.md` — лимиты имплантов.
- `.BRAIN/02-gameplay/combat/loot-hunt-system.md` — режимы, требующие быстрой смены loadout.
- `.BRAIN/02-gameplay/combat/arena-system.md` — требования арен к лодаутам и пресетам.
- `.BRAIN/02-gameplay/economy/equipment-matrix.md` — бренды, сигнатуры и бонусы комплектов.

### Связанные документы/таски

- `API-SWAGGER/tasks/active/queue/task-038-combat-abilities-api.md`
- `API-SWAGGER/tasks/active/queue/task-043-implants-types-api.md`
- `API-SWAGGER/tasks/active/queue/task-244-arena-system-api.md`
- `API-SWAGGER/tasks/active/queue/task-247-loot-hunt-system-api.md`

---

## 📁 Целевая структура API

**Репозиторий:** `API-SWAGGER`  
**Целевой файл:** `api/v1/gameplay/combat/loadouts.yaml`  
**Формат:** OpenAPI 3.0.3 (при необходимости вынести компоненты/события в подпапки)

```
API-SWAGGER/
└── api/
    └── v1/
        └── gameplay/
            └── combat/
                ├── loadouts.yaml               ← основной файл (paths + события)
                ├── loadouts-components.yaml    ← если потребуется вынести схемы
                └── loadouts-events.yaml        ← при вынесении Async сообщений
```

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend
- **Микросервис:** gameplay-service
- **Порт:** 8083
- **API Base:** `/api/v1/gameplay/combat/loadouts*`
- **Интеграции:** inventory-service (проверка предметов), economy-service (брендовые бонусы, стоимость расширений), social-service (командные пресеты), realtime-service (активация во время матчей), matchmaking-service (валидация ролей), notification-service (алерты об изменениях).

### Frontend
- **Модуль:** `modules/combat/loadouts`
- **State Store:** `useLoadoutsStore`
- **UI компоненты:** `LoadoutGrid`, `LoadoutEditor`, `KitLibraryPanel`, `RoleCompatibilityBadge`, `EnergyBudgetMeter`, `PresetCarousel`, `AuditTimeline`
- **Формы:** `LoadoutForm`, `KitBuilderForm`, `LoadoutActivationForm`, `PresetPublishForm`
- **Хуки:** `useLoadoutValidation`, `usePresetLibrary`, `useLoadoutActivation`, `useLoadoutAudit`

### Комментарий для YAML

```yaml
# Target Architecture:
# - Microservice: gameplay-service (port 8083)
# - API Base: /api/v1/gameplay/combat/loadouts*
# - Dependencies: inventory, economy, social, realtime, matchmaking, notification
# - Frontend Module: modules/combat/loadouts (useLoadoutsStore)
# - UI: LoadoutGrid, LoadoutEditor, KitLibraryPanel, RoleCompatibilityBadge, EnergyBudgetMeter, PresetCarousel, AuditTimeline
# - Forms: LoadoutForm, KitBuilderForm, LoadoutActivationForm, PresetPublishForm
# - Hooks: useLoadoutValidation, usePresetLibrary, useLoadoutActivation, useLoadoutAudit
```

---

## ✅ Что нужно сделать (детальный план)

1. Извлечь из документа структуру лодаутов, комплектов и категорий; составить перечень сущностей/ограничений.
2. Спроектировать REST endpoints для CRUD лодаутов, комплектов, пресетов, публикации командных комплектов и аудита.
3. Добавить endpoints для активации, быстрого переключения, проверки совместимости и расчёта бюджета (validation APIs).
4. Описать модели (`CombatLoadout`, `LoadoutKit`, `LoadoutSlot`, `LoadoutPreset`, `LoadoutAuditEntry`, `LoadoutActivationRequest`, `ValidationResult`, `EnergyBudget`) в components и вынести, если требуется.
5. Спроектировать события (`loadout.updated`, `loadout.activated`, `kit.shared`, `preset.published`, `loadout.validation.failed`) в AsyncAPI секции.
6. Указать безопасность: `BearerAuth`, scopes (`loadouts:read`, `loadouts:write`, `loadouts:activate`, `loadouts:share`), поддержку idempotency.
7. Подготовить примеры запросов/ответов/событий, описать лимиты (слоты, вес, cooldown).
8. Прописать зависимости: проверка имплантов и способностей (refs на другие API), интеграция с аренами, рейдами, экстракциями.
9. Описать критерии приемки, FAQ, чеклист, инструкции по обновлению `brain-mapping.yaml` и `.BRAIN`.

---

## 🔀 Требуемые эндпоинты

1. `GET /api/v1/gameplay/combat/loadouts` — список лодаутов игрока (фильтры по категории, роли, активности).
2. `POST /api/v1/gameplay/combat/loadouts` — создание лодаута из комплектов (поддержка глобальных пресетов).
3. `GET /api/v1/gameplay/combat/loadouts/{loadoutId}` — подробности лодаута (энергия, человечность, совместимость, история).
4. `PATCH /api/v1/gameplay/combat/loadouts/{loadoutId}` — обновление (замена комплектов, ролей, гаджетов).
5. `DELETE /api/v1/gameplay/combat/loadouts/{loadoutId}` — удаление/архивация (с поддержкой восстановления).
6. `POST /api/v1/gameplay/combat/loadouts/{loadoutId}/activate` — активация лодаута (в безопасной зоне/перед матчем).
7. `POST /api/v1/gameplay/combat/loadouts/{loadoutId}/duplicate` — копирование лодаута и его комплектов.
8. `GET /api/v1/gameplay/combat/loadouts/{loadoutId}/validation` — результаты последней проверки.
9. `POST /api/v1/gameplay/combat/loadouts/{loadoutId}/validate` — принудительная проверка совместимости и ограничений.
10. `GET /api/v1/gameplay/combat/kits` — библиотека комплектов (личных, командных, фракционных).
11. `POST /api/v1/gameplay/combat/kits` — создание/обновление комплекта (оружейный, имплант, кибердек и т.д.).
12. `POST /api/v1/gameplay/combat/presets/publish` — публикация пресета в гильдейскую/фракционную библиотеку.
13. `GET /api/v1/gameplay/combat/loadouts/presets` — глобальные/фракционные пресеты (с фильтрами).
14. `GET /api/v1/gameplay/combat/loadouts/audit` — журнал изменений лодаутов (пагинация, фильтр по оператору/событию).
15. `POST /api/v1/gameplay/combat/loadouts/{loadoutId}/share` — расшарить лодаут со сквадом/гильдией (лимиты, TTL).

Все мутационные операции требуют заголовков `Idempotency-Key` и `X-Audit-Id`. Использовать стандартные ответы через `$ref` (`shared/common/responses.yaml`, `shared/common/pagination.yaml`).

---

## 🧱 Модели данных

- **CombatLoadout** — `loadoutId`, `ownerId`, `category`, `roleId`, `kits[]`, `energyBudget`, `humanityCost`, `weight`, `status`, `createdAt`, `updatedAt`, `version`.
- **LoadoutKit** — `kitId`, `type` (`WEAPON`, `ABILITY`, `IMPLANT`, `CYBERDECK`, `SKILL`, `CONSUMABLE`, `DRONE`), `slots[]`, `constraints`, `sharedFrom`, `locked`.
- **LoadoutSlot** — `slotId`, `entityType`, `entityId`, `rarity`, `brand`, `modifiers`, `consumption`.
- **LoadoutPreset** — `presetId`, `source` (`FACTION`, `CORPORATE`, `EVENT`, `PLAYER`), `metadata`, `requirements`, `boosts`, `allowedEdits`.
- **LoadoutValidationResult** — `loadoutId`, `isValid`, `issues[]`, `warnings[]`, `calculatedMetrics` (energy, weight, humanity), `timestamp`.
- **LoadoutActivationRequest** — `loadoutId`, `context` (`SAFE_ZONE`, `MATCHMAKING`, `ARENA`, `EXTRACTION`, `RAID`), `targetMatchId`, `requestedBy`, `overrideFlags`.
- **LoadoutAuditEntry** — `entryId`, `loadoutId`, `action`, `changedBy`, `changes[]`, `previousVersion`, `timestamp`.
- **KitSharingPayload** — `kitId`, `sharedBy`, `sharedWith`, `expiresAt`, `permissions`.
- **LoadoutCategory**, **LoadoutRoleAssignment**, **EnergyBudget**, **HumanityBudget** — enum/embedded схемы.
- **Async Event Payloads** — `LoadoutUpdated`, `LoadoutActivated`, `LoadoutValidationFailed`, `PresetPublished`, `KitShared`.

---

## 🧭 Принципы и правила

- Безопасность: `BearerAuth`, роли/scopes `loadouts:*`, поддержка JWT с in-game session.
- Проверки совместимости должны ссылаться на API имплантов, способностей, ролей (через `$ref` и cross-links).
- Ограничения: максимум 10 лодаутов; лимит на расшаривание; cooldown на активацию (например, 60 секунд); audit trail обязателен.
- Валидация должна учитывать энергию, человечность, вес, доступность предметов в инвентаре, конфликтующие импланты/способности, артефакты экономики.
- События должны публиковаться для realtime/notification сервисов, описать payload и каналы (`/topics/loadouts`).
- Учесть интеграцию с матчмейкингом: `loadout.activate` блокируется при несоответствии роли/пресета требованиям матча.
- DRY: выносить повторяющиеся схемы (budget, slot, kit) в `components`.
- Соблюдать лимит 400 строк: при необходимости вынести схемы и события во вспомогательные файлы.

---

## ✅ Критерии приемки

1. Все 15 эндпоинтов описаны с параметрами, ответами, примерами (>=70% покрытие).
2. Схемы вынесены в `components` и переиспользуются через `$ref`.
3. Асинхронные события описаны (как минимум `loadout.updated`, `loadout.activated`, `kit.shared`) с payload и каналами.
4. Указаны связи с имплантами, способностями, ролями, экономикой (ссылки на соответствующие API).
5. Для всех POST/PATCH/DELETE присутствуют `Idempotency-Key`, `X-Audit-Id`, описан аудит.
6. Валидация и ограничения (энергия, человечность, вес, cooldown) отражены в схемах и описаниях.
7. Прописаны категории/контексты активации (safe zone, matchmaking, arena, extraction, raid).
8. Задокументированы пресеты (источники, ограничения редактирования, публикация/отзыв).
9. Определены и описаны права доступа и роли (`loadouts:read`, `loadouts:write`, `loadouts:activate`, `loadouts:share`).
10. Добавлены примеры для активации, проверки, шаринга, публикации пресета.
11. Документ содержит `x-target-architecture` блок.
12. Checklist и FAQ заполнены, указаны шаги обновления `brain-mapping.yaml` и `.BRAIN`.

---

## 📎 Checklist перед сдачей

- [ ] Все разделы шаблона заполнены, ссылки на `.BRAIN` и связанные API добавлены.
- [ ] Endpoints/события/схемы проходят lint и укладываются в лимит строк (при необходимости — вынос в подфайлы).
- [ ] Безопасность, аудит и idempotency задокументированы.
- [ ] Примеры отражают ключевые сценарии (создание, активация, валидация, публикация).
- [ ] Обновлены `tasks/config/brain-mapping.yaml` и `.BRAIN/02-gameplay/combat/combat-loadouts-system.md` (секция API Tasks Status).

---

## ❓ FAQ

**Q:** Можно ли активировать лодаут в бою?  
**A:** Нет. Разрешено только в безопасных зонах, матчмейкинг-лобби или через специальные станции (экстракция). В API описать ограничения и ошибки (`BIZ_LOADOUT_ACTIVATION_BLOCKED`).

**Q:** Как обрабатываются расшаренные комплекты?  
**A:** Через `kit.shared` события и временные разрешения. В API указать TTL, права на редактирование и аудит.

**Q:** Что делать, если предметы отсутствуют в инвентаре?  
**A:** Валидация должна возвращать предупреждение/ошибку (`missingItems[]`), а активация — блокировать до решения (покупка, крафт, замена).

---

## 🔗 Связность и последующие шаги

- После генерации обновить mapping и `.BRAIN` документ (статус `queued` / ID `API-TASK-299`).
- Синхронизировать контракты с заданиями арен, экстракций и клановых операций.
- Подготовить при необходимости отдельные фронтенд/бекенд задачи (loadout UI, матчмейкинг проверки) на следующих этапах.



### OpenAPI (обязательно)

- Заполни `info.x-microservice` (name, port, domain, base-path, package) по данным целевого микросервиса.
- В секции `servers` оставь Production gateway `https://api.necp.game/v1` и пример локальной разработки `http://localhost:8080/api/v1`.
- WebSocket маршруты публикуй только через `wss://api.necp.game/v1/...`.

