# Task ID: API-TASK-321
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** queued  
**Создано:** 2025-11-08 13:05  
**Создатель:** GPT-5 Codex  
**Зависимости:** [API-TASK-319] – world effects; [API-TASK-320] – economy indices; [API-TASK-317] – core player orders

---

## 📋 Краткое описание

Сформировать OpenAPI спецификацию `api/v1/social/player-orders/news.yaml`, описывающую новостную ленту, уведомления и публичные сообщения о заказах игроков.

**Что нужно сделать:** На основе `.BRAIN/02-gameplay/social/player-orders-world-impact-детально.md` задокументировать REST API social-service для генерации, выдачи и подписки на новости/уведомления о заказах, кризисах и достижениях.

---

## 🎯 Цель задания

Обеспечить социальный сервис контрактом для новостной витрины и уведомлений, отражающих активность заказов, кризисы, рейтинги и экономические события.

**Зачем это нужно:**
- Дать UI данные для ленты новостей, баннеров событий и push-уведомлений.
- Связать world/economy эффекты и рейтинги с социальным взаимодействием игроков.
- Поддержать storytelling и вовлечённость через освещение крупных заказов и кризисов.

---

## 📚 Источники информации

### Основной источник

**Репозиторий:** `.BRAIN`  
**Документ:** `.BRAIN/02-gameplay/social/player-orders-world-impact-детально.md`  
**Версия:** 1.0.0  
**Последнее обновление:** 2025-11-08 12:55  
**Статус:** approved  

**Ключевые элементы:**
- Разделы «Социальные эффекты», «Влияние на города и локации», «UX и визуализация», «Kafka события».
- Сущности: новости, события, карты влияния, оповещения.
- Требования к интеграциям с world/economy сервисами.
- UX: карта влияния, новостные ленты, уведомления.

### Дополнительные источники

- `.BRAIN/02-gameplay/social/player-orders-system-детально.md` – общий процесс и контент.
- `.BRAIN/02-gameplay/social/player-orders-creation-детально.md` – мастер создания (события публикации).
- `.BRAIN/02-gameplay/social/player-orders-reputation-детально.md` – рейтинги и санкции.
- `.BRAIN/02-gameplay/social/relationships-system-детально.md` – доверие и таргетинг уведомлений.
- `.BRAIN/05-technical/content-generation/city-life-population-algorithm.md` – активность городов.
- `visual-style-locations-детально.md` – визуальные активы для баннеров/хабов.
- Kafka события из world/economy спецификаций (`world.player-orders.impact`, `economy.player-orders.index`).

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/social/player-orders/news.yaml`  
**Дополнительные файлы (если необходимо):** `news-components.yaml`, `news-examples.yaml`

**API версия:** v1  
**Тип:** OpenAPI 3.0.3 (YAML)

**Структура:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── social/
            └── player-orders/
                ├── news.yaml                ← создать
                ├── news-components.yaml     ← вынести схемы при необходимости
                └── news-examples.yaml       ← примеры (если понадобится)
```

**Примечания:** использовать `$ref` на `shared/common` (responses, pagination, security).

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend
- **Микросервис:** social-service  
- **Порт:** 8084  
- **API Base Path:** `/api/v1/social/player-orders/*`  
- **Домен:** социальные новости, уведомления, публичные фиды  
- **Зависимости:** world-service (effects/crises), economy-service (indexes), notification-service, analytics-service, auth-service, telemetry-service

### Frontend
- **Модуль:** `modules/social/player-orders/news`  
- **State Store:** `useSocialStore` (`playerOrderNews`, `alerts`, `subscriptions`)  
- **UI компоненты @shared/ui:** `NewsFeed`, `EventBanner`, `CrisisTicker`, `CityActivityMap`, `HighlightCard`  
- **Формы @shared/forms:** `NewsFilterForm`, `SubscriptionForm`  
- **Layouts @shared/layouts:** `SocialDashboardLayout`, `NewsroomLayout`  
- **Хуки @shared/hooks:** `useRealtime`, `useWebsocket`, `usePagination`, `useNotification`, `useFilters`, `useMap`

### Комментарий для спецификации
В начале `news.yaml` указать микросервис, зависимости, фронтенд модуль, UI/формы/хуки, Kafka события, базовый путь.

---

## ✅ Детальный план

### Шаг 1: Анализ требований
1. Выделить типы новостей: публикации заказов, кризисы, рейтинги, экономические изменения, успехи игроков/NPC.
2. Определить поля новостного элемента, статусы, теги, связи с городами и заказами.
3. Уточнить требования к уведомлениям и подпискам.

### Шаг 2: Проектирование endpoints
1. Спланировать REST точки:
   - `GET /social/player-orders/news`
   - `GET /social/player-orders/news/{newsId}`
   - `GET /social/player-orders/news/highlights`
   - `POST /social/player-orders/news/publish`
   - `POST /social/player-orders/news/subscriptions`
   - `DELETE /social/player-orders/news/subscriptions/{subscriptionId}`
2. Определить фильтры (cityId, tag, severity, orderId, timeframe).
3. Определить ответы и коды ошибок (400/401/403/404/409/422/429).

### Шаг 3: Модели и компоненты
1. Описать схемы: `PlayerOrderNewsItem`, `NewsHighlight`, `NewsPublishRequest`, `NewsSubscription`, `NewsSubscriptionRequest`, `NewsAttachment`, `NewsTag`, `NewsAlert`.
2. Указать связь с заказами, городами, индексами.
3. Подготовить примеры (обычная новость, кризис, уведомление, subscription).

### Шаг 4: События и интеграции
1. Документировать `x-kafka-topics`: `social.player-orders.news`, `social.player-orders.alert`, `social.player-orders.subscription.updated`.
2. Описать как news API получает события из world/economy (через internal bus) и публикует их в feed.
3. Задокументировать зависимость от notification-service для push/webhook каналов.

### Шаг 5: Финализация
1. Проверить лимит 400 строк, вынести схемы при необходимости.
2. Добавить `info.version` (1.0.0) и ссылку на источники.
3. Запустить `npx swagger-cli validate api/v1/social/player-orders/news.yaml` (обязательный критерий).
4. Убедиться в полном покрытии чеклиста (примеры, архитектура, безопасность, FAQ).

---

## 🛣️ Endpoints (предложение)

1. **GET `/social/player-orders/news`** – лента новостей с пагинацией, фильтрами (cityId, tag, severity, orderId, template, period).  
2. **GET `/social/player-orders/news/{newsId}`** – подробности новости (контент, связанные заказы, города, индексы).  
3. **GET `/social/player-orders/news/highlights`** – подборка хайлайтов (крупные контракты, кризисы, турниры).  
4. **POST `/social/player-orders/news/publish`** – публикация новости (для админов/модераторов).  
5. **POST `/social/player-orders/news/subscriptions`** – подписка на категории (город, тип, severity).  
6. **DELETE `/social/player-orders/news/subscriptions/{subscriptionId}`** – отмена подписки.  
7. **GET `/social/player-orders/news/subscriptions`** – список подписок текущего пользователя.

Все endpoints используют `BearerAuth`, стандартные ответы из `shared/common`, и учитывают rate limiting.

---

## 🧱 Основные модели

- **PlayerOrderNewsItem** (`newsId`, `title`, `summary`, `body`, `orderIds[]`, `cityIds[]`, `tags[]`, `severity`, `publishedAt`, `source`, `attachments[]`).  
- **NewsHighlight** (`highlightId`, `title`, `description`, `type`, `score`, `media`, `link`).  
- **NewsPublishRequest** (`title`, `summary`, `body`, `orderIds[]`, `cityIds[]`, `tags[]`, `severity`, `media`, `visibility`).  
- **NewsSubscription** (`subscriptionId`, `subscriberId`, `channel`, `filters`, `createdAt`).  
- **NewsSubscriptionRequest** (`channel`, `cityIds[]`, `tags[]`, `severity`, `language`, `deliveryWindow`).  
- **NewsAlert** (`alertId`, `type`, `severity`, `description`, `actions`, `expiresAt`).  
- **NewsAttachment** (`attachmentId`, `type`, `url`, `caption`).  
- **NewsTag** (`tagId`, `label`, `category`).

Добавить enums (severity, channel, tag categories), required поля, примеры.

---

## 📏 Принципы

- SOLID, DRY, KISS; переиспользовать общие компоненты (pagination, responses, security).  
- OpenAPI 3.0.3, RESTful стиль.  
- Не хардкодить тексты и медиапути – использовать ссылки на content-service.  
- Указать бизнес-правила: модерация, фильтрация контента, локализация, подписки.  
- Обеспечить безопасность: BearerAuth + scopes `social:read`, `social:manage`.  
- Максимум 400 строк на файл, при необходимости разбивать.

---

## 🧪 Примеры

- **Новость о крупном заказе** – включает связанный `orderId`, города, индексы.  
- **Новость о кризисе** – severity=critical, ссылка на world effects и alerts.  
- **Подборка highlights** – несколько карточек с медиа и CTA.  
- **Подписка** – запрос для игрока с каналом `push`.  
- **Уведомление** – пример `social.player-orders.alert` payload.  
- **Ошибки** – 422 при недопустимом контенте, 409 при конфликте публикации.

---

## 🔗 Связности

- **world-service:** источник кризисов и событий (привязка к эффектам).  
- **economy-service:** индексы, влияющие на новости.  
- **notification-service:** доставка уведомлений (push, email, webhook).  
- **analytics-service:** сбор статистики по просмотрам/кликам.  
- **content-service:** хранение медиа и вложений.  
- **relationships-service:** таргетирование новостей по доверительным связям.  
- **telemetry-service:** аудит публикаций и подписок.

---

## ✅ Критерии приёмки

1. Создан `api/v1/social/player-orders/news.yaml` с версией `1.0.0` и архитектурным комментарием.  
2. Описаны все запланированные endpoints, параметры, запросы, ответы, ошибки.  
3. Добавлены схемы новостей, хайлайтов, подписок, alerts, вложений.  
4. Примеры JSON покрывают обычные новости, кризисы, подписки, ошибки.  
5. Kafka события документированы в `x-kafka-topics`.  
6. Используются общие компоненты `responses.yaml`, `pagination.yaml`, `security.yaml`.  
7. Указаны зависимости от world/economy/notification сервисов и бизнес-правила.  
8. Безопасность: BearerAuth + scopes, модерация публикаций.  
9. `npx swagger-cli validate api/v1/social/player-orders/news.yaml` проходит без ошибок.  
10. Размер каждого YAML файла ≤400 строк, при необходимости вынос компонентов.  
11. Frontend модуль `modules/social/player-orders/news` получает перечисленные данные и UI элементы.  
12. FAQ включает ответы по локализации, фильтрам, подпискам и модерации.

---

## ❓ FAQ

**Q:** Каковы источники новостей?  
**A:** Новости генерируются автоматически из событий (world/economy), публикаций заказов и вручную модераторами. Источник фиксируется в поле `source`.

**Q:** Как обрабатывается токсичный контент?  
**A:** `POST /news/publish` проверяет контент через content-service; при ошибке возвращается 422 с деталями.

**Q:** Можно ли сегментировать новости по языкам?  
**A:** Предусмотрено поле `language` и фильтр в `GET /news`, также при подписке можно указать предпочитаемый язык.

**Q:** Как интегрируются новости с картой влияния?  
**A:** Новость содержит `cityIds` и ссылки на `world/player-orders/effects`, что позволяет отображать на карте активные точки.

**Q:** Как отменить подписку?  
**A:** Через `DELETE /news/subscriptions/{subscriptionId}` или вручную через UI; уведомления прекращаются сразу.

---

## 📌 История выполнения

- 2025-11-08 – создано задание на основе `.BRAIN/02-gameplay/social/player-orders-world-impact-детально.md`, статус `queued`.



### OpenAPI (обязательно)

- Заполни `info.x-microservice` (name, port, domain, base-path, package) по данным целевого микросервиса.
- В секции `servers` оставь Production gateway `https://api.necp.game/v1` и пример локальной разработки `http://localhost:8080/api/v1`.
- WebSocket маршруты публикуй только через `wss://api.necp.game/v1/...`.

