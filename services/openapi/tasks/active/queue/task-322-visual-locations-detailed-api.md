# Task ID: API-TASK-322
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** queued  
**Создано:** 2025-11-08 16:20  
**Создатель:** AI Agent (ДУАПИТАСК)  
**Зависимости:** none

---

## 📋 Краткое описание

Создать OpenAPI спецификацию для детализированных профилей локаций мира.

**Что нужно сделать:** Описать `api/v1/world/visuals/locations-detailed.yaml`, включив расширенные фильтры, экспорт визуальных снапшотов и ссылку на Kafka события по обновлениям визуалов.

---

## 🎯 Цель задания

Обеспечить world-service полноценным REST API, которое выдаёт утверждённые визуальные профили зон Night City и глобальных территорий с детализацией освещения, погоды, социального трафика и ассетов.

**Зачем это нужно:**
- World-команде требуется единый источник правды для визуального контента, согласованный с art/UX и маркетингом.
- Экспорт снапшотов должен снабжать маркетинг и UI-команды актуальными ассетами без ручных выгрузок.
- Социальные и экономические сервисы получают синхронизированный контекст о состоянии локаций для событий и торговых сценариев.

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`  
**Путь к документу:** `.BRAIN/03-lore/_03-lore/visual-guides/visual-style-locations-детально.md`  
**Версия документа:** 1.0.0  
**Дата последнего обновления:** 2025-11-08 11:06  
**Статус документа:** approved (api-readiness: ready)

**Что важно из этого документа:**
- Атмосферные профили макро-зон (Night City Megapolis, Neo Tokyo Arcology, Lagos Skyport, Amazon Cloud Basin).
- Детальный разбор районов Night City и социальных объектов с цветовыми кодами, NPC, безопасностью.
- Требования к REST/kafka контурам, JSON схемам и метрикам (VisualFidelityScore, EventVisualImpact).
- Связь с world-service, social-service, economy-service и маркетинговыми пакетами.

### Дополнительные источники

- `.BRAIN/03-lore/_03-lore/visual-guides/visual-style-locations.md` — базовая сетка визуальных профилей, на которую опирается детальная версия.
- `.BRAIN/03-lore/_03-lore/visual-guides/visual-style-assets.md` — перечень ассетов и визуальных тегов для привязки к профилям.
- `.BRAIN/02-gameplay/world/world-state/player-impact-mechanics.md` — влияние игроков на состояние зон.
- `API-SWAGGER/api/v1/world/cities/population.yaml` — существующий пример структурирования world-service данных (для единообразия схем и ссылок на общие компоненты).

### Связанные документы

- `.BRAIN/02-gameplay/social/player-orders-world-impact-детально.md` — определяет зависимости по мировым событиям и витринам.
- `.BRAIN/05-technical/content-generation/city-life-population-algorithm.md` — данные для синхронизации NPC и потоков.
- Kafka топики из раздела 11 документа (`world.visuals.location.detailed.updated`, `world.visuals.event.triggered`).

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/world/visuals/locations-detailed.yaml`  
**API версия:** v1  
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── world/
            └── visuals/
                ├── schemas/
                │   ├── visual-location-detailed.yaml   ← вынести компоненты
                │   └── visual-snapshot.yaml            ← вынести запрос/ответ экспорта
                └── locations-detailed.yaml              ← основной файл с paths
```

Если директория `visuals/` отсутствует — создать с README, описывающим назначение и связь с другими world визуалами.

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервис)
- **Микросервис:** world-service  
- **Порт:** 8086  
- **API Base:** `/api/v1/world/visuals/*`  
- **Домен:** визуальные профили и события мира  
- **Зависимости:** social-service (данные NPC и хабов), economy-service (торговые витрины), marketing-service (ассет-пакеты, порт 8089)

### OpenAPI (обязательно)

- Заполни `info.x-microservice` (name, port, domain, base-path, package) по данным целевого микросервиса.
- В секции `servers` оставь Production gateway `https://api.necp.game/v1` и пример локальной разработки `http://localhost:8080/api/v1`.
- WebSocket маршруты публикуй только через `wss://api.necp.game/v1/...`.

### Frontend (модуль)
- **Модуль:** modules/world/atlas  
- **State Store:** useWorldStore (visualProfiles, atmosphericStates, snapshotJobs)  
- **UI компоненты (@shared/ui):** LocationCard, EventCard, MapView, AmbientTagList, Timeline  
- **Формы (@shared/forms):** VisualFilterForm, SnapshotRequestForm  
- **Layouts:** GameLayout  
- **Хуки:** useDebounce, useRealtime, useWorldStoreSync  
- **Доп. интеграции:** modules/marketing/showcase (pull ассет-пакетов), modules/social/hubs (подсветка активных хабов)

### Kafka и телеметрия
- producers: world-service (`world.visuals.location.detailed.updated`)  
- consumers: analytics, frontend-theme-service, marketing-service  
- Метрики: VisualFidelityScore, EventVisualImpact, HubAmbienceRetention (через telemetry-service API)

Добавить блок `Target Architecture` в начале итогового файла OpenAPI.

---

## 🧭 Подробный план реализации

1. **Подготовить структуру директорий** — создать `api/v1/world/visuals/` и README, настроить подпапку `schemas/`, проверить отсутствие конфликтов имен.
2. **Сформировать components** — описать `VisualLocationDetailedProfile`, вложенные структуры атмосферы, ассетов, социальных связей в `schemas/visual-location-detailed.yaml`.
3. **Определить endpoints** — задокументировать `GET /world/visuals/locations/detailed`, `GET /world/visuals/locations/detailed/{locationId}` и `POST /world/visuals/locations/snapshots`, указать параметры фильтрации, пагинации и body.
4. **Подключить общие компоненты** — использовать `$ref` на `shared/common/responses.yaml` для ошибок и `shared/common/pagination.yaml` для метаданных.
5. **Задокументировать Kafka и метрики** — добавить раздел `x-streaming`/`x-messaging` или описание webhook-подписок, описать связь с метриками в разделе `components`.
6. **Проверить по чеклисту** — убедиться в соответствии размеру файла, заполнить все секции, сверить с исходным документом и readiness-требованиями.

---

## 🔀 Endpoints

### 1. GET `/api/v1/world/visuals/locations/detailed`
- **Назначение:** вернуть список детализированных профилей локаций с фильтрами по городу, зоне, атмосфере, активности.
- **Параметры запроса:**
  - `cityId` (query, string, optional) — идентификатор мегаполиса (NightCity, NeoTokyo, Lagos, AmazonBasin).
  - `zoneId` (query, string, optional) — точечная зона (QuantumPlaza, Pacifica, Westbrook и т.д.).
  - `ambienceTag` (query, array[string], optional) — визуальные метки (neon, bioluminescent, corporate).
  - `weatherProfile` (query, string, optional, enum: neutral, tense, extreme).
  - `timeOfDay` (query, string, optional, enum: dawn, day, dusk, night).
  - `page`, `size` (pagination) — подключить общий компонент пагинации.
- **Ответ 200:** объект `VisualLocationDetailedCollection` c полями `data[]`, `meta` (пагинация), `filters`.
- **Ответы ошибок:** 400/404/500 — ссылка на `#/components/responses/BadRequest`, `#/components/responses/NotFound`, `#/components/responses/InternalError`.
- **Пример:** включить Night City Megapolis (ambience `neon`, `crowded`).

### 2. GET `/api/v1/world/visuals/locations/detailed/{locationId}`
- **Назначение:** вернуть полный профиль единичной локации, включая атмосферные слои, социальные узлы, безопасность, связанные события.
- **Параметры пути:** `locationId` (string, required).
- **Ответ 200:** `VisualLocationDetailedProfile`.
- **Расширения:** `x-related` — ссылки на `/api/v1/social/visuals/hubs/detailed` и `/api/v1/world/visuals/events`.
- **Ошибки:** 404 если не найден, 410 если архивирован (использовать общее описание).

### 3. POST `/api/v1/world/visuals/locations/snapshots`
- **Назначение:** инициировать экспорт ассетов и палитр для выбранной локации и временного сценария.
- **Тело запроса:** `VisualSnapshotRequest` (locationId, atmosphereOverrides, deliverTo: marketing | ui | telemetry, outputFormats).
- **Ответ 202:** `VisualSnapshotJob` с `jobId`, `status`, `estimatedReadyAt`.
- **Заголовки:** `Idempotency-Key` (пользоваться для повторов), `X-Marketing-Campaign` (optional).
- **Side-effect:** задокументировать Kafka событие `marketing.visuals.package.generated` как результат выполнения джоба.
- **Ошибки:** 400 (валидация), 409 (уже существует активный job), 503 (маркетинг недоступен).

---

## 🧱 Модели данных

- **VisualLocationDetailedProfile**
  - `locationId` (string, pattern `^[A-Z][A-Za-z0-9]+$`) — уникальный идентификатор.
  - `name` (string) — локализуемое название.
  - `city` (string, enum: NightCity, NeoTokyo, LagosSkyport, AmazonCloudBasin, Badlands, FloatingMangroveFarms, RedWastes).
  - `zoneType` (string, enum: macro, district, hub, raid, hidden, frontier).
  - `ambienceTags` (array[string], minItems 1) — визуальные теги.
  - `weatherCycle` (`VisualWeatherCycle`) — набор погодных сцен (нейтральная, напряжённая, экстремальная).
  - `lightingProfile` (`VisualLightingProfile`) — дневные/ночные сценарии, настройки неона.
  - `audioPalette` (array[`AudioLayerReference`], maxItems 8).
  - `factionPresence` (array[`FactionVisualPresence`]) — корпорации/банды, цвета, логотипы.
  - `npcTraffic` (`NpcTrafficProfile`) — плотность, типы NPC, активность.
  - `securityLevel` (`SecurityVisualProfile`) — патрули, системы предупреждений.
  - `socialNodes` (array[`SocialHubReference`]) — связанные хабы и их визуальные состояния.
  - `relatedEvents` (array[`VisualEventBrief`]) — активные или предстоящие события.
  - `assets` (array[`VisualAssetReference`]) — наборы ассетов, ar/vr панели, арт-панели.
  - `metrics` (`VisualMetricSnapshot`) — текущие значения VisualFidelityScore, EventVisualImpact.
  - `updatedAt` (string, format date-time).

- **VisualSnapshotRequest**
  - `locationId` (string, required).
  - `timeOfDay` (string, enum).
  - `weatherProfile` (string, enum).
  - `includeAudio` (boolean, default false).
  - `includeBlueprints` (boolean, default false).
  - `deliverTo` (array[string], items enum: marketing, frontend, analytics, qa).
  - `outputFormats` (array[string], enum: glb, png, wav, json, fbx).
  - `notes` (string, maxLength 500).

- **VisualSnapshotJob**
  - `jobId` (string, uuid).
  - `locationId` (string).
  - `status` (string, enum: queued, processing, ready, failed).
  - `requestedAt`, `estimatedReadyAt`, `completedAt` (date-time).
  - `deliverTo` (array[string]).
  - `downloadLinks` (array[`SnapshotAssetLink`]) — заполняется при статусе ready.

- **VisualWeatherCycle**, **VisualLightingProfile**, **AudioLayerReference**, **FactionVisualPresence**, **NpcTrafficProfile**, **SecurityVisualProfile**, **SocialHubReference**, **VisualEventBrief**, **VisualAssetReference**, **VisualMetricSnapshot**, **SnapshotAssetLink** — оформить в отдельном файле `schemas/visual-location-detailed.yaml` с типами, валидацией и примерами (учесть примеры из Night City Megapolis и Pacifica).

Все модели должны ссылаться на общие enum/типы, где возможно, и переиспользовать `shared/common/pagination.yaml`.

---

## 📏 Принципы и правила

- Соблюдать SOLID/DRY/KISS при описании схем — не дублировать структуры, выносить повторяющиеся блоки в components.
- Использовать OpenAPI 3.0.3, `$ref` на `shared/common/responses.yaml` и `shared/common/security.yaml`.
- Максимальный размер каждого файла ≤ 400 строк; вынести схемы в отдельные YAML.
- Все ошибки оформлять через общую модель `Error` (ENUM кодов: `WORLD_VISUAL_NOT_FOUND`, `VAL_SNAPSHOT_INVALID`, `VAL_SNAPSHOT_CONFLICT`).
- Описать безопасность (BearerAuth), подчеркнуть, что доступ ограничен ролями `world.visuals.read`, `world.visuals.manage`.
- Добавить `x-feature-flags` (visuals.detailed.enabled) и `x-telemetry` ссылки на метрики.

---

## ✅ Критерии приемки

1. Paths в `locations-detailed.yaml` полностью покрывают функциональность GET списка, GET детали и POST снапшота.
2. Все query-параметры имеют чёткие описания, примеры и ограничения, соответствующие документу.
3. Схемы вынесены в `schemas/visual-location-detailed.yaml` и связаны через `$ref`.
4. Пагинация подключена через `shared/common/pagination.yaml`.
5. Ответы ошибок используют общие `Error` компоненты без дублирования.
6. Примеры включают Night City Megapolis, Pacifica Reclamation Zone и Amazon Cloud Basin.
7. Kafka события описаны в разделе `components` (`x-messaging`), с полезной нагрузкой и потребителями.
8. Определены требуемые scopes безопасности и хедеры (`Idempotency-Key`, `X-Marketing-Campaign`).
9. `VisualSnapshotRequest` валидируется на уникальность job (409) и наличие обязательных полей.
10. Документация включает перекрёстные ссылки на social hubs и визуальные события.
11. README для директории `visuals/` обновлён с описанием структуры и целей.
12. Файл проходит валидацию `scripts/validate-swagger.ps1` без ошибок.

---

## ❓ FAQ

- **Нужно ли включать маркетинговый endpoint `/marketing/visuals/packages`?**  
  Нет, он будет оформлен в отдельной задаче для marketing-service; здесь достаточно упомянуть зависимость в `deliverTo` и Kafka событии.

- **Как обрабатывать устаревшие профили?**  
  Использовать статус `archived` в ответе 200 и возвращать 410 при прямом запросе; описать поле `lifecycleStatus`.

- **Что делать с локациями без соц-хабов?**  
  Поле `socialNodes` допускает пустой массив; описать это в схемах и примерах (Badlands Storm Belt).

- **Где хранить ссылки на ассеты?**  
  В `VisualAssetReference.downloadUri`; подчеркнуть, что URI управляется CDN и выдаётся после успешного снапшота.

- **Нужна ли поддержка реального времени?**  
  Основной обмен идёт через Kafka; дополнительно указать, что фронтенд подписывается на `world.visuals.location.detailed.updated` через realtime-шлюз, но сам REST API остаётся статичным.

---



