# Task ID: API-TASK-322
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** queued  
**Создано:** 2025-11-08 16:30  
**Создатель:** AI Agent (GPT-5 Codex)  
**Зависимости:** [API-TASK-241], [API-TASK-300], [API-TASK-314]

---

## 📋 Краткое описание

Создать спецификацию `api/v1/world/visuals/locations-detailed.yaml`, описывающую расширенные визуальные профили макро- и микро-локаций мира NECPGAME, экспорт ассетов и связь с погодой, освещением и насыщенностью NPC.

**Что нужно сделать:** Сформировать OpenAPI 3.0.3 спецификацию для world-service, охватывающую детальные `DetailedVisualProfile`, API экспорта снапшотов и привязку к связанным сервисам.

---

## 🎯 Цель задания

Задокументировать и стандартизировать выдачу визуальных профилей локаций для world-service, чтобы команды арт-пайплайна, маркетинга и социального взаимодействия могли синхронно использовать одни и те же данные.

**Зачем это нужно:**
- Убедиться, что визуальные состояния локаций (погода, освещение, NPC-плотность) доступны из одной точки.
- Поддержать экспорт ассетов и палитр для маркетинговых пакетов и внутриигровых карт.
- Синхронизировать данные с `city-life-population` алгоритмами и социальными событиями.

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`  
**Путь к документу:** `.BRAIN/03-lore/_03-lore/visual-guides/visual-style-locations-детально.md`  
**Версия документа:** 1.0.0  
**Дата последнего обновления:** 2025-11-08 11:06  
**Статус документа:** approved (api-readiness: ready)

**Что важно из этого документа:**
- Расширенные профили макро-локаций (Night City, Neo Tokyo, Lagos, Amazon Cloud Basin) и районов Night City.
- Атмосферные настройки (погода, свет, аудиослои) и фракционное влияние.
- Требования к `DetailedVisualProfile`, `DistrictVisualProfile`, снапшотам ассетов и Kafka потокам `world.visuals.location.detailed.updated`.

### Дополнительные источники

- `.BRAIN/05-technical/content-generation/city-life-population-algorithm.md` — плотность NPC и сценарии заполняемости.
- `.BRAIN/03-lore/visual-guides/visual-style-locations-детально.md` — asset mapping, JSON источники, ссылки на фронтенд компоненты.
- `API-SWAGGER/api/v1/world/cities/population.yaml` — формат world-state данных для городов.

### Связанные документы

- `.BRAIN/02-gameplay/social/player-orders-creation-детально.md` — влияние социальных заказов на оформление районов.
- `.BRAIN/02-gameplay/social/player-orders-world-impact-детально.md` — события, меняющие визуальные состояния.
- `.BRAIN/03-lore/locations/locations-overview.md` — базовые идентификаторы городов и регионов.

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/world/visuals/locations-detailed.yaml`  
**API версия:** v1  
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── world/
            └── visuals/
                └── locations-detailed.yaml
```

Если файл существует — аккуратно обновить, иначе создать новый.

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервис)
- **Микросервис:** world-service  
- **Порт:** 8092  
- **API Base:** `/api/v1/world/visuals/*`  
- **Зависимости:** auth-service (JWT), social-service (хабы), economy-service (рынки), marketing-service (ассет-пакеты)

### OpenAPI (обязательно)

- Заполни `info.x-microservice` (name, port, domain, base-path, package) по данным целевого микросервиса.
- В секции `servers` оставь Production gateway `https://api.necp.game/v1` и пример локальной разработки `http://localhost:8080/api/v1`.
- WebSocket маршруты публикуй только через `wss://api.necp.game/v1/...`.

### Frontend (модуль)
- **Модуль:** `modules/world/atlas`  
- **State Store:** `useWorldStore` (visualProfiles, snapshotQueue)  
- **UI компоненты (@shared/ui):** LocationCard, EventCard, MapView, WeatherBadge, LightingLegend  
- **Формы (@shared/forms):** VisualFilterForm, SnapshotExportForm  
- **Layouts:** WorldAtlasLayout (`@shared/layouts`)  
- **Hooks:** useWorldFilters, useSnapshotExport (см. @shared/hooks)

### Комментарий
Потребовать в шапке YAML-файла блок:
```yaml
# Target Architecture:
# - Microservice: world-service (port 8092)
# - Frontend Module: modules/world/atlas
# - UI Components: @shared/ui (LocationCard, EventCard, MapView, WeatherBadge, LightingLegend)
# - Forms: @shared/forms (VisualFilterForm, SnapshotExportForm)
# - State: useWorldStore (visualProfiles, snapshotQueue)
# - API Base: /api/v1/world/visuals/*
```

---

## ✅ Что нужно сделать (детальный план)

1. **Сбор требований**  
   Выделить из `.BRAIN` карту макро- и микро-локаций, погодных сценариев, фракционных отметок, asset mapping и требования к снапшотам.

2. **Определение эндпоинтов**  
   Спроектировать CRUD-потоки выдачи профилей, получения отдельных районов и экспорта снапшотов. Закрепить обязательные фильтры (`macroZone`, `faction`, `dayPhase`) и пагинацию.

3. **Проектирование моделей**  
   Сформировать `DetailedVisualProfile`, `DistrictVisualProfile`, `VisualLayer`, `DynamicEffect`, `VisualSnapshotRequest`, `VisualSnapshotBundle` с примерами. Вынести повторяющиеся структуры в `components`.

4. **Документация ошибок и безопасности**  
   Подключить `Error` из `api/v1/shared/common/responses.yaml`, описать use-case 400/404/409/503. Добавить `BearerAuth` из `api/v1/shared/security/security.yaml`.

5. **Kafka и события**  
   Задокументировать publish для `world.visuals.location.detailed.updated` и subscribe для маркетинга/analytics, указать payload.

6. **Метрики и интеграции**  
   В info или description зафиксировать метрики (`VisualFidelityScore`, `HubAmbienceRetention`), зависимость от population API, player-orders задач, контракты с marketing-service.

7. **Валидация**  
   Проверить OpenAPI линтером, убедиться в корректности `$ref`, убедиться в ссылках на shared-компоненты, соответствие <400 строк (иначе разбить компоненты).

---

## 🔀 Endpoints

1. **GET `/api/v1/world/visuals/locations/detailed`**  
   - Назначение: список детальных профилей с фильтрами по зоне, фракции, уровню напряжения.  
   - Query: `macroZone` (enum), `factionId`, `ambienceTag`, `dayPhase`, `weather`, `limit`, `offset`, `includeDistricts` (boolean).  
   - Ответ 200: пагинированный список `DetailedVisualProfile`.  
   - Ошибки: 400 (невалидные фильтры), 401/403 (auth), 503 (недоступность world-service).

2. **GET `/api/v1/world/visuals/locations/{locationId}/detailed`**  
   - Path: `locationId` (string, regex `LOC-[A-Z0-9-]+`).  
   - Назначение: полный профиль с визуальными слоями, погодой, динамическими эффектами, asset mapping.  
   - Ответ 200: `DetailedVisualProfile`.  
   - Ошибки: 404 (локация отсутствует), 409 (профиль в процессе обновления).

3. **GET `/api/v1/world/visuals/locations/{locationId}/districts/{districtId}`**  
   - Возвращает `DistrictVisualProfile` (Quantum Plaza, Pacifica и т.д.) с показателями безопасности, социальными объектами, рекомендованными UI модулями.  
   - Ошибки: 404 (район отсутствует), 412 (нет связанного макро-профиля).

4. **POST `/api/v1/world/visuals/locations/snapshots`**  
   - Тело: `VisualSnapshotRequest` (локации, требуемые слои, формат, аудитория).  
   - Назначение: запрос на генерацию ассет-пакета и аудиослоёв.  
  - Ответ 202: `SnapshotTicket` (id, expiresAt, status).  
  - Ошибки: 400, 409 (есть активный тикет), 503.

5. **GET `/api/v1/world/visuals/locations/snapshots/{snapshotId}`**  
   - Возвращает `VisualSnapshotBundle` с ссылками на CDN, палитрами, аудио.  
   - Ошибки: 404, 410 (истёк), 423 (в обработке).

Все ответы ошибок ссылаться на `#/components/responses/ErrorResponse` из `shared/common/responses.yaml`.

---

## 🧱 Модели данных

- **DetailedVisualProfile**  
  Поля: `locationId`, `name`, `macroZone`, `assetId`, `palette` (primary/secondary/accent), `lighting`, `weatherSet[]`, `soundscape[]`, `dynamicEffects` (npcDensity, trafficLayers, hazardLevels), `factionInfluence[]`, `microHubs[]`, `safetyLevel`, `lastUpdated`. Включить пример для Night City.

- **DistrictVisualProfile**  
  Поля: `districtId`, `name`, `visualLayers[]`, `signatureEvents[]`, `ambientAudio`, `interactionHotspots[]`, `securityState`, `romanceSuitability`, `marketDensity`.

- **VisualLayer** (`type`, `description`, `intensityScore`, `assets[]`).  
- **DynamicEffect** (`effectType`, `trigger`, `visualCue`, `impactScore`).  
- **VisualSnapshotRequest** (`targets[]`, `includeAssets`, `includeAudio`, `channels` (marketing/ui/analytics), `format`, `requestedBy`).  
- **SnapshotTicket** (`snapshotId`, `status`, `expiresAt`, `estimatedReadyAt`).  
- **VisualSnapshotBundle** (`snapshotId`, `locationId`, `cdnLinks[]`, `palette`, `audioMix`, `kafkaEventRef`).  
- **Pagination** — использовать `api/v1/shared/common/pagination.yaml#/components/schemas/PageResponse`.

Прописать `required`, валидацию, enum для `macroZone`, `dayPhase`, `weatherSet`, `factionInfluence`.

---

## 📡 Kafka и интеграции

- **Producer:** world-service публикует `world.visuals.location.detailed.updated` с payload `{ locationId, version, atmosphere, weatherSet[], palette, updatedAt }`.  
- **Consumers:** frontend-theme-service, analytics, marketing-service.  
- Сложить описание в разделе `x-events` (vendor extension) или отдельном `components/messages`.
- Зафиксировать связь с snapshoterом (при готовности снапшота генерировать `marketing.visuals.package.generated` — указать как зависимость от API-TASK-324).

---

## 📊 Метрики и аналитика

- `VisualFidelityScore` — точность соответствия ассетов утверждённым профилям.  
- `EventVisualImpact` и `HubAmbienceRetention` как связанные показатели (читать из API-TASK-324 и API-TASK-323).  
- Описать необходимость передачи метрик в telemetry, хранение `metrics` блока в ответах (опционально).

---

## ⚙️ Принципы и правила реализации

- Соблюдать SOLID/DRY/KISS, избегать дублирования схем.  
- OpenAPI 3.0.3, единственный источник правды.  
- Не хардкодить данные — все идентификаторы должны ссылаться на БД/registry.  
- Указывать ссылки на `.BRAIN` документ и связанные задания в описании.  
- При необходимости выносить `schemas` в `api/v1/world/visuals/components/locations-detailed.yaml` (если >400 строк) с README.

---

## ✔️ Критерии приемки (минимум 10)

1. Файл `api/v1/world/visuals/locations-detailed.yaml` создан/обновлён, содержит блок Target Architecture.  
2. Описаны все 5 эндпоинтов с примерами запросов/ответов.  
3. Пагинация и фильтры реализованы через общие компоненты.  
4. Все ошибки подключены через `shared/common/responses.yaml`.  
5. Все модели данных имеют `required`, `enum`, примеры (Night City, Pacifica).  
6. Документированы зависимости на `city-life-population` и `player-orders` APIs.  
7. Kafka событие `world.visuals.location.detailed.updated` описано с payload.  
8. Описаны метрики и ссылки на telemetry.  
9. Файл проходит валидацию `scripts/validate-swagger.ps1`.  
10. Размер файла ≤400 строк или обоснованное вынесение компонентов с README.  
11. В Info/description даны ссылки на `.BRAIN/03-lore/_03-lore/visual-guides/visual-style-locations-детально.md`.  
12. QA/UX требования из документа учтены (workshop 2025-11-08).  
13. Snapshot flow (POST/GET) поддерживает экспирацию и статусы.

---

## ❓ FAQ

- **Вопрос:** Нужен ли отдельный endpoint для обновления визуального профиля?  
  **Ответ:** Нет, обновление выполняет внутренний пайплайн. Достаточно описать GET/POST и Kafka событие обновления.

- **Вопрос:** Где хранить enum значений `macroZone` и `dayPhase`?  
  **Ответ:** В БД world-service; в OpenAPI зафиксировать enum со значениями из `.BRAIN` (NightCity, NeoTokyo, LagosSkyport, AmazonCloudBasin, BadlandsStormBelt и т.д.).

- **Вопрос:** Как обрабатывать маркетинговые запросы?  
  **Ответ:** Через `visualSnapshot` с `channels` включающими `marketing`. Для готового пака указать ссылку на API-TASK-324 и Kafka `marketing.visuals.package.generated`.

- **Вопрос:** Можно ли объединить district и macro профили?  
  **Ответ:** Нет, держим отдельный endpoint, чтобы фронтенд atlas мог лениво подгружать районы и не перегружать главный список.

- **Вопрос:** Нужно ли логировать генерацию снапшота?  
  **Ответ:** Да, указать requirement на аудиторский трейс в world-service и ссылку на метрики (Telemetry pipeline).

---

## 📌 История выполнения

- 2025-11-08 — Задание создано AI агентом GPT-5 Codex на основе `.BRAIN/03-lore/_03-lore/visual-guides/visual-style-locations-детально.md`.


