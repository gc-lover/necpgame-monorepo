# Task ID: API-TASK-328
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** queued  
**Создано:** 2025-11-08 17:30  
**Создатель:** AI Task Creator Agent  
**Зависимости:** none

---

## 📋 Краткое описание

Создать спецификацию `Character Visual Archetypes API`, описывающую базовые визуальные профили архетипов игроков и ключевых NPC, их палитры, аксессуары и связанные ассеты.  
**Целевой файл:** `api/v1/character/visuals/archetypes.yaml`

---

## 🎯 Цель задания

Предоставить character-service контракт-first API, который:
- возвращает каталог визуальных архетипов с основными атрибутами, силой силуэта и принадлежностью к фракциям;
- позволяет получать детализированные карточки архетипа с разбиением по ролям, материалам, акцентам и косметике;
- поддерживает фильтрацию и поиск по фракциям, ролям, типам освещения, состояниям (стандарт, боевой, романтический);
- предоставляет экспорт базового набора ассетов и палитр для UI, маркетинга и внутриигровой энциклопедии.

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`  
**Путь:** `.BRAIN/03-lore/_03-lore/visual-guides/visual-style-assets.md`  
**Версия документа:** 1.0.0  
**Дата последнего обновления:** 2025-11-08 11:12  
**Статус документа:** approved (api-readiness: ready)

**Что важно из документа:**
- Описание архетипов игроков и NPC (Corpo Operative, Street Ronin, Nomad Mechanist, Synth Mystic и др.) с силуетами, материалами, акцентами.
- Фракционные палитры (Arasaka, Militech, Maelstrom, Valentinos, Voodoo Boys и т.д.) и правила использования логотипов.
- Списки аксессуаров, имплантов, романтических состояний и метрик визуальной вовлеченности.
- Kafka события `character.visuals.archetype.updated`, метрики `ArchetypeAdoptionRate`, UX/QA подтверждения.

### Дополнительные источники

- `.BRAIN/03-lore/characters/characters-overview.md` — иерархия NPC/архетипов.  
- `.BRAIN/02-gameplay/combat/combat-implants-types.md` — импланты и боевые моды.  
- `.BRAIN/05-technical/content-generation/city-life-population-algorithm.md` — синхронизация с генерацией NPC.  
- `API-SWAGGER/api/v1/world/visuals/locations-detailed.yaml` — ссылки на локационные визуалы.

### Связанные документы

- `.BRAIN/03-lore/_03-lore/visual-guides/visual-style-locations-детально.md` — контекст локаций.  
- `.BRAIN/04-narrative/dialogues/quest-main-001-first-steps.md` — романтические состояния NPC.  
- `.BRAIN/02-gameplay/social/player-orders-creation-детально.md` — влияния на социальные механики.

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/character/visuals/archetypes.yaml`  
**API версия:** v1  
**Тип файла:** OpenAPI 3.0.3 Specification (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── character/
            └── visuals/
                ├── README.md
                ├── components/
                │   ├── schemas/
                │   ├── responses/
                │   └── examples/
                └── archetypes.yaml  ← создать/обновить
```

Компоненты при превышении 400 строк вынести в `components/schemas/` и `components/examples/`.

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервис):
- **Микросервис:** character-service
- **Порт:** 8091
- **API пути:** `/api/v1/character/visuals/*`
- **Интеграции:** gameplay-service (боевые моды), economy-service (marketplace витрины), narrative-service (романтические сцены), marketing-service (ассет-пакеты)
- **Kafka:** `character.visuals.archetype.updated`, `character.visuals.romance.highlight`

### OpenAPI (обязательно)

- Заполни `info.x-microservice` (name, port, domain, base-path, package) по данным целевого микросервиса.
- В секции `servers` оставь Production gateway `https://api.necp.game/v1` и пример локальной разработки `http://localhost:8080/api/v1`.
- WebSocket маршруты публикуй только через `wss://api.necp.game/v1/...`.

### Frontend (модуль):
- **Модуль:** modules/characters/encyclopedia
- **State Store:** `useCharacterStore(visuals)`
- **UI компоненты:** `CharacterProfileCard`, `ArchetypePaletteStrip`, `EquipmentSlotGrid`, `MetricChip`, `FactionBadge`
- **Формы:** `ArchetypeFilterForm`, `PaletteExportForm`
- **Layouts:** `CharacterEncyclopediaLayout`, `GameLayout`
- **Хуки:** `useCharacterFilters`, `usePalettePreview`, `useRealtime`

### Комментарий в YAML:
```
# Target Architecture:
# - Microservice: character-service (port 8091)
# - Frontend Module: modules/characters/encyclopedia
# - State Store: useCharacterStore(visuals)
# - UI: CharacterProfileCard, ArchetypePaletteStrip, EquipmentSlotGrid, MetricChip, FactionBadge
# - Forms: ArchetypeFilterForm, PaletteExportForm
# - Layouts: CharacterEncyclopediaLayout, GameLayout
# - Hooks: useCharacterFilters, usePalettePreview, useRealtime
# - Events: character.visuals.archetype.updated, character.visuals.romance.highlight
# - API Base: /api/v1/character/visuals/*
```

---

## ✅ Что нужно сделать (детальный план)

1. **Собрать атрибуты архетипов:** выделить обязательные поля (силуэт, материалы, акценты, фракция, романтические состояния, аксессуары).  
   _Результат:_ таблица полей для `VisualArchetypeProfile`.
2. **Спроектировать схемы:** описать модели `VisualArchetypeProfile`, `VisualState`, `FactionBranding`, `AccessorySet`, `PaletteSwatch`, `RomanceState`.  
   _Результат:_ схемы с описаниями, enum-ами, примерами.
3. **Определить endpoints:** сформировать список, детальный просмотр, фильтры, экспорт палитр.  
   _Результат:_ секция `paths` минимум с четырьмя endpoint-ами.
4. **Документировать Kafka и метрики:** указать payload событий и метрики вовлечённости.  
   _Результат:_ `x-events`, `x-metrics` или отдельные разделы.
5. **Подключить общие компоненты:** ошибки, безопасность, пагинация через `shared/common/*.yaml`.  
   _Результат:_ единообразные ответы, security схемы.
6. **Добавить примеры:** Corop Operative, Street Ronin, Nomad Mechanist, Synth Mystic.  
   _Результат:_ `components/examples` и request/response samples.
7. **Проверить спецификацию:** запустить `scripts/validate-swagger.ps1`, убедиться в ≤400 строк и корректных ссылках на компоненты.  
   _Результат:_ валидная OpenAPI 3.0.3 спецификация.

---

## 🔌 Эндпоинты

1. **GET `/character/visuals/archetypes`**  
   - Возвращает страницу архетипов.  
   - Параметры: `role`, `faction`, `visualTier`, `romanceState`, `page`, `pageSize`.  
   - Ответы: `200 OK` (`PaginatedVisualArchetypeProfile`), `400` (`VAL_INVALID_FILTER`), `401/403`, `500`.

2. **GET `/character/visuals/archetypes/{archetypeId}`**  
   - Детальное описание архетипа (визуальные состояния, аксессуары, фракционные акценты).  
   - Ответы: `200 OK` (`VisualArchetypeProfile`), `404` (`BIZ_ARCHETYPE_NOT_FOUND`), `409` (`BIZ_ARCHETYPE_LOCKED`), `500`.

3. **GET `/character/visuals/archetypes/{archetypeId}/palette`**  
   - Возвращает набор палитр, материалов и световых схем.  
   - Параметры: `lighting` (enum: neutral, neon, combat, romantic).  
   - Ответы: `200 OK` (`PaletteSwatchSet`), `400`, `404`, `500`.

4. **POST `/character/visuals/archetypes/export`**  
   - Экспорт базового набора ассетов и описаний.  
   - Тело: `VisualArchetypeExportRequest` (archetypeIds[], channels[], includeAccessories, format).  
   - Ответы: `202 Accepted` (`VisualArchetypeExportResponse`), `400`, `409`, `503`.

---

## 🧱 Модели данных

- **VisualArchetypeProfile** — базовые поля (id, name, role, faction, silhouette, materials[], accents[], accessories[], romanceStates[], metrics).  
- **VisualState** — контекст (stateType, description, emissiveLevel, animationCue).  
- **AccessorySet** — предметы и импланты, ссылки на equipment каталоги.  
- **FactionBranding** — палитра, логотипы, брендовые элементы.  
- **PaletteSwatch** — цветовые наборы (primary/secondary/accent), материал, emissive settings.  
- **VisualArchetypeExportRequest / Response** — параметры экспорта (channels: ui, marketing, analytics, narrative).  
- **PaginatedVisualArchetypeProfile** — `items[]`, пагинация из `shared/common/pagination.yaml`.

Каждую схему снабдить `description`, валидацией (`enum`, `maxItems`, `pattern`), ссылками на связанные API.

---

## 📏 Принципы и правила

- OpenAPI 3.0.3, ≤400 строк; компоненты вынести в подпапку.  
- Использовать `shared/common/security.yaml` (bearerAuth, serviceToken) и `shared/common/responses.yaml`.  
- Ошибки с `x-error-code` (`VAL_*`, `BIZ_*`, `INT_*`).  
- В `info.description` указать источники `.BRAIN` и дату.  
- Соблюдать SOLID/DRY/KISS, не дублировать модели, ссылаться на shared компоненты.  
- Добавить `x-sources` и `x-related-apis`.

---

## ✅ Критерии приемки

1. Файл `api/v1/character/visuals/archetypes.yaml` существует, валиден (`scripts/validate-swagger.ps1`).  
2. В начале файла присутствует блок `Target Architecture`.  
3. Список архетипов поддерживает фильтры и пагинацию через общие компоненты.  
4. Описаны модели `VisualArchetypeProfile`, `VisualState`, `AccessorySet`, `PaletteSwatch`, `PaginatedVisualArchetypeProfile`.  
5. Экспорт ассетов реализован через `POST /character/visuals/archetypes/export` с кодом `202`.  
6. Kafka события и метрики задокументированы.  
7. Ошибки подключены через `$ref` на shared responses, указаны `x-error-code`.  
8. Добавлены примеры для Corpo Operative, Street Ronin, Nomad Mechanist, Synth Mystic.  
9. Секции `security` и `tags` оформлены, указаны зависимости от других API.  
10. README в `character/visuals` обновлен ссылкой на новый файл (в рамках реализации).

---

## ❓ FAQ

**Q:** Как связать архетипы с игровыми ролями и классами?  
A: Используйте `role` и `gameplayClassId`, а также ссылки на `api/v1/gameplay/progression/classes.yaml` (при наличии).

**Q:** Нужно ли включать детальные состояния (battle/romance)?  
A: Да, но на уровне базовых параметров; детальные эффекты и анимации вынесены в `API-TASK-325`.

**Q:** Как синхронизировать палитры с маркетинговыми пакетами?  
A: Через экспорт (`channels` включает `marketing`), а также `marketing-service` слушает событие `character.visuals.archetype.updated`.

**Q:** Требуется ли отдельный endpoint для метрик?  
A: Нет, достаточно включить метрики в профиль и описать их в `x-metrics`; аналитика приходит через telemetry.

---

**Следующие действия исполнителя:** подготовить файловую структуру, реализовать спецификацию, добавить README-ссылку и прогнать валидацию.

