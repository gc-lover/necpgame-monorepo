# Task ID: API-TASK-361
**Тип:** API Generation
**Приоритет:** высокий
**Статус:** queued
**Создано:** 2025-11-08 17:20
**Создатель:** GPT-5 Codex (API Task Creator)
**Зависимости:** API-TASK-322, API-TASK-323, API-TASK-324, API-TASK-337

---

## 📋 Краткое описание

Подготовить OpenAPI-спецификацию `World Visual Location Profiles`, описывающую REST API для выдачи визуальных профилей городов, районов, хабов и рейдовых зон.

**Что нужно сделать:** Создать `api/v1/world/visuals/locations.yaml` с компонентами и примерами, обеспечив доступ к палитрам, освещению, mood-тегам и asset ID из документа `.BRAIN/03-lore/visual-guides/visual-style-locations.md`.

---

## 🎯 Цель задания

Дать world-service единый контракт для выдачи визуальных профилей и ассет-метаданных, которыми пользуются social-service, economy-service и фронтенд.

**Зачем это нужно:**
- Синхронизировать арт-гайды с данными world-service и сократить ручную синхронизацию.
- Позволить UI модулям `modules/world/visual-guides` и `modules/social/hubs` получать готовые палитры и ключевые элементы по ID.
- Привязать asset ID к фронтенд-компонентам и JSON выгрузкам без чтения `.BRAIN`.

---

## 📚 Источники информации

### Основной документ

**Репозиторий:** `.BRAIN`
**Путь к документу:** `.BRAIN/03-lore/visual-guides/visual-style-locations.md`
**Версия документа:** v1.0.0 (2025-11-08 09:41)
**Статус документа:** approved, api-readiness: ready

**Что важно из этого документа:**
- Перечень макро- и микро-локаций: города, районы Night City, социальные хабы, корпоративные комплексы, рейдовые зоны, подземные пространства, природные области.
- Ассет-реестр с assetId, Figma/3D источниками, пакетами контента и ссылками на UI компоненты.
- Требования к палитрам (primary/secondary/accent), типам освещения, moodTags, ключевым элементам и связям с world/social/economy сервисами.
- JSON схема `VisualProfile` и пример REST payload с `keyElements`.

### Дополнительные источники

- `.BRAIN/03-lore/visual-guides/visual-style-assets.md` — визуальные профили персонажей и предметов (для перекрёстных ссылок `assetId`).
- `.BRAIN/03-lore/visual-guides/visual-style-locations-детально.md` — расширенные поля (ambient audio, npcCostumes) для планируемых версий 1.1+.
- `.BRAIN/05-technical/content-generation/city-life-population-algorithm.md` — требования к `visualProfileId` в распределении NPC.
- `API-SWAGGER/api/v1/world/visuals/locations-detailed.yaml` (будущий файл из API-TASK-322) — обеспечить совместимость структур.

### Связанные задания

- `API-SWAGGER/tasks/active/queue/task-322-world-visuals-locations-detailed-api.md`
- `API-SWAGGER/tasks/active/queue/task-323-visuals-hubs-detailed-api.md`
- `API-SWAGGER/tasks/active/queue/task-324-world-visuals-events-api.md`
- `API-SWAGGER/tasks/active/queue/task-337-visuals-analytics-metrics-api.md`

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/world/visuals/locations.yaml`
> ⚠️ Максимум 400 строк. Вынести общие схемы в `api/v1/world/visuals/components/visual-location-schemas.yaml`, если объём превысит лимит.
**API версия:** v1 (semantic version 1.0.0)
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── world/
            └── visuals/
                ├── locations.yaml
                └── components/
                    └── visual-location-schemas.yaml (при необходимости)
```

**Если `locations.yaml` уже существует:**
- Тщательно объединить контент, сохранить версии и ссылки на общие компоненты.
- Не превышать лимит строк, использовать `$ref` на `shared/common/pagination.yaml` и `shared/common/responses.yaml`.

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервис)

- **Микросервис:** world-service
- **Порт:** 8086
- **API Base Path:** `/api/v1/world/visuals/*`
- **Домен:** визуальные профили локаций, синхронизация ассетов, выдача палитр.
- **Зависимости:**
  - social-service (для `hubVisualMood` и синхронизации рейтингов)
  - economy-service (привязка `stallVisual` для торговцев)
  - gameplay-service (рейдовые зоны, dungeon карты)
  - analytics-service (метрики использования ассетов)
  - auth-service (валидация ролей art-admin/dev)

**Event Streams:** `world.visuals.updated`, `world.visuals.asset-mapping`

### Frontend (модуль)

- **Модуль:** `modules/world/visual-guides`
- **State Store:** `useWorldStore`
- **State:** `visualProfiles`, `cityFilters`, `raidHighlights`
- **UI компоненты (@shared/ui):** LocationCard, DistrictPreview, RaidZoneCard, MoodTagList, PaletteSwatch
- **Формы (@shared/forms):** VisualFilterForm, VisualProfileAdminForm
- **Hooks (@shared/hooks):** useDebounce, useQueryState, usePalettePreview
- **Layouts (@shared/layouts):** GameLayout, WorldAtlasLayout

**Комментарий в OpenAPI:** В начале файла зафиксировать блок архитектуры (см. шаблон) с указанием микросервиса, модуля, компонентов UI и стейта.

### OpenAPI

- В `info.x-microservice` указать: `name: world-service`, `port: 8086`, `domain: world`, `base-path: /api/v1/world/visuals`, `package: com.necpgame.worldservice`.
- В `servers` использовать только `https://api.necp.game/v1` и `http://localhost:8080/api/v1` (описания: Production API Gateway, Local API Gateway).
- Подключить стандартные схемы ошибок через `$ref: '../../shared/common/responses.yaml#/responses/ErrorResponse'`.

---

## ✅ Что нужно сделать (детальный план)

### Шаг 1: Анализ локаций и ассет-реестра

**Действия:**
1. Выписать категории из `.BRAIN/03-lore/visual-guides/visual-style-locations.md` (макро, районы, хабы, корпоративные комплексы, рейды, подземка, природа, инфраструктура, сервисы).
2. Сопоставить каждой категории минимальный набор полей: `assetId`, `visualType`, `city`, `faction`, `palette`, `lighting`, `moodTags`, `keyElements`, `uiComponents`, `contentPackages`.
3. Выделить общие поля для модели `VisualLocationProfile` и различия для специализированных профилей (например, `raidMechanics`, `hubServices`).

**Ожидаемый результат:** черновик моделей данных и перечисления справочников (`visualType`, `lighting`, `moodTags`).

### Шаг 2: Спроектировать endpoints

**Обязательные REST endpoints:**
1. `GET /world/visuals/locations` — получить список визуальных профилей с фильтрами `city`, `visualType`, `faction`, `lighting`, `moodTag`, пагинация (использовать `shared/common/pagination.yaml`).
2. `GET /world/visuals/locations/{visualId}` — детальная карточка профиля c `keyElements`, `ambientAudio`, `relatedAssets`, `uiComponents`.
3. `GET /world/visuals/locations/{visualId}/assets` — вернуть связанные ассеты (art package, figma link, ui component ids) для пайплайна арт-команды.
4. `GET /world/visuals/locations/lookup` — справочники (enum справка по lighting, palette presets, visualType). Допускается query `include=lighting,palettePresets`.
5. `POST /world/visuals/locations:bulk-sync` — импорт обновлённых профилей art-admin (валидировать payload по схеме, ограничить роль `art-admin`).

**Дополнительно:** предусмотреть хедеры `X-Tenant`, `X-Trace-Id`, `X-Request-Source`.

**Ожидаемый результат:** секция `paths` с описанием параметров, запросов/ответов и кодов ошибок (200, 400, 401, 403, 404, 409, 422, 500 через `shared/common/responses.yaml`).

### Шаг 3: Модели данных и компоненты

**Обязательные схемы:**
- `VisualLocationProfile` — основной объект (перечислить обязательные поля, `required`).
- `VisualPalette` — три поля `primary`, `secondary`, `accent` (regex для HEX).
- `VisualMood` — `moodTags`, `ambientAudioId`, `crowdDensity`, `weatherProfile`.
- `VisualAssetReference` — `assetId`, `figmaRef`, `contentPackage`, `uiComponentId`.
- `VisualLocationSummary` — `visualId`, `title`, `visualType`, `city`, `faction`, `lighting`.
- `VisualLocationSyncRequest` — массив `VisualLocationProfileInput` (минимум 1, максимум 100), валидация assetId (регулярка из документа).

**Перечисления:**
- `visualType` (`megapolis`, `district`, `hub`, `corporate`, `raid`, `underground`, `wildzone`, `infrastructure`, `service`).
- `lighting` (`neon`, `bioluminescent`, `industrial`, `natural`, `mixed`).
- `factionTag` (Arasaka, Militech, Valentinos, Nomads, Zetatech, Biotechnica, независимые и т.д. — минимум 10 значений).

**Ожидаемый результат:** раздел `components.schemas` (или вынесенный файл) с валидацией, примерами (использовать `example` и `x-codeSamples`).

### Шаг 4: Безопасность и политики доступа

**Действия:**
1. Подключить `security` из `api/v1/shared/common/security.yaml` (схема `bearerAuth`).
2. Прописать роли: чтение — `player`, `game-master`, `content-designer`; bulk-sync — только `art-admin`, `world-admin`.
3. Добавить заголовки аудита (`X-Request-Source`, `X-Trace-Id`) в `header` компонентов.

**Ожидаемый результат:** секция `security`, описания ролей в `description` эндпоинтов, ответы `403` и `401` через общие компоненты.

### Шаг 5: Примеры и интеграции

**Действия:**
1. Добавить `examples` для каждого эндпоинта (успешный ответ, ошибка валидации, конфликт при `bulk-sync`).
2. Добавить `x-frontend` расширение с ссылками на `modules/world/visual-guides` и конкретные компоненты.
3. Задокументировать Kafka события `world.visuals.updated` (payload `VisualLocationEvent`) в разделе `components.messages` или описать ссылку на событие в `bulk-sync` ответе.

**Ожидаемый результат:** полноценные примеры JSON, связь с фронтендом, инструкция по событиям.

### Шаг 6: Валидация и QA

**Действия:**
1. Проверить файл через `scripts/validate-swagger.ps1`.
2. Убедиться в совместимости с детализированными спецификациями (API-TASK-322/323) и отсутствия дублирования схем.
3. Пройти чеклист из `tasks/config/checklist.md` перед передачей задания.

**Ожидаемый результат:** валидный OpenAPI, подтверждение по чеклисту, зафиксированные зависимости.

---

## 📏 Критерии приемки (минимум 10)

1. `api/v1/world/visuals/locations.yaml` создан и валиден по OpenAPI 3.0.3.
2. В `info.x-microservice` указаны `world-service`, порт 8086, домен `world`, base-path `/api/v1/world/visuals`, пакет `com.necpgame.worldservice`.
3. Секция `servers` содержит только gateway URL (prod и localhost) без прямых портов.
4. Все endpoints используют стандартные ответы (`shared/common/responses.yaml`).
5. `GET /world/visuals/locations` реализует пагинацию через `shared/common/pagination.yaml`.
6. Модель `VisualLocationProfile` валидирует `assetId` по регулярному выражению из `.BRAIN`.
7. `bulk-sync` ограничен 100 элементами за вызов и описывает конфликт версий (409) при дублировании assetId.
8. Каждая схема снабжена примером (`example` или `x-codeSamples`).
9. В описаниях эндпоинтов перечислены роли доступа и требуемые заголовки аудита.
10. Документация содержит ссылку на Kafka события `world.visuals.updated` и `world.visuals.asset-mapping`.
11. Добавлено расширение `x-frontend` со списком модулей/компонентов для UI потребления.
12. Проверка через `scripts/validate-swagger.ps1` проходит без ошибок.

---

## ❓ FAQ

**В: Нужно ли описывать WebSocket?**  
О: Нет, этот документ покрывает только REST. WebSocket каналы фиксируются в задании API-TASK-324.

**В: Где хранить расширенные поля (ambient audio, npcCostumes)?**  
О: В этой версии перечислить их в `VisualLocationProfile` с `nullable: true`, но детализированные схемы вынесут API-TASK-322.

**В: Как синхронизировать с экономикой и социальными сервисами?**  
О: Использовать `relatedServices` массив в `VisualLocationProfile`, перечисляя `world`, `social`, `economy` и ID интеграций.

**В: Можно ли поддержать локализацию названий?**  
О: Да, добавить `localizedNames` как объект с ключами `en`, `ru`, `ja` (минимум два языка).

**В: Что с версионностью?**  
О: Первичная версия `1.0.0`. Следующие расширения (ambient audio, romance hooks) вести через минорные версии и ссылаться на задания API-TASK-322/337.
