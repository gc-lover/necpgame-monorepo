# Task ID: API-TASK-362
**Тип:** API Generation
**Приоритет:** высокий
**Статус:** queued
**Создано:** 2025-11-08 17:45
**Создатель:** GPT-5 Codex (API Task Creator)
**Зависимости:** API-TASK-325, API-TASK-326, API-TASK-327, API-TASK-328, API-TASK-329, API-TASK-330, API-TASK-333, API-TASK-337

---

## 📋 Краткое описание

Разработать OpenAPI-спецификацию `World Visual Assets Catalog`, описывающую REST API для доступа к визуальным профилям персонажей, оружия, техники и предметов.

**Что нужно сделать:** Создать `api/v1/world/visuals/assets.yaml`, отразив структуру ассет-реестра, фильтры по категориям и bulk-синхронизацию с пайплайном `content-pipeline/assets-registry.csv` из `.BRAIN/03-lore/visual-guides/visual-style-assets.md`.

---

## 🎯 Цель задания

Сформировать единый источник данных в world-service для визуальных ассетов, чтобы character-service, gameplay-service, economy-service и фронтенд могли получать валидированные профили без обращения к `.BRAIN`.

**Зачем это нужно:**
- Упростить синхронизацию между арт-командой, gameplay и UI через стандартизированный API.
- Связать asset ID с JSON схемами и DTO, обеспечивая консистентность в `world`, `combat`, `social`, `economy` сервисах.
- Автоматизировать обновление ассет-каталога и трансляцию событий `world.visual.assets.updated`.

---

## 📚 Источники информации

### Основной документ

**Репозиторий:** `.BRAIN`
**Путь к документу:** `.BRAIN/03-lore/visual-guides/visual-style-assets.md`
**Версия документа:** v1.0.0 (2025-11-08 09:42)
**Статус документа:** approved, api-readiness: ready

**Что важно из этого документа:**
- Архетипы персонажей, классы оружия, экипировка, транспорт, импланты и артефакты с визуальными описаниями.
- Asset registry: assetId, art-источники, игровые пакеты и ссылки на UI/DTO.
- JSON схемы `CharacterVisualProfile`, `WeaponVisualProfile`, `DroneVisualProfile` и требования к палитрам, силуэтам, эффектам.
- REST/Kafka интеграции для world/social/combat сервисов и экспортных скриптов.

### Дополнительные источники

- `.BRAIN/03-lore/visual-guides/visual-style-assets-детально.md` — расширенные поля для будущих версий (ambientAudio, animationSets).
- `.BRAIN/03-lore/visual-guides/visual-style-locations.md` — связь ассетов с локациями.
- `.BRAIN/02-gameplay/combat/combat-weapon-classes-detailed.md` — валидация классов оружия.
- `.BRAIN/05-technical/content-generation/city-life-population-algorithm.md` — требования к `visualProfileId` для NPC.

### Связанные задания

- `API-SWAGGER/tasks/active/queue/task-325-character-visuals-archetypes-detailed-api.md`
- `API-SWAGGER/tasks/active/queue/task-326-gameplay-visuals-equipment-detailed-api.md`
- `API-SWAGGER/tasks/active/queue/task-327-character-visuals-romance-states-api.md`
- `API-SWAGGER/tasks/active/queue/task-337-visuals-analytics-metrics-api.md`

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/world/visuals/assets.yaml`
> ⚠️ Ограничить файл ≤400 строк. При необходимости вынести схемы в `api/v1/world/visuals/components/visual-assets-schemas.yaml`.
**API версия:** v1 (semantic version 1.0.0)
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── world/
            └── visuals/
                ├── assets.yaml
                └── components/
                    └── visual-assets-schemas.yaml (опционально)
```

**Если файл уже существует:**
- Объединить данные осторожно, сохранив ссылки на общие компоненты.
- Не дублировать модели из `visuals/*-detailed.yaml`, использовать `$ref` при совпадении.

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервис)

- **Микросервис:** world-service
- **Порт:** 8086
- **API Base Path:** `/api/v1/world/visuals/*`
- **Домен:** визуальные ассеты персонажей, оружия, техники, предметов.
- **Зависимости:**
  - character-service (синхронизация NPC appearance)
  - gameplay-service (оружие и импланты)
  - social-service (романтические персонажи, хабы)
  - economy-service (товары, торговые витрины)
  - analytics-service (телеметрия ассетов)
  - auth-service (роль art-admin)

**Event Streams:** `world.visual.assets.updated`, `world.visual.assets.bulk-sync`

### Frontend (модули)

- **Основной модуль:** `modules/world/visual-guides`
- **Дополнительные:** `modules/combat/armory`, `modules/social/romance`, `modules/economy/market`
- **State Stores:** `useWorldStore` (`visualAssets`), `useCombatStore` (`armoryAssets`)
- **UI компоненты (@shared/ui):** CharacterCard, WeaponCard, DroneCard, ItemTile, PaletteSwatch
- **Формы (@shared/forms):** VisualAssetFilterForm, VisualAssetBulkUploadForm
- **Hooks (@shared/hooks):** useQueryState, usePalettePreview, useDebounce
- **Layouts:** GameLayout, ArmoryLayout

**Комментарий:** Добавить в начало файла блок архитектуры (как в шаблоне) с перечислением микросервиса, модулей и компонентов UI.

### OpenAPI

- Заполнить `info.x-microservice`: `name: world-service`, `port: 8086`, `domain: world`, `base-path: /api/v1/world/visuals`, `package: com.necpgame.worldservice`.
- Секция `servers`: `https://api.necp.game/v1` (Production API Gateway) и `http://localhost:8080/api/v1` (Local API Gateway).
- Подключить `shared/common/security.yaml` и `shared/common/responses.yaml`, `shared/common/pagination.yaml`.

---

## ✅ Что нужно сделать (детальный план)

### Шаг 1: Категоризация ассетов

**Действия:**
1. Сгруппировать ассеты по категориям: `character`, `npc`, `weapon`, `gear`, `drone`, `vehicle`, `implant`, `artifact`, `environment`.
2. Определить поля для базовой модели (`assetId`, `visualType`, `faction`, `palette`, `materials`, `effects`, `uiComponentId`, `contentPackage`).
3. Выделить специализированные подмодели (например, `WeaponVisualProfile`, `CharacterVisualProfile`, `DroneVisualProfile`).

**Ожидаемый результат:** схема наследования (`oneOf`) и контрольные перечисления (`visualCategory`, `weaponClass`, `factionTag`).

### Шаг 2: Проектирование endpoints

**Обязательные REST endpoints:**
1. `GET /world/visuals/assets` — список ассетов с фильтрами `category`, `faction`, `visualTag`, `weaponClass`, `rarity`, `supportsRomance`, пагинация.
2. `GET /world/visuals/assets/{assetId}` — детальный профиль с расширенными полями (`palette`, `silhouette`, `materials`, `effects`, `uiComponents`, `animationSets`).
3. `GET /world/visuals/assets/categories/{category}` — агрегированная выдача по категории с срезами (например, все `weapon` ассеты, сгруппированные по производителю).
4. `GET /world/visuals/assets/lookup` — справочники (перечень категорий, доступные `weaponClass`, `silhouette`, `factionTag`).
5. `POST /world/visuals/assets:bulk-sync` — приём пакета обновлений от art-admin (до 200 записей, оптимистичная блокировка по `version`).

**Дополнительно:** При `bulk-sync` возвращать массив обновлённых `assetId`, события Kafka и предупреждения о несогласованных компонентных путях.

### Шаг 3: Схемы и компоненты

**Действия:**
1. Описать `VisualAssetSummary`, `VisualAssetProfile`, `CharacterVisualProfile`, `WeaponVisualProfile`, `DroneVisualProfile`, `GearVisualProfile`, `VisualAssetSyncRequest`.
2. В `VisualAssetProfile` использовать `oneOf` для специальных схем (character, weapon, drone и т. д.).
3. Валидация assetId через регулярные выражения (например, `^ASSET-CHAR-[A-Z]{3,}-\d{3}$`, `^ASSET-WEAPON-[A-Z]{2,4}-[A-Z]{3}-\d{3}$`).
4. Добавить `x-codeSamples` для запросов (curl/TypeScript fetch) и JSON `example`.

**Ожидаемый результат:** полные схемы с required-полями, default-значениями и связями.

### Шаг 4: Безопасность и аудиторские требования

**Действия:**
1. Наследовать `security` из `shared/common/security.yaml`.
2. Указать роли: просмотр (`player`, `designer`, `gm`), bulk-sync (`art-admin`, `world-admin`).
3. Обязательно документировать заголовки `X-Trace-Id`, `X-Request-Source`, `X-Content-Version`.
4. Для `bulk-sync` вернуть `202 Accepted` с ссылкой на задачу и `202` описание в `responses`.

### Шаг 5: События и интеграции

**Действия:**
1. Добавить раздел `components.messages.visualAssetUpdated` и `components.messages.visualAssetBulkSynced` с payload ссылками на схемы.
2. Описать, что при успехе `bulk-sync` публикуется Kafka топик `world.visual.assets.updated`.
3. Добавить `x-frontend` расширение с указанием модулей UI и DTO путей.

### Шаг 6: Валидация и чеклист

**Действия:**
1. Прогнать `scripts/validate-swagger.ps1`.
2. Пройти чеклист `tasks/config/checklist.md` (в частности, разделы 4, 7, 9, 12).
3. Убедиться, что файл <400 строк и все схемы вынесены при необходимости.

---

## 📏 Критерии приемки (12 пунктов)

1. Создан `api/v1/world/visuals/assets.yaml` и проходит `scripts/validate-swagger.ps1` без ошибок.
2. Заполнен `info.x-microservice` с данными world-service (порт 8086, base-path `/api/v1/world/visuals`).
3. `servers` содержит только gateway URL (`https://api.necp.game/v1`, `http://localhost:8080/api/v1`).
4. `GET /world/visuals/assets` поддерживает пагинацию и фильтры, подключает `shared/common/pagination.yaml`.
5. Модель `VisualAssetProfile` включает обязательные поля `assetId`, `category`, `palette`, `faction`, `uiComponents`.
6. Регулярные выражения для assetId соответствуют шаблонам из `.BRAIN` (CHAR, NPC, WEAPON, DRONE, ITEM, VEHICLE и т. д.).
7. `bulk-sync` ограничивает запрос 200 объектами и описывает ошибки `409 Conflict` и `422 Unprocessable Entity` через общие ответы.
8. Каждая категория ассетов имеет пример ответа (`example`) и `x-codeSamples` (curl/TypeScript).
9. В описании эндпоинтов указаны роли доступа и требуемые заголовки аудита.
10. Документирован Kafka поток `world.visual.assets.updated` с payload ссылки на схемы.
11. Добавлено `x-frontend` с перечислением модулей и компонентов.
12. Подготовлена таблица соответствия DTO (`world/npcs/*.json`, `combat/weapons/*.json`) в разделе описания и ссылок.

---

## ❓ FAQ

**В: Нужно ли дублировать детализированные схемы из API-TASK-325/326?**  
О: Нет, использовать `oneOf` и ссылаться на отдельные файлы компонентов, избегая дублирования.

**В: Как обрабатывать архивированные ассеты?**  
О: Добавить поле `status` (`active`, `deprecated`, `archived`) с дефолтным значением `active` и фильтром в `GET /world/visuals/assets`.

**В: Допускается ли локализация описаний?**  
О: Да, добавить объект `localizedDescriptions` (ключи `en`, `ru`, `ja`, минимум один язык).

**В: Как синхронизировать с frontend DTO?**  
О: Через `x-frontend.dtoPaths` и примеры `scripts/export-visual-assets.ps1`, чтобы фронтенд ссылка была явной.

**В: Какие заголовки обязательны в bulk-sync?**  
О: `X-Trace-Id`, `X-Request-Source`, `If-Match` (для версионности). Без них запрос отклоняется с `400`.
