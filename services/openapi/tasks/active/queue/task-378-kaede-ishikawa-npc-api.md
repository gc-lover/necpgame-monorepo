# Task ID: API-TASK-378
**Тип:** API Generation
**Приоритет:** высокий
**Статус:** queued
**Создано:** 2025-11-08 21:25
**Создатель:** GPT-5 Codex (API Task Creator)
**Зависимости:** API-TASK-376, API-TASK-375, API-TASK-377

---

## 📋 Краткое описание

Нужно сформировать OpenAPI спецификацию для управления NPC Каэдэ Исикавой (двойной агент Helios/Specter) на основе `.BRAIN/04-narrative/npc-lore/important/npc-kaede-ishikawa.md`.

**Что нужно сделать:** Создать `api/v1/narrative/npcs/kaede-ishikawa.yaml` (и, при необходимости, вынести крупные схемы в `api/v1/narrative/components/npc-kaede-schemas.yaml`) для narrative-service, описав получение профиля NPC, обновление состояния лояльности, триггеры миссий и телеметрию.

---

## 🎯 Цель задания

Обеспечить narrative-service контрактом, который позволяет игровым клиентам и административным инструментам работать с NPC Каэдэ: читать биографию, актуальное состояние лояльности, активировать связанные задания и события, синхронизировать world/social/economy эффекты.

**Зачем это нужно:**
- Поддержать сюжетную линию после рейдов Specter Surge и Helios Countermesh Conspiracy.
- Управлять флагами лояльности (`specter`, `helios`, `balanced`), семейными угрозами и запросами NPC.
- Предоставить данные для UI (панели лояльности, интерактивные выборы, виджеты миссий).
- Собирать телеметрию взаимодействий и распределения лояльности для балансировки.

---

## 📚 Источники информации

### Основной документ

- **Репозиторий:** `.BRAIN`
- **Путь:** `.BRAIN/04-narrative/npc-lore/important/npc-kaede-ishikawa.md`
- **Версия:** 1.0.0
- **Дата обновления:** 2025-11-08 00:25
- **Статус:** approved, `api-readiness: ready`

**Что важно:**
- Биография и мотивации двойного агента.
- Триггеры флагов (`flag.kaede.loyalty`, `flag.kaede.family-threatened`, `flag.kaede.betrayal`).
- Narrative hooks в квестах (`Deep Cover`, `Split Allegiance`, City Unrest Crisis).
- Gameplay интеграции (скидки, шансы лута, ветки контрактов).
- API и telemetry наброски (`GET /api/v1/narrative/npc/kaede`, события `NPC_KAEDE_LoyaltyChanged`).

### Дополнительные источники

- `.BRAIN/04-narrative/quests/raid/2025-11-07-quest-helios-countermesh-conspiracy.md`
- `.BRAIN/04-narrative/quests/raid/2025-11-07-raid-specter-surge.md`
- `.BRAIN/04-narrative/cutscenes/2025-11-07-helios-conspiracy-cutscenes.md`
- `.BRAIN/04-narrative/dialogues/npc-kaede-ishikawa.md`
- `.BRAIN/02-gameplay/world/helios-countermesh-ops.md` (для контекста операций Helios)
- `.BRAIN/02-gameplay/world/specter-hq.md` (контекст Specter HQ)

### Связанные задания

- API-TASK-375 (Specter Surge Raid)
- API-TASK-376 (Helios Countermesh Conspiracy Quest)
- API-TASK-377 (Helios Countermesh Cutscenes)
- API-TASK-243 (Social Resonance API)
- API-TASK-241 (World Interaction Suite)

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

- **Целевой файл:** `api/v1/narrative/npcs/kaede-ishikawa.yaml`
- **Компоненты (опционально):** `api/v1/narrative/components/npc-kaede-schemas.yaml`
- **API версия:** v1 (semantic 1.0.0)
- **Тип:** OpenAPI 3.0.3

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── narrative/
            ├── npcs/
            │   └── kaede-ishikawa.yaml
            └── components/
                └── npc-kaede-schemas.yaml   # при необходимости вынести крупные схемы
```
> Ограничить основной файл ≤360 строк; при превышении вынести модели (`NpcProfile`, `NpcState`, `MissionHook`, `LoyaltyTelemetry`) в компонентный файл.

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервисная архитектура)

- **Микросервис:** narrative-service
- **Порт:** 8087
- **API Base Path:** `/api/v1/narrative/npcs/*`
- **Домен:** Нарративные персонажи и их состояние
- **Зависимости:**
  - world-service (City Unrest, Specter HQ upgrades)
  - social-service (репутация Specter/Helios, resonance)
  - economy-service (контракты, скидки, лут)
  - analytics-service (телеметрия лояльности и взаимодействий)

### Frontend (модульная архитектура)

- **Модуль:** `modules/narrative/npcs/kaede-ishikawa`
- **State Store:** `useNarrativeStore (npcState)` + `useWorldStore (cityUnrest)` + `useSocialStore (factionReputation)`
- **Состояние:** npcProfile, loyaltyState, missionHooks, pendingRequests, telemetry
- **UI компоненты (@shared/ui):** NpcProfileCard, LoyaltyMeter, MissionHookList, RequestBanner
- **Формы (@shared/forms):** LoyaltyDecisionForm, FamilyRescueForm, InformantContractForm
- **Layouts (@shared/layouts):** NarrativeNpcLayout
- **Hooks (@shared/hooks):** useDialogueState, useQuestState, useTelemetry

**Комментарий:** В начале OpenAPI файла добавить блок комментария с архитектурой (микросервис, модуль, UI компоненты, state).

### OpenAPI / Security

- `info.x-microservice`: `name: narrative-service`, `port: 8087`, `domain: narrative`, `base-path: /api/v1/narrative/npcs`, `package: com.necpgame.narrativeservice`.
- `servers`: `https://api.necp.game/v1`, `http://localhost:8080/api/v1`.
- Подключить `shared/common/security.yaml`, `shared/common/responses.yaml`, `shared/common/pagination.yaml`.
- `security`: `BearerAuth` с ролями `npc-view`, `npc-manage`, `npc-analytics`.
- `x-events`: `narrative.npc.kaede.loyaltyChanged`, `narrative.npc.kaede.request`, `narrative.npc.kaede.familyThreat`.

---

## 📡 Endpoints

- **GET `/narrative/npcs/kaede-ishikawa`** — базовый профиль NPC, текущая лояльность, активные миссии (`NpcProfileResponse`).
- **GET `/narrative/npcs/kaede-ishikawa/state`** — развернутое состояние флагов (`NpcState`), семейные запросы, pending actions.
- **POST `/narrative/npcs/kaede-ishikawa/state`** — обновление флагов лояльности (specter/helios/balanced), запросы помощи, последствия (`NpcStateUpdateRequest/Response`).
- **POST `/narrative/npcs/kaede-ishikawa/missions`** — активация/завершение связанных миссий (Intel Board, Evacuate Family, Neutral Contracts).
- **POST `/narrative/npcs/kaede-ishikawa/events`** — публикация событий (`NPC_KAEDE_*`), триггеров world/social/economy.
- **GET `/narrative/npcs/kaede-ishikawa/history`** — история решений/взаимодействий игроков (пагинация, фильтры).
- **GET `/narrative/npcs/kaede-ishikawa/telemetry`** — распределение лояльности, участие в миссиях, уровень city unrest влияния.
- **POST `/narrative/npcs/kaede-ishikawa/dialogue-state`** — обновление состояний диалогов Kaori/Kaede/Player (для UI).
- **POST `/narrative/npcs/kaede-ishikawa/reset`** — административный сброс состояния NPC (для тестирования).

Каждый endpoint должен описывать:
- Параметры (`playerId`, `partyId`, `questId`, `loyalty`, `cityUnrest`, `reputation`, `requestId`).
- Ответы `200/202` и ошибки `400`, `401`, `403`, `404`, `409`, `422`, `429`, `500`.
- `x-integrations` с указанием целевого сервиса (world/social/economy) для побочных эффектов.
- Примеры JSON (успех, конфликт).

---

## 🧩 Модели данных

- **NpcProfileResponse** — базовая информация (биография, мотивации, текущая лояльность, связанные квесты, доступные миссии).
- **NpcState** — флаги (`loyaltyState`, `familyThreatened`, `betrayal`), таймеры, cooldowns, pending requests.
- **NpcStateUpdateRequest/Response** — изменение лояльности, подтверждение, side-effects (reputation, unrest).
- **MissionHook** — id, тип (intel, rescue, neutral), условия, награды, связанные сервисы.
- **NpcRequest** — id, описание, срочность, последствия при игноре.
- **EventEmission** — список событий и их payload (world, social, economy).
- **LoyaltyTelemetry** — распределение лояльности, показатели взаимодействия, Kaede trust index.
- **HistoryEntry** — timestamp, playerId, action (mission, dialogue, update), результаты.
- **DialogueStateUpdate** — диалоговые флаги, доступные ветки.
- **AuditEntry** — кто и когда изменял состояние NPC.

Добавить `required`, `description`, `example`, `enum` для всех полей, а также расширения:
- `x-frontend` (используемые UI компоненты)
- `x-storage` (PostgreSQL schema, Redis cache, Kafka topics)
- `x-monitoring` (Grafana панели, PagerDuty alerts)
- `x-privacy` (retention для логов, PII sensitivity)
- `x-governance` (ревью и compliance требования)

---

## ✅ Детальный план

### Шаг 1: Анализ документа NPC
- Выписать биографию, мотивации, флаги и narrative hooks.
- Зафиксировать связанные квесты/рейды/кат-сцены.
- Определить зависимости world/social/economy.

**Результат:** карта сущностей и состояний NPC.

### Шаг 2: Определение структуры API
- Выбрать целевой путь (`api/v1/narrative/npcs/kaede-ishikawa.yaml`).
- Подготовить архитектурный комментарий, `info.x-microservice`, `servers`.
- Решить, какие модели вынести в components.

**Результат:** каркас файла спецификации.

### Шаг 3: Проработка endpoints
- Описать `paths` с полнотой параметров, запросов, ответов.
- Добавить `operationId`, `tags`, примеры, `x-integrations`.
- Включить стандартные ошибки и использование общих компонентов (responses, pagination).

**Результат:** полный раздел `paths`.

### Шаг 4: Модели и расширения
- Создать схемы (`NpcProfileResponse`, `NpcState`, `MissionHook`, `EventEmission`, `LoyaltyTelemetry`, `HistoryEntry`).
- Добавить `x-frontend`, `x-storage`, `x-monitoring`, `x-privacy`, `x-governance`.
- Подготовить примеры JSON.

**Результат:** раздел `components/schemas` готов и переиспользуем.

### Шаг 5: Security, telemetry, FAQ
- Настроить `security` (roles/scopes).
- Описать telemetry события, SLA (latency, mission activation).
- Подготовить FAQ (double agent баланс, семейные миссии, повторные запросы).

**Результат:** спецификация отражает эксплуатационные требования.

### Шаг 6: Валидация и чеклист
- Прогнать `scripts/validate-swagger.ps1 api/v1/narrative/npcs/kaede-ishikawa.yaml`.
- Проверить `tasks/config/checklist.md` (все блоки).
- Убедиться, что основной файл ≤360 строк, при необходимости вынести схемы.

**Результат:** валидная спецификация, готовая к исполнению.

---

## 📏 Критерии приёмки (12)

1. `api/v1/narrative/npcs/kaede-ishikawa.yaml` создан и проходит `scripts/validate-swagger.ps1`.
2. `info.x-microservice` заполнен для narrative-service (порт 8087, base-path `/api/v1/narrative/npcs`).
3. `GET /narrative/npcs/kaede-ishikawa` возвращает `NpcProfileResponse` с биографией, мотивациями, текущей лояльностью и связанными квестами.
4. `POST /narrative/npcs/kaede-ishikawa/state` принимает `NpcStateUpdateRequest`, поддерживает смену лояльности и возвращает эффекты (reputation, cityUnrest).
5. `POST /narrative/npcs/kaede-ishikawa/missions` описывает активацию миссий (intel, rescue, neutral) и эффекты с внешними сервисами.
6. `POST /narrative/npcs/kaede-ishikawa/events` документирует события `NPC_KAEDE_*` и их payload, включая интеграции.
7. `GET /narrative/npcs/kaede-ishikawa/telemetry` предоставляет метрики распределения лояльности и trust index.
8. Все endpoints используют стандартизированные ответы (`shared/common/responses.yaml`) и поддерживают ошибки 400/401/403/404/409/422/429/500.
9. Крупные схемы вынесены в компонентный файл или основной файл ≤360 строк; все схемы имеют `required`, `description`, `example`.
10. Добавлены `x-frontend`, `x-storage`, `x-monitoring`, `x-privacy`, `x-governance` расширения для ключевых моделей.
11. В спецификации описаны зависимости с задачами API-TASK-375/376/377 и ссылки на источники `.BRAIN`.
12. FAQ объясняет работу двойного агента, повторные запросы, семейные миссии и политику хранения данных.

---

## ❓ FAQ

**В: Как обрабатывать параллельные запросы на смену лояльности?**  
О: Использовать `stateVersion`/`etag` в `NpcStateUpdateRequest`; при конфликте возвращать `409 Conflict` с инструкцией повторить после обновления данных.

**В: Что делать, если City Unrest уже на максимуме?**  
О: В ответе `NpcStateUpdateResponse` вернуть предупреждение (`x-warnings`), что эффект ограничен; UI должен показать, что миссия даст альтернативную награду.

**В: Можно ли повторно выполнять миссию «Evacuate Family»?**  
О: Да, но с cooldown. Включите поле `cooldownEndsAt` в `MissionHook` и документируйте поведение при раннем повторе (возврат 409).

**В: Нужно ли отображать романтическое влияние?**  
О: Если игрок имеет романтическую линию с Kaori, добавить поле `kaoriRelationshipModifier` в `NpcProfileResponse` и указать влияние на лояльность.

**В: Как обеспечивается приватность логов взаимодействий?**  
О: Использовать `x-privacy` с retention 30 дней и отметить, что доступ к истории ограничен ролями `npc-analytics`.

---

**Примечание:** После завершения работы обновить `brain-mapping.yaml`, добавить секцию статуса в документ `.BRAIN/04-narrative/npc-lore/important/npc-kaede-ishikawa.md` и зафиксировать изменения через `scripts/autocommit.ps1`.

