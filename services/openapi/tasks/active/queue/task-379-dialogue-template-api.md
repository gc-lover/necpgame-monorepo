# Task ID: API-TASK-379
**Тип:** API Generation
**Приоритет:** высокий
**Статус:** queued
**Создано:** 2025-11-08 21:45
**Создатель:** GPT-5 Codex (API Task Creator)
**Зависимости:** API-TASK-376, API-TASK-378

---

## 📋 Краткое описание

Нужно описать OpenAPI спецификацию для управления диалоговыми шаблонами (состояния, узлы, D&D проверки) на основе `.BRAIN/04-narrative/dialogues/DIALOGUE-TEMPLATE.md`.

**Что нужно сделать:** Создать `api/v1/narrative/dialogues/templates.yaml` (при необходимости вынести схемы в `api/v1/narrative/components/dialogue-templates-schemas.yaml`) для narrative-service, чтобы команды могли создавать, просматривать и обновлять шаблоны диалогов.

---

## 🎯 Цель задания

Предоставить narrative-service контракт для хранения и раздачи стандартизированных диалоговых шаблонов, поддерживающих NPC, квесты и событийные сцены в стиле D&D с проверками и ветвлениями.

**Зачем это нужно:**
- Обеспечить единый формат для создания/редактирования диалогов, используемых в NPC и квестах.
- Интегрировать шаблоны с системами репутации, world events и skill checks.
- Дать UI/авторским инструментам API для CRUD операций с шаблонами.
- Поддержать экспорт/версионирование и telemetry по использованию шаблонов.

---

## 📚 Источники информации

### Основной документ

- **Репозиторий:** `.BRAIN`
- **Путь:** `.BRAIN/04-narrative/dialogues/DIALOGUE-TEMPLATE.md`
- **Версия:** 1.0.0
- **Дата обновления:** 2025-11-07
- **Статус:** approved, `api-readiness: ready`

**Что важно:**
- Структура шаблона: контекст, состояния, таблицы, YAML узлы, проверки.
- Требуемые поля: состояния, триггеры, проверки, ветвления, последствия.
- Интеграции с репутацией, world-state, quest D&D checks.
- Версионность шаблонов и связь с другими документами.

### Дополнительные источники

- `.BRAIN/05-technical/ui/dialogue-system.md` (если существует, для UI контекста).
- `.BRAIN/02-gameplay/social/reputation-formulas.md`
- `.BRAIN/02-gameplay/world/events/world-events-framework.md`
- `.BRAIN/04-narrative/quests/raid/2025-11-07-quest-helios-countermesh-conspiracy.md`
- `.BRAIN/04-narrative/npc-lore/important/npc-kaede-ishikawa.md`
- `.BRAIN/04-narrative/dialogues/npc-kaede-ishikawa.md`

### Связанные задания

- API-TASK-376 (Helios Countermesh Conspiracy Quest)
- API-TASK-378 (NPC Kaede Ishikawa API)
- API-TASK-377 (Helios Cutscenes API)
- API-TASK-243 (Social Resonance API)

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

- **Целевой файл:** `api/v1/narrative/dialogues/templates.yaml`
- **Компоненты (опционально):** `api/v1/narrative/components/dialogue-templates-schemas.yaml`
- **API версия:** v1 (semantic 1.0.0)
- **Тип:** OpenAPI 3.0.3

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── narrative/
            ├── dialogues/
            │   └── templates.yaml
            └── components/
                └── dialogue-templates-schemas.yaml   # если вынести общие схемы
```
> Ограничить основной файл ≤360 строк; крупные схемы (`DialogueTemplate`, `DialogueState`, `DialogueNode`, `SkillCheck`) вынести в components.

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервисная архитектура)

- **Микросервис:** narrative-service
- **Порт:** 8087
- **API Base Path:** `/api/v1/narrative/dialogues/*`
- **Домен:** Нарративные диалоги и шаблоны
- **Зависимости:**
  - social-service (репутация, социальные показатели)
  - world-service (world events, глобальные флаги)
  - gameplay-service (skill check определения)
  - analytics-service (использование шаблонов, ветвления)

### Frontend (модульная архитектура)

- **Модули:** `modules/narrative/tools/dialogues`, `modules/world/events`, `modules/social/reputation`
- **State Store:** `useNarrativeStore (dialogueTemplates)` + `useSocialStore (reputationThresholds)`
- **Состояние:** templates, versions, states, nodes, checks, linkedResources
- **UI компоненты (@shared/ui):** DialogueTemplateList, DialogueEditorCanvas, StateTriggerTable, SkillCheckMatrix
- **Формы (@shared/forms):** DialogueTemplateForm, SkillCheckForm, EventReactionForm
- **Layouts (@shared/layouts):** NarrativeToolsLayout
- **Hooks (@shared/hooks):** useFormState, useAutosave, useTelemetry

**Комментарий:** В заголовке OpenAPI добавить блок-комментарий с архитектурой (микросервис, модуль, UI компоненты, state store).

### OpenAPI / Security

- `info.x-microservice`: `name: narrative-service`, `port: 8087`, `domain: narrative`, `base-path: /api/v1/narrative/dialogues`, `package: com.necpgame.narrativeservice`.
- `servers`: `https://api.necp.game/v1`, `http://localhost:8080/api/v1`.
- Подключить `shared/common/security.yaml`, `shared/common/responses.yaml`, `shared/common/pagination.yaml`.
- `security`: `BearerAuth` с ролями `dialogue-view`, `dialogue-manage`, `dialogue-admin`.
- `x-events`: `narrative.dialogue.template.created`, `narrative.dialogue.template.updated`, `narrative.dialogue.template.published`.

---

## 📡 Endpoints

- **GET `/narrative/dialogues/templates`** — список шаблонов с фильтрами по типу, статусу, последним обновлениям.
- **POST `/narrative/dialogues/templates`** — создание нового шаблона (черновик).
- **GET `/narrative/dialogues/templates/{templateId}`** — детальная структура шаблона (контекст, состояния, узлы, проверки).
- **PUT `/narrative/dialogues/templates/{templateId}`** — обновление шаблона (редактирование).
- **PATCH `/narrative/dialogues/templates/{templateId}/status`** — смена статуса (draft → review → approved).
- **POST `/narrative/dialogues/templates/{templateId}/publish`** — публикация для конкретных NPC/квестов.
- **GET `/narrative/dialogues/templates/{templateId}/history`** — история версий и изменений.
- **POST `/narrative/dialogues/templates/{templateId}/clone`** — клонирование шаблона для новой ветки.
- **POST `/narrative/dialogues/templates/{templateId}/validate`** — проверка на целостность (проверки, ссылки).
- **GET `/narrative/dialogues/templates/analytics`** — telemetry по использованию шаблонов, ветвлениям, проверкам.
- **POST `/narrative/dialogues/templates/import`** — загрузка шаблона из внешнего YAML/JSON (авторский инструмент).
- **POST `/narrative/dialogues/templates/export`** — выгрузка шаблона (для ревью/архива).

Каждый endpoint должен описывать параметры, тела запросов/ответов, ошибки, `x-integrations` (например, при валидации проверяются справочники репутации и событий).

---

## 🧩 Модели данных

- **DialogueTemplate** — метаданные (title, version, status, type, author, linkedDocs, target modules).
- **DialogueContext** — цели, краткий синопсис, связанные системы.
- **DialogueState** — состояние, описание, триггеры, флаги, события.
- **DialogueNode** — nodeId, entryCondition, playerOptions[], npcResponses, outcomes.
- **PlayerOption** — optionId, текст, требования (stat-check, reputation, flag), outcomes (success/failure/critical).
- **SkillCheckDefinition** — stat, dc, modifiers, outcomes.
- **EventReaction** — событие, условие, реплика, последствия.
- **RewardEffect** — репутация, предметы, контракты, world-state изменения.
- **TemplateHistoryEntry** — версия, автор, timestamp, изменение.
- **ValidationResult** — список ошибок/предупреждений.
- **AnalyticsReport** — usageCount, branchesPerState, successRates, failurePoints.
- **TemplateCloneRequest/Response**, **TemplateStatusUpdate**, **TemplatePublishRequest**.

Обязательные поля, `enum`, `description`, `example`. Добавить расширения:
- `x-frontend`, `x-storage`, `x-monitoring`, `x-privacy`, `x-governance`.

---

## ✅ Детальный план

### Шаг 1: Анализ шаблона
- Выделить обязательные секции и данные (контекст, состояния, узлы, проверки).
- Зафиксировать требования к статусовому переходу и версионированию.
- Определить интеграции с репутацией и world events.

**Результат:** карта сущностей шаблона.

### Шаг 2: Структура спецификации
- Определить целевой путь и архитектурный комментарий.
- Настроить `info.x-microservice`, `servers`, `security`.
- Решить, какие модели вынести в components.

**Результат:** каркас OpenAPI файла.

### Шаг 3: Описание endpoints
- Детализировать CRUD/публикацию/валидацию/аналитику.
- Добавить `operationId`, `tags`, примеры, `x-integrations`.
- Обеспечить стандартные ответы и обработки ошибок.

**Результат:** завершённый раздел `paths`.

### Шаг 4: Модели и расширения
- Реализовать схемы `DialogueTemplate`, `DialogueState`, `DialogueNode`, `SkillCheckDefinition`, `ValidationResult`, `AnalyticsReport`.
- Добавить расширения `x-frontend`, `x-storage`, `x-monitoring`, `x-privacy`.
- Подготовить примеры полезной нагрузки.

**Результат:** раздел `components/schemas` готов.

### Шаг 5: Версионирование и governance
- Документировать статусные переходы, `x-governance` (review board).
- Указать telemetry метрики, SLA (для валидации ≤ 1 сек, публикации ≤ 2 сек).
- Подготовить FAQ (клонирование, импорт/экспорт, совместимость).

**Результат:** спецификация учитывает процессы и контроль качества.

### Шаг 6: Проверки
- Прогнать `scripts/validate-swagger.ps1 api/v1/narrative/dialogues/templates.yaml`.
- Проверить по чеклисту `tasks/config/checklist.md`.
- Убедиться, что основной файл ≤360 строк (при необходимости вынести модели).

**Результат:** валидная спецификация, готовая к передаче АПИТАСК.

---

## 📏 Критерии приёмки (12)

1. `api/v1/narrative/dialogues/templates.yaml` создан и проходит `scripts/validate-swagger.ps1`.
2. `info.x-microservice` заполнен для narrative-service (порт 8087, base-path `/api/v1/narrative/dialogues`).
3. `GET /narrative/dialogues/templates` поддерживает фильтры/пагинацию, возвращает `DialogueTemplateList`.
4. `POST /narrative/dialogues/templates` принимает `DialogueTemplateCreateRequest`, создаёт черновик с версией 0.1.0.
5. `GET /narrative/dialogues/templates/{templateId}` возвращает полную структуру (`DialogueTemplate`) с states/nodes/checks.
6. `PUT /narrative/dialogues/templates/{templateId}` обновляет шаблон, отслеживая версионность и историю.
7. `PATCH /narrative/dialogues/templates/{templateId}/status` описывает процесс переходов (draft → review → approved) и проверки.
8. `POST /narrative/dialogues/templates/{templateId}/validate` возвращает `ValidationResult` с списком ошибок/предупреждений.
9. `GET /narrative/dialogues/templates/analytics` предоставляет `AnalyticsReport` с использованием шаблонов.
10. `POST /narrative/dialogues/templates/export`/`import` документированы, включая безопасность и ограничения.
11. Схемы вынесены в components или основной файл ≤360 строк; присутствуют `x-frontend`, `x-storage`, `x-monitoring`, `x-governance`, `x-privacy`.
12. FAQ объясняет клонирование, ревью, совместимость со старыми шаблонами и вопросы безопасности.

---

## ❓ FAQ

**В: Можно ли публиковать шаблон без стадии review?**  
О: Нет, `PATCH /status` должен запрещать переход `draft → approved` напрямую; возвращать `409 Conflict` с указанием на необходимость review.

**В: Как обрабатывать импорт YAML?**  
О: Endpoint `/import` должен проверять схему, возвращать `ValidationResult`. При ошибках не создавать шаблон, а возвращать `422`.

**В: Что с backward-compatibility?**  
О: В `TemplateHistoryEntry` добавить поле `breakingChanges`; при попытке обновления с breaking изменить поведение публикации (нужен флаг подтверждения).

**В: Можно ли автоматически распространять изменения на существующие диалоги?**  
О: Да, но через отдельный процесс. Добавить поле `propagationMode` (manual/auto) и предупредить в FAQ, что автоматическое распространение требует подтверждения ревью.

**В: Как защищаем telemetry?**  
О: Использовать `x-privacy` (retention 90 дней, доступ роли `dialogue-analytics`), а также указать, что персональные данные игроков агрегируются.

---

**Примечание:** После создания задания обновить `brain-mapping.yaml`, добавить секцию статуса в `.BRAIN/04-narrative/dialogues/DIALOGUE-TEMPLATE.md` и зафиксировать изменения через `scripts/autocommit.ps1`.

