# Task ID: API-TASK-036
**Тип:** API Generation
**Приоритет:** высокий
**Статус:** queued
**Создано:** 2025-11-06 22:00
**Создатель:** AI Agent (API Task Creator)
**Зависимости:** API-TASK-028 (Locations API), API-TASK-035 (Character Status API)

---

## 📋 Краткое описание

Создать API спецификацию для системы случайных событий в игре (патрули NCPD, конфликты банд, предложения фиксеров, находки).

**Что нужно сделать:** Создать API для получения и обработки случайных событий на основе `events.json` и `ui-main-game.md`.

---

## 🎯 Цель задания

Создать полноценный API для системы случайных событий для MVP текстовой версии NECPGAME.

**Зачем это нужно:**
- Оживить игровой мир динамическими событиями
- Добавить вариативность и непредсказуемость в игровой процесс
- Реализовать влияние на репутацию с фракциями
- Создать возможности для получения случайных наград
- Добавить атмосферность через события дня/ночи

**Бизнес-логика:**
- События генерируются случайно на основе локации, времени суток, уровня опасности
- Игрок может выбрать один из вариантов реакции на событие
- Выборы влияют на репутацию, получение предметов, деньги, опыт
- Некоторые события могут инициировать бой
- События зависят от контекста (локация, время, фракции)

**Связь с общими целями проекта:**
- Реализация живого мира MMORPG
- Динамическое взаимодействие с окружением
- Система репутации с фракциями
- Вариативность игрового процесса

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`
**Путь к документу:** `.BRAIN/05-technical/mvp-content/events.json`
**Версия документа:** v1.0.0
**Дата последнего обновления:** 2025-11-04
**Статус документа:** ready

**Что важно из этого документа:**
- 5 базовых случайных событий (патруль NCPD, конфликт банд, фиксер, находка, корп встреча)
- 5 исторических событий (с привязкой к лору 1998-2077)
- События локаций (исследование)
- События времени суток (день/ночь)
- Структура события: id, name, description, locationTypes, dangerLevel, options, effects

### Дополнительные источники

- `.BRAIN/05-technical/ui-main-game.md` - интерфейс событий (для контекста)
- `.BRAIN/05-technical/mvp-content-summary.md` - обзор контента событий
- `.BRAIN/05-technical/mvp-content/travel-events-epochs.json` - события путешествий (для будущего)

### Связанные документы

- [Locations API Task](./task-028-locations-api.md) - зависимость (события привязаны к локациям)
- [Character Status API Task](./task-035-character-status-api.md) - зависимость (события влияют на репутацию)

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/events/random-events.yaml`
**API версия:** v1
**Тип файла:** OpenAPI 3.0.3 Specification (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── events/
            ├── README.md                # Обзор Events API
            └── random-events.yaml       # Random Events API (создать)
```

**Если файл уже существует:**
- Обновить существующий файл
- Сохранить совместимость
- Обновить документацию

---

## ✅ Что нужно сделать (детальный план)

### Шаг 1: Создать директорию и README

**Действия:**
1. Создать директорию `api/v1/events/` (если не существует)
2. Создать `README.md` с обзором Events API

### Шаг 2: Анализ исходных документов

**Действия:**
1. Прочитать `.BRAIN/05-technical/mvp-content/events.json`
2. Выделить ключевые концепции:
   - Типы событий: random, location, time
   - Структура события: id, name, description, options
   - Опции выбора: id, text, description, effects
   - Эффекты: reputation, combat, money, items, experience
3. Выделить бизнес-правила:
   - События зависят от locationTypes (corporate, industrial, criminal, residential)
   - События зависят от dangerLevel (low, medium, high, extreme)
   - События зависят от времени суток (day, night)
   - Выборы игрока влияют на репутацию с фракциями
   - Некоторые выборы инициируют бой

### Шаг 3: Определение API endpoints

**Что нужно создать:**

#### Основные endpoints:

1. **GET `/api/v1/events/random`**
   - **Назначение:** Получить случайное событие для текущего контекста
   - **Query параметры:**
     - `characterId` (UUID, required) - ID персонажа
     - `locationId` (UUID, required) - ID локации
     - `timeOfDay` (string, optional) - время суток (day, night)
     - `eventType` (string, optional) - тип события (random, location, time)
   - **Ответы:**
     - `200 OK` - случайное событие
     - `204 No Content` - нет доступных событий
     - `401 Unauthorized` - не авторизован
     - `404 Not Found` - персонаж или локация не найдены

2. **GET `/api/v1/events/{eventId}`**
   - **Назначение:** Получить детальную информацию о событии
   - **Path параметры:**
     - `eventId` (string, required) - ID события
   - **Ответы:**
     - `200 OK` - информация о событии
     - `404 Not Found` - событие не найдено

3. **POST `/api/v1/events/handle`**
   - **Назначение:** Обработать выбор игрока в событии
   - **Request body:**
     - `characterId` (UUID, required) - ID персонажа
     - `eventId` (string, required) - ID события
     - `optionId` (string, required) - ID выбранной опции
     - `context` (object, optional) - контекст (locationId, timeOfDay)
   - **Ответы:**
     - `200 OK` - результат обработки
     - `400 Bad Request` - невалидные параметры
     - `403 Forbidden` - выбор недоступен
     - `404 Not Found` - событие или персонаж не найдены

4. **GET `/api/v1/events/available`**
   - **Назначение:** Получить список доступных событий для контекста
   - **Query параметры:**
     - `characterId` (UUID, required) - ID персонажа
     - `locationId` (UUID, required) - ID локации
     - `timeOfDay` (string, optional) - время суток
     - `limit` (integer, optional) - количество событий (default: 10)
   - **Ответы:**
     - `200 OK` - список событий
     - `401 Unauthorized` - не авторизован

### Шаг 4: Определение моделей данных

**Что нужно создать:**

1. **RandomEvent** (Случайное событие)
   - `id` (string) - ID события
   - `name` (string) - название
   - `description` (string) - описание
   - `category` (string) - категория (random, location, time, historical)
   - `locationTypes` (array) - типы локаций
   - `dangerLevel` (array) - уровни опасности
   - `timeOfDay` (string, optional) - время суток
   - `options` (array) - варианты выбора
   - `probability` (number, optional) - вероятность события

2. **EventOption** (Вариант выбора в событии)
   - `id` (string) - ID опции
   - `text` (string) - текст кнопки
   - `description` (string) - описание результата
   - `requirements` (object, optional) - требования (уровень, навыки)
   - `effects` (EventEffects) - эффекты выбора

3. **EventEffects** (Эффекты события)
   - `reputation` (array, optional) - изменения репутации
     - `faction` (string) - фракция
     - `amount` (integer) - изменение
   - `combat` (boolean, optional) - инициировать бой
   - `money` (integer, optional) - изменение денег
   - `experience` (integer, optional) - опыт
   - `items` (array, optional) - получаемые предметы
   - `quest` (string, optional) - получить квест
   - `information` (string, optional) - получить информацию

4. **HandleEventRequest** (Запрос обработки события)
   - `characterId` (UUID, required) - ID персонажа
   - `eventId` (string, required) - ID события
   - `optionId` (string, required) - ID опции
   - `context` (EventContext, optional) - контекст

5. **HandleEventResponse** (Результат обработки события)
   - `success` (boolean) - успешно ли
   - `eventName` (string) - название события
   - `optionChosen` (string) - выбранная опция
   - `outcome` (string) - описание результата
   - `effects` (EventEffectsApplied) - примененные эффекты
   - `combatInitiated` (boolean) - начат ли бой
   - `combatId` (UUID, optional) - ID боя (если начат)

6. **EventEffectsApplied** (Примененные эффекты)
   - `reputationChanges` (array) - изменения репутации
     - `faction` (string)
     - `oldValue` (integer)
     - `newValue` (integer)
     - `change` (integer)
   - `moneyChange` (integer, optional) - изменение денег
   - `experienceGained` (integer, optional) - полученный опыт
   - `itemsReceived` (array, optional) - полученные предметы
   - `questReceived` (string, optional) - полученный квест
   - `informationGained` (string, optional) - полученная информация

7. **EventContext** (Контекст события)
   - `locationId` (UUID) - ID локации
   - `timeOfDay` (string) - время суток (day, night)
   - `dangerLevel` (string) - уровень опасности локации

8. **EventRequirements** (Требования для опции)
   - `level` (integer, optional) - минимальный уровень
   - `skills` (object, optional) - требуемые навыки
   - `reputation` (object, optional) - требуемая репутация

9. **ReputationChange** (Изменение репутации)
   - `faction` (string) - фракция
   - `amount` (integer) - изменение (-100 до +100)

### Шаг 5: Создание OpenAPI спецификации

**Требования к файлу:**
- OpenAPI 3.0.3
- Info блок с описанием и ссылками на .BRAIN
- Servers блок
- Paths со всеми endpoints (4 endpoints)
- Components/schemas со всеми моделями (9 моделей)
- Использование общих компонентов через $ref
- Примеры запросов/ответов
- Валидация параметров
- Security схемы

### Шаг 6: Валидация и проверка

**Что проверить:**
1. Валидация YAML
2. OpenAPI 3.0.3 спецификация
3. Все 4 endpoints описаны
4. Все 9 моделей описаны
5. Размер файла (не более 400 строк)
6. Использование общих компонентов

---

## 🎨 Принципы и правила

### Общие принципы проекта

1. **API First** - сначала спецификация, потом реализация
2. **RESTful API** - стандартные HTTP методы и коды ответов
3. **Версионирование** - семантическое версионирование API
4. **Документация** - все endpoints, модели и примеры задокументированы
5. **Безопасность** - все endpoints требуют аутентификации (Bearer Token)

### Стиль кодирования

1. **Именование:**
   - Endpoints: kebab-case (`/api/v1/events/random`, `/api/v1/events/handle`)
   - Модели: PascalCase (`RandomEvent`, `HandleEventResponse`)
   - Параметры: camelCase (`characterId`, `eventId`, `timeOfDay`)

2. **Формат:**
   - YAML формат
   - Отступы: 2 пробела
   - Строки не длиннее 120 символов

3. **Документация:**
   - Все поля должны иметь `description`
   - Все enum должны иметь описания значений
   - Примеры обязательны для основных endpoints
   - Ссылки на .BRAIN документы в `info.description`

### Правила из workspace rules

1. **SOLID** - одна фича - один файл
2. **DRY** - избегать дублирования (используй общие компоненты)
3. **KISS** - простота решений
4. **Данные в БД** - не хардкодить, хранить в БД
5. **Максимум 400 строк на файл** - если больше, разбить

### Обработка ошибок - МИНИМАЛИСТИЧНО

- Используй ЕДИНУЮ модель Error из `shared/common/responses.yaml`
- Backend определяет HTTP статус по коду ошибки
- Коды ошибок:
  - `AUTH_*` → 401/403
  - `BIZ_*` → 404/409
  - `VAL_*` → 400
  - `INT_*` → 500/503

### Именование моделей - ИЗБЕГАЙ КОНФЛИКТОВ

- НЕ используй имена встроенных классов: `Event`
- Используй уточняющие префиксы: `RandomEvent`, `GameEvent`
- В этом задании используем: `RandomEvent`, `EventOption`, `EventEffects`

---

## 📝 Примеры и шаблоны

### Пример endpoint:

```yaml
/events/random:
  get:
    summary: Получить случайное событие
    description: |
      Возвращает случайное событие на основе контекста игрока.
      
      **Бизнес-логика:**
      - События фильтруются по типу локации
      - События фильтруются по уровню опасности
      - События фильтруются по времени суток
      - Используется вероятностный выбор события
      - Может не вернуть событие (204 No Content)
      
      **Источник:** `.BRAIN/05-technical/mvp-content/events.json`
    operationId: getRandomEvent
    tags:
      - Random Events
    parameters:
      - name: characterId
        in: query
        required: true
        schema:
          type: string
          format: uuid
        description: ID персонажа
      - name: locationId
        in: query
        required: true
        schema:
          type: string
          format: uuid
        description: ID локации
      - name: timeOfDay
        in: query
        required: false
        schema:
          type: string
          enum: [day, night]
        description: Время суток
    responses:
      '200':
        description: Случайное событие
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RandomEvent'
      '204':
        description: Нет доступных событий
      '404':
        $ref: '../../shared/common/responses.yaml#/components/responses/NotFound'
    security:
      - BearerAuth: []
```

---

## 🔗 Связанные API и зависимости

### Зависимости (должны существовать раньше):

- `api/v1/locations/locations.yaml` - **API-TASK-028** - локации для событий
- `api/v1/characters/status.yaml` - **API-TASK-035** - репутация персонажа
- `api/v1/shared/common/responses.yaml` - общие ответы
- `api/v1/shared/common/security.yaml` - схемы безопасности

### Связанные API (используют этот API):

- `api/v1/combat/combat.yaml` - **API-TASK-032** - события могут инициировать бой
- `api/v1/quests/quests.yaml` - **API-TASK-030** - события могут давать квесты
- `api/v1/economy/inventory/inventory.yaml` - **API-TASK-029** - события могут давать предметы

---

## ✅ Критерии приемки

Задание считается выполненным, если:

1. ✅ **Файл создан:** `api/v1/events/random-events.yaml` существует
2. ✅ **README создан:** `api/v1/events/README.md` существует
3. ✅ **OpenAPI валидный:** Файл проходит валидацию OpenAPI 3.0.3
4. ✅ **Все endpoints:** Все 4 требуемых endpoints описаны
5. ✅ **Все модели:** Все 9 требуемых моделей данных описаны
6. ✅ **Документация:** Все элементы задокументированы
7. ✅ **Примеры:** Примеры запросов/ответов включены
8. ✅ **Соответствие:** API соответствует `events.json`
9. ✅ **Валидация:** Все параметры имеют валидацию
10. ✅ **Ошибки:** Используется ЕДИНАЯ модель Error
11. ✅ **Общие компоненты:** Используются $ref на shared/common/
12. ✅ **Размер:** Файл не превышает 400 строк
13. ✅ **Связность:** API готов к использованию бекендом и фронтендом

---

## ❓ Возможные вопросы и ответы

**Q: Как определять вероятность события?**
A: Backend использует locationTypes, dangerLevel, timeOfDay для фильтрации, затем случайный выбор.

**Q: Что делать если событие инициирует бой?**
A: В response возвращается combatInitiated=true и combatId. Фронтенд переходит к бою.

**Q: Как обрабатывать исторические события (эпохи)?**
A: В MVP они доступны, фильтруются по дате игрового мира (если реализована система времени).

**Q: Нужно ли проверять требования для опций?**
A: Да, backend проверяет requirements и возвращает только доступные опции или ошибку 403.

---

## 📞 Дополнительная информация

**Контекст игры:**
- **События MVP:** 5 базовых + 5 исторических = 10 событий
- **Типы локаций:** corporate, industrial, criminal, residential
- **Уровни опасности:** low, medium, high, extreme
- **Время суток:** day, night
- **Фракции репутации:** Arasaka, Militech, Valentinos, Maelstrom, NCPD

---

## 📊 История выполнения

- `2025-11-06 22:00` - Задание создано

---

**ВНИМАНИЕ:** Это задание для AI агента Cursor. Выполняй его пошагово, следуя всем инструкциям выше.


### OpenAPI (обязательно)

- Заполни `info.x-microservice` (name, port, domain, base-path, package) по данным целевого микросервиса.
- В секции `servers` оставь Production gateway `https://api.necp.game/v1` и пример локальной разработки `http://localhost:8080/api/v1`.
- WebSocket маршруты публикуй только через `wss://api.necp.game/v1/...`.


