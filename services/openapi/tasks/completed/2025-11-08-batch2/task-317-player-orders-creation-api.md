# Task ID: API-TASK-317
**Тип:** API Generation  
**Приоритет:** высокий  
**Статус:** completed  
**Создано:** 2025-11-08 12:10  
**Создатель:** GPT-5 Codex  
**Зависимости:** [API-TASK-158] – базовые социальные механики и общие компоненты player-orders

---

## 📋 Краткое описание

Создать и актуализировать спецификацию `api/v1/social/player-orders.yaml` для мастера создания заказов игроков, включая черновики, оценку бюджета, полную валидацию и публикацию с гарантиями.

**Что нужно сделать:** Обновить OpenAPI спецификацию и вспомогательные компоненты для создания заказов игроков на основе детального процесса из `.BRAIN/02-gameplay/social/player-orders-creation-детально.md`.

---

## 🎯 Цель задания

Обеспечить API-конракт social-service для полного конвейера создания заказов: от заполнения брифа и расчёта бюджета до валидации, гарантий и публикации с интеграциями в economy, world и factions сервисы.

**Зачем это нужно:**
- Синхронизировать API с финализированным UX мастером и бизнес-формулами бюджета.
- Дать фронтенду единый REST-контракт для пошагового мастера и симулятора бюджета.
- Обеспечить корректную интеграцию escrow, страховок и проверок санкций между social-service и economy/world/factions микросервисами.

---

## 📚 Источники информации

### Основной источник концепции

**Репозиторий:** `.BRAIN`  
**Путь к документу:** `.BRAIN/02-gameplay/social/player-orders-creation-детально.md`  
**Версия документа:** v1.0.0  
**Дата последнего обновления:** 2025-11-08 09:53  
**Статус документа:** approved  

**Что важно из этого документа:**
- Пятишаговый конвейер: подготовка брифа → валидация → оценка сложности/бюджета → выбор гарантий → публикация.
- Формулы расчёта бюджета (`ComplexityScore`, `RiskModifier`, `MarketIndex`, `TimeModifier`, escrow и комиссии).
- Требования к проверкам санкций, юридическому фильтру и токсичному контенту.
- Разделение режимов публикации (публичный, пригласительный, закрытый) и адресные приглашения.
- Kafka события и интеграции с economy, factions, world, relationships, npc и content сервисами.

### Дополнительные источники

- `.BRAIN/02-gameplay/social/player-orders-system.md` – общий контур экосистемы заказов.
- `.BRAIN/02-gameplay/social/player-orders-reputation-детально.md` – влияние заказов на рейтинги исполнителей.
- `.BRAIN/02-gameplay/social/player-orders-economy.md` – требования к escrow, страхованию и комиссиям.
- `.BRAIN/02-gameplay/social/relationships-system-детально.md` – адресные приглашения и доверительные связи.
- `.BRAIN/02-gameplay/social/npc-hiring-system-детально.md` – подключение NPC брокеров.
- Существующие спецификации: `api/v1/social/player-orders-components.yaml`, `api/v1/social/player-orders.yaml` (для анализа текущего состояния и обратной совместимости).

### Связанные документы

- `API-SWAGGER/api/v1/social/player-orders-components.yaml` – общий пул схем и параметров.
- `API-SWAGGER/api/v1/economy/player-orders/budget.yaml` (будет создаваться отдельной задачей) – зависимости по escrow.
- `API-SWAGGER/api/v1/world/cities/validation.yaml` (предстоящая задача) – проверки территорий и санкций.

---

## 📁 Целевая структура API

### Репозиторий: `API-SWAGGER`

**Целевой файл:** `api/v1/social/player-orders.yaml`  
**Вспомогательные файлы:**  
- `api/v1/social/player-orders-components.yaml`  
- при необходимости новые подкомпоненты: `api/v1/social/player-orders-validation-components.yaml`, `api/v1/social/player-orders-budget-components.yaml`

**API версия:** v1  
**Тип файла:** OpenAPI 3.0.3 (YAML)

**Структура директории:**
```
API-SWAGGER/
└── api/
    └── v1/
        └── social/
            ├── player-orders.yaml              ← обновить
            ├── player-orders-components.yaml    ← расширить
            ├── player-orders-budget-components.yaml (если потребуется)
            └── player-orders-validation-components.yaml (если потребуется)
```

**Если файл уже существует:**
- Выполнить мажорное обновление до версии `1.1.0`, отразив новые процессы.
- Сохранить совместимость по ключевым схемам (drafts, publish), добавив миграционные заметки.
- Обновить ссылку на источник и добавить архитектурный комментарий.

---

## 🏗️ Целевая архитектура (⚠️ ОБЯЗАТЕЛЬНО)

### Backend (микросервисная архитектура)
- **Микросервис:** social-service  
- **Порт:** 8084  
- **API Base Path:** `/api/v1/social/player-orders/*`  
- **Домен:** социальные механики, создание и публикация заказов  
- **Внешние зависимости:** economy-service (escrow, страховки), world-service (территории/санкции), factions-service (корпоративные проверки), relationships-service (адресные приглашения), npc-service (NPC брокеры), content-service (вложения), telemetry-service (логирование), auth-service (аутентификация)

### Frontend (модульная архитектура)
- **Модуль:** `modules/social/player-orders/create-wizard`  
- **State Store:** `useSocialStore` (`playerOrders`, `budgetSimulator`, `validationSummary`)  
- **UI компоненты @shared/ui:** `OrderCreateWizard`, `BudgetSimulator`, `OrderValidationSummary`, `GuaranteeSelector`, `RiskLevelBadge`  
- **Формы @shared/forms:** `OrderBriefForm`, `OrderBudgetForm`, `OrderGuaranteeForm`, `InviteListForm`  
- **Layouts @shared/layouts:** `GameLayout`, `WizardLayout`  
- **Хуки @shared/hooks:** `useEconomyRates`, `useFactionPermissions`, `useRelationshipsSearch`, `useRealtime`, `useDebounce`

### Комментарий для OpenAPI файла
Необходимо обновить блок комментария в начале `player-orders.yaml`, указав микросервис, зависимости, модуль фронтенда, UI/формы/хуки и базовый путь, а также список Kafka событий (draft.saved, validation.failed, published, escrow.locked).

---

## ✅ Что нужно сделать (детальный план)

### Шаг 1: Анализ и ревизия текущих файлов
1. Изучить текущую версию `player-orders.yaml` и `player-orders-components.yaml`.
2. Сопоставить существующие endpoints/схемы с конвейером из `.BRAIN` документа.
3. Зафиксировать, какие элементы требуют переименования, переработки или удаления (например, устаревшие пути `/drafts` vs `/player-orders/draft`).
**Результат:** список расхождений и план миграции схем.

### Шаг 2: Спроектировать обновлённые endpoints
1. Утвердить REST-подход, описанный в документе (см. раздел Endpoints ниже).
2. Определить структуру запросов/ответов с учётом шаблонов брифа, бюджета, гарантий, валидации и публикации.
3. Спроектировать общие параметры (фильтры по шаблону, режиму публикации, уровню риска).
**Результат:** финальный список endpoints и параметров.

### Шаг 3: Обновить модели данных и компоненты
1. Расширить схему брифа (`PlayerOrderBrief`, `PlayerOrderObjectives`, `RiskProfile`, `TeamRequirements`).
2. Добавить схему бюджета по формуле (`ComplexityScore`, `RiskModifier`, `MarketIndex`, `TimeModifier`, `BaseReward`, `Escrow`, `Commission`, рекомендованные бонусы/штрафы).
3. Описать структуру валидации (`ValidationChecklist`, `ValidationIssue`, `SanctionCheck`, `ToxicityCheck`).
4. Обновить/создать примеры запросов/ответов, включая edge-cases (превышение медианы, корпоративные заказы).
**Результат:** актуальные компоненты с примерами и required полями.

### Шаг 4: Описать процессы, события и безопасности
1. Добавить `security` (BearerAuth) и ссылки на общие ответы (`responses.yaml`, `pagination.yaml`, `security.yaml`).
2. Обновить `x-kafka-topics`: `social.player-orders.draft.saved`, `social.player-orders.validation.failed`, `social.player-orders.published`, `economy.player-orders.escrow.locked`, добавить `telemetry.player-orders.anomaly.detected` при ошибках валидации.
3. Документировать зависимости и сценарии ошибок (422 для невалидного бюджета, 409 для санкций).
**Результат:** полная документация потоков и событий.

### Шаг 5: Финализация и проверка
1. Пересобрать `player-orders.yaml`, обеспечить <400 строк (при необходимости вынести разделы в отдельные файлы с `$ref`).
2. Обновить версию `info.version` до `1.1.0` и описание, указав источники.
3. Запустить `npx swagger-cli validate api/v1/social/player-orders.yaml`.
4. Пройтись по чеклисту задания, убедиться в наличии примеров и критериев.
**Результат:** валидная OpenAPI 3.0.3 спецификация, отражающая новый конвейер.

---

## 🛣️ Endpoints (предложенная структура)

1. **GET `/social/player-orders/templates`**  
   Возвращает список шаблонов заказов с подсказками, рекомендованными гарантиями и типовыми рисками (боевой, хакерский, экономический, социальный, исследовательский).

2. **POST `/social/player-orders/draft`**  
   Создаёт черновик заказа, принимая бриф, сроки, риски, требования к команде, режим приватности и вложения. Возвращает `orderId`, статус `draft` и базовые рекомендации.

3. **PATCH `/social/player-orders/{orderId}/draft`**  
   Обновляет бриф, вложения, гарантии и режим приватности. Позволяет менять `orderTemplate`, `objectives`, `riskProfile`, `teamRequirements`.

4. **POST `/social/player-orders/{orderId}/estimate`**  
   Рассчитывает бюджет: принимает `complexityFactors`, `riskModifier`, `marketIndex`, `timeModifier`, `bonuses`, `penalties`. Возвращает `BaseReward`, `Escrow`, `Commission`, медиану рынка, рекомендации.

5. **POST `/social/player-orders/{orderId}/validate`**  
   Запускает полный чеклист: проверка обязательных полей, юридических ограничений, санкций, токсичных формулировок, лимитов бюджета. Возвращает список `ValidationIssue[]`, статус `passed/failed`, severity, рекомендации.

6. **POST `/social/player-orders/{orderId}/publish`**  
   Публикует заказ: принимает выбранные гарантии/страховку, список приглашённых (игроки/NPC), флаг `releaseEscrowImmediately`. Возвращает `publishToken`, состояние `publishing/published`.

7. **POST `/social/player-orders/{orderId}/cancel`**  
   Отменяет заказ, освобождает escrow и уведомляет участников; причины и политика возврата обязательны.

8. **GET `/social/player-orders/{orderId}`**  
   Возвращает объединённую информацию (бриф, бюджет, гарантии, публикация) для UI обзора.

9. **GET `/social/player-orders`** (опционально)  
   Список заказов текущего игрока с фильтрами по статусу (draft, pending_validation, ready_to_publish, published) и шаблону.

10. **POST `/social/player-orders/{orderId}/attachments`**  
    Загрузка вложений в `content-service`, возврат метаданных (URL, mimeType).

Все endpoints должны использовать общие ответы из `shared/common/responses.yaml`, а параметры `orderId`, `template`, `visibility` и `riskLevel` вынести в компоненты.

---

## 🧱 Основные модели данных

- **PlayerOrderBrief** – бриф заказа (goal, objectives[], deadlines, checkpoints, riskLevel, legalStatus, privacy, documents[]).  
- **ComplexityFactors** – структура расчёта `ComplexityScore` (zones[], stages, requiredSkills[], hostileFactions[]).  
- **BudgetEstimate** – объект с полями из формулы (`complexityScore`, `riskModifier`, `marketIndex`, `timeModifier`, `baseReward`, `escrow`, `commission`, `medianDeviation`, `recommendations`).  
- **GuaranteeSelection** – выбранные уровни страховки и escrow, расчёт бонусов/штрафов.  
- **ValidationChecklistResult** – шаги проверки (completeness, legal, sanctions, toxicity, budgetRanges) с `status`, `issues[]`, `serviceSource`.  
- **PublicationPayload** – режим публикации, список приглашённых исполнителей/NPC, escrow policy, notification channels.  
- **PlayerOrderSummary** – объединяет черновик, бюджет, гарантии, публикацию и текущие статусы.

Каждая модель должна иметь `required`, `enum`, `minimum/maximum` и примеры. Вынести повторяющиеся структуры (`RiskModifier`, `MarketIndexSnapshot`) в отдельные компоненты.

---

## 📏 Принципы и правила

- Принципы: SOLID, DRY, KISS, API First.  
- OpenAPI 3.0.3, RESTful стиль, чистые глаголы (create, estimate, validate, publish).  
- Ответы должны ссылаться на `shared/common` компоненты (responses, pagination, security).  
- Не хардкодим данные – все справочники (шаблоны, рейтинги, медианы рынка) описываем как ссылки на внешние сервисы.  
- Размер каждого YAML файла ≤ 400 строк, при необходимости дробим и используем `$ref`.  
- Указать зависимость от Kafka событий и техпроцесс (логирование в telemetry-service).

---

## 🧪 Примеры (минимум добавить в спецификацию)

- **Черновик заказа** (боевой): цель, задачи, риски, сроки, рекомендации гарантий.  
- **Запрос на расчёт бюджета** с `complexityScore=2.4`, `riskModifier=1.3`, `marketIndex=1.12`, `timeModifier=1.1`.  
- **Ответ бюджета** с расчётами (`baseReward=72000`, `escrow=21600`, `commission=5400`, `medianDeviation=+12%`, `notes`).  
- **Ответ валидации (успешный)** и пример с провалами (токсичная формулировка, санкции, превышение бюджета).  
- **Публикация** с приглашёнными игроками и NPC брокером, статус `publishing`, событие `economy.player-orders.escrow.locked`.

---

## 🔗 Связности и зависимости

- **Economy-service:** расчёт escrow, страховок, комиссий, блокировка средств (REST + Kafka `economy.player-orders.escrow.locked`).  
- **World-service:** проверка зон, санкций и правового статуса.  
- **Factions-service:** корпоративные заказы и рейтинги допуска.  
- **Relationships-service:** адресные приглашения (whitelist).  
- **NPC-service:** подбор NPC для корпоративных заказов.  
- **Telemetry-service / Monitoring:** логирование ошибок валидации, аномалий бюджета.  
- **Content-service:** управление вложениями (хранение брифов, схем, карт).

Нужно описать внешние зависимости в разделе описания и в FAQ.

---

## ✅ Критерии приемки (минимум 11)

1. Обновлённый `player-orders.yaml` отражает все этапы конвейера (draft → estimate → validate → publish → cancel).  
2. Версия спецификации повышена до `1.1.0`, указаны источники и архитектурный комментарий.  
3. Для каждого endpoint задокументированы запросы, ответы, ошибки и примеры.  
4. Схема бюджета соответствует формуле из документа (`ComplexityScore`, `RiskModifier`, `MarketIndex`, `TimeModifier`, `BaseReward`, `Escrow`, `Commission`).  
5. Схема валидации содержит результаты по всем сервисам (legal, sanctions, toxicity, budget).  
6. Примеры JSON соответствуют схемам и демонстрируют как успешные, так и ошибочные сценарии.  
7. Все ошибки используют общие ответы из `shared/common/responses.yaml`.  
8. Kafka события перечислены в `x-kafka-topics` с ссылками на payload схемы.  
9. Спецификация проходит `npx swagger-cli validate api/v1/social/player-orders.yaml` без ошибок.  
10. Общий объём каждого файла ≤ 400 строк (при необходимости секции вынесены в отдельные YAML файлы).  
11. Документация описывает зависимости на economy/world/factions/relationships/npc сервисы и требования к аутентификации.  
12. В заданиях указано, как обновить фронтенд модуль (`modules/social/player-orders/create-wizard`) с новыми контрактами.

---

## ❓ FAQ

**Q:** Почему мы переходим от `/drafts` к `/player-orders/draft`?  
**A:** Документ описывает единый мастер, где черновик – этап создания заказа. REST-структура с ресурсом `player-orders` упрощает навигацию и согласуется с публикацией и валидацией.

**Q:** Где хранить справочники шаблонов и медианы рынка?  
**A:** Они приходят из `economy-service` и `content-service`. API возвращает ссылки/идентификаторы, без хардкода значений.

**Q:** Как обрабатывать корпоративные заказы?  
**A:** В запросы добавляем флаг `corporate=true` и `factionId`. Валидация должна вызывать `factions-service` и проверять санкции, описываем в схеме `ValidationChecklist`.

**Q:** Что делать при превышении бюджета от медианы?  
**A:** В ответе `BudgetEstimate` включаем поле `medianDeviation` и массив `warnings[]`. Публикация должна блокироваться до подтверждения заказчиком.

**Q:** Как валидировать токсичный контент?  
**A:** Сервис вызывает `content-service` и возвращает issues с типом `toxicity`. Это отражается в `ValidationChecklistResult`.

---

## 📌 История выполнения

- 2025-11-08 – создано задание на основе `.BRAIN/02-gameplay/social/player-orders-creation-детально.md`, статус `queued`.



### OpenAPI (обязательно)

- Заполни `info.x-microservice` с актуальными данными:
  - name: social-service
  - port: 8084
  - domain: social
  - base-path: /api/v1/social
  - package: com.necpgame.socialservice
- В секции `servers` используй gateway:
  - https://api.necp.game/v1/social
  - http://localhost:8080/api/v1/social
- WebSocket маршруты публикуй только через wss://api.necp.game/v1/social/...

