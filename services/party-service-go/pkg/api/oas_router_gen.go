// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/social/party/"

			if l := len("/social/party/"); len(elem) >= l && elem[0:l] == "/social/party/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'c': // Prefix: "create"
				origElem := elem
				if l := len("create"); len(elem) >= l && elem[0:l] == "create" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "POST":
						s.handleCreatePartyRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "POST")
					}

					return
				}

				elem = origElem
			case 'i': // Prefix: "invite/"
				origElem := elem
				if l := len("invite/"); len(elem) >= l && elem[0:l] == "invite/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "inviteId"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'a': // Prefix: "accept"

						if l := len("accept"); len(elem) >= l && elem[0:l] == "accept" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleAcceptInviteRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					case 'd': // Prefix: "decline"

						if l := len("decline"); len(elem) >= l && elem[0:l] == "decline" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleDeclineInviteRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					}

				}

				elem = origElem
			}
			// Param: "partyId"
			// Match until "/"
			idx := strings.IndexByte(elem, '/')
			if idx < 0 {
				idx = len(elem)
			}
			args[0] = elem[:idx]
			elem = elem[idx:]

			if len(elem) == 0 {
				switch r.Method {
				case "DELETE":
					s.handleDisbandPartyRequest([1]string{
						args[0],
					}, elemIsEscaped, w, r)
				case "GET":
					s.handleGetPartyRequest([1]string{
						args[0],
					}, elemIsEscaped, w, r)
				default:
					s.notAllowed(w, r, "DELETE,GET")
				}

				return
			}
			switch elem[0] {
			case '/': // Prefix: "/"

				if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'i': // Prefix: "invite"

					if l := len("invite"); len(elem) >= l && elem[0:l] == "invite" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleInvitePlayerRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				case 'k': // Prefix: "kick"

					if l := len("kick"); len(elem) >= l && elem[0:l] == "kick" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleKickMemberRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				case 'l': // Prefix: "l"

					if l := len("l"); len(elem) >= l && elem[0:l] == "l" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'e': // Prefix: "eave"

						if l := len("eave"); len(elem) >= l && elem[0:l] == "eave" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleLeavePartyRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					case 'o': // Prefix: "oot/roll"

						if l := len("oot/roll"); len(elem) >= l && elem[0:l] == "oot/roll" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleRollForLootRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					}

				case 's': // Prefix: "settings"

					if l := len("settings"); len(elem) >= l && elem[0:l] == "settings" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "PATCH":
							s.handleUpdateSettingsRequest([1]string{
								args[0],
							}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "PATCH")
						}

						return
					}

				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/social/party/"

			if l := len("/social/party/"); len(elem) >= l && elem[0:l] == "/social/party/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'c': // Prefix: "create"
				origElem := elem
				if l := len("create"); len(elem) >= l && elem[0:l] == "create" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "POST":
						r.name = CreatePartyOperation
						r.summary = "Создать группу"
						r.operationID = "createParty"
						r.operationGroup = ""
						r.pathPattern = "/social/party/create"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

				elem = origElem
			case 'i': // Prefix: "invite/"
				origElem := elem
				if l := len("invite/"); len(elem) >= l && elem[0:l] == "invite/" {
					elem = elem[l:]
				} else {
					break
				}

				// Param: "inviteId"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'a': // Prefix: "accept"

						if l := len("accept"); len(elem) >= l && elem[0:l] == "accept" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = AcceptInviteOperation
								r.summary = "Принять приглашение"
								r.operationID = "acceptInvite"
								r.operationGroup = ""
								r.pathPattern = "/social/party/invite/{inviteId}/accept"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 'd': // Prefix: "decline"

						if l := len("decline"); len(elem) >= l && elem[0:l] == "decline" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = DeclineInviteOperation
								r.summary = "Отклонить приглашение"
								r.operationID = "declineInvite"
								r.operationGroup = ""
								r.pathPattern = "/social/party/invite/{inviteId}/decline"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				}

				elem = origElem
			}
			// Param: "partyId"
			// Match until "/"
			idx := strings.IndexByte(elem, '/')
			if idx < 0 {
				idx = len(elem)
			}
			args[0] = elem[:idx]
			elem = elem[idx:]

			if len(elem) == 0 {
				switch method {
				case "DELETE":
					r.name = DisbandPartyOperation
					r.summary = "Распустить группу (только лидер)"
					r.operationID = "disbandParty"
					r.operationGroup = ""
					r.pathPattern = "/social/party/{partyId}"
					r.args = args
					r.count = 1
					return r, true
				case "GET":
					r.name = GetPartyOperation
					r.summary = "Получить информацию о группе"
					r.operationID = "getParty"
					r.operationGroup = ""
					r.pathPattern = "/social/party/{partyId}"
					r.args = args
					r.count = 1
					return r, true
				default:
					return
				}
			}
			switch elem[0] {
			case '/': // Prefix: "/"

				if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'i': // Prefix: "invite"

					if l := len("invite"); len(elem) >= l && elem[0:l] == "invite" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = InvitePlayerOperation
							r.summary = "Пригласить игрока"
							r.operationID = "invitePlayer"
							r.operationGroup = ""
							r.pathPattern = "/social/party/{partyId}/invite"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				case 'k': // Prefix: "kick"

					if l := len("kick"); len(elem) >= l && elem[0:l] == "kick" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = KickMemberOperation
							r.summary = "Исключить участника (только лидер)"
							r.operationID = "kickMember"
							r.operationGroup = ""
							r.pathPattern = "/social/party/{partyId}/kick"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				case 'l': // Prefix: "l"

					if l := len("l"); len(elem) >= l && elem[0:l] == "l" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'e': // Prefix: "eave"

						if l := len("eave"); len(elem) >= l && elem[0:l] == "eave" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = LeavePartyOperation
								r.summary = "Покинуть группу"
								r.operationID = "leaveParty"
								r.operationGroup = ""
								r.pathPattern = "/social/party/{partyId}/leave"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 'o': // Prefix: "oot/roll"

						if l := len("oot/roll"); len(elem) >= l && elem[0:l] == "oot/roll" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = RollForLootOperation
								r.summary = "Бросить кубик на предмет"
								r.operationID = "rollForLoot"
								r.operationGroup = ""
								r.pathPattern = "/social/party/{partyId}/loot/roll"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				case 's': // Prefix: "settings"

					if l := len("settings"); len(elem) >= l && elem[0:l] == "settings" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "PATCH":
							r.name = UpdateSettingsOperation
							r.summary = "Изменить настройки группы (только лидер)"
							r.operationID = "updateSettings"
							r.operationGroup = ""
							r.pathPattern = "/social/party/{partyId}/settings"
							r.args = args
							r.count = 1
							return r, true
						default:
							return
						}
					}

				}

			}

		}
	}
	return r, false
}
