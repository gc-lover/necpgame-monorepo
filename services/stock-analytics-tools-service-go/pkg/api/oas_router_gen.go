// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "api/v1/"

				if l := len("api/v1/"); len(elem) >= l && elem[0:l] == "api/v1/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "analysis/"

					if l := len("analysis/"); len(elem) >= l && elem[0:l] == "analysis/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'c': // Prefix: "correlations"

						if l := len("correlations"); len(elem) >= l && elem[0:l] == "correlations" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetCorrelationsRequest([0]string{}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					case 'f': // Prefix: "fundamental/"

						if l := len("fundamental/"); len(elem) >= l && elem[0:l] == "fundamental/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "symbol"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetFundamentalAnalysisRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					case 't': // Prefix: "technical/"

						if l := len("technical/"); len(elem) >= l && elem[0:l] == "technical/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "symbol"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetTechnicalAnalysisRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					case 'v': // Prefix: "volatility/"

						if l := len("volatility/"); len(elem) >= l && elem[0:l] == "volatility/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "symbol"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetVolatilityAnalysisRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					}

				case 'b': // Prefix: "backtesting/strategy"

					if l := len("backtesting/strategy"); len(elem) >= l && elem[0:l] == "backtesting/strategy" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleBacktestStrategyRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				case 'p': // Prefix: "p"

					if l := len("p"); len(elem) >= l && elem[0:l] == "p" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'o': // Prefix: "ortfolio/optimization"

						if l := len("ortfolio/optimization"); len(elem) >= l && elem[0:l] == "ortfolio/optimization" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleOptimizePortfolioRequest([0]string{}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					case 'r': // Prefix: "rediction/price/"

						if l := len("rediction/price/"); len(elem) >= l && elem[0:l] == "rediction/price/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "symbol"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handlePredictPriceRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					}

				case 'r': // Prefix: "risk/assessment"

					if l := len("risk/assessment"); len(elem) >= l && elem[0:l] == "risk/assessment" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "POST":
							s.handleAssessRiskRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "POST")
						}

						return
					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleHealthCheckRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			case 'm': // Prefix: "metrics"

				if l := len("metrics"); len(elem) >= l && elem[0:l] == "metrics" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleMetricsRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			case 'r': // Prefix: "ready"

				if l := len("ready"); len(elem) >= l && elem[0:l] == "ready" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch r.Method {
					case "GET":
						s.handleReadinessCheckRequest([0]string{}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "GET")
					}

					return
				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/"

			if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				break
			}
			switch elem[0] {
			case 'a': // Prefix: "api/v1/"

				if l := len("api/v1/"); len(elem) >= l && elem[0:l] == "api/v1/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "analysis/"

					if l := len("analysis/"); len(elem) >= l && elem[0:l] == "analysis/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'c': // Prefix: "correlations"

						if l := len("correlations"); len(elem) >= l && elem[0:l] == "correlations" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetCorrelationsOperation
								r.summary = "Get correlation analysis between stocks"
								r.operationID = "getCorrelations"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/analysis/correlations"
								r.args = args
								r.count = 0
								return r, true
							default:
								return
							}
						}

					case 'f': // Prefix: "fundamental/"

						if l := len("fundamental/"); len(elem) >= l && elem[0:l] == "fundamental/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "symbol"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetFundamentalAnalysisOperation
								r.summary = "Get fundamental analysis for a stock"
								r.operationID = "getFundamentalAnalysis"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/analysis/fundamental/{symbol}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 't': // Prefix: "technical/"

						if l := len("technical/"); len(elem) >= l && elem[0:l] == "technical/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "symbol"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetTechnicalAnalysisOperation
								r.summary = "Get comprehensive technical analysis"
								r.operationID = "getTechnicalAnalysis"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/analysis/technical/{symbol}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 'v': // Prefix: "volatility/"

						if l := len("volatility/"); len(elem) >= l && elem[0:l] == "volatility/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "symbol"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetVolatilityAnalysisOperation
								r.summary = "Get volatility analysis and metrics"
								r.operationID = "getVolatilityAnalysis"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/analysis/volatility/{symbol}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				case 'b': // Prefix: "backtesting/strategy"

					if l := len("backtesting/strategy"); len(elem) >= l && elem[0:l] == "backtesting/strategy" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = BacktestStrategyOperation
							r.summary = "Backtest trading strategy"
							r.operationID = "backtestStrategy"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/backtesting/strategy"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				case 'p': // Prefix: "p"

					if l := len("p"); len(elem) >= l && elem[0:l] == "p" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'o': // Prefix: "ortfolio/optimization"

						if l := len("ortfolio/optimization"); len(elem) >= l && elem[0:l] == "ortfolio/optimization" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = OptimizePortfolioOperation
								r.summary = "Optimize portfolio allocation"
								r.operationID = "optimizePortfolio"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/portfolio/optimization"
								r.args = args
								r.count = 0
								return r, true
							default:
								return
							}
						}

					case 'r': // Prefix: "rediction/price/"

						if l := len("rediction/price/"); len(elem) >= l && elem[0:l] == "rediction/price/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "symbol"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = PredictPriceOperation
								r.summary = "Get price prediction using ML models"
								r.operationID = "predictPrice"
								r.operationGroup = ""
								r.pathPattern = "/api/v1/prediction/price/{symbol}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				case 'r': // Prefix: "risk/assessment"

					if l := len("risk/assessment"); len(elem) >= l && elem[0:l] == "risk/assessment" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "POST":
							r.name = AssessRiskOperation
							r.summary = "Perform risk assessment analysis"
							r.operationID = "assessRisk"
							r.operationGroup = ""
							r.pathPattern = "/api/v1/risk/assessment"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

				}

			case 'h': // Prefix: "health"

				if l := len("health"); len(elem) >= l && elem[0:l] == "health" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = HealthCheckOperation
						r.summary = "Service health check"
						r.operationID = "healthCheck"
						r.operationGroup = ""
						r.pathPattern = "/health"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 'm': // Prefix: "metrics"

				if l := len("metrics"); len(elem) >= l && elem[0:l] == "metrics" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = MetricsOperation
						r.summary = "Service metrics"
						r.operationID = "metrics"
						r.operationGroup = ""
						r.pathPattern = "/metrics"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			case 'r': // Prefix: "ready"

				if l := len("ready"); len(elem) >= l && elem[0:l] == "ready" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					// Leaf node.
					switch method {
					case "GET":
						r.name = ReadinessCheckOperation
						r.summary = "Service readiness check"
						r.operationID = "readinessCheck"
						r.operationGroup = ""
						r.pathPattern = "/ready"
						r.args = args
						r.count = 0
						return r, true
					default:
						return
					}
				}

			}

		}
	}
	return r, false
}
