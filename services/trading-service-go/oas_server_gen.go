// Code generated by ogen, DO NOT EDIT.

package api

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// ConfirmTradeSession implements confirmTradeSession operation.
	//
	// **Confirm and execute trade session**
	// Finalizes the trade session, executes item transfers and currency transactions through escrow
	// system.
	// **Atomicity:** All operations are executed in single transaction.
	//
	// POST /trade-sessions/{sessionId}/confirm
	ConfirmTradeSession(ctx context.Context, params ConfirmTradeSessionParams) (ConfirmTradeSessionRes, error)
	// CreateExample implements createExample operation.
	//
	// **Enterprise-grade creation endpoint**
	// Validates business rules, applies security checks, and ensures data consistency.
	// Supports optimistic locking for concurrent operations.
	// **Performance:** <50ms P95, includes validation and business logic.
	//
	// POST /examples
	CreateExample(ctx context.Context, req *CreateExampleRequest) (CreateExampleRes, error)
	// CreateTradeSession implements createTradeSession operation.
	//
	// **Create secure P2P trade session with escrow protection**
	// Initiates a new trade session between multiple players with automatic escrow handling and fraud
	// prevention.
	// **Security:** All trades are escrowed until both parties confirm **Performance:** <50ms P99
	// latency for trade creation.
	//
	// POST /trade-sessions
	CreateTradeSession(ctx context.Context, req *CreateTradeSessionRequest) (CreateTradeSessionRes, error)
	// DeleteExample implements deleteExample operation.
	//
	// **Enterprise-grade deletion endpoint**
	// Supports soft deletes with audit trails and cleanup scheduling.
	// Ensures referential integrity and cascading deletes.
	// **Performance:** <15ms P95, includes cleanup operations.
	//
	// DELETE /examples/{example_id}
	DeleteExample(ctx context.Context, params DeleteExampleParams) (DeleteExampleRes, error)
	// GetExample implements getExample operation.
	//
	// **Enterprise-grade retrieval endpoint**
	// Optimized with proper caching strategies and database indexing.
	// Supports conditional requests with ETags.
	// **Performance:** <5ms P95 with Redis caching.
	//
	// GET /examples/{example_id}
	GetExample(ctx context.Context, params GetExampleParams) (GetExampleRes, error)
	// GetTradeSession implements getTradeSession operation.
	//
	// **Retrieve detailed trade session information**
	// Returns complete trade session data including current offers, participants, and transaction
	// history.
	// **Security:** Only participants can view session details.
	//
	// GET /trade-sessions/{sessionId}
	GetTradeSession(ctx context.Context, params GetTradeSessionParams) (GetTradeSessionRes, error)
	// GetWallet implements getWallet operation.
	//
	// **Retrieve wallet information and balance**
	// Returns wallet details including current balance, transaction history, and frozen status.
	// **Security:** Only wallet owner can access details.
	//
	// GET /wallets/{walletId}
	GetWallet(ctx context.Context, params GetWalletParams) (GetWalletRes, error)
	// GetWalletTransactions implements getWalletTransactions operation.
	//
	// **Get paginated transaction history for wallet**
	// Returns chronological list of all transactions for the specified wallet.
	// **Performance:** Indexed queries with pagination optimization.
	//
	// GET /wallets/{walletId}/transactions
	GetWalletTransactions(ctx context.Context, params GetWalletTransactionsParams) (GetWalletTransactionsRes, error)
	// ListExamples implements listExamples operation.
	//
	// **Enterprise-grade listing endpoint**
	// Supports complex filtering, sorting, and pagination patterns.
	// Optimized for high-throughput scenarios with proper indexing.
	// **Performance:** <10ms P95, supports 1000+ concurrent requests.
	//
	// GET /examples
	ListExamples(ctx context.Context, params ListExamplesParams) (ListExamplesRes, error)
	// ListTradeSessions implements listTradeSessions operation.
	//
	// **Get paginated list of trade sessions**
	// Returns active trade sessions for the authenticated user with pagination and filtering support.
	// **Performance:** Optimized with database indexes on status and timestamps.
	//
	// GET /trade-sessions
	ListTradeSessions(ctx context.Context, params ListTradeSessionsParams) (*ListTradeSessionsOK, error)
	// TradingServiceHealthCheck implements tradingServiceHealthCheck operation.
	//
	// **Enterprise-grade health check endpoint**
	// Provides real-time health status of the trading service microservice.
	// Critical for service discovery, load balancing, and monitoring.
	// **Performance:** <1ms response time, cached for 30 seconds.
	//
	// GET /health
	TradingServiceHealthCheck(ctx context.Context, params TradingServiceHealthCheckParams) (TradingServiceHealthCheckRes, error)
	// UpdateExample implements updateExample operation.
	//
	// **Enterprise-grade update endpoint**
	// Supports partial updates, optimistic locking, and audit trails.
	// Ensures data consistency with event sourcing patterns.
	// **Performance:** <25ms P95, includes validation and conflict resolution.
	//
	// PUT /examples/{example_id}
	UpdateExample(ctx context.Context, req *UpdateExampleRequest, params UpdateExampleParams) (UpdateExampleRes, error)
	// UpdateTradeSession implements updateTradeSession operation.
	//
	// **Update trade session offer or status**
	// Allows participants to modify trade offers, accept/reject terms, or update session status.
	// **Concurrency:** Optimistic locking prevents race conditions.
	//
	// PUT /trade-sessions/{sessionId}
	UpdateTradeSession(ctx context.Context, req *UpdateTradeSessionReq, params UpdateTradeSessionParams) (UpdateTradeSessionRes, error)
	// NewError creates *ErrRespStatusCode from error returned by handler.
	//
	// Used for common default response.
	NewError(ctx context.Context, err error) *ErrRespStatusCode
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h   Handler
	sec SecurityHandler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, sec SecurityHandler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		sec:        sec,
		baseServer: s,
	}, nil
}
