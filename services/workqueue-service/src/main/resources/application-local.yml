spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5442/workqueue
    username: workqueue
    password: workqueue
security:
  api:
    enabled: false

