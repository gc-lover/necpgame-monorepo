// Code generated by ogen, DO NOT EDIT.

package api

import (
	"net/http"
	"net/url"
	"strings"

	"github.com/ogen-go/ogen/uri"
)

func (s *Server) cutPrefix(path string) (string, bool) {
	prefix := s.cfg.Prefix
	if prefix == "" {
		return path, true
	}
	if !strings.HasPrefix(path, prefix) {
		// Prefix doesn't match.
		return "", false
	}
	// Cut prefix from the path.
	return strings.TrimPrefix(path, prefix), true
}

// ServeHTTP serves http request as defined by OpenAPI v3 specification,
// calling handler that matches the path or returning not found error.
func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	elem := r.URL.Path
	elemIsEscaped := false
	if rawPath := r.URL.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
			elemIsEscaped = strings.ContainsRune(elem, '%')
		}
	}

	elem, ok := s.cutPrefix(elem)
	if !ok || len(elem) == 0 {
		s.notFound(w, r)
		return
	}
	args := [1]string{}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/world/events"

			if l := len("/world/events"); len(elem) >= l && elem[0:l] == "/world/events" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				switch r.Method {
				case "GET":
					s.handleListWorldEventsRequest([0]string{}, elemIsEscaped, w, r)
				case "POST":
					s.handleCreateWorldEventRequest([0]string{}, elemIsEscaped, w, r)
				default:
					s.notAllowed(w, r, "GET,POST")
				}

				return
			}
			switch elem[0] {
			case '/': // Prefix: "/"

				if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "active"
					origElem := elem
					if l := len("active"); len(elem) >= l && elem[0:l] == "active" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetActiveWorldEventsRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

					elem = origElem
				case 'b': // Prefix: "by-"
					origElem := elem
					if l := len("by-"); len(elem) >= l && elem[0:l] == "by-" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'f': // Prefix: "frequency/"

						if l := len("frequency/"); len(elem) >= l && elem[0:l] == "frequency/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "frequency"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetWorldEventsByFrequencyRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					case 's': // Prefix: "scale/"

						if l := len("scale/"); len(elem) >= l && elem[0:l] == "scale/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "scale"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetWorldEventsByScaleRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					case 't': // Prefix: "type/"

						if l := len("type/"); len(elem) >= l && elem[0:l] == "type/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "type"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "GET":
								s.handleGetWorldEventsByTypeRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "GET")
							}

							return
						}

					}

					elem = origElem
				case 'p': // Prefix: "planned"
					origElem := elem
					if l := len("planned"); len(elem) >= l && elem[0:l] == "planned" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch r.Method {
						case "GET":
							s.handleGetPlannedWorldEventsRequest([0]string{}, elemIsEscaped, w, r)
						default:
							s.notAllowed(w, r, "GET")
						}

						return
					}

					elem = origElem
				}
				// Param: "id"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					switch r.Method {
					case "DELETE":
						s.handleDeleteWorldEventRequest([1]string{
							args[0],
						}, elemIsEscaped, w, r)
					case "GET":
						s.handleGetWorldEventRequest([1]string{
							args[0],
						}, elemIsEscaped, w, r)
					case "PUT":
						s.handleUpdateWorldEventRequest([1]string{
							args[0],
						}, elemIsEscaped, w, r)
					default:
						s.notAllowed(w, r, "DELETE,GET,PUT")
					}

					return
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'a': // Prefix: "a"

						if l := len("a"); len(elem) >= l && elem[0:l] == "a" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'c': // Prefix: "ctivate"

							if l := len("ctivate"); len(elem) >= l && elem[0:l] == "ctivate" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleActivateWorldEventRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "POST")
								}

								return
							}

						case 'n': // Prefix: "nnounce"

							if l := len("nnounce"); len(elem) >= l && elem[0:l] == "nnounce" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch r.Method {
								case "POST":
									s.handleAnnounceWorldEventRequest([1]string{
										args[0],
									}, elemIsEscaped, w, r)
								default:
									s.notAllowed(w, r, "POST")
								}

								return
							}

						}

					case 'd': // Prefix: "deactivate"

						if l := len("deactivate"); len(elem) >= l && elem[0:l] == "deactivate" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch r.Method {
							case "POST":
								s.handleDeactivateWorldEventRequest([1]string{
									args[0],
								}, elemIsEscaped, w, r)
							default:
								s.notAllowed(w, r, "POST")
							}

							return
						}

					}

				}

			}

		}
	}
	s.notFound(w, r)
}

// Route is route object.
type Route struct {
	name           string
	summary        string
	operationID    string
	operationGroup string
	pathPattern    string
	count          int
	args           [1]string
}

// Name returns ogen operation name.
//
// It is guaranteed to be unique and not empty.
func (r Route) Name() string {
	return r.name
}

// Summary returns OpenAPI summary.
func (r Route) Summary() string {
	return r.summary
}

// OperationID returns OpenAPI operationId.
func (r Route) OperationID() string {
	return r.operationID
}

// OperationGroup returns the x-ogen-operation-group value.
func (r Route) OperationGroup() string {
	return r.operationGroup
}

// PathPattern returns OpenAPI path.
func (r Route) PathPattern() string {
	return r.pathPattern
}

// Args returns parsed arguments.
func (r Route) Args() []string {
	return r.args[:r.count]
}

// FindRoute finds Route for given method and path.
//
// Note: this method does not unescape path or handle reserved characters in path properly. Use FindPath instead.
func (s *Server) FindRoute(method, path string) (Route, bool) {
	return s.FindPath(method, &url.URL{Path: path})
}

// FindPath finds Route for given method and URL.
func (s *Server) FindPath(method string, u *url.URL) (r Route, _ bool) {
	var (
		elem = u.Path
		args = r.args
	)
	if rawPath := u.RawPath; rawPath != "" {
		if normalized, ok := uri.NormalizeEscapedPath(rawPath); ok {
			elem = normalized
		}
		defer func() {
			for i, arg := range r.args[:r.count] {
				if unescaped, err := url.PathUnescape(arg); err == nil {
					r.args[i] = unescaped
				}
			}
		}()
	}

	elem, ok := s.cutPrefix(elem)
	if !ok {
		return r, false
	}

	// Static code generated router with unwrapped path search.
	switch {
	default:
		if len(elem) == 0 {
			break
		}
		switch elem[0] {
		case '/': // Prefix: "/world/events"

			if l := len("/world/events"); len(elem) >= l && elem[0:l] == "/world/events" {
				elem = elem[l:]
			} else {
				break
			}

			if len(elem) == 0 {
				switch method {
				case "GET":
					r.name = ListWorldEventsOperation
					r.summary = "Получить список всех событий с фильтрацией"
					r.operationID = "listWorldEvents"
					r.operationGroup = ""
					r.pathPattern = "/world/events"
					r.args = args
					r.count = 0
					return r, true
				case "POST":
					r.name = CreateWorldEventOperation
					r.summary = "Создать новое событие"
					r.operationID = "createWorldEvent"
					r.operationGroup = ""
					r.pathPattern = "/world/events"
					r.args = args
					r.count = 0
					return r, true
				default:
					return
				}
			}
			switch elem[0] {
			case '/': // Prefix: "/"

				if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
					elem = elem[l:]
				} else {
					break
				}

				if len(elem) == 0 {
					break
				}
				switch elem[0] {
				case 'a': // Prefix: "active"
					origElem := elem
					if l := len("active"); len(elem) >= l && elem[0:l] == "active" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetActiveWorldEventsOperation
							r.summary = "Получить список активных событий"
							r.operationID = "getActiveWorldEvents"
							r.operationGroup = ""
							r.pathPattern = "/world/events/active"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

					elem = origElem
				case 'b': // Prefix: "by-"
					origElem := elem
					if l := len("by-"); len(elem) >= l && elem[0:l] == "by-" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'f': // Prefix: "frequency/"

						if l := len("frequency/"); len(elem) >= l && elem[0:l] == "frequency/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "frequency"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetWorldEventsByFrequencyOperation
								r.summary = "Получить события по частоте"
								r.operationID = "getWorldEventsByFrequency"
								r.operationGroup = ""
								r.pathPattern = "/world/events/by-frequency/{frequency}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 's': // Prefix: "scale/"

						if l := len("scale/"); len(elem) >= l && elem[0:l] == "scale/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "scale"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetWorldEventsByScaleOperation
								r.summary = "Получить события по масштабу"
								r.operationID = "getWorldEventsByScale"
								r.operationGroup = ""
								r.pathPattern = "/world/events/by-scale/{scale}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					case 't': // Prefix: "type/"

						if l := len("type/"); len(elem) >= l && elem[0:l] == "type/" {
							elem = elem[l:]
						} else {
							break
						}

						// Param: "type"
						// Leaf parameter, slashes are prohibited
						idx := strings.IndexByte(elem, '/')
						if idx >= 0 {
							break
						}
						args[0] = elem
						elem = ""

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "GET":
								r.name = GetWorldEventsByTypeOperation
								r.summary = "Получить события по типу"
								r.operationID = "getWorldEventsByType"
								r.operationGroup = ""
								r.pathPattern = "/world/events/by-type/{type}"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

					elem = origElem
				case 'p': // Prefix: "planned"
					origElem := elem
					if l := len("planned"); len(elem) >= l && elem[0:l] == "planned" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						// Leaf node.
						switch method {
						case "GET":
							r.name = GetPlannedWorldEventsOperation
							r.summary = "Получить список запланированных событий"
							r.operationID = "getPlannedWorldEvents"
							r.operationGroup = ""
							r.pathPattern = "/world/events/planned"
							r.args = args
							r.count = 0
							return r, true
						default:
							return
						}
					}

					elem = origElem
				}
				// Param: "id"
				// Match until "/"
				idx := strings.IndexByte(elem, '/')
				if idx < 0 {
					idx = len(elem)
				}
				args[0] = elem[:idx]
				elem = elem[idx:]

				if len(elem) == 0 {
					switch method {
					case "DELETE":
						r.name = DeleteWorldEventOperation
						r.summary = "Удалить событие"
						r.operationID = "deleteWorldEvent"
						r.operationGroup = ""
						r.pathPattern = "/world/events/{id}"
						r.args = args
						r.count = 1
						return r, true
					case "GET":
						r.name = GetWorldEventOperation
						r.summary = "Получить детали события"
						r.operationID = "getWorldEvent"
						r.operationGroup = ""
						r.pathPattern = "/world/events/{id}"
						r.args = args
						r.count = 1
						return r, true
					case "PUT":
						r.name = UpdateWorldEventOperation
						r.summary = "Обновить событие"
						r.operationID = "updateWorldEvent"
						r.operationGroup = ""
						r.pathPattern = "/world/events/{id}"
						r.args = args
						r.count = 1
						return r, true
					default:
						return
					}
				}
				switch elem[0] {
				case '/': // Prefix: "/"

					if l := len("/"); len(elem) >= l && elem[0:l] == "/" {
						elem = elem[l:]
					} else {
						break
					}

					if len(elem) == 0 {
						break
					}
					switch elem[0] {
					case 'a': // Prefix: "a"

						if l := len("a"); len(elem) >= l && elem[0:l] == "a" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							break
						}
						switch elem[0] {
						case 'c': // Prefix: "ctivate"

							if l := len("ctivate"); len(elem) >= l && elem[0:l] == "ctivate" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = ActivateWorldEventOperation
									r.summary = "Активировать событие"
									r.operationID = "activateWorldEvent"
									r.operationGroup = ""
									r.pathPattern = "/world/events/{id}/activate"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						case 'n': // Prefix: "nnounce"

							if l := len("nnounce"); len(elem) >= l && elem[0:l] == "nnounce" {
								elem = elem[l:]
							} else {
								break
							}

							if len(elem) == 0 {
								// Leaf node.
								switch method {
								case "POST":
									r.name = AnnounceWorldEventOperation
									r.summary = "Анонсировать событие"
									r.operationID = "announceWorldEvent"
									r.operationGroup = ""
									r.pathPattern = "/world/events/{id}/announce"
									r.args = args
									r.count = 1
									return r, true
								default:
									return
								}
							}

						}

					case 'd': // Prefix: "deactivate"

						if l := len("deactivate"); len(elem) >= l && elem[0:l] == "deactivate" {
							elem = elem[l:]
						} else {
							break
						}

						if len(elem) == 0 {
							// Leaf node.
							switch method {
							case "POST":
								r.name = DeactivateWorldEventOperation
								r.summary = "Деактивировать событие"
								r.operationID = "deactivateWorldEvent"
								r.operationGroup = ""
								r.pathPattern = "/world/events/{id}/deactivate"
								r.args = args
								r.count = 1
								return r, true
							default:
								return
							}
						}

					}

				}

			}

		}
	}
	return r, false
}
