// Code generated by ogen, DO NOT EDIT.

package api

import (
	"context"
)

// Handler handles operations described by OpenAPI v3 specification.
type Handler interface {
	// GetScheduledWorldEvents implements getScheduledWorldEvents operation.
	//
	// Получить список запланированных событий.
	//
	// GET /scheduler/world-events/scheduled
	GetScheduledWorldEvents(ctx context.Context) (GetScheduledWorldEventsRes, error)
	// GetWorldEventsCalendar implements getWorldEventsCalendar operation.
	//
	// Получить календарь событий.
	//
	// GET /scheduler/world-events/calendar
	GetWorldEventsCalendar(ctx context.Context, params GetWorldEventsCalendarParams) (GetWorldEventsCalendarRes, error)
	// ScheduleWorldEvent implements scheduleWorldEvent operation.
	//
	// Запланировать событие.
	//
	// POST /scheduler/world-events/schedule
	ScheduleWorldEvent(ctx context.Context, req *ScheduleWorldEventRequest) (ScheduleWorldEventRes, error)
	// TriggerScheduledWorldEvent implements triggerScheduledWorldEvent operation.
	//
	// Ручной запуск запланированного события.
	//
	// POST /scheduler/world-events/{id}/trigger
	TriggerScheduledWorldEvent(ctx context.Context, params TriggerScheduledWorldEventParams) (TriggerScheduledWorldEventRes, error)
}

// Server implements http server based on OpenAPI v3 specification and
// calls Handler to handle requests.
type Server struct {
	h   Handler
	sec SecurityHandler
	baseServer
}

// NewServer creates new Server.
func NewServer(h Handler, sec SecurityHandler, opts ...ServerOption) (*Server, error) {
	s, err := newServerConfig(opts...).baseServer()
	if err != nil {
		return nil, err
	}
	return &Server{
		h:          h,
		sec:        sec,
		baseServer: s,
	}, nil
}
