# Архив `brain-mapping`

- Формат файла: `YYYY-MM.yaml`.
- Поля записи: `source`, `version`, `task_id`, `task_file`, `api_path`, `status`, `created`, `archived_at`, `notes`.
- Используйте архив только после очистки активного `brain-mapping.yaml`.

