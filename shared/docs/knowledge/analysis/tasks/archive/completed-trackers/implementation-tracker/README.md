# Архив `implementation-tracker`

- Формат файла: `YYYY-MM.yaml`.
- Каждая запись: `api_path`, `task_id`, статусы backend/frontend, времена, агент, commit, `archived_at`.
- Сохраняем порядок по дате завершения.

