# Архив `readiness-tracker`

- Формат файла: `YYYY-MM.yaml`.
- Поля записи: `path`, `version`, `status`, `priority`, `checked`, `checker`, `api_target`, `archived_at`, `notes`.
- Используйте при переносе записей после завершения цикла API/реализации.

